---
title: "APEX-Searcher: Augmenting LLMs' Search Capabilities through Agentic Planning and Execution"
title_zh: APEX-Searcher：通过智能体规划与执行增强大语言模型的搜索能力
authors: "Kun Chen, Qingchao Kong, Zhao Feifei, Wenji Mao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13853v2"
tags: ["query:agent"]
score: 9.0
evidence: 用于搜索能力的智能体规划与执行
tldr: 针对现有检索增强生成（RAG）在处理复杂多跳问题时存在的检索路径模糊和奖励稀疏挑战，本文提出了 APEX-Searcher 框架。该框架将检索过程解耦为规划与执行两阶段，利用强化学习优化规划策略，并通过监督微调增强执行能力，显著提升了模型在多跳 RAG 和任务规划上的性能。
selection_source: fresh_fetch
motivation: 现有的多轮检索方法在端到端训练中面临检索路径模糊和奖励稀疏的问题，导致复杂多跳问题的检索结果不准确。
method: 提出两阶段智能体框架，利用强化学习优化战略规划，并通过监督微调赋予模型稳健的迭代子任务执行能力。
result: 在多个基准测试上的实验表明，该方法在多跳 RAG 和任务规划性能上均实现了显著提升。
conclusion: 通过解耦规划与执行过程，APEX-Searcher 有效增强了大语言模型处理复杂搜索任务的能力。
---

## 摘要
基于大语言模型（LLMs）的检索增强生成（RAG）是在各种领域应用中检索和利用外部知识的一种至关重要的方法。当面对复杂的多跳问题时，单轮检索往往不足以进行准确的推理和问题解决。为了增强复杂任务的搜索能力，大多数现有工作通过端到端训练将多轮迭代检索与推理过程相结合。尽管这些方法显著提高了问题解决性能，但它们在任务推理和模型训练方面仍面临挑战，特别是端到端强化学习（RL）过程中模糊的检索执行路径和稀疏奖励，导致检索结果不准确和性能下降。为了解决这些问题，本文提出了 APEX-Searcher，这是一种新颖的智能体规划与执行框架，旨在增强大语言模型的搜索能力。具体而言，我们引入了一个两阶段的智能体框架，将检索过程解耦为规划与执行：它首先采用带有分解特定奖励的强化学习来优化策略规划；基于子任务分解，随后在高质量多跳轨迹上应用监督微调，赋予模型鲁棒的迭代子任务执行能力。大量实验表明，我们提出的框架在多个基准测试中的多跳 RAG 和任务规划性能方面均取得了显著提升。

## Abstract
Retrieval-augmented generation (RAG), based on large language models (LLMs), serves as a vital approach to retrieving and leveraging external knowledge in various domain applications. When confronted with complex multi-hop questions, single-round retrieval is often insufficient for accurate reasoning and problem solving. To enhance search capabilities for complex tasks, most existing works integrate multi-round iterative retrieval with reasoning processes via end-to-end training. While these approaches significantly improve problem-solving performance, they are still faced with challenges in task reasoning and model training, especially ambiguous retrieval execution paths and sparse rewards in end-to-end reinforcement learning (RL) process, leading to inaccurate retrieval results and performance degradation. To address these issues, in this paper, we proposes APEX-Searcher, a novel Agentic Planning and Execution framework to augment LLM search capabilities. Specifically, we introduce a two-stage agentic framework that decouples the retrieval process into planning and execution: It first employs RL with decomposition-specific rewards to optimize strategic planning; Built on the sub-task decomposition, it then applies supervised fine-tuning on high-quality multi-hop trajectories to equip the model with robust iterative sub-task execution capabilities. Extensive experiments demonstrate that our proposed framework achieves significant improvements in both multi-hop RAG and task planning performances across multiple benchmarks.

---

## 论文详细总结（自动生成）

# 论文总结：APEX-Searcher

## 1. 论文的核心问题与整体含义
*   **研究背景**：检索增强生成（RAG）是解决大语言模型（LLM）知识局限性和幻觉问题的重要手段。然而，面对需要综合多个信息源的复杂多跳问题时，传统的单轮检索往往失效。
*   **核心问题**：现有的迭代检索或智能体 RAG 方法通常采用端到端训练，存在两大瓶颈：
    1.  **检索执行路径模糊**：缺乏全局任务规划视角，容易陷入重复查询或无限推理循环。
    2.  **奖励稀疏**：在端到端强化学习（RL）中，由于错误累积和反馈信号稀疏，导致模型学习效率低，检索结果不准确。
*   **整体含义**：本文提出将复杂的检索推理过程显式解耦为“规划”与“执行”两个独立阶段，通过分而治之的策略，分别优化任务分解能力和工具使用能力，从而提升 LLM 处理复杂搜索任务的性能。

## 2. 论文提出的方法论
本文提出了 **APEX-Searcher** 框架，核心思想是将检索过程解耦为 **Agentic Planning（智能体规划）** 和 **Iterative Sub-Task Execution（迭代子任务执行）** 两个阶段，并采用混合训练策略。

### 2.1 阶段一：基于强化学习的智能体规划
该阶段旨在将复杂问题分解为逻辑清晰的子问题序列。
*   **模型策略**：将规划视为序列决策问题，使用 **GRPO (Group Relative Policy Optimization)** 算法优化策略模型，无需额外的 Critic 模型。
*   **奖励函数设计**：这是关键创新点。奖励基于生成计划与金标准计划的语义对齐度。
    1.  **语义相似度计算**：使用 Sentence-Transformer 将子问题编码为向量，计算余弦相似度。
    2.  **最优二分图匹配**：构建成本矩阵，利用匈牙利算法找到生成子问题与金标准子问题的一对一最优匹配。
    3.  **F1-Score 奖励**：基于匹配结果计算 F1 值作为最终奖励 $R_{plan}$，引导模型生成高质量、无冗余的分解计划。
*   **Prompt 模板**：设计了特定的指令模板，要求模型生成 2-4 个子问题，并支持使用 `#1`, `#2` 等占位符引用前序答案，显式建模依赖关系。

### 2.2 阶段二：基于监督微调的智能体执行
该阶段旨在让模型学会根据规划执行具体的检索和问答操作。
*   **数据构建**：从 2WikiMultiHopQA、HotpotQA、MuSiQue 采样种子任务，利用 Qwen2.5-32B-Instruct 和 Deepseek-v3 生成多轮检索轨迹，经过严格过滤得到 14,604 条高质量指令数据。
*   **训练方式**：在上述数据集上进行全参数监督微调（SFT），使模型掌握“知识评估-查询生成-检索-判断继续/回答”的迭代执行能力。

### 2.3 推理流程
1.  **规划**：Planning Agent 将输入问题 $Q$ 分解为子问题序列 $\{s_1, s_2, ..., s_n\}$。
2.  **执行**：Execution Agent 依次处理子问题：
    *   解析引用（如将 `#1` 替换为实际答案）。
    *   检查已有知识库是否充足。
    *   若不足，进入多跳检索循环（生成查询 -> 检索文档 -> 去重 -> 判断是否继续）。
    *   生成子答案并更新知识库。
3.  **综合**：所有子问题解决后，综合生成最终答案。

## 3. 实验设计
*   **数据集**：使用了四个多跳问答基准数据集：**2WikiMultiHopQA**、**HotpotQA**、**MuSiQue** 和 **Bamboogle**。
*   **评估指标**：主要使用 **Exact Match (EM)** 作为评估指标，因为数据集答案多为短实体。
*   **基线方法**：
    *   **Non-Retrieval**：Direct Inference, CoT。
    *   **Standard RAG**：标准单轮检索增强。
    *   **Iterative RAG**：IRCoT。
    *   **Agentic RAG**：Search-o1, Search-R1, ZeroSearch-instruct, StepSearch-instruct, ReasonRAG, DecEx-RAG。
*   **模型设置**：基于 **Qwen2.5-7B-Instruct** 和 **Qwen2.5-3B-Instruct** 进行实验。

## 4. 资源与算力
*   **训练框架**：RL 阶段使用 `verl` 框架，SFT 阶段使用 `360Llamafactory` 框架。
*   **训练配置细节**：
    *   **RL 训练**：Batch size 512，最大序列长度 1024，训练 3 个 epochs，学习率 5e-6，使用 bfloat16 精度。
    *   **SFT 训练**：Batch size 1 (per device) 累积 2 步，最大序列长度 32768，训练 2 个 epochs，学习率 5e-6，使用 DeepSpeed ZeRO-3 和 Flash Attention 2 优化。
*   **缺失信息**：论文**未明确提及**具体的 GPU 型号（如 A100/H100）、显卡数量以及具体的训练时长。

## 5. 实验数量与充分性
*   **主实验**：在 4 个数据集上对比了 10 种基线方法，涵盖了不同参数规模（3B/7B）的模型，实验量充足。
*   **消融实验**：详细拆解了 Planning 模块、RL 训练、SFT 训练各自的贡献，验证了混合训练策略的有效性。
*   **参数敏感性分析**：分析了检索文档数量和最大推理跳数对性能的影响，确定了最优参数配置。
*   **规划能力分析**：对比了训练前后的规划得分、奖励收敛曲线及响应长度变化，并提供了 Case Study。
*   **评价**：实验设计非常详实，从定量结果、消融分析、参数调优到定性案例，多维度验证了方法的有效性，对比方法选择合理，具有公平性。

## 6. 论文的主要结论与发现
*   **性能提升显著**：APEX-Searcher 在多个基准上超越了现有的 Agentic RAG 方法，相比原始模型平均提升超过 20%。
*   **解耦的有效性**：证明了将规划与执行解耦是解决复杂检索任务的有效途径。RL 能有效提升规划质量，SFT 能显著增强执行能力，两者结合效果最佳。
*   **小模型的收益**：SFT 对小模型（3B）的提升尤为明显，能弥补其基础能力的不足；而规划模块若不经过 RL 优化，对小模型可能反而引入噪声导致性能下降。

## 7. 优点
*   **架构创新**：针对端到端训练的痛点，创造性地提出了“规划-执行”解耦框架，思路清晰。
*   **训练策略合理**：针对不同阶段的特点采用不同的训练方式（RL 优化规划策略，SFT 学习执行模式），避免了单一训练方式的局限性。
*   **奖励设计巧妙**：设计了基于语义匹配和匈牙利算法的 F1 奖励函数，有效解决了规划路径评估难的问题。
*   **实验详尽**：不仅验证了最终性能，还深入分析了各组件的作用和参数影响，说服力强。

## 8. 不足与局限
*   **算力透明度不足**：未公开具体的硬件资源和训练时间成本，难以评估方法的实际落地门槛。
*   **数据依赖性**：规划阶段的 RL 训练依赖 MuSiQue 数据集的金标准分解，执行阶段的 SFT 依赖高级模型生成的轨迹，这可能限制了其在数据稀缺领域或分布外任务上的泛化能力。
*   **任务类型单一**：实验主要集中在多跳问答任务，未涉及长文本生成、代码检索等其他类型的搜索场景。
*   **推理开销**：显式的规划和多轮迭代执行可能会增加推理延迟，论文未对推理效率进行分析。

（完）

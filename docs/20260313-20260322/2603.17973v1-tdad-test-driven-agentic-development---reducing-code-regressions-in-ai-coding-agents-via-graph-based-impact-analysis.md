---
title: "TDAD: Test-Driven Agentic Development - Reducing Code Regressions in AI Coding Agents via Graph-Based Impact Analysis"
title_zh: TDAD：测试驱动智能体开发——通过基于图的影响分析减少AI编程智能体中的代码回归
authors: Pepe Alonso
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17973v1"
tags: ["query:agent"]
score: 9.0
evidence: AI代码智能体的测试驱动开发工具
tldr: "AI 编程智能体常引入代码回归，现有基准对此关注不足。本文提出 TDAD，利用基于抽象语法树的代码-测试图和影响分析识别受影响测试。实验表明，该方法将回归率降低 70% 并提升了解决率，证明提供上下文信息比规定程序性工作流更有效。"
selection_source: fresh_fetch
motivation: AI 编程智能体常引入代码回归，且现有基准主要关注解决率而忽视了回归行为。
method: 提出 TDAD 方法，结合基于抽象语法树的代码-测试图构建与加权影响分析，以定位最可能受变更影响的测试。
result: "在 SWE-bench 上，该方法将测试级回归降低了 70%，解决率从 24% 提升至 32%，且自动改进循环实现了零回归。"
conclusion: 研究发现，对于 AI 智能体工具设计，提供上下文信息的效果优于规定程序性工作流。
---

## 摘要
AI编程智能体能够解决现实世界的软件问题，但它们经常引入回归，导致先前通过的测试失败。当前的基准测试几乎完全关注解决率，导致回归行为研究不足。本文提出了TDAD（测试驱动智能体开发），这是一个开源工具和基准方法，它结合了基于抽象语法树（AST）的代码-测试图构建与加权影响分析，以呈现最可能受提议变更影响的测试。在SWE-bench Verified上使用两个本地模型（100个实例上的Qwen3-Coder 30B和25个实例上的Qwen3.5-35B-A3B）进行评估，当作为智能体技能部署时，TDAD的GraphRAG工作流将测试级回归减少了70%（从6.08%降至1.82%），并将解决率从24%提高到32%。一个令人惊讶的发现是，单独使用TDD提示反而增加了回归（9.94%），这揭示了较小的模型从上下文信息（验证哪些测试）中获得的收益多于过程性指令（如何进行TDD）。在一个包含10个实例的子集上，自主自动改进循环将解决率从12%提高到60%，且回归率为0%。这些发现表明，对于AI智能体工具设计而言，呈现上下文信息优于规定过程性工作流。所有代码、数据和日志均可在 https://github.com/pepealonso95/TDAD 公开获取。

## Abstract
AI coding agents can resolve real-world software issues, yet they frequently introduce regressions, breaking tests that previously passed. Current benchmarks focus almost exclusively on resolution rate, leaving regression behavior under-studied. This paper presents TDAD (Test-Driven Agentic Development), an open-source tool and benchmark methodology that combines abstract-syntax-tree (AST) based code-test graph construction with weighted impact analysis to surface the tests most likely affected by a proposed change. Evaluated on SWE-bench Verified with two local models (Qwen3-Coder 30B on 100 instances and Qwen3.5-35B-A3B on 25 instances), TDAD's GraphRAG workflow reduced test-level regressions by 70% (6.08% to 1.82%) and improved resolution from 24% to 32% when deployed as an agent skill. A surprising finding is that TDD prompting alone increased regressions (9.94%), revealing that smaller models benefit more from contextual information (which tests to verify) than from procedural instructions (how to do TDD). An autonomous auto-improvement loop raised resolution from 12% to 60% on a 10-instance subset with 0% regression. These findings suggest that for AI agent tool design, surfacing contextual information outperforms prescribing procedural workflows. All code, data, and logs are publicly available at https://github.com/pepealonso95/TDAD.

---

## 论文详细总结（自动生成）

# 论文总结：TDAD——通过基于图的影响分析减少AI编程智能体中的代码回归

## 1. 论文的核心问题与整体含义
*   **核心问题**：当前的AI编程智能体在解决GitHub Issue时，经常引入“代码回归”，即破坏了原本通过的测试。现有的基准测试（如SWE-bench）几乎只关注“解决率”，忽视了回归行为，导致智能体可能生成“顾此失彼”的低质量补丁。
*   **现实挑战**：智能体缺乏对代码依赖关系的感知。若运行所有测试耗时过长，若只运行变更文件附近的测试则可能漏掉间接依赖导致的故障。
*   **研究意义**：本文主张将“回归率”提升为与“解决率”同等重要的评估指标，并提出了一种轻量级工具，通过提供上下文信息而非冗长的过程指令，帮助智能体减少回归。

## 2. 论文提出的方法论
*   **核心思想**：TDAD (Test-Driven Agentic Development)。核心观点是：对于AI智能体，告知其“验证哪些测试”（上下文信息）比告知其“如何做TDD”（过程性指令）更有效。
*   **技术架构**：
    *   **索引阶段**：解析Python仓库的AST（抽象语法树），构建代码-测试依赖图。图包含文件、函数、类、测试四种节点，以及包含、调用、导入、测试、继承五种边。
    *   **测试链接**：通过命名约定（如 `test_foo.py` 对应 `foo.py`）、前缀匹配和目录邻近度三种策略，将测试节点与被测代码节点关联。
    *   **影响分析**：给定变更文件，并行运行四种策略（直接测试、传递调用、覆盖率、导入）计算受影响测试的得分。
*   **算法流程**：
    *   得分计算公式：$score = (1 - c_w) \cdot w_{strategy} + c_w \cdot confidence$。其中 $c_w=0.3$ 为置信度权重，$w_{strategy}$ 为策略权重（如直接测试权重0.95，传递调用权重0.70）。
    *   根据得分将测试分为高、中、低三个优先级，生成静态的 `test_map.txt` 文件。
*   **智能体集成**：生成一个仅20行的 `SKILL.md` 指令文件和一个静态测试映射文件。智能体只需使用 `grep` 查找相关测试并运行，无需数据库或API调用，实现了零依赖集成。

## 3. 实验设计
*   **数据集**：SWE-bench Verified（包含500个经人工验证的GitHub Issue）。实验分为两个阶段：Phase 1 使用前100个实例；Phase 2 选取了25个多样化的实例。
*   **评估基准**：
    *   **解决率**：补丁通过所有特定测试的比例。
    *   **回归率**：补丁导致原本通过的测试失败的比例（主要指标）。
    *   **生成率**：生成非空补丁的比例。
*   **对比方法**：
    *   **Vanilla**：默认提示，无TDD或图信息。
    *   **TDD Prompt**：仅添加TDD工作流指令。
    *   **GraphRAG + TDD**：本文提出的TDAD方法（图上下文 + 简短指令）。
    *   **Baseline vs TDAD Skill**：在Phase 2 中对比OpenCode智能体在使用TDAD技能前后的表现。

## 4. 资源与算力
*   **模型配置**：
    *   Phase 1：Qwen3-Coder 30B（Q4_K_M 4-bit量化），通过llama.cpp在消费级硬件上运行。
    *   Phase 2：Qwen3.5-35B-A3B（4-bit量化），通过MLX在Apple Silicon上运行。
*   **算力说明**：论文未明确提及具体的GPU型号、数量或训练时长。实验设计强调在本地消费级硬件上运行，无API成本，旨在验证方法在资源受限环境下的有效性。

## 5. 实验数量与充分性
*   **实验规模**：
    *   Phase 1：100个实例，3种配置。
    *   Phase 2：25个实例，2种配置。
    *   自动改进循环：10个实例子集，15次迭代。
    *   共记录了28组实验数据。
*   **充分性与客观性**：
    *   **充分性**：样本量相对较小（100+25），未进行统计显著性检验，作者承认这可能限制了对较小效应的检测能力。
    *   **客观性**：使用了两个不同的模型和智能体框架进行验证，增强了泛化性结论的可信度。设置了温度为0以确保输出的确定性。但也存在内部有效性威胁，如配置是顺序运行而非交错运行，可能受硬件热节流等因素影响。

## 6. 论文的主要结论与发现
*   **显著降低回归**：TDAD将测试级回归率降低了70%（从6.08%降至1.82%），P2P失败数从562降至155。
*   **TDD提示悖论**：单纯的TDD过程性指令反而增加了回归率（从6.08%升至9.94%）。原因在于冗长的指令挤占了小模型的上下文窗口，且缺乏定位导致盲目修改。
*   **解决率提升**：作为智能体技能部署时，TDAD将解决率从24%提升至32%，生成率从40%提升至68%。
*   **上下文优于过程**：对于参数量较小的模型，提供“检查哪些测试”的上下文信息比规定“如何做TDD”的过程性指令更有效。
*   **自动改进效果**：自动改进循环将解决率从12%提升至60%，且回归率为0%。其中最有效的改进是将107行的指令简化为20行。

## 7. 优点
*   **实用性强**：工具设计为零依赖，仅需 `pip install tdad`，无需图数据库或外部API，易于集成。
*   **指标创新**：明确指出了现有基准测试忽视回归率的缺陷，并提出了复合指标建议（净得分 = 解决率 - α * 回归率）。
*   **反直觉发现**：揭示了TDD提示对小模型的负面影响，为智能体工具设计提供了重要洞察。
*   **开源贡献**：代码、数据、日志全部公开，便于复现和进一步研究。

## 8. 不足与局限
*   **样本与模型限制**：实验仅在SWE-bench Verified的子集上进行，且仅测试了较小的本地模型（30B/35B），未验证在GPT-4等前沿大模型上的效果。
*   **语言支持**：目前仅支持Python，扩展至其他语言需重新实现解析器。
*   **静态分析局限**：无法捕获动态分发、Monkey-patching或运行时生成的代码依赖。
*   **过拟合风险**：自动改进循环仅在10个实例上评估，可能存在过拟合风险，尽管作者指出改进是结构性的而非针对特定实例。

（完）

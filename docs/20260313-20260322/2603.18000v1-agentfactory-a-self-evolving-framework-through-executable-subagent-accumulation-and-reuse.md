---
title: "AgentFactory: A Self-Evolving Framework Through Executable Subagent Accumulation and Reuse"
title_zh: AgentFactory：一种基于可执行子智能体积累与复用的自演化框架
authors: "Zhang Zhang, Shuqi Lu, Hongjin Qian, Di He, Zheng Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18000v1"
tags: ["query:agent"]
score: 9.0
evidence: 构建LLM智能体的自进化框架
tldr: 针对现有LLM智能体自演化方法依赖文本记录导致重执行效率低的问题，本文提出AgentFactory框架。该框架将成功任务方案保存为可执行的子智能体代码，通过执行反馈持续优化，实现了能力的积累与复用，显著降低了处理相似任务的成本。
selection_source: fresh_fetch
motivation: 现有基于文本记录的自演化方法难以保证复杂场景下的任务高效重执行。
method: 提出AgentFactory，将成功任务方案保存为可执行的Python子智能体代码，并基于执行反馈持续优化。
result: 实验表明该框架能实现能力的持续积累，随着子智能体库的增长，处理相似任务所需的人力显著减少。
conclusion: AgentFactory通过积累可执行子智能体代码，实现了无需人工干预的智能体自演化与能力提升。
---

## 摘要
构建基于大语言模型（LLM）的智能体已变得愈发重要。近期关于基于LLM的智能体自演化的工作主要将成功经验记录为文本提示或反思，这在复杂场景下无法可靠地保证高效的任务重执行。我们提出了AgentFactory，这是一种新的自演化范式，它将成功的任务解决方案保存为可执行子智能体代码，而非文本经验。关键在于，这些子智能体基于执行反馈不断优化，随着遇到的任务增多，它们变得更加鲁棒和高效。保存的子智能体是带有标准化文档的纯Python代码，可在任何支持Python的系统间移植。我们展示了AgentFactory能够实现持续的能力积累：其可执行子智能体库随时间增长和改进，无需人工干预即可逐步减少相似任务所需的工作量。我们的实现代码已在 https://github.com/zzatpku/AgentFactory 开源，演示视频可在 https://youtu.be/iKSsuAXJHW0 查看。

## Abstract
Building LLM-based agents has become increasingly important. Recent works on LLM-based agent self-evolution primarily record successful experiences as textual prompts or reflections, which cannot reliably guarantee efficient task re-execution in complex scenarios. We propose AgentFactory, a new self-evolution paradigm that preserves successful task solutions as executable subagent code rather than textual experience. Crucially, these subagents are continuously refined based on execution feedback, becoming increasingly robust and efficient as more tasks are encountered. Saved subagents are pure Python code with standardized documentation, enabling portability across any Python-capable system. We demonstrate that AgentFactory enables continuous capability accumulation: its library of executable subagents grows and improves over time, progressively reducing the effort required for similar tasks without manual intervention. Our implementation is open-sourced at https://github.com/zzatpku/AgentFactory, and our demonstration video is available at https://youtu.be/iKSsuAXJHW0.

---

## 论文详细总结（自动生成）

# 论文总结：AgentFactory

## 1. 论文的核心问题与整体含义

*   **研究背景**：基于大语言模型（LLM）的智能体在推理、规划和问题解决方面表现出色，但现有框架（如 LangChain、AutoGPT）通常将智能体行为视为静态的，执行过程中获得的知识无法保留供未来使用。
*   **核心问题**：现有的自演化方法主要通过文本提示、反思或推理轨迹记录成功经验。然而，在复杂的现实任务中，这种文本形式的经验无法可靠保证任务的高效重执行，且缺乏可移植性。
*   **整体含义**：作者提出一种新的自演化范式，旨在通过将成功的任务解决方案固化为可执行的代码（而非易变的文本），实现智能体能力的持续积累、复用与跨平台迁移，从而解决智能体无法从过往经验中高效学习的痛点。

## 2. 论文提出的方法论

*   **核心思想**：提出 **AgentFactory** 框架，将成功的任务解决方案保存为**可执行的子智能体代码**，而非文本经验。这些子智能体基于执行反馈不断优化，形成可复用的能力库。
*   **架构组件**：
    *   **Meta-Agent（元智能体）**：中央编排器，负责分解任务、分配工具、调用子智能体并根据执行反馈进行迭代优化。
    *   **Skill System（技能系统）**：包含三层技能：
        1.  **Meta Skills**：固定的元操作（如创建、修改、运行子智能体）。
        2.  **Tool Skills**：基础工具（如网页搜索、浏览器自动化、Shell命令）。
        3.  **Subagent Skills**：动态生成的 Python 脚本，封装了解决特定子任务的逻辑，是演化的核心载体。
    *   **Workspace Manager（工作空间管理器）**：提供隔离的执行环境，确保代码执行安全，防止失败操作污染技能库。
*   **三阶段生命周期流程**：
    1.  **Install（安装）**：面对新任务，Meta-Agent 从零构建子智能体。流程为：任务分析 -> 任务分解 -> 动态生成 Python 脚本 -> 执行 -> 持久化保存（代码 + SKILL.md 文档）。
    2.  **Self-Evolve（自演化）**：遇到相似任务时，检索已有子智能体并尝试复用。若失败或效果不佳，分析执行反馈，自主修改子智能体代码（如增加错误处理、扩展功能），使其更鲁棒。
    3.  **Deploy（部署）**：将成熟的子智能体导出为独立的 Python 模块，可被其他 AI 框架（如 LangChain、Claude Code）直接调用。

## 3. 实验设计

*   **数据集/场景**：
    *   自建了两批任务集（Batch 1 和 Batch 2），每批包含 15 个真实世界任务。
    *   任务类型涵盖：网络信息检索、数据可视化、浏览器自动化、音频处理等。
    *   Batch 2 的任务结构与 Batch 1 相似，但具体要求不同，用于测试子智能体的复用与迁移能力。
*   **Benchmark**：
    *   主要评估指标为**编排模型的平均输出 Token 数**（Average output tokens per task）。该指标排除了子智能体内部的消耗，专门衡量顶层编排的效率（Token 越少，说明复用效率越高）。
*   **对比方法**：
    1.  **ReAct**：标准基线，无记忆，每次从零开始解决任务。
    2.  **Self-Evolving Agent (Textual Experience)**：模仿现有方法（如 Reflexion），将经验保存为文本摘要，后续任务可检索这些文本作为提示。
*   **模型骨干**：使用了 Claude Opus 4.6 和 Claude Sonnet 4.6 两种 LLM 作为骨干进行测试。

## 4. 资源与算力

*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **资源消耗度量**：由于该框架主要依赖 LLM API 进行推理和代码生成，论文侧重于报告 LLM 的 Token 消耗量作为计算成本的度量，而非传统深度学习的算力指标。

## 5. 实验数量与充分性

*   **实验数量**：
    *   共设计了 30 个任务（2 Batches × 15 Tasks）。
    *   对比了 2 种基线方法。
    *   使用了 2 种模型骨干。
    *   包含了定性演示（如 README 生成任务的迭代优化、跨系统复用演示）和定量分析。
*   **充分性与客观性评价**：
    *   **充分性**：任务集规模相对较小（30 个），虽然涵盖了多领域，但对于验证“持续积累”和“长期演化”的特性来说，规模可能略显不足。
    *   **客观性**：实验假设所有任务均能无运行时错误完成，主要对比效率而非成功率。这可能导致忽略了代码生成失败的情况。不过，Token 消耗作为效率指标是客观且直接的。
    *   **公平性**：对比基线的设计较为合理，涵盖了无记忆和文本记忆两种主流范式，能够有效突出代码记忆的优势。

## 6. 论文的主要结论与发现

*   **效率显著提升**：在 Batch 2 任务中，AgentFactory 的 Token 消耗显著低于基线方法，证明复用可执行代码比检索文本经验或从零开始更高效。
*   **强模型的即时复用能力**：在使用更强的模型（Opus 4.6）时，即使在 Batch 1（初始构建阶段），Token 消耗也低于 ReAct 基线。这表明强模型能在早期就识别并复用刚创建的子智能体。
*   **代码演化的有效性**：演示表明，AgentFactory 能通过执行反馈自主修改代码（例如从硬编码路径演化为正则匹配），提高了鲁棒性。
*   **跨平台移植性**：生成的子智能体是纯 Python 代码，可被其他系统（如 Claude Code）直接调用，实现了能力的跨框架迁移。

## 7. 优点

*   **范式创新**：将智能体记忆从“文本反思”转变为“可执行代码”，解决了文本经验在重执行时不可靠的问题。
*   **闭环演化**：构建了“构建-复用-反馈-修改”的完整闭环，使智能体能够真正通过经验积累提升能力，而非仅依靠上下文学习。
*   **高度可移植**：生成的子智能体不依赖特定运行时环境，作为标准 Python 模块可被任何支持 Python 的系统使用，具有很高的实用价值。
*   **架构清晰**：提出的 Install -> Self-Evolve -> Deploy 三阶段生命周期逻辑严密，符合软件工程中的模块化思想。

## 8. 不足与局限

*   **评估规模有限**：仅使用了 30 个任务进行评估，对于验证“自演化”和“能力积累”这一宏大目标，数据规模可能不足以覆盖所有边缘情况或展示长期演化效果。
*   **安全风险**：框架涉及自主生成和执行代码，尽管文中提到有安全检查，但 Shell 命令执行仍存在潜在风险。
*   **依赖模型能力**：方法高度依赖底层 LLM 的代码生成和反思能力，若模型能力较弱（如 Sonnet 在 Batch 1 的表现不如 Opus），演化效果可能受限。
*   **指标单一**：主要关注 Token 效率，未详细报告任务成功率的变化或代码生成的错误率，可能掩盖了代码生成失败带来的额外重试成本。

（完）

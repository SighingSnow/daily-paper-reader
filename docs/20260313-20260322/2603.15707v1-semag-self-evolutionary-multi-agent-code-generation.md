---
title: "SEMAG: Self-Evolutionary Multi-Agent Code Generation"
title_zh: SEMAG：自演化多智能体代码生成
authors: "Yulin Peng, Haowen Hou, Xinxin Zhu, Ying Tiffany He, F. Richard Yu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15707v1"
tags: ["query:agent"]
score: 9.0
evidence: 自进化多智能体代码生成框架
tldr: 现有大语言模型代码生成方法依赖人工选择和固定流程，难以适应变化的任务复杂度。本文提出SEMAG框架，模拟人类编程实践，将任务分解为规划、编码、调试等阶段，并根据任务难度自适应调整工作流。该框架支持实时访问最新模型并自动升级骨干模型。实验表明，SEMAG在多个基准测试中达到SOTA，在CodeContests上优于先前方法，展示了卓越的有效性和适应性。
selection_source: fresh_fetch
motivation: 现有代码生成方法依赖人工模型选择和固定工作流，限制了其适应任务复杂度变化的能力。
method: 提出SEMAG框架，模仿人类编程将任务分解为多阶段，根据难度调整工作流，并支持智能体自动升级骨干模型。
result: "SEMAG在基准测试中创下SOTA，在CodeContests上使用相同骨干模型优于先前方法3.3%，结合自进化模型选择准确率达52.6%。"
conclusion: SEMAG证明了其作为框架的有效性以及对不断发展的LLM能力的适应性。
---

## 摘要
大语言模型 (LLMs) 在处理复杂编程任务方面取得了显著进展。然而，现有方法依赖于手动模型选择和固定工作流，限制了其适应任务复杂性变化的能力。为解决这一问题，我们提出了 SEMAG，这是一个模仿人类编码习惯的自演化多智能体代码生成框架。它将编程任务分解为规划、编码、调试和讨论等阶段，并根据任务难度调整工作流。其自演化智能体能够实时访问最新模型，并自动升级骨干模型。SEMAG 在各项基准测试中创造了新的最先进 Pass@1 准确率记录。在使用相同骨干模型的情况下，SEMAG 在 CodeContests 上比先前方法高出 3.3%。当引入能够自动识别最佳骨干模型的自演化模型选择机制时，SEMAG 达到了 52.6% 的准确率，展示了框架的有效性及其对不断演进的 LLM 能力的适应性。

## Abstract
Large Language Models (LLMs) have made significant progress in handling complex programming tasks. However, current methods rely on manual model selection and fixed workflows, which limit their ability to adapt to changing task complexities. To address this, we propose SEMAG, a Self-Evolutionary Multi-Agent code Generation framework that mimics human coding practices. It decomposes programming tasks into stages, including planning, coding, debugging, and discussion, while adapting workflows to task difficulty. Its self-evolutionary agents can access the latest models in real time and automatically upgrade the backbone model. SEMAG sets new state-of-the-art Pass@1 accuracy across benchmarks. Using identical backbone models, SEMAG outperforms prior methods by 3.3% on CodeContests. When augmented with self-evolutionary model selection that automatically identifies optimal backbones, SEMAG reaches 52.6%, showcasing both framework effectiveness and adaptability to evolving LLM capabilities.

---

## 论文详细总结（自动生成）

# 论文总结：SEMAG: Self-Evolutionary Multi-Agent Code Generation

## 1. 论文的核心问题与整体含义
*   **核心问题**：现有的基于大语言模型（LLM）的代码生成方法存在三大局限：
    1.  **固定推理深度**：现有框架（如Self-Debugging, LDB）通常采用固定的工作流，对简单任务造成计算冗余，对复杂任务则推理深度不足。
    2.  **单次调试局限**：依赖单次调试迭代，当初始输出偏差较大时，容易陷入局部最优解。
    3.  **模型耦合度高**：现有系统通常绑定单一骨干模型，无法根据任务特性动态切换或利用最新发布的最优模型。
*   **整体含义**：本文旨在提出一种能够模仿人类编程习惯、自适应任务复杂度、并能自动选择与升级模型的代码生成框架，以解决现有方法僵化、适应性差的问题，提升代码生成的成功率和资源效率。

## 2. 论文提出的方法论
*   **核心思想**：提出 **SEMAG**（Self-Evolutionary Multi-Agent Code Generation）框架。该框架模仿人类编程实践，采用分层架构处理任务，并引入“自演化”机制，使智能体能够实时获取最新模型信息并自动切换最优骨干模型。
*   **关键技术细节**：
    *   **分层代码合成框架**：将代码生成过程分为四个层级，逐级增加计算投入：
        *   **Level 1（直接生成）**：直接生成代码，适用于简单任务。
        *   **Level 2（规划与验证）**：生成解题计划，并通过输入输出模拟进行验证。
        *   **Level 3（轨迹引导调试）**：嵌入执行轨迹，分析代码语义，提出修改建议并进行调试。
        *   **Level 4（多智能体协作优化）**：当调试陷入停滞时，引入辩论机制，多个智能体提出方案，由决策智能体选择最优解。
    *   **自适应层级转换机制**：基于执行轨迹的相似度（$\rho$）动态决定是否进入下一层级。若当前层级的调试无法显著改变执行轨迹（即陷入局部最优），则触发层级转换。
    *   **自演化机制**：
        *   智能体通过搜索工具获取最新的模型评测信息。
        *   筛选相关链接并总结内容。
        *   根据任务特性提出候选模型并打分，最终通过加权投票选出最适合当前任务的骨干模型。
*   **算法流程**：
    1.  尝试直接生成代码（Level 1）。
    2.  若失败，生成计划并验证（Level 2）。
    3.  若仍失败，进入调试循环，嵌入轨迹、解释代码、提出建议并修正（Level 3）。
    4.  若调试停滞，触发多智能体辩论与决策（Level 4）。
    5.  全过程可由自演化智能体动态选择最优的底层LLM。

## 3. 实验设计
*   **数据集**：使用了7个文本到代码的基准数据集，分为两类：
    *   **基础数据集**：HumanEval, MBPP, HumanEval-ET, MBPP-ET。
    *   **竞赛级数据集**：APPS, LiveCode, CodeContests。
*   **评估指标**：Pass@1 准确率。
*   **对比方法**：
    *   **Direct**：直接提示生成。
    *   **Self-Planning**：任务分解规划。
    *   **MapCoder**：多智能体检索与调试。
    *   **LDB**：基于控制流图的调试。
    *   **LPW**：当前SOTA方法，基于规划的逐步验证。
*   **骨干模型**：主要使用 GPT-3.5 和 GPT-4o 进行对比实验；在自演化实验中涉及 Claude-3.7-Sonnet, DeepSeek-v3 等。

## 4. 资源与算力
*   **算力说明**：论文主要关注推理阶段的框架设计，实验基于API调用（如GPT-4o, GPT-3.5等），**未明确提及具体的GPU型号、数量或训练时长**。
*   **资源消耗分析**：文中重点分析了Token消耗。SEMAG的分层策略在简单任务上减少了Token使用（相比LPW减少约15-20%），但在复杂任务上因需要更多推理步骤，Token消耗略有增加或持平。

## 5. 实验数量与充分性
*   **实验数量**：
    *   **主实验**：在7个数据集上对比了5种方法，使用了2种主要骨干模型。
    *   **消融实验**：验证了规划验证、调试建议、辩论决策三个核心组件的贡献；分析了工具使用的影响。
    *   **参数分析**：网格搜索了尝试次数（$M_{try}$）和调试迭代次数（$M_{debug}$）的影响；分析了温度参数的影响。
    *   **自演化分析**：分析了爬虫深度对模型选择准确率的影响。
*   **充分性与客观性**：实验设计较为充分，覆盖了从基础到竞赛级的多种难度场景。对比方法包含了当前的SOTA（LPW），且控制了骨干模型变量（如均使用GPT-4o），保证了对比的公平性。消融实验详细验证了各模块的有效性。

## 6. 论文的主要结论与发现
*   **性能提升**：SEMAG在所有7个基准测试中均取得了SOTA性能。在使用相同骨干模型（GPT-4o）的情况下，SEMAG在CodeContests上达到38.0%，比LPW高出3.3%。
*   **自演化优势**：结合自演化模型选择机制，SEMAG自动选择了Claude-3.7-Sonnet作为骨干模型，在CodeContests上的准确率进一步提升至52.6%，证明了框架对模型能力的适应性。
*   **效率优化**：分层机制有效平衡了性能与成本，在简单任务上显著降低了计算开销。
*   **组件有效性**：消融实验证明，规划验证、调试建议和辩论决策三个模块缺一不可，共同贡献了性能提升。

## 7. 优点
*   **自适应性强**：分层架构能够根据任务难度动态调整推理深度，避免了“一刀切”的资源浪费或能力不足。
*   **创新的自演化机制**：首次在代码生成框架中引入自动搜索并切换最优LLM的机制，解决了模型迭代快、框架难以跟进的问题。
*   **多智能体协作**：引入辩论与决策机制，有效解决了传统调试方法容易陷入局部最优的问题。
*   **鲁棒性好**：在多种难度和类型的数据集上均表现出色，且对超参数设置具有一定的稳定性。

## 8. 不足与局限
*   **超参数依赖**：框架涉及多个超参数（如尝试次数、调试迭代次数），虽然文中给出了推荐值，但在特定场景下可能仍需调整。
*   **延迟问题**：对于复杂任务，多层级迭代和多智能体辩论会增加推理延迟，可能不适用于对实时性要求极高的场景。
*   **外部依赖风险**：自演化模型选择依赖网络搜索结果，可能受到搜索引擎偏差、信息时效性或噪音的影响。
*   **安全风险**：论文提到执行生成代码存在潜在安全风险，建议在沙箱环境中运行，但这增加了部署的复杂性。

（完）

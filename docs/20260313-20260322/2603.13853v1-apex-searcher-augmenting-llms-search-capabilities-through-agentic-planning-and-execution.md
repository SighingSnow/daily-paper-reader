---
title: "APEX-Searcher: Augmenting LLMs' Search Capabilities through Agentic Planning and Execution"
title_zh: APEX-Searcher：通过智能体规划与执行增强大语言模型的搜索能力
authors: "Kun Chen, Qingchao Kong, Zhao Feifei, Wenji Mao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13853v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于多跳搜索的智能体规划与执行
tldr: 针对现有RAG方法在复杂多跳问题中存在的检索路径模糊和强化学习奖励稀疏问题，本文提出了APEX-Searcher框架。该框架通过智能体规划与执行解耦检索过程，利用特定奖励的强化学习优化规划，并结合监督微调增强执行能力，显著提升了多跳RAG与任务规划性能。
selection_source: fresh_fetch
motivation: 现有端到端多轮检索方法面临检索路径模糊和强化学习奖励稀疏的挑战，难以处理复杂多跳问题。
method: 提出两阶段智能体框架APEX-Searcher，利用带分解奖励的强化学习优化规划，并通过监督微调增强子任务执行能力。
result: 实验表明，该方法在多个基准测试中显著提升了多跳RAG和任务规划的性能。
conclusion: APEX-Searcher通过解耦规划与执行，有效解决了现有方法的局限性，成功增强了大模型的搜索能力。
---

## 摘要
基于大语言模型（LLM）的检索增强生成（RAG）是在各种领域应用中检索和利用外部知识的一种至关重要的方法。当面对复杂的多跳问题时，单轮检索往往不足以进行准确的推理和问题解决。为了增强复杂任务的搜索能力，大多数现有工作通过端到端训练将多轮迭代检索与推理过程相结合。尽管这些方法显著提高了解题性能，但在任务推理和模型训练方面仍面临挑战，特别是检索执行路径模糊以及端到端强化学习（RL）过程中的稀疏奖励，导致检索结果不准确和性能下降。为了解决这些问题，本文提出了APEX-Searcher，一种新颖的智能体规划与执行框架，旨在增强LLM的搜索能力。具体而言，我们引入了一个两阶段的智能体框架，将检索过程解耦为规划和执行：它首先采用带有针对分解的特定奖励的强化学习（RL）来优化策略规划；基于子任务分解，随后在高质量多跳轨迹上应用监督微调，使模型具备鲁棒的迭代子任务执行能力。大量实验表明，我们提出的框架在多个基准测试上的多跳RAG和任务规划性能均取得了显著提升。

## Abstract
Retrieval-augmented generation (RAG), based on large language models (LLMs), serves as a vital approach to retrieving and leveraging external knowledge in various domain applications. When confronted with complex multi-hop questions, single-round retrieval is often insufficient for accurate reasoning and problem solving. To enhance search capabilities for complex tasks, most existing works integrate multi-round iterative retrieval with reasoning processes via end-to-end training. While these approaches significantly improve problem-solving performance, they are still faced with challenges in task reasoning and model training, especially ambiguous retrieval execution paths and sparse rewards in end-to-end reinforcement learning (RL) process, leading to inaccurate retrieval results and performance degradation. To address these issues, in this paper, we proposes APEX-Searcher, a novel Agentic Planning and Execution framework to augment LLM search capabilities. Specifically, we introduce a two-stage agentic framework that decouples the retrieval process into planning and execution: It first employs RL with decomposition-specific rewards to optimize strategic planning; Built on the sub-task decomposition, it then applies supervised fine-tuning on high-quality multi-hop trajectories to equip the model with robust iterative sub-task execution capabilities. Extensive experiments demonstrate that our proposed framework achieves significant improvements in both multi-hop RAG and task planning performances across multiple benchmarks.

---

## 论文详细总结（自动生成）

# 论文总结：APEX-Searcher

## 1. 论文的核心问题与整体含义
*   **研究背景**：检索增强生成（RAG）是解决大语言模型（LLM）知识局限性和幻觉问题的重要手段。然而，面对需要整合多处信息的复杂多跳问题时，传统的单轮检索或简单的迭代检索方法往往表现不佳。
*   **核心问题**：现有的迭代 RAG 方法主要关注增加检索次数或优化查询，忽视了 LLM 的**任务规划能力**。这导致检索过程缺乏全局视野，容易出现检索路径模糊、过度规划或重复查询等问题。此外，端到端的强化学习训练在复杂推理任务中面临奖励稀疏的挑战。
*   **整体含义**：论文提出应将“战略规划”与“战术执行”解耦，通过显式的任务分解和规划来指导检索过程，从而系统性地提升 LLM 在复杂搜索任务中的表现。

## 2. 论文提出的方法论
论文提出了 **APEX-Searcher** 框架，包含两个核心阶段：

### 核心思想
将复杂的检索过程解耦为**高层任务规划**和**低层多轮检索**。首先利用强化学习（RL）训练模型进行问题分解，随后利用监督微调（SFT）训练模型执行具体的检索与推理。

### 关键技术细节
#### 第一阶段：基于 RL 的智能体规划
*   **目标**：将复杂问题 $Q$ 分解为逻辑有序的子问题序列 $S=\{s_1, s_2, ..., s_n\}$。
*   **算法**：采用 **GRPO (Group Relative Policy Optimization)** 算法优化策略，无需额外的价值函数模型。
*   **奖励函数设计**：
    1.  **语义相似度计算**：使用 Sentence-Transformer 将生成的子问题和金标准子问题编码为向量，计算余弦相似度。
    2.  **二分图匹配**：构建代价矩阵，利用匈牙利算法找到最优的一对一匹配关系（阈值 $\tau=0.8$）。
    3.  **F1 分数计算**：基于匹配结果计算 Precision 和 Recall，最终奖励 $R_{plan}$ 为两者的调和平均数（F1 Score）。

#### 第二阶段：基于 SFT 的智能体探索
*   **目标**：执行分解后的子问题，进行多轮检索与推理。
*   **流程**：
    1.  **知识充分性评估**：判断当前知识库是否足以回答子问题，决定是否检索。
    2.  **自适应多跳检索循环**：包含查询生成、文档检索、去重过滤、上下文管理。模型动态决定是继续检索还是生成答案。
    3.  **知识库更新**：将生成的子问答对存入累积知识库，供后续步骤使用。
*   **训练数据构建**：利用 Self-instruct 方法，基于 Qwen2.5-32B 和 Deepseek-v3 生成多轮推理轨迹，经过严格过滤得到 14,604 条高质量指令数据进行 SFT。

## 3. 实验设计
*   **数据集**：使用了四个多跳问答基准数据集，包括 **2WikiMultiHopQA**、**HotpotQA**、**MuSiQue** 和 **Bamboogle**。
*   **评价指标**：主要使用 **Exact Match (EM)** 作为评价指标，因为目标答案多为简短实体。
*   **对比方法**：
    *   **无检索方法**：Direct Inference, CoT。
    *   **标准 RAG**：Standard RAG。
    *   **迭代 RAG**：IRCoT。
    *   **智能体 RAG**：Search-o1, Search-R1, ZeroSearch-instruct, StepSearch-instruct, ReasonRAG, DecEx-RAG。
*   **基座模型**：Qwen2.5-3B-Instruct 和 Qwen2.5-7B-Instruct。

## 4. 资源与算力
*   **训练框架**：RL 阶段使用 **verl** 框架，SFT 阶段使用 **360Llamafactory** 框架。
*   **训练配置**：
    *   RL 训练：3 个 epoch，学习率 5e-6，全局批次大小 512。
    *   SFT 训练：2 个 epoch，学习率 5e-6，最大序列长度 32,768 tokens。
    *   优化技术：使用了 DeepSpeed ZeRO-3 和 Flash Attention 2。
*   **缺失信息**：论文**未明确提及**具体的 GPU 型号、数量以及总训练时长。

## 5. 实验数量与充分性
*   **实验数量**：
    *   主实验：在 4 个数据集上对比了约 10 种方法，涵盖了 3B 和 7B 两种模型尺寸。
    *   消融实验：设计了 6 组消融实验，分别验证了规划模块、RL 优化和 SFT 优化的独立贡献。
    *   参数分析：分析了检索文档数量和最大推理跳数对性能的影响。
    *   规划能力分析：展示了训练过程中的奖励曲线、响应长度变化及案例研究。
*   **充分性与客观性**：实验设计较为充分，不仅验证了最终性能，还深入分析了各组件的作用机制。对比方法涵盖了当前主流的 RAG 范式，且使用了相同的环境设置以保证公平性。

## 6. 论文的主要结论与发现
*   **性能提升**：APEX-Searcher 在所有基准测试中均显著优于现有方法，相比原始模型平均提升超过 20%。
*   **规划的重要性**：显式的任务规划能有效指导检索过程，单纯增加检索轮次不如先制定计划有效。
*   **组件协同效应**：RL 优化规划质量与 SFT 优化执行能力缺一不可。特别是对于小模型（3B），仅引入规划模块而缺乏针对性训练反而会导致性能下降，SFT 对提升小模型的执行能力至关重要。
*   **训练效果**：RL 训练不仅提高了规划得分，还减少了响应长度（去除了冗余分解），提高了效率。

## 7. 优点
*   **解耦设计**：创新性地将规划与执行解耦，有效缓解了端到端训练中奖励稀疏和路径模糊的问题。
*   **奖励机制**：设计了基于语义匹配和二分图的 F1 奖励函数，为规划能力的 RL 训练提供了精准的监督信号。
*   **数据构建**：通过自动化生成与严格过滤构建了高质量的 SFT 数据集，增强了模型在多轮检索中的鲁棒性。

## 8. 不足与局限
*   **实验覆盖**：部分对比方法（如 ReasonRAG, DecEx-RAG）在部分数据集上缺失结果，可能影响全面性评估。
*   **资源透明度**：未披露具体的算力消耗（GPU 型号、数量、时长），增加了复现难度评估。
*   **应用限制**：目前主要针对多跳 QA 任务，尚未验证在更广泛的搜索场景（如开放域 Web 搜索）中的表现。
*   **数据依赖**：规划阶段的 RL 训练依赖于金标准的任务分解数据，数据的获取成本和质量可能限制模型的泛化能力。

（完）

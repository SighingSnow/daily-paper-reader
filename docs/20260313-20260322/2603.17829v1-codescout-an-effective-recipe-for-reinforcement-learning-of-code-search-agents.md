---
title: "CodeScout: An Effective Recipe for Reinforcement Learning of Code Search Agents"
title_zh: CodeScout：代码搜索智能体强化学习的有效配方
authors: "Lintang Sutawika, Aditya Bharat Soni, Bharath Sriraam R R, Apurva Gandhi, Taha Yassine, Sanidhya Vijayvargiya, Yuchen Li, Xuhui Zhou, Yilin Zhang, Leander Melroy Maben, Graham Neubig"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17829v1"
tags: ["query:agent"]
score: 10.0
evidence: 基于强化学习的代码搜索智能体
tldr: 本文提出CodeScout，一种利用强化学习训练代码搜索智能体的有效方案。针对代码定位任务，该方法摒弃了复杂的专用工具，仅依靠标准Unix终端即可训练。实验表明，该模型在多个基准测试中表现优异，超越了参数量大2-18倍的模型，甚至接近闭源模型Claude Sonnet的性能，并已开源。
selection_source: fresh_fetch
motivation: 现有的代码定位智能体依赖复杂的专用工具，本文旨在证明仅通过标准Unix终端配合有效的强化学习配方也能实现高效代码搜索。
method: 作者提出了一种强化学习配方，通过重新利用编码智能体环境、设计奖励机制及优化策略，训练仅配备标准Unix终端的智能体。
result: 在SWE-Bench等基准测试中，该模型性能优于参数量大2-18倍的模型，且在某些情况下接近Claude Sonnet等闭源模型。
conclusion: 研究证明了简单工具结合有效强化学习配方的巨大潜力，并开源了CodeScout模型系列及配套代码数据。
---

## 摘要
编码智能体在大型代码库上执行任务的先决条件是代码定位——即识别需要处理的相关文件、类和函数。虽然代码库级别的代码定位此前已通过基于嵌入的检索方法（如向量搜索）实现，但近期的工作侧重于开发智能体来定位相关代码，这既可以是执行实际工作前的独立前置步骤，也可以与实际工作交错进行。大多数先前的智能体代码搜索方法都为智能体配备了复杂的专用工具，例如通过静态分析得出的代码库图谱。在本文中，我们证明了利用一种有效的强化学习配方，仅配备标准 Unix 终端的编码智能体即可经过训练取得优异的结果。我们在三个基准测试（SWE-Bench Verified、Pro 和 Lite）上的实验表明，与参数量大 2 到 18 倍的基础及后训练大语言模型相比，我们的模型始终取得更优或具有竞争力的性能；即使在使用专用支架的情况下，有时甚至能接近 Claude Sonnet 等闭源模型的性能。我们的工作特别关注将现有编码智能体环境改造用于代码搜索的技术、奖励设计以及强化学习优化。我们发布了由此产生的模型系列 CodeScout，以及所有代码和数据，供社区在此基础上进行构建。

## Abstract
A prerequisite for coding agents to perform tasks on large repositories is code localization - the identification of relevant files, classes, and functions to work on. While repository-level code localization has been performed using embedding-based retrieval approaches such as vector search, recent work has focused on developing agents to localize relevant code either as a standalone precursor to or interleaved with performing actual work. Most prior methods on agentic code search equip the agent with complex, specialized tools, such as repository graphs derived from static analysis. In this paper, we demonstrate that, with an effective reinforcement learning recipe, a coding agent equipped with nothing more than a standard Unix terminal can be trained to achieve strong results. Our experiments on three benchmarks (SWE-Bench Verified, Pro, and Lite) reveal that our models consistently achieve superior or competitive performance over 2-18x larger base and post-trained LLMs and sometimes approach performance provided by closed models like Claude Sonnet, even when using specialized scaffolds. Our work particularly focuses on techniques for re-purposing existing coding agent environments for code search, reward design, and RL optimization. We release the resulting model family, CodeScout, along with all our code and data for the community to build upon.

---

## 论文详细总结（自动生成）

# 论文总结：CodeScout：代码搜索智能体强化学习的有效配方

## 1. 论文的核心问题与整体含义
*   **研究背景**：在大型代码库中，智能体执行任务（如自动修复Bug）的前提是精准的“代码定位”，即识别出需要修改的相关文件、类和函数。现有的代码定位智能体通常依赖复杂的专用工具（如基于静态分析的代码图谱），这增加了系统的复杂性和依赖性。
*   **核心问题**：是否可以摒弃复杂的专用工具，仅利用标准的Unix终端环境，通过有效的强化学习配方，训练出性能优异的代码搜索智能体？
*   **整体含义**：本文证明了简单的工具（Bash终端）结合精心设计的强化学习策略，足以训练出超越参数量大2-18倍基座模型的智能体，甚至在某些场景下接近闭源模型（如Claude Sonnet）的性能。这挑战了“复杂工具是高性能必要条件”的传统观念，展示了强化学习在提升智能体推理和工具使用能力上的巨大潜力。

## 2. 论文提出的方法论
*   **核心思想**：提出 **CODE SCOUT** 训练配方，利用强化学习（RL）微调大语言模型，使其能够仅通过标准Unix终端命令（如 `grep`, `find`, `sed`）进行代码定位，无需专用解析工具。
*   **智能体支架**：
    *   使用 **OpenHands-Bash** 环境，仅提供一个 `terminal` 工具。
    *   智能体通过执行Unix命令探索代码库，最终调用 `localization_finish` 工具提交定位结果（文件、模块、函数）。
*   **数据构建**：
    *   基于 SWE-Bench 数据集，从真实的代码补丁中提取“真值定位目标”。
    *   定位粒度分为三级：文件级、模块级、函数级。
*   **奖励函数**：
    *   主要奖励：计算预测定位结果与真值之间的 **F1分数**（综合考虑精确率和召回率），并在三个粒度上取平均。
    *   辅助奖励：对于14B模型，引入辅助项 $r_{turn}(\tau, k)$ 以鼓励在有限的交互轮次内完成任务。
*   **强化学习算法（Modified GSPO）**：
    *   基于 **GSPO (Group Sequence Policy Optimization)** 算法。
    *   **关键修改**：在分词阶段保留早期轮次的token（默认行为通常会移除），保持“序列扩展属性”，允许将多轮轨迹合并为单一训练序列，提高训练效率。
    *   **损失掩码**：对非模型生成的token（如系统提示、环境观察）进行损失掩码，仅计算模型生成部分的损失。
*   **训练流程**：
    *   **CODE SCOUT-4B/14B**：直接在基座模型上进行RL训练。
    *   **CODE SCOUT-1.7B**：由于基座模型性能极差，先使用 CODE SCOUT-14B 采样成功轨迹进行拒绝采样微调，再进行RL训练。

## 3. 实验设计
*   **数据集与场景**：
    *   **SWE-Bench Verified**：500个实例。
    *   **SWE-Bench Lite**：300个实例。
    *   **SWE-Bench Pro (Python子集)**：266个实例，难度最高。
*   **评估指标**：
    *   主要指标：文件级、模块级、函数级的 **F1分数**。
    *   辅助指标：精确率和召回率。
*   **对比方法**：
    *   **开源基线**：RepoNavigator, RepoSearcher, LocAgent, OrcaLoca, CoSIL, Agentless（这些方法多依赖复杂的专用工具或检索流程）。
    *   **基座模型对比**：Qwen3系列（1.7B, 4B, 14B, 32B），对比基座模型与经过RL训练后的模型性能。
    *   **闭源模型**：GPT-5, Claude-Sonnet-4.5, Claude-3.7-Sonnet。

## 4. 资源与算力
*   **硬件环境**：所有实验均在 **8 × H100 GPUs** 上进行。
*   **训练细节**：
    *   **CODE SCOUT-4B**：训练200步，Batch size 8，最大上下文40K tokens。
    *   **CODE SCOUT-14B**：训练300步，Batch size 32，最大上下文50K tokens（使用YaRN扩展）。
    *   **CODE SCOUT-1.7B**：RFT阶段训练1轮，RL阶段训练100步，最大上下文32K tokens。
    *   学习率均为 1e-6，使用 AdamW 优化器。

## 5. 实验数量与充分性
*   **实验规模**：
    *   在3个主流基准数据集上进行了全面评估。
    *   进行了详细的消融实验，对比了不同RL算法（GSPO, GRPO, SAPO, Dr.GRPO）及不同配置（损失归约、优势归一化）的影响。
    *   进行了下游任务验证，将定位结果用于增强问题解决智能体，验证了实用价值。
    *   分析了训练过程中工具使用行为的演变。
*   **客观性与公平性**：
    *   实验对比了不同参数规模的模型，证明了参数效率。
    *   对于闭源模型，指出了其提示词敏感性（如需添加提醒消息才能正常提交），并在相同支架下进行了对比，评价较为客观。
    *   实验设置覆盖了从简单到困难的不同场景，充分性较高。

## 6. 论文的主要结论与发现
*   **性能卓越**：CODE SCOUT 模型显著优于同等或更大参数量的基座模型。例如，CODE SCOUT-1.7B 在多项指标上超越了 8倍参数量的 Qwen3-14B；CODE SCOUT-4B/14B 甚至超越了 18倍参数量的 Qwen3-32B。
*   **工具简化**：经过RL训练后，智能体的工具使用行为发生收敛，从初期使用多种命令收敛到主要依赖 `ripgrep (rg)` 和 `sed` 等少数高效工具，证明简单工具足以应对复杂任务。
*   **缩小闭源差距**：CODE SCOUT 在某些指标上接近甚至超越了部分闭源模型（如 GPT-5-Chat + RepoNavigator），且发现简单的Bash支架有时比复杂的专用支架表现更好。
*   **下游增益**：将 CODE SCOUT 的定位结果提供给问题解决智能体，可显著提高修复成功率并减少Token消耗。

## 7. 优点
*   **方法创新**：证明了“简单工具 + 强化学习”可以超越“复杂工具 + 监督/提示工程”，为智能体训练提供了新思路。
*   **参数效率高**：小参数模型经过RL训练后能超越大参数模型，降低了部署成本。
*   **开源贡献**：公开了模型权重、代码和数据，具有较高的复现价值和社区价值。
*   **分析深入**：不仅关注性能指标，还深入分析了智能体行为演变（如工具收敛现象）和RL算法细节的影响。

## 8. 不足与局限
*   **语言限制**：实验主要局限于 Python 代码库，缺乏对其他编程语言的验证（受限于数据集）。
*   **真值偏差**：训练数据的真值定位目标来源于代码补丁，可能遗漏了虽未修改但对理解问题至关重要的上下文代码。
*   **任务范围**：仅关注代码定位任务，未涉及更广泛的代码库问答或通用搜索任务。
*   **闭源模型对比限制**：对于闭源模型，受限于API调用成本，部分实验限制了后端轮次，可能未完全发挥其潜力。

（完）

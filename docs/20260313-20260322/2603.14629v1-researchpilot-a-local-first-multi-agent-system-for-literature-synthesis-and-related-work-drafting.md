---
title: "ResearchPilot: A Local-First Multi-Agent System for Literature Synthesis and Related Work Drafting"
title_zh: ResearchPilot：一个用于文献综合与相关工作撰写的本地优先多智能体系统
authors: Peng Zhang
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14629v1"
tags: ["query:agent"]
score: 9.0
evidence: 文献检索与综合的多智能体系统
tldr: ResearchPilot是一个开源、本地优先的多智能体系统，旨在辅助文献综述与相关工作撰写。该系统接收自然语言研究问题，自动检索论文、提取结构化发现并综合跨论文模式，生成引用感知的草稿。其采用FastAPI与DSPy等技术栈，强调透明度与自托管能力，作为一项系统贡献，有效提升了研究效率。
selection_source: fresh_fetch
motivation: 旨在构建一个透明、可自托管且开源的系统，以辅助研究人员高效地进行文献综述和相关工作撰写。
method: 系统采用本地优先的多智能体架构，结合FastAPI、DSPy和Qdrant等技术，实现从论文检索、结构化提取到跨文献综合生成的全流程。
result: 通过自动化测试和端到端本地运行验证了系统的有效性，成功实现了从研究问题到相关工作草稿的自动生成。
conclusion: 该工作作为一项系统贡献，展示了构建透明研究助手的工程权衡与设计思路，同时也指出了语料库覆盖和引用验证等方面的局限性。
---

## 摘要
ResearchPilot 是一个开源、可自托管的多智能体文献综述辅助系统。给定一个自然语言研究问题，该系统能从 Semantic Scholar 和 arXiv 检索论文，从摘要中提取结构化发现，综合跨论文模式，并起草包含引用的相关工作章节。该系统采用本地优先架构，整合了 FastAPI、Next.js、DSPy、SQLite 和 Qdrant，支持自带密钥（bring-your-own-key）的模型访问以及远程或本地嵌入。本文介绍了系统设计、类型化智能体接口、持久化与历史搜索机制，以及构建透明研究助手所涉及的工程权衡。我们不宣称算法创新，而是将 ResearchPilot 作为一项系统贡献提出，并通过自动化测试和端到端本地运行对其进行评估。最后，我们讨论了系统的局限性，包括外部 API 速率限制、仅限摘要提取、语料库覆盖不全以及缺乏引用验证。

## Abstract
ResearchPilot is an open-source, self-hostable multi-agent system for literature-review assistance. Given a natural-language research question, it retrieves papers from Semantic Scholar and arXiv, extracts structured findings from paper abstracts, synthesizes cross-paper patterns, and drafts a citation-aware related-work section. The system combines FastAPI, Next.js, DSPy, SQLite, and Qdrant in a local-first architecture that supports bring-your-own-key model access and remote-or-local embeddings. This paper describes the system design, typed agent interfaces, persistence and history-search mechanisms, and the engineering tradeoffs involved in building a transparent research assistant. Rather than claiming algorithmic novelty, we present ResearchPilot as a systems contribution and evaluate it through automated tests and end-to-end local runs. We discuss limitations including external API rate limits, abstract-only extraction, incomplete corpus coverage, and the lack of citation verification.

---

## 论文详细总结（自动生成）

# 论文总结：ResearchPilot

## 1. 论文的核心问题与整体含义
*   **核心问题**：传统的文献综述过程耗时且繁琐，而现有的大语言模型（LLM）一次性提示方法在处理文献综述时存在明显缺陷，容易混淆检索与推理过程，且缺乏透明度，难以溯源验证。
*   **研究动机**：作者旨在构建一个透明、可检查、模块化的系统，将文献综述任务分解为显式的步骤，而非依赖“黑盒”式的生成。
*   **整体含义**：本研究提出了一种“本地优先”的开源架构，展示了如何通过系统设计而非单纯的算法改进，来提升AI辅助学术研究的透明度和可控性，为构建可信赖的研究助手提供了工程基线。

## 2. 论文提出的方法论
*   **核心思想**：将文献综述任务解耦为四个固定阶段的流水线，每个阶段由独立的智能体负责，具有明确的输入输出契约。
*   **系统架构**：采用全栈本地优先架构，前端使用 Next.js，后端使用 FastAPI，编排层使用 DSPy，持久化层结合 SQLite（存储报告）和 Qdrant（向量检索）。
*   **四阶段智能体流程**：
    1.  **SearchAgent（检索）**：异步查询 Semantic Scholar 和 arXiv，进行去重和容错处理（单源失败不影响整体），最终保留最多10篇论文。
    2.  **ExtractionAgent（提取）**：利用 DSPy 签名处理每篇论文摘要，提取结构化 JSON 数据（包含 claims, methods, datasets, results, limitations）。
    3.  **SynthesisAgent（综合）**：整合多篇论文的提取结果，分析跨论文的共识、矛盾和研究空白。
    4.  **WriterAgent（撰写）**：基于综合结果生成带有引用标签（如 [R1]）的 Markdown 格式相关工作草稿。
*   **接口与持久化**：设计了流式 API 推送生命周期事件，支持历史报告的语义搜索，并支持远程或本地（如 BAAI/bge-small-en-v1.5）嵌入模式。

## 3. 实验设计
*   **评估场景**：本文将 ResearchPilot 视为系统原型而非算法模型，因此未采用传统的基准测试。
*   **验证方法**：
    *   **自动化测试**：包含 11 个后端测试和 1 个前端测试，覆盖流式传输、错误处理、搜索逻辑、嵌入模式选择等核心功能。
    *   **端到端手动验证**：作者在本地部署环境中输入自然语言问题（如 "What are the recent trends in retrieval-augmented generation for question answering?"），观察系统运行状态。
*   **对比方法**：无。论文明确表示不进行算法性能对比，而是侧重于系统功能的正确性验证。

## 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长，因为该系统主要是一个推理和编排框架，不涉及模型训练。
*   **模型配置**：系统支持“自带密钥”模式。在评估中，作者使用了 Groq 托管的 `llama-3.3-70b-versatile` 模型作为 LLM 提供商，并使用本地嵌入模型 `BAAI/bge-small-en-v1.5`。

## 5. 实验数量与充分性
*   **实验数量**：实验数量较少。自动化测试主要验证代码逻辑；手动测试记录了一次成功的完整运行（耗时 12.47 秒，检索 10 篇论文）和几次因外部 API 限流而失败的运行。
*   **充分性与客观性**：作为系统原型的功能性验证是充分的，证明了架构的可行性。但作为学术评估，缺乏大规模的定量分析、与其他工具（如 ChatGPT 直接问答、LangChain 链式调用）的横向对比以及用户主观评分，客观性主要基于功能实现而非性能指标。

## 6. 论文的主要结论与发现
*   **系统可行性**：证明了文献综述可以通过透明的多阶段流水线实现，而非单一提示词，且中间产物可查、可追溯。
*   **工程挑战**：系统的稳定性受限于外部因素，如 Semantic Scholar 的 API 速率限制（HTTP 429）和 LLM 提供商的令牌限制，这表明构建鲁棒的研究助手需要完善的容错机制。
*   **架构优势**：DSPy 适合作为编排层处理类型化的智能体接口，支持通过配置切换模型提供商而无需重写提示词。

## 7. 优点
*   **透明度高**：将检索、提取、综合、撰写解耦，用户可以检查每个阶段的中间输出，解决了 LLM “黑盒”问题。
*   **本地优先与开源**：支持自托管，用户数据隐私得到保护，且不依赖单一中心化服务，支持多种模型后端。
*   **工程健壮性**：设计了细致的容错逻辑（如单源检索失败降级、JSON 解析防御、嵌入回退机制），提升了系统的鲁棒性。

## 8. 不足与局限
*   **数据源受限**：目前仅支持 Semantic Scholar 和 arXiv，缺乏对特定领域（如医学、工程）封闭数据库的支持。
*   **内容深度不足**：仅基于摘要进行提取，无法获取论文全文中的具体实验设置或量化结果，信息量有限。
*   **引用验证缺失**：生成的引用仅是标签匹配，系统并未验证引用内容的真实性，存在幻觉风险。
*   **评估局限**：缺乏与其他基线方法的对比实验和用户研究，无法量化评估生成内容的质量和实用性。

（完）

---
title: "When the Specification Emerges: Benchmarking Faithfulness Loss in Long-Horizon Coding Agents"
title_zh: 当规格说明涌现时：长视界编程智能体忠实度损失的基准测试
authors: "Lu Yan, Xuan Chen, Xiangyu Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17104v1"
tags: ["query:agent"]
score: 9.0
evidence: 长程代码智能体基准测试
tldr: 现有编码智能体基准通常预先提供完整任务规范，而真实研究编码往往通过交互逐步揭示目标。本文提出了一个新基准来研究涌现式规范下的忠实度损失（SLUMP），包含20篇ML论文和交互脚本。实验发现涌现式规范导致实现忠实度下降，作者提出的ProjectGuard工具能有效缓解这一问题，显著提升了代码实现的忠实度。
selection_source: fresh_fetch
motivation: 现有基准测试预先提供完整规范，无法反映真实研究编码中通过交互逐步揭示任务目标并需长期跟踪设计承诺的场景。
method: 构建了一个包含20篇ML论文和交互脚本的新基准，定义了涌现式规范下的忠实度损失（SLUMP），并引入ProjectGuard工具进行缓解研究。
result: "实验显示涌现式规范导致实现忠实度显著下降，而ProjectGuard在Claude Code上恢复了90%的忠实度差距并减少了严重失败。"
conclusion: 研究识别出规范跟踪是长视野编码智能体的重要评估目标，并证明外部状态层能有效缓解涌现式规范带来的忠实度损失。
---

## 摘要
当前的编程智能体基准测试通常预先提供完整的任务规格说明。真实的研究编程往往并非如此：目标系统通过交互逐步显现，要求智能体在长会话中跟踪持久的设计承诺。我们为此场景引入了一个基准测试，并研究了涌现式规格说明下的忠实度损失（SLUMP），定义为相对于单次规格说明对照，涌现式规格说明下最终实现忠实度的降低。该基准测试包含 20 篇近期的机器学习论文（10 篇 ICML 2025，10 篇 NeurIPS 2025）、371 个原子可验证组件，以及约 60 个编程请求的交互脚本，这些脚本逐步披露目标设计而不揭示论文本身。最终代码库使用五级组件忠实度评分标准进行评分，并附带暴露审计以验证评分组件可从可见交互中恢复。在 Claude Code 和 Codex 上的评估表明，单次规格说明对照分别在 16/20 和 14/20 篇论文上实现了更高的整体实现保真度。在涌现式规格说明下，两个平台的结构整合均出现退化，而语义忠实度损失在 Claude Code 上显著，在 Codex 上较小。作为缓解案例研究，我们引入了 ProjectGuard，这是一个用于规格说明跟踪的外部项目状态层。在 Claude Code 上，ProjectGuard 恢复了 90% 的忠实度差距，将完全忠实组件从 118 个增加到 181 个，并将严重失败从 72 个减少到 49 个。这些结果确定了规格说明跟踪是长视界编程智能体的一个独特评估目标。

## Abstract
Current coding-agent benchmarks usually pro- vide the full task specification upfront. Real research coding often does not: the intended system is progressively disclosed through in- teraction, requiring the agent to track durable design commitments across a long session. We introduce a benchmark for this setting and study faithfulne Ss Loss U nder eM ergent s Pecification (SLUMP), defined as the reduc- tion in final implementation faithfulness un- der emergent specification relative to a single- shot specification control. The benchmark con- tains 20 recent ML papers (10 ICML 2025, 10 NeurIPS 2025), 371 atomic verifiable compo- nents, and interaction scripts of approximately 60 coding requests that progressively disclose the target design without revealing the paper itself. Final repositories are scored with a five-level component-faithfulness rubric and accompanied by an exposure audit to verify that scored components are recoverable from the visible interaction. Evaluated on Claude Code and Codex, the single-shot specification control achieves higher overall implementation fidelity on 16/20 and 14/20 papers, respectively. Structural integration degrades under emergent specification on both platforms, while seman- tic faithfulness loss is substantial on Claude Code and small on Codex. As a mitigation case study, we introduce ProjectGuard, an exter- nal project-state layer for specification tracking. On Claude Code, ProjectGuard recovers 90% of the faithfulness gap, increases fully faith- ful components from 118 to 181, and reduces severe failures from 72 to 49. These results identify specification tracking as a distinct eval- uation target for long-horizon coding agents.

---

## 论文详细总结（自动生成）

以下是对论文《When the Specification Emerges: Benchmarking Faithfulness Loss in Long-Horizon Coding Agents》的详细总结：

### 1. 论文的核心问题与整体含义

*   **研究背景**：现有的编程智能体基准测试（如 PaperBench, MLR-Bench）通常假设任务规格说明在会话开始时就已经完整提供。然而，真实的研究编程流程并非如此，目标系统往往是在长期的交互过程中逐步显现和确定的。
*   **核心问题**：在“涌现式规格说明”的场景下，智能体需要在长会话中跟踪持久的设计承诺。论文旨在探究：当任务规格说明不再是“一次性给予”，而是“逐步涌现”时，编程智能体的实现忠实度会受到何种影响？
*   **整体含义**：论文定义了 **SLUMP**（涌现式规格说明下的忠实度损失）这一现象，即相对于一次性获得完整规格说明，渐进式披露目标设计会导致最终实现的忠实度下降。这揭示了当前长视界编程智能体在“规格跟踪”能力上的短板。

### 2. 论文提出的方法论

论文主要提出了一个基准测试方法以及一个缓解工具。

#### 核心思想
*   **SLUMP 基准测试**：构建一个模拟真实研究流程的基准，通过多轮交互脚本逐步披露设计目标，而非直接提供论文，以此衡量智能体在长程交互中的忠实度损失。
*   **ProjectGuard 工具**：引入一个外部项目状态层，通过维护语义和结构状态来辅助智能体跟踪设计承诺，缓解 SLUMP 现象。

#### 关键技术细节与流程
*   **基准构建流程**：
    1.  **规范规格说明提取**：从源论文 PDF 中提取详细的实现计划、数学公式和设计决策。
    2.  **原子组件分解**：将规格说明分解为 371 个可独立验证的原子组件（如特定函数、模块连接方式）。
    3.  **潜在轨迹生成**：模拟研究过程，生成 10-20 个中间版本，从模糊的想法逐步收敛到最终设计。
    4.  **交互脚本合成**：将轨迹转化为约 60 个自然语言编程请求，确保智能体只能通过交互推断目标，而非直接看到论文。
*   **评估指标**：
    *   **MCF (Mean Component Faithfulness)**：衡量语义忠实度，即代码是否实现了预期的算法内容。评分标准为 0-4 分（Absent, Wrong, Simplified, Equivalent, Faithful）。
    *   **DIR (Dependency Integration Ratio)**：衡量结构整合度，即后期代码是否复用了前期模块，而非绕过或重写。
    *   **IF50**：综合 MCF 和 DIR 的主要端点指标。
    *   **SLUMP 定义公式**：$SLUMP_F(p, a) = F(R^{ss}_{p,a}) - F(R^{em}_{p,a})$，即单次规格说明得分减去涌现式规格说明得分。
*   **ProjectGuard 机制**：
    *   **语义状态**：记录设计承诺、决策依据等持久知识。
    *   **结构状态**：记录代码库的文件结构、接口关系。
    *   **兼容性感知预测**：在每次编码前，结合历史请求和外部状态生成项目简报，提示智能体注意兼容性和复用。

### 3. 实验设计

*   **数据集**：包含 20 篇最近的机器学习论文（10 篇 ICML 2025，10 篇 NeurIPS 2025），涵盖优化算法、几何深度学习、扩散模型等多个领域。
*   **Benchmark 设置**：
    *   **涌现式条件**：智能体接收多轮交互脚本，需自行推断最终设计。
    *   **单次规格说明对照**：智能体一次性接收完整的论文 PDF。
*   **对比方法与平台**：
    *   **平台**：Claude Code 和 Codex 两个商业长视界编程系统。
    *   **对比设置**：主要对比涌现式条件与单次对照条件的表现差异，以及引入 ProjectGuard 前后的差异。
*   **公平性验证**：进行了“暴露审计”，验证评分组件确实可以从可见的交互脚本中推断出来，确保基准测试的公平性。

### 4. 资源与算力

*   **算力说明**：论文中未明确提及具体的 GPU 型号、数量或训练时长。由于该研究主要涉及基准测试构建和智能体推理评估，主要消耗的是 LLM API 调用资源（用于构建基准、运行智能体和自动评分），而非传统的模型训练算力。

### 5. 实验数量与充分性

*   **实验规模**：共进行了 80 次运行（20 篇论文 × 2 个平台 × 2 种条件），加上 ProjectGuard 的消融实验。
*   **充分性与客观性**：
    *   **评分校准**：使用了自动评分，并与两位独立的人类标注员进行了校准，加权 Cohen’s κ 达到 0.882-0.927，一致性较高。
    *   **审计机制**：引入暴露审计排除了不可解任务，确保了评估的客观性。
    *   **多维度评估**：不仅看最终代码是否通过测试（测试通过率与 IF50 相关性低），而是深入到组件级忠实度和结构整合度，实验设计较为充分且细致。

### 6. 论文的主要结论与发现

*   **SLUMP 现象普遍存在**：在 Claude Code 上，单次规格说明对照在 16/20 篇论文上表现更好；在 Codex 上，这一数字为 14/20。这证明渐进式披露确实会带来显著的忠实度损失。
*   **损失来源分析**：
    *   **结构整合退化**：两个平台在涌现式条件下 DIR 指标均显著下降，说明智能体容易遗忘或绕过早期模块。
    *   **语义忠实度差异**：Claude Code 在涌现式条件下语义忠实度（MCF）下降明显，而 Codex 的 MCF 变化较小。
*   **缓解方案有效**：ProjectGuard 在 Claude Code 上恢复了 90% 的忠实度差距，将完全忠实的组件数量增加了 53%（118 -> 181），严重失败减少了 32%。

### 7. 优点

*   **场景真实**：打破了传统基准“一次性给全文档”的假设，更贴近真实的研究编程场景（需求逐步明确）。
*   **评估维度深入**：区分了“语义忠实度”（代码对不对）和“结构整合度”（代码是否复用），精准诊断了智能体在长程任务中的短板。
*   **方法论严谨**：通过“暴露审计”确保了基准测试的合理性，通过人工校准验证了自动评分的可靠性。
*   **解决方案轻量**：ProjectGuard 作为一个外部状态层，无需修改底层模型即可显著提升性能，具有很高的实用价值。

### 8. 不足与局限

*   **样本范围**：仅包含 20 篇 ML 论文，且集中在具有较强数学结构的方法实现上，可能无法完全代表所有软件工程任务或其他领域的研究。
*   **合成轨迹**：交互脚本是合成的，虽然设计上模拟了研究过程，但可能无法完全复现真实用户的行为模式（如模糊性、修正风格）。
*   **端点测量**：SLUMP 定义为最终结果的差距，未深入测量会话内部的时间漂移机制。
*   **工具局限**：ProjectGuard 目前仅作为案例研究，未与广泛的记忆管理基线进行对比，且其组件的必要性尚未通过消融实验完全验证。

（完）

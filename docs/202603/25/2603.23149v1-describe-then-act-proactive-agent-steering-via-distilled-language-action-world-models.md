---
title: "Describe-Then-Act: Proactive Agent Steering via Distilled Language-Action World Models"
title_zh: 先描述后行动：基于蒸馏语言-动作世界模型的主动智能体引导
authors: "Massimiliano Pappa, Luca Romani, Valentino Sacco, Alessio Palma, Stéphane Lathuilière, Fabio Galasso, Xavier Alameda-Pineda, Indro Spinelli"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23149v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 用于智能体引导的蒸馏语言-动作世界模型
tldr: 针对现有视觉世界模型延迟过高的问题，本文提出DILLO模型，将范式从“模拟-然后-行动”转变为“描述-然后-行动”。该方法通过跨模态蒸馏训练大语言模型预测动作结果，绕过了繁重的视觉生成过程。实验表明，该方法在实现14倍推理加速的同时，显著提升了智能体的任务成功率。
selection_source: fresh_fetch
motivation: 现有基于视觉模拟的世界模型延迟过高，难以满足安全关键型智能体实时预见后果的需求。
method: 提出DILLO模型，利用跨模态蒸馏训练潜在条件大语言模型，通过纯文本推理预测行动后果以引导策略。
result: 实验表明该方法相比基线实现了14倍推理加速，并将任务平均成功率提升了9.3个百分点。
conclusion: 视觉模拟对于故障预防并非必要，利用策略潜在状态进行语言预测是一种高效且有效的智能体引导方案。
---

## 摘要
部署安全攸关智能体要求在动作执行前预判其后果。尽管世界模型为这种前瞻性预见提供了一种范式，但当前依赖视觉仿真的方法会产生极高的延迟，每步往往耗时数秒。在本文中，我们挑战了“视觉处理对于失败预防必不可少”这一假设。我们表明，经训练的策略之潜在状态结合其计划动作，已编码了足以预判动作结果的信息，从而使得视觉仿真在失败预防中变得冗余。为此，我们提出了 DILLO（蒸馏语言-动作世界模型），这是一个快速引导层，将范式从“先仿真后行动”转变为“先描述后行动”。DILLO 通过跨模态蒸馏进行训练，其中拥有特权的视觉语言模型教师对离线轨迹进行标注，而以潜在状态为条件的大语言模型学生学习预测语义结果。这构建了一条纯文本推理路径，完全绕过了繁重的视觉生成，相比基线实现了 14 倍的加速。在 MetaWorld 和 LIBERO 上的实验表明，DILLO 能生成高保真的下一状态描述，并能够引导策略，使回合成功率最高提升 15 个百分点，任务平均提升 9.3 个百分点。

## Abstract
Deploying safety-critical agents requires anticipating the consequences of actions before they are executed. While world models offer a paradigm for this proactive foresight, current approaches relying on visual simulation incur prohibitive latencies, often exceeding several seconds per step. In this work, we challenge the assumption that visual processing is necessary for failure prevention. We show that a trained policy's latent state, combined with its planned actions, already encodes sufficient information to anticipate action outcomes, making visual simulation redundant for failure prevention. To this end, we introduce DILLO (DIstiLLed Language-ActiOn World Model), a fast steering layer that shifts the paradigm from "simulate-then-act" to "describe-then-act." DILLO is trained via cross-modal distillation, where a privileged Vision Language Model teacher annotates offline trajectories and a latent-conditioned Large Language Model student learns to predict semantic outcomes. This creates a text-only inference path, bypassing heavy visual generation entirely, achieving a 14x speedup over baselines. Experiments on MetaWorld and LIBERO demonstrate that DILLO produces high-fidelity descriptions of the next state and is able to steer the policy, improving episode success rate by up to 15 pp and 9.3 pp on average across tasks.

---

## 论文详细总结（自动生成）

# 论文总结：先描述后行动：基于蒸馏语言-动作世界模型的主动智能体引导

## 1. 论文的核心问题与整体含义
*   **研究背景**：在安全攸关的智能体部署中（如机器人操控），智能体需要在执行动作前预见后果以防止失败。现有的“世界模型”虽然能模拟未来状态，但依赖视觉仿真的方法计算开销巨大，单步推理延迟往往高达数秒，无法满足实时控制的需求。
*   **核心问题**：如何在不依赖高延迟视觉仿真的前提下，实现实时、准确的动作后果预测与失败预防？
*   **整体含义**：本文挑战了“视觉处理对于失败预防必不可少”的传统假设，提出了“潜在充分性假设”。该假设认为，训练好的策略其内部潜在状态已编码了任务关键特征，足以预测动作结果。基于此，论文将范式从“模拟-然后-行动”转变为“描述-然后-行动”，实现了无需视觉生成的实时安全引导。

## 2. 论文提出的方法论
*   **核心思想**：提出 DILLO（Distilled Language-Action World Model），通过跨模态知识蒸馏，将拥有特权的视觉语言模型（VLM）教师的语义预见能力，迁移到一个轻量级的、以潜在状态为条件的大语言模型（LLM）学生模型中。
*   **关键技术细节**：
    *   **潜在充分性假设**：数学上表达为 $I(o_{t:t+k}; y_{t+k}) \approx I(z_t; y_{t+k})$，即策略潜在状态 $z_t$ 与未来语义结果 $y_{t+k}$ 的互信息近似等于完整观测历史与结果的互信息。
    *   **教师模型**：利用仿真器特权信息（视觉上下文、几何增量、结果信号）标注离线轨迹，生成自然语言描述 $d_T$ 和二元判决 $c_T$（Positive/Negative）。
    *   **学生模型**：基于 Gemma 架构，输入为策略潜在状态 $z_t$ 和动作序列 $a_{t:t+k}$，通过投影器映射到 LLM 嵌入空间，输出预测描述和判决。
    *   **训练流程**：采用三阶段课程学习。
        1.  **投影对齐**：冻结 LLM，仅训练线性投影器。
        2.  **描述蒸馏**：引入 LoRA 微调，优化描述生成损失 $L_{Desc}$。
        3.  **判决优化**：联合优化描述损失和判决分类损失 $L_{Total} = L_{Desc} + \lambda \cdot L_{CE}$。
    *   **推理机制**：潜在拒绝采样。策略生成 $N$ 个候选动作，DILLO 在无视觉输入的情况下快速评估，拒绝负面动作，选择正面动作执行。

## 3. 实验设计
*   **数据集/场景**：
    *   **MetaWorld**：包含 Soccer、Sweep-Into、Drawer-Open 三个单任务操控场景，基于强化学习策略。
    *   **LIBERO**：包含 Goal、Object、Spatial、10、90 五个多任务/长序列场景，基于模仿学习策略。
*   **Benchmark**：主要对比基线包括 Zero-Shot (ZS)、Few-Shot (FS)、Few-Shot Captioning (CAP) 以及基于视觉仿真的 Forewarn 方法。
*   **对比方法**：
    *   **VLM 基线**：直接使用 Gemma-3-1B/4B 模型，输入当前观测或未来观测进行预测。
    *   **Forewarn**：作为延迟对比的基线，代表传统的视觉仿真方法。

## 4. 资源与算力
*   **推理硬件**：使用 NVIDIA RTX 3090（消费级显卡）进行 DILLO 的推理延迟测试；对比基线 Forewarn 使用 NVIDIA RTX A6000（企业级显卡）。
*   **数据收集**：致谢中提到使用 NVIDIA DGX Spark 进行数据收集和仿真。
*   **训练细节**：论文正文未明确说明具体的训练时长、GPU 数量或显存占用细节。

## 5. 实验数量与充分性
*   **实验数量**：
    *   在 2 个仿真环境、8 个具体任务上进行了测试。
    *   对比了 2 种模型规模（1B 和 4B 参数）。
    *   进行了多维度评估：文本保真度（T2O）、语义评分、成功率（ES）、判决准确率、推理延迟。
    *   包含消融实验（对比不同基线）和人类/AI 评估研究。
*   **充分性与客观性**：
    *   实验设计较为充分，涵盖了单任务与多任务、RL 与模仿学习策略。
    *   对比公平，使用了同规模的 LLM/VLM 作为基线，且在更低的硬件配置下展示了优越的性能。
    *   引入了人类验证研究，证实了模型生成的描述具有可解释性和准确性。

## 6. 论文的主要结论与发现
*   **潜在状态优于像素**：实验证明，DILLO-4B 仅利用潜在状态的预测效果甚至优于拥有未来视觉信息的 CAP 基线，验证了潜在充分性假设。
*   **显著的性能提升**：DILLO 实现了约 14 倍的推理加速（0.26s vs 3.7s），使得实时引导成为可能。
*   **成功率提高**：在 MetaWorld 和 LIBERO 上，DILLO 引导的策略平均成功率提升了 9.3 个百分点，最高提升 15 个百分点。
*   **判决准确性**：在无视觉输入的情况下，DILLO-4B 的二元判决准确率达到 91.4%。

## 7. 优点
*   **高效性**：完全绕过了视觉生成过程，极大降低了计算延迟，适合实时控制回路。
*   **可解释性**：输出自然语言描述，不仅提供二元判决，还能向人类监督者解释预期发生的物理交互。
*   **通用性**：作为控制器无关的安全层，可叠加在任何现有策略之上，无需修改底层策略架构。

## 8. 不足与局限
*   **架构依赖**：当更换新的策略架构时，需要重新对齐投影器，因为潜在空间是架构特定的。
*   **数据依赖**：教师模型依赖仿真器的特权信息（如 6DoF 姿态）进行标注，真实世界部署需依赖姿态估计算法，可能引入感知噪声。
*   **极端多样性限制**：在包含 90 个不同任务的 LIBERO-90 套件中，单一模型的蒸馏效果受限，表现略低于基线，提示需要更大规模的数据收集。

（完）

---
title: "Probing the Latent World: Emergent Discrete Symbols and Physical Structure in Latent Representations"
title_zh: 探测潜在世界：潜在表示中的涌现离散符号与物理结构
authors: Liu hung ming
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20327v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 使用JEPA探测视频世界模型
tldr: 针对视频世界模型V-JEPA存在的结构可解释性差距，本文提出了AI Mother Tongue (AIM)框架。该框架作为一种被动量化探针，在不修改编码器的前提下，将连续潜在向量转化为离散符号序列。实验揭示了冻结的JEPA潜在空间中存在可发现的结构化符号流形，验证了模型对物理结构的内在表征能力。
selection_source: fresh_fetch
motivation: 视频世界模型V-JEPA消除了视觉验证路径，导致其编码器学习到的物理结构难以被检查，存在结构可解释性差距。
method: 提出AI Mother Tongue (AIM)框架，作为一种轻量级、无词汇表的被动量化探针，将冻结的V-JEPA 2连续潜在向量转换为离散符号序列。
result: 实验显示AIM符号分布在物理维度上差异显著，揭示了V-JEPA 2潜在空间具有紧凑性，语义差异表现为分级分布变化。
conclusion: 研究证明了结构化符号流形是冻结JEPA潜在空间的可发现属性，开启了构建动作条件符号世界模型的路线图。
---

## 摘要
基于联合嵌入预测架构（JEPA）训练的视频世界模型通过在潜在空间中预测掩码区域而非重建像素，获取了丰富的时空表示。这移除了生成模型的视觉验证路径，造成了结构可解释性鸿沟：编码器学习到了无法以任何可检查形式访问的物理结构。现有的探测方法要么在没有结构化中间层的连续空间中运作，要么附加了生成组件，其参数混淆了对编码器行为的归因。我们提出AI母语（AIM）框架作为一种被动量化探针：一种轻量级、无词表的探针，无需特定任务的监督或修改编码器，即可将V-JEPA 2连续潜在向量转换为离散符号序列。由于编码器保持完全冻结，AIM码本中的任何符号结构都完全归因于V-JEPA 2的预训练表示——而非探针。我们在Kinetics-mini数据集上通过类别对比实验，沿三个物理维度进行评估：抓取角度、物体几何形状和运动时间结构。AIM符号分布在所有三个实验中均表现出显著差异（卡方检验 p < 10^{-4}；互信息 MI 0.036--0.117 bits，归一化互信息 NMI 占3比特最大值的1.2--3.9%；JS散度 JSD 高达0.342；码本活跃率 62.5%）。实验表明，V-JEPA 2潜在空间显著紧凑：多样化的动作类别共享一个共同的表示核心，语义差异被编码为分级分布变化而非类别边界。这些结果确立了通向动作条件符号世界模型的四阶段路线图的第一阶段，证明了结构化符号流形是冻结JEPA潜在空间的可发现属性。

## Abstract
Video world models trained with Joint Embedding Predictive Architectures (JEPA) acquire rich spatiotemporal representations by predicting masked regions in latent space rather than reconstructing pixels. This removes the visual verification pathway of generative models, creating a structural interpretability gap: the encoder has learned physical structure inaccessible in any inspectable form. Existing probing methods either operate in continuous space without a structured intermediate layer, or attach generative components whose parameters confound attribution of behavior to the encoder.   We propose the AI Mother Tongue (AIM) framework as a passive quantization probe: a lightweight, vocabulary-free probe that converts V-JEPA 2 continuous latent vectors into discrete symbol sequences without task-specific supervision or modifying the encoder. Because the encoder is kept completely frozen, any symbolic structure in the AIM codebook is attributable entirely to V-JEPA 2 pre-trained representations -- not to the probe.   We evaluate through category-contrast experiments on Kinetics-mini along three physical dimensions: grasp angle, object geometry, and motion temporal structure. AIM symbol distributions differ significantly across all three experiments (chi^2 p < 10^{-4}; MI 0.036--0.117 bits, NMI 1.2--3.9% of the 3-bit maximum; JSD up to 0.342; codebook active ratio 62.5%). The experiments reveal that V-JEPA 2 latent space is markedly compact: diverse action categories share a common representational core, with semantic differences encoded as graded distributional variations rather than categorical boundaries. These results establish Stage 1 of a four-stage roadmap toward an action-conditioned symbolic world model, demonstrating that structured symbolic manifolds are discoverable properties of frozen JEPA latent spaces.

---

## 论文详细总结（自动生成）

以下是对论文《Probing the Latent World: Emergent Discrete Symbols and Physical Structure in Latent Representations》的详细总结：

# 1. 论文的核心问题与整体含义

*   **研究背景**：基于联合嵌入预测架构（JEPA）的视频世界模型（如V-JEPA 2）通过在潜在空间预测掩码区域而非重建像素来学习表示。这种设计虽然提升了性能，但也导致了“结构可解释性鸿沟”：由于缺乏像素重建路径，研究者无法直观地检查编码器内部学习到的物理结构。
*   **现有局限**：现有的探测方法存在缺陷。判别式探针（如线性分类器）不提供结构化的中间表示；生成式探针（如语言模型头）引入了自身参数，导致“归因问题”——难以区分观察到的行为是源于编码器的表示还是探针自身的学习能力。
*   **核心问题**：冻结的V-JEPA 2潜在空间中是否已经存在结构化的流形，能够被一个无词汇表的离散探针检测并进行统计表征？

# 2. 论文提出的方法论

*   **核心思想**：提出“被动量化探针”方法。将AI Mother Tongue (AIM) 框架作为一个轻量级的向量量化（VQ）模块，附加在完全冻结的V-JEPA 2编码器之后。该方法不修改编码器，不使用预定义符号或任务监督，将连续潜在向量转化为离散符号序列。
*   **架构设计**：
    *   **Layer 1（潜在模型层）**：冻结的V-JEPA 2 ViT-L编码器，输入视频 $x$ 映射为连续潜在向量 $z$。
    *   **Layer 2（离散语义层）**：AIM量化器，将 $z$ 映射为离散符号 $s$。
    *   **Layer 3（语言接口层）**：将符号序列转化为自然语言（本文未涉及，仅作为未来规划）。
*   **关键技术细节与流程**：
    1.  **投影与归一化**：针对V-JEPA 2输出向量范数极大且均匀（~97.7）导致直接量化会崩溃的问题，设计了线性投影层（1024→256维），接LayerNorm和L2归一化，将向量映射到单位超球面。
    2.  **最近邻分配**：计算投影向量与码本向量的距离，分配最近的码本索引。
        *   公式：$s(i) = \arg\min_k \| \hat{z}(i) - c_k \|_2$
    3.  **直通估计器（STE）**：前向传播使用码本向量，反向传播梯度直接传给投影层，绕过不可导的argmin操作。
    4.  **EMA码本更新**：码本向量通过指数移动平均（EMA）更新，而非梯度下降。设定衰减率 $\gamma=0.90$ 以快速适应潜在空间分布。
    5.  **死码重置**：定期重置使用率过低的码本条目，防止码本崩溃。

# 3. 实验设计

*   **数据集**：使用 Kinetics-mini 验证集，选取5个动作类别，构建了3个对比组：
    *   **抓取角度**：射箭 vs 保龄球。
    *   **物体几何形状**：放风筝 vs 跳高。
    *   **运动时间结构**：行进（周期性） vs 射箭（非周期性）。
*   **评估指标**：
    *   **H1 符号稳定性**：验证管道的确定性（同一视频多次运行应产生相同符号）。
    *   **H2 统计显著性**：
        *   **卡方检验（$\chi^2$）**：检验符号分布是否独立于物理条件。
        *   **互信息（MI）**：测量符号与条件标签的相关性。
        *   **JS散度（JSD）**：测量不同条件下符号分布的距离。
    *   **码本健康度**：活跃率和困惑度。
*   **对比方法**：主要与“随机基线”（高斯噪声输入）对比，验证符号结构非随机产生；同时定性对比了现有探测方法（判别式/生成式探针）的归因缺陷。

# 4. 资源与算力

*   **算力信息缺失**：论文未明确说明使用的GPU型号、数量或具体训练时长。
*   **计算规模**：实验基于预计算的潜在向量（75,264个token向量），量化器训练步数为3000步，批量大小为64。由于编码器冻结，计算开销相对较小。

# 5. 实验数量与充分性

*   **实验数量**：
    *   **主实验**：3组类别对比实验（抓取、几何、时间）。
    *   **消融实验**：对量化器超参数（EMA衰减率 $\gamma$ 和承诺损失系数 $\beta$）进行了小规模消融（测试了3种配置）。
    *   **基线测试**：高斯噪声基线测试。
*   **充分性与客观性评价**：
    *   作为“第一阶段”的诊断性研究，实验设计充分验证了架构兼容性和潜在空间的结构性。
    *   **局限性**：数据集规模较小（仅48个视频用于训练），且类别对的选择存在混淆因素（如射箭与保龄球不仅抓取角度不同，场景也不同）。作者明确承认了“类别代理策略”的局限，指出无法隔离单一物理变量的影响。

# 6. 论文的主要结论与发现

*   **架构兼容性**：AIM探针成功附加在冻结的V-JEPA 2上，无需修改源码，训练稳定且码本健康（活跃率62.5%）。
*   **统计显著性**：在所有三个物理维度的对比实验中，符号分布均表现出显著差异（$p < 10^{-4}$），证明了潜在空间包含物理结构信息。
*   **潜在空间的紧凑性**：V-JEPA 2的潜在空间非常紧凑。不同动作类别主要映射到同一个主导符号（#5），语义差异表现为次级符号分布的微小变化（分级分布变化），而非类别边界。这符合世界模型学习共享物理规律（如重力、运动学）的预期。
*   **时间维度的敏感性**：运动时间结构的对比（行进vs射箭）产生了最强的信号（JSD最高），这与V-JEPA 2的时间预测目标一致。

# 7. 优点

*   **方法论创新**：提出了“被动探针”概念，通过冻结编码器解决了生成式探针的“归因问题”，确保观察到的结构完全源于模型本身。
*   **无监督与无词汇表**：探针不需要任何语义标签或预定义词汇，符号完全涌现，避免了探针引入先验知识。
*   **统计可审计性**：将连续潜在向量转化为离散符号，使得可以使用标准的统计检验（卡方、MI）来量化物理变量的编码情况，提供了可审计的接口。
*   **深刻洞察**：不仅验证了结构存在，还揭示了V-JEPA 2潜在空间的“紧凑性”特征，解释了为何不同类别共享主导符号。

# 8. 不足与局限

*   **类别代理策略的混淆因素**：实验使用动作类别对作为物理变量的代理，无法隔离单一物理变量（如抓取角度）的影响，混淆了场景、物体材质等其他因素。
*   **数据规模小**：仅使用了Kinetics-mini的少量样本，统计效力受限，且无法进行细粒度的类内分析。
*   **码本分辨率限制**：$K=8$的码本较小，导致“主导符号碰撞”现象，可能掩盖了更细微的语义差异。
*   **因果性未确立**：实验仅建立了统计关联，未证明模型具有因果理解能力。
*   **算力信息缺失**：论文未提供具体的计算资源消耗数据。

（完）

---
title: "EVA: Aligning Video World Models with Executable Robot Actions via Inverse Dynamics Rewards"
title_zh: EVA：通过逆向动力学奖励将视频世界模型与可执行机器人动作对齐
authors: "Ruixiang Wang, Qingming Liu, Yueci Deng, Guiliang Liu, Zhen Liu, Kui Jia"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17808v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 机器人视频世界模型与逆动力学
tldr: 针对视频世界模型生成的视频可能违反物理约束导致动作不可执行的问题，本文提出EVA框架。该方法利用逆动力学模型构建奖励信号，通过强化学习后训练对齐视频生成与可执行动作，有效减少了物理不一致的伪影，并在RoboTwin基准和真实机器人实验中显著提升了任务执行成功率。
selection_source: fresh_fetch
motivation: 现有视频世界模型生成的视频可能违反物理约束，导致解码出的机器人动作不可执行，存在“可执行性差距”。
method: 提出EVA强化学习后训练框架，将逆动力学模型转化为奖励模型，以此评估生成视频并惩罚不可执行的动作序列。
result: 在RoboTwin基准和真实双臂机器人上的实验表明，该方法减少了生成视频中的具身伪影并提高了任务执行成功率。
conclusion: EVA通过将可执行性差距转化为训练信号，有效对齐了视频生成与物理执行，提升了机器人任务的稳定性与成功率。
---

## 摘要
视频生成模型正日益被用作机器人领域的世界模型，此类模型基于当前观测和任务指令生成未来的视觉推演，随后逆向动力学模型（IDM）将生成的帧转换为可执行的机器人动作。然而，当前的视频世界模型缺乏显式的可执行性约束。因此，视觉上连贯的推演仍可能违背刚体与运动学一致性，导致经IDM解码后产生不稳定或不可行的控制指令。我们将这种视觉生成与物理可执行控制之间的不匹配称为“可执行性鸿沟”。虽然可以通过拒绝采样等技术缓解推理时的这一鸿沟，但由于视频生成的高成本，此类方法效率低下。在本文中，我们利用可执行性鸿沟作为训练信号，提出了可执行视频对齐（EVA），这是一个用于对齐视频世界模型的强化学习后训练框架。EVA在真实机器人轨迹上训练逆向动力学模型，并将其重新用作奖励模型，通过生成的视频所诱导的动作序列来评估这些视频，鼓励以速度、加速度和加加速度衡量的平滑运动，同时惩罚违背具身约束的动作。重要的是，即使生成的视频包含严重的视觉伪影，该奖励仍具有信息量，因为这些伪影通常会转化为不稳定或越界的动作。在RoboTwin基准测试和真实双臂机器人上的实验表明，EVA减少了生成推演中具身特定的伪影，并提高了下游任务执行的成功率。

## Abstract
Video generative models are increasingly used as world models for robotics, where a model generates a future visual rollout conditioned on the current observation and task instruction, and an inverse dynamics model (IDM) converts the generated frames into executable robot actions. However, current video world models lack explicit executability constraints. As a result, visually coherent rollouts may still violate rigid-body and kinematic consistency, producing unstable or infeasible control commands when decoded by an IDM. We refer to this mismatch between visual generation and physically executable control as the executability gap. While this gap can be mitigated at inference time using techniques such as rejection sampling, such approaches are inefficient due to the high cost of video generation. In this paper, we leverage the executability gap as a training signal and introduce Executable Video Alignment (EVA), a reinforcement-learning post-training framework for aligning video world models. EVA trains an inverse dynamics model on real robot trajectories and repurposes it as a reward model that evaluates generated videos through the action sequences they induce, encouraging smooth motions measured by velocity, acceleration, and jerk while penalizing actions that violate embodiment constraints. Importantly, the reward remains informative even when generated videos contain severe visual artifacts, since such artifacts typically translate into unstable or out-of-bound actions. Experiments on the RoboTwin benchmark and a real bimanual robot show that EVA reduces embodiment-specific artifacts in generated rollouts and improves downstream task execution success.

---

## 论文详细总结（自动生成）

# 论文总结：EVA: Aligning Video World Models with Executable Robot Actions via Inverse Dynamics Rewards

## 1. 论文的核心问题与整体含义
*   **研究背景**：视频生成模型正逐渐被用作机器人的世界模型，其流程通常是由视频模型生成未来视觉轨迹，再通过逆向动力学模型（IDM）将其解码为可执行的机器人动作。
*   **核心问题**：现有的视频世界模型缺乏显式的物理可执行性约束。生成的视频虽然在视觉上连贯，但可能违反刚体运动学一致性（如机械臂变形、关节突变），导致IDM解码出的动作不可行或不稳定。
*   **概念定义**：作者将这种视觉生成与物理可执行控制之间的不匹配定义为“可执行性鸿沟”。
*   **研究动机**：虽然可以通过推理时的拒绝采样来缓解此问题，但视频生成成本高昂，效率极低。因此，作者提出利用该鸿沟作为训练信号，通过强化学习后训练来对齐视频生成模型。

## 2. 论文提出的方法论
*   **框架名称**：EVA (Executable Video Alignment)，一种基于强化学习的后训练框架。
*   **核心思想**：训练一个IDM作为奖励模型，评估生成视频所诱导的动作序列的物理可行性与平滑度，以此微调视频生成模型。
*   **关键技术细节**：
    *   **逆向动力学模型 (IDM)**：在真实机器人轨迹上训练，输入为视频帧窗口，输出为关节动作。架构采用卷积骨干网络 + 空间Softmax层 + MLP，以提取稳定的二维关键点特征。
    *   **奖励函数构建**：
        *   给定生成视频 $V$，冻结的IDM预测动作序列 $A$。
        *   计算关节空间的运动学指标：速度 $v_t$、加速度 $\alpha_t$ 和加加速度 $j_t$。
        *   **平滑度惩罚**：使用Huber损失惩罚过大的加速度和加加速度，公式为 $P_\alpha = E_t[\text{Huber}(\alpha_t)]$ 和 $P_j = E_t[\text{Huber}(j_t)]$。
        *   **边界惩罚**：惩罚超出机器人速度和加速度限制的动作，公式为 $P_{vel} = E_t[\|\max(|v_t| - v_{max}, 0)\|^2]$。
        *   **最终奖励**：将总惩罚 $P(A)$ 映射为有界奖励 $R(V) = (1 + P(A)/P_0)^{-\gamma}$。
*   **优化算法**：采用 Group Relative Policy Optimization (GRPO) 算法。针对Flow-matching模型，通过引入扩散项将确定性ODE转化为随机SDE以定义策略分布，利用LoRA进行参数高效微调。

## 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：RoboTwin 2.0 双臂操作基准，包含21个多样化任务。
    *   **真实环境**：Agilex CobotMagic 双臂机器人平台，包含5个已见任务和5个分布外（OOD）任务。
*   **Benchmark**：RoboTwin 2.0 基准测试。
*   **对比方法**：
    *   **视频生成质量**：对比 Vidar (Wan2.2)。
    *   **策略执行性能**：对比 ACT, Diffusion Policy (DP), RDT, $\pi_0$, GE-Act。
    *   **消融对比**：对比未经过RL对齐的基线模型 EVA (w/o RL)。

## 4. 资源与算力
*   **硬件配置**：8 张 NVIDIA A800 GPUs。
*   **训练时长与设置**：
    *   **SFT阶段**：Batch size为32，仿真数据训练约4500步，真实数据训练约2580步。
    *   **GRPO阶段**：仿真环境训练约136步（耗时约6天），真实环境训练约46步（耗时约3天）。使用LoRA (rank 32) 进行微调。

## 5. 实验数量与充分性
*   **实验规模**：
    *   **视觉质量评估**：在210个操作提示词上进行人工评分，涵盖运动学合理性、交互合理性等4个维度。
    *   **仿真实验**：21个任务，每任务20次试验。
    *   **真机实验**：10个任务（含OOD），每任务20次试验。
    *   **消融实验**：包含IDM架构消融（Spatial Softmax的作用）和奖励有效性分析。
*   **充分性评价**：实验设计较为全面，涵盖了仿真与真实环境、已见与未见任务。对比了主流的VLA方法和视频世界模型方法，并详细分析了奖励信号与视觉质量及执行成功率的相关性，实验结果客观且具有说服力。

## 6. 论文的主要结论与发现
*   EVA有效减少了生成视频中的具身伪影（如机械臂变形、关节模糊），提升了视觉规划质量。
*   在RoboTwin基准上，EVA的任务成功率（52.6%）显著优于未对齐的基线（46.2%）及其他VLA方法。
*   在真实机器人实验中，EVA在已见任务和OOD任务上均表现出更高的执行成功率和稳定性，证明了其对物理约束的对齐效果。
*   基于IDM的奖励信号能够有效捕捉视频中的物理不一致性，即使视频存在视觉伪影，奖励模型仍能给出有效的惩罚信号。

## 7. 优点
*   **问题定义精准**：敏锐地捕捉到了视频世界模型在机器人应用中的“可执行性鸿沟”问题。
*   **方法创新**：巧妙地将IDM转化为奖励模型，无需复杂的物理引擎即可引入运动学约束，实现了视频生成与物理执行的解耦与对齐。
*   **效果显著**：实验证明该方法不仅改善了视频质量，更直接提升了下游任务的执行成功率，具有很高的实用价值。

## 8. 不足与局限
*   **动力学建模缺失**：奖励函数主要关注运动学可行性与平滑度，未显式建模接触动力学（如力、摩擦、力矩），这对精细接触密集型任务可能存在局限。
*   **计算效率**：视频生成及GRPO训练过程计算成本高昂，限制了其在高频反应控制中的实时应用。
*   **奖励黑客风险**：文中提到长时间优化可能导致模型产生静止或错误的交互行为以骗取高奖励，需依赖早停策略。

（完）

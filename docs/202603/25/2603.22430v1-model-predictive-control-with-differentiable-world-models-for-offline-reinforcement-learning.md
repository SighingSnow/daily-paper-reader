---
title: Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning
title_zh: 用于离线强化学习的基于可微分世界模型的模型预测控制
authors: "Rohan Deb, Stephen J. Wright, Arindam Banerjee"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22430v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 离线强化学习中用于MPC的可微分世界模型
tldr: 本文针对离线强化学习在推理阶段无法利用即时信息优化策略的问题，提出了一种基于模型预测控制（MPC）的可微分世界模型（DWM）框架。该方法利用预训练策略和可微分世界模型，在推理时通过想象的轨迹进行端到端梯度计算，实时优化策略参数。实验表明，该方法在D4RL基准测试中优于现有基线，显著提升了性能。
selection_source: fresh_fetch
motivation: 现有的离线强化学习方法在推理时无法利用即时信息动态优化策略参数。
method: 提出一种基于模型预测控制（MPC）的可微分世界模型框架，在推理阶段通过想象的轨迹进行端到端梯度计算以实时优化策略参数。
result: 在D4RL连续控制基准测试中，该方法相比强离线强化学习基线取得了一致的性能提升。
conclusion: 利用推理时的信息优化策略参数能够有效提升离线强化学习的性能。
---

## 摘要
离线强化学习（RL）旨在从固定的离线数据集中学习最优策略，而无需与环境进行进一步交互。此类方法训练一个离线策略（或价值函数），并在推理阶段直接应用，不再进行进一步优化。我们提出了一种受模型预测控制（MPC）启发的推理阶段自适应框架，该框架利用预训练策略以及学习到的状态转移和奖励世界模型。尽管现有的世界模型和扩散规划方法利用学习到的动力学在训练期间生成想象轨迹，或在推理阶段采样候选计划，但它们并未利用推理阶段的信息来实时优化策略参数。相比之下，我们的设计是一个可微分世界模型（DWM）流程，它能够基于MPC在推理阶段通过想象推演进行端到端的梯度计算，从而实现策略优化。我们在D4RL连续控制基准（MuJoCo运动任务和AntMaze）上评估了我们的算法，结果表明，利用推理阶段信息优化策略参数能够比强大的离线RL基线获得持续的提升。

## Abstract
Offline Reinforcement Learning (RL) aims to learn optimal policies from fixed offline datasets, without further interactions with the environment. Such methods train an offline policy (or value function), and apply it at inference time without further refinement. We introduce an inference time adaptation framework inspired by model predictive control (MPC) that utilizes a pretrained policy along with a learned world model of state transitions and rewards. While existing world model and diffusion-planning methods use learned dynamics to generate imagined trajectories during training, or to sample candidate plans at inference time, they do not use inference-time information to optimize the policy parameters on the fly. In contrast, our design is a Differentiable World Model (DWM) pipeline that enables endto-end gradient computation through imagined rollouts for policy optimization at inference time based on MPC. We evaluate our algorithm on D4RL continuous-control benchmarks (MuJoCo locomotion tasks and AntMaze), and show that exploiting inference-time information to optimize the policy parameters yields consistent gains over strong offline RL baselines.

---

## 论文详细总结（自动生成）

# 论文总结：用于离线强化学习的基于可微分世界模型的模型预测控制

## 1. 论文的核心问题与整体含义
*   **核心问题**：现有的离线强化学习方法通常在训练阶段学习一个固定的策略，在推理阶段直接应用，无法利用推理时的即时状态信息对策略参数进行进一步优化。这导致策略在面对具体状态时，无法像大语言模型中的“思维链”或“搜索”那样，通过额外的计算来提升决策质量。
*   **研究动机**：作者指出，在离线RL中，Q值的估计往往存在误差且难以准确反映长视界回报，而世界模型（状态转移动力学和奖励函数）是局部对象，更容易从离线数据中准确学习。因此，作者提出利用学习到的可微分世界模型，在推理阶段进行模型预测控制（MPC），通过想象轨迹的梯度反传来实时优化策略参数。
*   **整体含义**：该研究打破了离线RL“训练后即固定”的传统范式，证明了在推理阶段引入基于世界模型的优化计算可以显著提升策略性能，为离线RL的推理时自适应提供了新的思路。

## 2. 论文提出的方法论
*   **核心思想**：提出一种名为 **MPCwDWM (Model Predictive Control with Differentiable World Model)** 的框架。该框架包含离线训练和推理时优化两个阶段。在推理时，利用预训练的策略、价值函数、奖励模型和扩散动力学模型，构建一个可微分的计算图，通过展开想象的轨迹并计算目标函数对策略参数的梯度，实时更新策略参数。
*   **关键技术细节**：
    *   **可微分世界模型**：
        *   **动力学模型**：使用扩散模型来模拟状态转移 $s_{t+1} = f_\theta(s_t, a_t, \epsilon_t)$。关键在于将扩散采样过程构建为确定性的计算图，使得 $f_\theta$ 对输入 $(s_t, a_t)$ 可微分。
        *   **奖励模型**：使用神经网络 $r_\xi(s, a)$ 预测即时奖励，通过监督学习训练。
        *   **策略与价值函数**：使用 ReBRAC 方法预训练策略 $\pi_\psi$ 和 Q 函数 $Q_\phi$。
    *   **推理时 MPC 流程**：
        1.  观察当前状态 $s_t$。
        2.  采样 $M$ 个噪声序列，利用动力学模型 $f_\theta$ 和当前策略 $\pi_\psi$ 展开 $H$ 步想象轨迹。
        3.  计算目标函数 $\hat{J}_t(\psi)$，包含预测奖励之和与终端价值估计。
        4.  通过反向传播计算梯度 $\nabla_\psi \hat{J}_t(\psi)$，更新策略参数 $\psi$（进行 $E$ 步梯度上升）。
        5.  执行更新后的策略动作 $a_t = \pi_\psi(s_t)$。
*   **算法流程与公式**：
    *   目标函数定义为：
        $$ L(\psi; \epsilon) = \sum_{j=0}^{H-1} \gamma^j r_\xi(\tilde{s}_j, \tilde{a}_j) + \gamma^H Q_\phi(\tilde{s}_H, \pi_\psi(\tilde{s}_H)) $$
    *   **梯度递推定理**：论文给出了目标函数对策略参数梯度的显式递推公式，通过策略雅可比矩阵和扩散动力学雅可比矩阵的链式法则推导，证明了通过扩散模型反向传播的可行性。

## 3. 实验设计
*   **数据集与场景**：使用了 D4RL 基准测试，包括：
    *   **Gym MuJoCo 运动任务**：HalfCheetah, Hopper, Walker2d，涵盖 random, medium, expert, medium-expert, medium-replay, full-replay 等不同数据质量，共 18 个数据集。
    *   **AntMaze 任务**：包括 umaze, umaze-diverse, medium-play, medium-diverse, large-play, large-diverse，共 6 个数据集。
*   **Benchmark**：使用 D4RL 标准化分数作为评价指标。
*   **对比方法**：
    *   **强离线 RL 基线**：TD3+BC, IQL, CQL, SAC-RND, ReBRAC。
    *   **生成式模型基线**：Decision Transformer (DT), Trajectory Transformer (TT), MOPO, MOReL, MBOP, Diffuser。

## 4. 资源与算力
*   **算力说明**：论文中未明确提及具体的 GPU 型号、数量或训练时长。仅在实验细节部分提到了扩散模型和奖励模型的训练步数（如 20k-200k steps），未提供硬件资源配置的具体信息。

## 5. 实验数量与充分性
*   **实验数量**：
    *   主要结果对比：在 18 个 MuJoCo 任务和 6 个 AntMaze 任务上进行了评估。
    *   模型性能验证：展示了扩散模型和奖励模型在不同训练步数下的预测误差（RMSE）。
    *   对比实验：与 5 种主流离线 RL 方法和 6 种生成式模型方法进行了对比。
*   **充分性与客观性**：
    *   数据集覆盖全面，包含了不同难度和稀疏奖励场景。
    *   对比方法具有代表性，涵盖了当前 SOTA 的离线 RL 和生成式规划方法。
    *   实验结果使用了归一化分数和标准差，结果客观。
    *   虽然未展示详细的消融实验（如不同规划步数 $H$ 的影响），但通过对比预训练策略与优化后策略的性能，充分证明了推理时优化的有效性。

## 6. 论文的主要结论与发现
*   **主要结论**：利用推理时的计算资源，通过可微分世界模型进行策略参数优化，能够显著提升离线 RL 的性能。
*   **具体发现**：
    *   MPCwDWM 在 D4RL MuJoCo 任务上的平均得分（85.33）优于所有基线方法（如 ReBRAC 81.53, SAC-RND 82.6）。
    *   在具有挑战性的 AntMaze 任务上，该方法取得了显著提升，平均得分从 ReBRAC 的 77.62 提升至 85.07，特别是在 large-play 和 large-diverse 等困难任务上提升明显。
    *   相比于仅在训练时使用世界模型或仅在推理时采样轨迹的方法，通过反向传播优化策略参数是有效的。

## 7. 优点
*   **创新性强**：首次将可微分世界模型与 MPC 结合用于离线 RL 的推理时优化，突破了离线策略固定不变的限制。
*   **理论严谨**：提供了详细的梯度推导定理，证明了通过扩散模型反向传播的可行性，为方法提供了坚实的理论基础。
*   **性能优越**：在多个基准测试上取得了 SOTA 或极具竞争力的结果，特别是在长视界和稀疏奖励任务上表现突出。

## 8. 不足与局限
*   **计算开销**：推理时需要进行多步梯度更新和轨迹展开，计算成本显著高于直接执行策略的方法，可能限制其在实时性要求高的场景中的应用。文中未详细讨论推理延迟问题。
*   **模型依赖**：性能高度依赖于世界模型（动力学和奖励）的准确性。如果模型在分布外区域预测不准，可能导致策略优化方向错误。
*   **实验细节缺失**：未提及具体的计算资源消耗，也未深入探讨超参数（如规划步数 $H$、优化步数 $E$）对性能的具体影响分析。

（完）

---
title: "From AI Assistant to AI Scientist: Autonomous Discovery of LLM-RL Algorithms with LLM Agents"
title_zh: 从AI助手到AI科学家：利用LLM智能体自主发现LLM-RL算法
authors: "Sirui Xia, Yikai Zhang, Aili Chen, Siye Wu, Siyu Yuan, Yanghua Xiao"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23951v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM智能体自主发现并实现可执行算法代码
tldr: 本文提出POISE框架，利用大模型智能体自动发现语言模型的策略优化算法。针对传统人工设计成本高的问题，该框架通过维护结构化档案实现闭环迭代优化。实验从GRPO出发，发现了方差缩放等改进机制，显著提升了数学推理性能，证明了自动化算法发现的可行性。
selection_source: fresh_fetch
motivation: 传统语言模型策略优化算法的发现过程成本高昂且依赖人工反复修改验证。
method: 提出POISE闭环框架，通过维护关联提案、实现、评估和反思的结构化档案来支持证据驱动的迭代搜索。
result: "在数学推理实验中，最佳变体将加权总分提升至52.5，AIME25 pass@32提升至43.3%。"
conclusion: 该研究证明了自动发现策略优化算法的可行性，并提供了可解释的设计原则。
---

## 摘要
发现改进的语言模型策略优化算法仍然是一个昂贵的人工过程，需要反复进行机制层面的修改与验证。与简单的组合代码搜索不同，该问题需要在重用跨迭代经验证据的同时，搜索与训练动态紧密耦合的算法机制。我们提出了POISE，一个用于自动发现语言模型策略优化算法的闭环框架。POISE维护着一个结构化、具有谱系关联的档案，将提议、可执行实现、标准化评估和自然语言反思联系起来，以支持证据驱动的迭代。在从GRPO开始的数学推理实验中，POISE评估了64种候选算法，并发现了改进的机制，包括解析方差缩放和有效性掩码。最佳变体将加权Overall得分从47.8提升至52.5（+4.6），并将AIME25 pass@32从26.7%提升至43.3%，证明了自动策略优化发现的可行性，同时支持可解释的设计原则。

## Abstract
Discovering improved policy optimization algorithms for language models remains a costly manual process requiring repeated mechanism-level modification and validation. Unlike simple combinatorial code search, this problem requires searching over algorithmic mechanisms tightly coupled with training dynamics while reusing empirical evidence across iterations. We propose POISE, a closed-loop framework for automated discovery of policy optimization algorithms for language models. POISE maintains a structured, genealogically linked archive linking proposals, executable implementations, standardized evaluations, and natural-language reflections to support evidence-driven iteration. In mathematical reasoning experiments starting from GRPO, POISE evaluates 64 candidate algorithms and discovers improved mechanisms, including analytic-variance scaling and validity masking. The best variant improves weighted Overall from 47.8 to 52.5 (+4.6) and increases AIME25 pass@32 from 26.7% to 43.3%, demonstrating the feasibility of automated policy optimization discovery while supporting interpretable design principles.

---

## 论文详细总结（自动生成）

以下是对论文《From AI Assistant to AI Scientist: Autonomous Discovery of LLM-RL Algorithms with LLM Agents》的详细总结：

# 论文总结

## 1. 论文的核心问题与整体含义
*   **核心问题**：发现适用于大语言模型（LLM）的策略优化算法（如 PPO、GRPO）通常是一个昂贵且依赖人工的过程。研究人员需要反复进行机制层面的修改（如损失函数设计、优势估计）和验证，这消耗大量计算资源和人力。
*   **整体含义**：论文探索了能否利用 LLM 智能体将这一过程自动化，即从“AI 助手”进化为“AI 科学家”。作者提出不仅要自动生成代码，还要能像人类研究员一样，基于历史实验证据进行推理、反思和迭代，从而自主发现新的、更优的算法机制。

## 2. 论文提出的方法论
*   **核心思想**：提出 **POISE (Policy Optimization through Iterative Search and Evidence)** 框架。这是一个闭环系统，通过维护一个结构化、具有谱系关联的档案，将算法提案、可执行代码、评估结果和自然语言反思连接起来，实现证据驱动的迭代搜索。
*   **关键流程**：POISE 将算法发现建模为“认知进化搜索”，分为三个阶段：
    1.  **提案生成**：
        *   利用 LLM 智能体基于历史档案和文献先验生成候选算法。
        *   引入采集函数 $s(d)$ 平衡 Pareto 前沿强度、性能、多样性和贝叶斯不确定性（预测后代潜力），以此选择父代节点。
        *   采用基于种群的生成策略，筛选出逻辑一致、可行且新颖的提案。
    2.  **实现、验证与评估**：
        *   在共享的训练接口下将提案转化为可执行代码。
        *   进行一致性检查和验证，确保实现符合设计意图。
        *   执行标准化的训练与评估流程，输出训练轨迹 $\tau$ 和指标 $y$。
    3.  **反思性分析与档案更新**：
        *   反思分析算子将定量的训练动态（如 KL 散度、熵、奖励曲线）转化为机制层面的诊断（自然语言反思）。
        *   更新档案，记录成功或失败的经验，作为下一轮迭代的证据基础。

## 3. 实验设计
*   **数据集**：
    *   **训练集**：从 MATH 数据集 Level 3-5 中采样 5000 个样本。
    *   **评估集**：AIME24, AIME25, AMC, MATH-500, Minerva, OlympiadBench。
*   **基准**：以 GRPO (Group Relative Policy Optimization) 算法作为基线进行迭代进化。
*   **对比方法**：主要对比了进化过程中的 64 种候选算法变体，重点分析了其中表现优异的变体（如 VM-AV-GRPO, AV-GRPO）与基线的差异。
*   **评估指标**：AIME 和 AMC 使用 pass@32，MATH、Minerva 和 OlympiadBench 使用 acc@1，并计算加权 Overall 得分。

## 4. 资源与算力
*   **硬件配置**：所有实验均在 **8× 80GB A100 GPU** 上进行。
*   **模型配置**：
    *   策略模型：Qwen2.5-Math-1.5B。
    *   推理引擎（用于生成提案和反思）：Gemini-3-pro-preview。
*   **训练设置**：学习率 1e-6，全局批次大小 256，组大小 G=8，训练 8 个 epoch。

## 5. 实验数量与充分性
*   **实验规模**：主实验评估了 **64 种** 候选算法（包含基线 GRPO 和 63 个进化变体）。此外，还进行了长度压缩约束下的分支实验（11 个节点）。
*   **充分性与公平性**：
    *   实验设计较为充分，不仅报告了最终性能，还详细展示了进化谱系、训练动态曲线（熵、奖励、长度）以及失败案例分析。
    *   评估协议标准化（统一训练预算、后端设置），且所有变体在同一接口下实现，保证了对比的公平性。
    *   实验覆盖了从简单到高难度的多个数学推理基准，验证了算法在不同难度下的泛化性。

## 6. 论文的主要结论与发现
*   **性能提升**：POISE 发现的最佳变体 **VM-AV-GRPO** 将加权 Overall 得分从 47.8 提升至 **52.5** (+4.6)，在极具挑战的 AIME25 数据集上，pass@32 从 26.7% 提升至 **43.3%**。
*   **发现的新机制**：
    *   **解析方差缩放**：替代经验方差，利用理论方差归一化优势，在稀疏奖励下放大成功信号。
    *   **有效性掩码**：仅使用格式正确的样本计算统计量，避免“格式正确但推理错误”的样本获得正向梯度。
*   **设计原则**：通过进化谱系分析，提炼出信号解耦、条件归一化、正确性优先的效率塑造等可解释的设计原则。
*   **可控性**：在长度压缩约束实验中，系统成功发现了在保持精度的同时显著缩短输出长度的算法（如 DACE-GRPO），证明自然语言指令可有效引导搜索方向。

## 7. 优点
*   **自动化闭环**：不仅实现了代码生成，还构建了包含反思和证据积累的完整科研闭环，模拟了人类研究者的试错与改进过程。
*   **可解释性强**：生成的算法变体附带自然语言反思，能够解释为何某些机制（如方差缩放）有效，而非单纯的黑盒搜索。
*   **机制创新**：发现了超越传统人工微调的新机制（如 Validity Masking），证明了 AI 在算法设计层面的创造力。

## 8. 不足与局限
*   **计算成本高昂**：每次候选评估都需要端到端的训练，限制了搜索广度和受控实验的数量。
*   **应用范围有限**：实验主要集中在数学推理领域和特定规模的模型（1.5B），发现的机制在通用对话、代码生成或更大规模模型上的迁移性尚未验证。
*   **因果解释的局限性**：虽然提供了基于证据的假设，但并未进行严格的消融实验来确立单一组件的因果贡献，解释仍属于假设层面。

（完）

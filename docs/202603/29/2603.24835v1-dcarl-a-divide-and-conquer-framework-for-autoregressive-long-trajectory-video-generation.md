---
title: "DCARL: A Divide-and-Conquer Framework for Autoregressive Long-Trajectory Video Generation"
title_zh: DCARL：一种用于自回归长轨迹视频生成的分治框架
authors: "Junyi Ouyang, Wenbin Teng, Gonglin Chen, Yajie Zhao, Haiwei Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24835v1"
tags: ["query:wmodel"]
score: 8.0
evidence: 用于世界建模长轨迹视频生成的自回归框架
tldr: 针对长轨迹视频生成中自回归模型存在的视觉漂移和可控性差的问题，本文提出了DCARL框架。该框架采用分治策略，结合关键帧生成器和插值生成器，实现了全局结构稳定与局部高保真生成的统一。实验表明，该方法在视觉质量和相机控制上均优于现有SOTA模型，能生成长达32秒的稳定视频。
selection_source: fresh_fetch
motivation: 现有视频扩散模型可扩展性有限，而自回归模型在长轨迹生成中面临视觉漂移和可控性差的挑战。
method: 提出DCARL分治自回归框架，先由关键帧生成器确立全局结构锚点，再通过插值生成器结合全局上下文与局部连贯性合成密集帧。
result: 该方法在视觉质量和相机依从性指标上均优于现有SOTA基线，成功实现了长达32秒的稳定高保真视频生成。
conclusion: DCARL有效解决了长轨迹视频生成中的漂移问题，成功实现了结构稳定与高保真生成的统一。
---

## 摘要
长轨迹视频生成是世界建模中一项关键但极具挑战的任务，这主要归因于现有视频扩散模型（VDMs）有限的可扩展性。自回归模型虽然提供了无限的推演能力，但存在视觉漂移和可控性差的问题。为了解决这些问题，我们提出了DCARL，这是一种新颖的分治自回归框架，有效地将分治方案的结构稳定性与VDMs的高保真生成能力相结合。我们的方法首先采用一个未经时间压缩训练的专用关键帧生成器，以建立长距离、全局一致的结构锚点。随后，插帧生成器以自回归方式通过重叠片段合成密集帧，利用关键帧提供全局上下文，并利用单个清晰的前帧保持局部连贯性。在大规模互联网长轨迹视频数据集上训练后，与最先进的自回归和分治基线相比，我们的方法在视觉质量（更低的FID和FVD）和相机遵循度（更低的ATE和ARE）方面均取得了优越的性能，展示了长达32秒的长轨迹视频的稳定且高保真的生成能力。

## Abstract
Long-trajectory video generation is a crucial yet challenging task for world modeling primarily due to the limited scalability of existing video diffusion models (VDMs). Autoregressive models, while offering infinite rollout, suffer from visual drift and poor controllability. To address these issues, we propose DCARL, a novel divide-and-conquer, autoregressive framework that effectively combines the structural stability of the divide-and-conquer scheme with the high-fidelity generation of VDMs. Our approach first employs a dedicated Keyframe Generator trained without temporal compression to establish long-range, globally consistent structural anchors. Subsequently, an Interpolation Generator synthesizes the dense frames in an autoregressive manner with overlapping segments, utilizing the keyframes for global context and a single clean preceding frame for local coherence. Trained on a large-scale internet long trajectory video dataset, our method achieves superior performance in both visual quality (lower FID and FVD) and camera adherence (lower ATE and ARE) compared to state-of-the-art autoregressive and divide-and-conquer baselines, demonstrating stable and high-fidelity generation for long trajectory videos up to 32 seconds in length.

---

## 论文详细总结（自动生成）

# 论文总结：DCARL

## 1. 论文的核心问题与整体含义
*   **研究背景**：长轨迹视频生成对于世界建模（如自动驾驶、场景重建）至关重要。现有的视频扩散模型虽然生成质量高，但在处理长序列时计算成本过高，难以扩展。
*   **核心问题**：自回归模型虽然支持无限推演，但存在严重的**视觉漂移**和**可控性差**的问题。这是由于“曝光偏差”导致的误差累积，使得模型在长序列生成中逐渐偏离真实分布，且难以精确遵循相机轨迹等控制信号。
*   **整体含义**：本文旨在解决长视频生成中质量与稳定性难以兼得的矛盾，提出一种能够限制误差累积、保持长期时序一致性和高保真度的生成框架。

## 2. 论文提出的方法论
*   **核心思想**：提出 **DCARL（Divide-and-Conquer Autoregressive Long-trajectory）** 框架，采用“分治策略”将长视频生成分解为两个阶段：**全局关键帧生成**确立结构锚点，**局部插值生成**填充细节。这种设计将纯自回归的无界误差转化为有界的局部误差。

*   **关键技术细节**：
    *   **关键帧生成器**：
        *   采用 DiT 架构，输入初始帧、相机轨迹和文本，生成稀疏的关键帧集合。
        *   **空间结构保留**：关键点在于训练时**不进行时间维度的压缩**，而是将关键帧视为独立的图像批次处理，避免了传统 VAE 时间压缩导致的大跨度空间位移信息丢失。
    *   **插值生成器**：
        *   基于关键帧锚点，以滑动窗口的方式自回归生成密集视频片段。
        *   **关键帧选择策略**：为每个片段选择邻近的关键帧作为全局上下文约束。
        *   **运动诱导噪声条件**：在训练和推理时对关键帧潜变量添加噪声，打破模型对关键帧像素的简单复制，强制模型学习运动动态。
        *   **无缝边界一致性**：在片段交界处采用潜变量替换策略，用上一片段的潜变量替换当前片段的起始潜变量，消除视觉跳变。

*   **理论支撑**：
    *   论文通过命题证明，纯自回归生成的误差随序列长度 $N$ 至少呈线性增长 $O(N)$，而 DCARL 通过关键帧锚定，将误差限制在局部片段内，误差上界独立于总序列长度 $N$，从而保证了长期稳定性。

## 3. 实验设计
*   **数据集**：
    *   **训练集**：OpenDV-YouTube (ODV-YT) 数据集的子集（480小时视频，10fps）。
    *   **测试集**：ODV-YT 验证集、nuScenes 验证集（零样本测试）、DL3DV-10K（附录中补充的小场景测试）。
*   **评估指标**：
    *   **视觉质量**：FID（Fréchet Inception Distance）和 FVD（Fréchet Video Distance）。
    *   **相机控制精度**：ATE（绝对轨迹误差）和 ARE（绝对旋转误差）。
    *   **片段连续性**：MS（运动平滑度）、PSNR、SSIM。
*   **对比方法**：
    *   自回归基线：Diffusion Forcing (DiffF), Self Forcing (SelfF), Deep Forcing (DeepF)。
    *   其他基线：Vista, Stable Virtual Camera (SEVA)。

## 4. 资源与算力
*   **硬件配置**：使用 8 张 NVIDIA H100 GPU。
*   **训练细节**：两个生成器均训练 30,000 步，使用 AdamW 优化器，有效批次大小为 16。

## 5. 实验数量与充分性
*   **实验规模**：
    *   主实验包含 2 个主要数据集（ODV-YT, nuScenes）上的定量对比。
    *   消融实验涵盖了 5 个关键方面：关键帧模型设计（时间压缩 vs 图像级）、关键帧采样步长、噪声关键帧设计、边界一致性设计、关键帧锚定的必要性。
    *   附录提供了理论证明、额外的定性对比（DL3DV-10K）及失败案例分析。
*   **充分性与客观性**：实验设计较为全面，不仅对比了 SOTA 方法，还通过消融实验详细验证了各个模块的有效性。评估指标涵盖了视觉质量和几何控制两个核心维度，且在未见过的数据集上进行了零样本测试，证明了泛化能力，整体实验客观且充分。

## 6. 论文的主要结论与发现
*   **性能优越**：DCARL 在视觉质量（FID/FVD）和相机控制精度（ATE/ARE）上均显著优于现有的自回归和分治基线模型。
*   **长序列稳定性**：该方法成功实现了长达 32 秒（320 帧）的高保真视频生成，有效解决了纯自回归模型在长序列中的漂移问题。
*   **模块有效性**：消融实验证明，去除时间压缩的关键帧生成、噪声条件化以及边界一致性模块对于维持高质量生成至关重要。

## 7. 优点
*   **理论创新**：不仅提出了框架，还通过数学推导证明了分治策略在限制误差累积上的理论优势，将误差从 $O(N)$ 降维到局部有界。
*   **针对性设计**：针对插值生成中的“关键帧复制”和“边界跳变”两个具体失败模式，分别提出了“运动诱导噪声条件”和“无缝边界一致性”模块，解决了实际工程痛点。
*   **架构解耦**：将全局结构规划与局部细节生成分离，既利用了扩散模型的高保真度，又规避了长序列生成的计算瓶颈。

## 8. 不足与局限
*   **实时性限制**：论文明确指出当前框架尚未针对实时交互应用进行优化，推理延迟较高。
*   **关键帧质量瓶颈**：关键帧生成器的质量受限于长距离数据分布的学习难度，在超长距离感知（如复杂的路口结构）上仍存在提升空间。
*   **失败案例**：在极端场景（如从桥下驶出）或罕见数据分布下，模型仍可能产生不自然的伪影或结构错误，表明对复杂光照和结构突变的鲁棒性仍需加强。

（完）

---
title: Dynamic analysis enhances issue resolution
title_zh: 动态分析增强问题解决能力
authors: "Mingwei Liu, Zihao Wang, Zhenxi Chen, Zheng Pei, Yanlin Wang, Zibin Zheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22048v2"
tags: ["query:agent"]
score: 9.0
evidence: DAIRA是用于自动化仓库级调试和代码修复的智能体
tldr: "现有自动化修复框架依赖静态分析，难以处理复杂Bug且效率低下。本文提出DAIRA框架，将动态分析原生嵌入智能体推理周期，通过测试追踪提取运行时数据并生成语义报告，实现从猜测到推演的转变。实验显示，DAIRA在SWE-bench Verified上达到79.4%的SOTA解决率，同时显著降低了推理成本和Token消耗。"
selection_source: fresh_fetch
motivation: 现有自动化修复方法依赖静态分析，缺乏运行时状态可见性，导致处理复杂Bug时陷入低效试错循环。
method: 提出DAIRA框架，通过测试追踪驱动方法提取运行时数据并生成结构化报告，将动态分析原生嵌入智能体推理周期。
result: "在SWE-bench Verified数据集上达到79.4%的SOTA解决率，同时降低约10%推理成本和25%输入Token消耗。"
conclusion: 动态分析的引入有效解决了复杂缺陷，将智能体行为从盲目猜测转变为基于证据的确定性推演。
---

## 摘要
将自然语言描述转化为可行的代码修复仍然是软件工程领域的一项根本性挑战。尽管智能体大语言模型的涌现极大地改善了自动化仓库级调试，但现有框架在处理诸如隐式类型降级和复杂多态控制流等复杂错误时仍面临瓶颈。由于这些方法严重依赖静态分析和浅层的执行反馈，它们缺乏对中间运行状态的可见性。因此，智能体被迫陷入昂贵且推测性的试错循环，浪费了计算Token，却未能成功定位根本原因。为了弥补这一差距，我们提出了DAIRA（动态分析增强的问题解决智能体），这是一个开创性的自动化修复框架，它将动态分析原生嵌入到智能体的推理循环中。在测试追踪驱动方法论的驱动下，DAIRA利用轻量级监视器提取关键的运行时数据（如变量变化和调用栈），并将其合成为结构化的语义报告。该机制将智能体的行为从盲目猜测根本性地转变为基于证据的确定性推演。在Gemini 3 Flash Preview的驱动下，DAIRA建立了新的最先进（SOTA）性能，在SWE-bench Verified数据集上实现了79.4%的解决率。与现有基线相比，我们的框架不仅攻克了高度复杂的缺陷，还将整体推理成本降低了约10%，并将输入Token消耗减少了约25%。

## Abstract
Translating natural language descriptions into viable code fixes remains a fundamental challenge in software engineering. While the proliferation of agentic large language models (LLMs) has vastly improved automated repository-level debugging, current frameworks hit a ceiling when dealing with sophisticated bugs like implicit type degradations and complex polymorphic control flows. Because these methods rely heavily on static analysis and superficial execution feedback, they lack visibility into intermediate runtime states. Consequently, agents are forced into costly, speculative trial-and-error loops, wasting computational tokens without successfully isolating the root cause.   To bridge this gap, we propose DAIRA (Dynamic Analysis-enhanced Issue Resolution Agent), a pioneering automated repair framework that natively embeds dynamic analysis into the agent's reasoning cycle. Driven by a Test Tracing-Driven methodology, DAIRA utilizes lightweight monitors to extract critical runtime data -- such as variable mutations and call stacks -- and synthesizes them into structured semantic reports. This mechanism fundamentally shifts the agent's behavior from blind guesswork to evidence-based, deterministic deduction. When powered by Gemini 3 Flash Preview, DAIRA establishes a new state-of-the-art (SOTA) performance, achieving a 79.4% resolution rate on the SWE-bench Verified dataset. Compared to existing baselines, our framework not only conquers highly complex defects but also cuts overall inference expenses by roughly 10% and decreases input token consumption by approximately 25%.

---

## 论文详细总结（自动生成）

# 论文总结：Dynamic analysis enhances issue resolution

## 1. 论文的核心问题与整体含义
*   **核心问题**：现有的自动化问题解决智能体严重依赖静态分析和浅层的执行反馈，缺乏对程序运行时中间状态（如变量变化、调用栈）的可见性。这导致在处理复杂缺陷（如隐式类型降级、复杂多态控制流）时，智能体被迫陷入盲目、昂贵的试错循环，无法精准定位根本原因。
*   **整体含义**：为了突破这一瓶颈，论文提出必须将动态分析深度集成到智能体的推理过程中。通过赋予智能体“调试”能力，使其能像人类开发者一样观察运行时行为，从而将问题解决过程从“盲目猜测”转变为“基于证据的确定性推演”。

## 2. 论文提出的方法论
*   **核心思想**：提出 DAIRA（Dynamic Analysis-enhanced Issue Resolution Agent）框架，采用“测试追踪驱动”范式，将动态执行轨迹作为智能体推理的一等公民证据。
*   **关键技术细节**：
    *   **动态分析工具**：基于开源 Hunter 引擎定制，利用 `sys.settrace` 实现非侵入式的原生进程内执行。该工具在沙箱环境中运行，自动捕获函数调用、返回、异常及变量状态。
    *   **时空追踪过滤**：为了降低噪音，采用按需激活机制（仅在目标函数调用时启动）和白名单过滤（排除标准库），仅保留关键执行节点。
    *   **自适应粒度迭代**：当追踪日志超出上下文窗口限制时，自动触发溢出信号，动态降低追踪深度并重新运行，防止上下文爆炸。
*   **算法流程**：
    1.  **执行追踪阶段**：智能体生成错误复现脚本和边缘情况脚本，调用动态分析工具执行，生成原始追踪日志。
    2.  **故障诊断阶段**：通过“追踪日志语义分析”模块，利用 LLM 将原始日志转化为结构化的语义报告（包含分层逻辑重构、关键函数分析、工作流介绍），帮助智能体理解代码意图并定位偏差。
    3.  **迭代修复与验证阶段**：基于诊断结果生成补丁，并利用之前的脚本进行动态验证和回归测试，确保修复的有效性。

## 3. 实验设计
*   **数据集**：使用了 **SWE-bench Verified** 数据集，包含 500 个经过人工验证的真实开源项目（如 scikit-learn, Flask, Django）Issue 实例。
*   **基准对比方法**：选取了当前最先进的（SOTA）智能体框架作为基线，包括 **SWE-agent**、**Live-SWE-agent**、**Mini-SWE-agent**、**OpenHands** 和 **SWE-search**。
*   **评估指标**：主要指标为 Issue 解决率，辅助指标包括平均推理成本和 Token 消耗量。
*   **基座模型**：主要使用了 Gemini 3 Flash Preview，并在部分实验中对比了 DeepSeek V3.2 和 Qwen-3-Coder Flash。

## 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。由于该方法主要涉及推理阶段的 API 调用和沙箱执行，论文仅设定了资源限制标准：每个实例最多 250 步操作，预算上限 $1.00。

## 5. 实验数量与充分性
*   **实验数量**：实验设计较为详尽，主要包含四组研究问题（RQ）：
    *   **RQ1（有效性）**：在 SWE-bench Verified 全量数据集上与 5 种基线方法对比。
    *   **RQ2（模型影响）**：在 3 种不同基座模型上测试 DAIRA 的性能与成本变化。
    *   **RQ3（鲁棒性）**：将任务按难度（简单、中等、困难）分层，评估不同难度下的表现。
    *   **RQ4（消融实验）**：移除动态分析工具、语义分析模块或工作流，验证各组件贡献。
*   **充分性与客观性**：实验覆盖了主流基准、不同模型能力及不同难度任务，并进行了详细的消融研究和案例分析，实验设计充分且对比公平（均采用相同的步数和预算限制）。

## 6. 论文的主要结论与发现
*   **性能突破**：DAIRA 在 Gemini 3 Flash Preview 驱动下达到了 **79.4%** 的解决率，创下 SOTA 记录，且解决了其他基线无法处理的 9 个高难度实例。
*   **效率提升**：相比基线，DAIRA 减少了约 **25%** 的输入 Token 消耗，并降低了约 **10%** 的整体推理成本。
*   **复杂任务优势**：在耗时超过 1 小时的高难度任务中，DAIRA 的解决率达到 **44.4%**，显著优于其他方法，证明了动态分析在处理深层逻辑缺陷时的关键作用。
*   **行为模式转变**：案例分析显示，DAIRA 将智能体的行为从“防御性修补”（治标）转变为“系统性修正”（治本），能够精准定位根因。

## 7. 优点
*   **创新性**：首次将动态分析原生嵌入智能体推理循环，弥补了静态分析在运行时可见性上的根本缺陷。
*   **工程价值**：提出的“测试追踪驱动”工作流和自适应粒度控制机制，有效解决了动态分析中日志噪音大和上下文溢出的工程难题。
*   **降本增效**：在提升性能的同时大幅降低了 Token 消耗，证明了“确定性推演”比“盲目试错”更具成本效益。

## 8. 不足与局限
*   **语言生态限制**：目前的动态分析工具依赖 Python 的 `sys.settrace` 钩子，尚未扩展至 Java (JVM) 或 C# (CLR) 等其他语言生态。
*   **依赖测试脚本质量**：方法的有效性高度依赖于智能体生成的测试脚本能成功复现问题，若复现脚本失败，动态追踪可能无法获取有效信息。
*   **追踪开销**：虽然总体成本下降，但动态追踪本身引入了额外的运行时开销，对于极长执行链路的程序可能存在性能瓶颈。

（完）

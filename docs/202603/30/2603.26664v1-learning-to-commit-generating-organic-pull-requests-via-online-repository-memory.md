---
title: "Learning to Commit: Generating Organic Pull Requests via Online Repository Memory"
title_zh: 学习提交：通过在线仓库记忆生成有机 Pull Requests
authors: "Mo Li, L. H. Xu, Qitai Tan, Ting Cao, Yunxin Liu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26664v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于LLM的代码智能体生成有机拉取请求
tldr: 现有LLM编码智能体生成的代码常因缺乏项目特定的“有机性”而被拒绝。本文提出了“Learning to Commit”框架，引入在线仓库记忆机制，通过让智能体在历史提交上进行对比反思并提炼技能，从而生成符合项目演化规律的代码。实验表明该方法有效提升了代码的有机性得分。
selection_source: fresh_fetch
motivation: LLM生成的代码常因缺乏项目特定的“有机性”（如忽略约定、重复API）而被拒绝，仅依靠仓库快照无法捕捉变更模式。
method: 提出在线仓库记忆框架，让智能体在历史提交上进行监督对比反思，将预测与真实差异的差距提炼为可复用的技能库，以此指导新PR生成。
result: 在专家维护的仓库上的实验表明，该方法在功能正确性、风格一致性及API复用率等多维度上有效提升了有机性得分。
conclusion: 通过在线仓库记忆学习历史变更模式，能够显著提升LLM生成代码与项目演化历史的契合度。
---

## 摘要
基于 LLM 的代码智能体在受控基准测试上取得了令人印象深刻的成果，但生成的 Pull Requests 却经常被真实维护者拒绝。根本原因并非功能错误，而是缺乏“有机性”：生成的代码忽略了项目特定的约定，重复了内部 API 已提供的功能，并且违反了长年开发积累下的隐式架构约束。仅仅让智能体接触最新的仓库快照是不够的：快照揭示了代码库的最终状态，却无法揭示达到该状态所经历的仓库特定变更模式。我们提出了“学习提交”，这是一个通过在线仓库记忆来弥补这一差距的框架。给定一个具有严格时间划分的仓库，智能体对较早的提交执行监督对比反思：它盲目尝试解决每个历史问题，将其预测与标准差异进行比较，并将差距提炼为持续增长的技能集——即捕获编码风格、内部 API 使用和架构不变量的可复用模式。当新的 PR 描述到达时，智能体基于这些积累的技能进行生成，产生的变更植根于项目自身的演化，而非通用的预训练先验。评估在真正的未来已合并 Pull Requests 上进行，这些数据在技能构建阶段无法被见到；评估涵盖多个维度，包括功能正确性、代码风格一致性、内部 API 复用率以及修改区域的合理性。在一个具有丰富提交历史的专家维护仓库上的实验表明，在线仓库记忆有效提高了留存未来任务上的有机性得分。

## Abstract
Large language model (LLM)-based coding agents achieve impressive results on controlled benchmarks yet routinely produce pull requests that real maintainers reject. The root cause is not functional incorrectness but a lack of organicity: generated code ignores project-specific conventions, duplicates functionality already provided by internal APIs, and violates implicit architectural constraints accumulated over years of development. Simply exposing an agent to the latest repository snapshot is not enough: the snapshot reveals the final state of the codebase, but not the repository-specific change patterns by which that state was reached. We introduce Learning to Commit, a framework that closes this gap through Online Repository Memory. Given a repository with a strict chronological split, the agent performs supervised contrastive reflection on earlier commits: it blindly attempts to resolve each historical issue, compares its prediction against the oracle diff, and distils the gap into a continuously growing set of skills-reusable patterns capturing coding style, internal API usage, and architectural invariants. When a new PR description arrives, the agent conditions its generation on these accumulated skills, producing changes grounded in the project's own evolution rather than generic pretraining priors. Evaluation is conducted on genuinely future, merged pull requests that could not have been seen during the skill-building phase, and spans multiple dimensions including functional correctness, code-style consistency, internal API reuse rate, and modified-region plausibility. Experiments on an expert-maintained repository with rich commit history show that Online Repository Memory effectively improves organicity scores on held-out future tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Commit: Generating Organic Pull Requests via Online Repository Memory》的详细总结：

### 1. 论文的核心问题与整体含义

*   **核心问题**：现有的基于大语言模型（LLM）的代码智能体虽然在 SWE-bench 等标准基准测试中表现优异，但在实际工业场景中生成的 Pull Requests (PRs) 常被维护者拒绝。
*   **根本原因**：并非代码功能错误，而是缺乏**“有机性”**。生成的代码往往像“外人”写的，忽略了项目特定的约定，重复实现了已有的内部 API 功能，或者违反了长期开发积累下的隐式架构约束。
*   **现有局限**：仅向智能体提供最新的代码库快照是不够的，因为快照只展示了最终状态，无法揭示达到该状态所经历的特定变更模式和演化逻辑。
*   **整体含义**：为了让 AI 真正融入工业级软件开发，必须使其具备类似人类工程师的“入职学习”能力，即通过学习历史提交来掌握项目的演化规律，而不仅仅是解决孤立的编程任务。

### 2. 论文提出的方法论

*   **核心思想**：提出 **“Learning to Commit”** 框架，引入**在线仓库记忆**机制。通过让智能体在历史提交上进行**监督对比反思**，将预测与真实差异的差距提炼为可复用的技能库，以此指导新 PR 的生成。
*   **关键流程**：
    1.  **时间切分**：将仓库提交历史严格按时间切分为历史集 $C^-$（用于学习）和未来集 $C^+$（用于测试），确保零数据泄露。
    2.  **仓库入职学习**：
        *   **盲目尝试**：智能体根据历史快照和合成的 Issue 描述，尝试生成补丁 $\hat{\Delta}_t$。
        *   **对比反思**：揭示 Oracle Diff（真实提交 $\Delta_t$），智能体对比自己的尝试与真实补丁，识别文件定位、逻辑实现、API 使用等方面的差异。
        *   **技能更新**：将反思结果提炼为结构化的技能文档 $M$，通过 CRUD 操作更新记忆库。公式为：$M^{(t)} = \text{UPDATE}(M^{(t-1)}, \hat{\Delta}_t, \Delta_t, d_t)$。
    3.  **技能条件化解题**：面对新任务，智能体基于积累的技能库 $M$ 进行推理和代码生成，确保变更符合项目演化规律。

### 3. 实验设计

*   **数据集**：使用一个内部专家维护的大规模强化学习训练仓库（包含 Agent 环境、Judge 评估等子系统）。从 2738 个非合并提交中筛选出 386 个高质量提交，分为 7 类（如架构设计、并发等）。
*   **Benchmark 构建**：构建了严格的时间切分基准，包含 24 个历史学习任务和 7 个未来测试任务。
*   **对比方法**：
    *   **Baseline**：无技能库的原始智能体（Claude Opus 4.6）。
    *   **实验变量**：交叉验证两种学习模式（顺序学习 Sequential vs. 并行学习 Parallel）和两种课程分配（按类别 By Category vs. 全量 All），共 4 种设置。
*   **评估指标**：
    *   **确定性指标**：文件 IoU（定位准确性）、轨迹步数（效率）、行偏差率（补丁膨胀度）。
    *   **多维 LLM 评判**：通过 A/B 测试评估范围对齐、逻辑相似性、冗余度、代码风格四个维度。

### 4. 资源与算力

*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。由于该方法主要基于 API 调用（Claude Opus 4.6 和 Gemini 3.1 Pro）进行推理和评估，属于无需训练的推理时增强框架，因此未涉及传统意义上的模型训练算力消耗统计。

### 5. 实验数量与充分性

*   **实验数量**：主要包含 4 组实验设置，每组对比 Skill Agent 与 Baseline。使用了两个独立的 LLM（Claude 和 Gemini）作为评判者以减少偏差。
*   **充分性与客观性**：
    *   **优点**：实验设计引入了严格的时间切分防止数据泄露，评估维度超越了单纯的功能正确性，涵盖了代码风格和架构契合度，较为全面。
    *   **局限**：测试集规模较小（仅 7 个未来任务），且仅在单一内部仓库验证，样本多样性受限。虽然使用了两个 LLM 评判，但仍存在模型主观偏差风险。

### 6. 论文的主要结论与发现

*   **效果提升**：引入在线仓库记忆后，智能体在文件定位上显著提升（File IoU 最高提升 19 个百分点），且生成的补丁更精简（行偏差率降低）。
*   **学习模式**：顺序学习且全量语料的设置效果最佳，说明渐进式的策略融合优于并行提取后合并。
*   **技能价值**：技能库主要帮助智能体理解“在哪里改”和“复用什么逻辑”，显著减少了冗余代码；但在微小的代码风格对齐上效果有限。
*   **案例发现**：技能库能帮助智能体正确识别并发 Bug 的位置，避免修改无关模块；但也可能导致过度防御性编程。

### 7. 优点

*   **创新视角**：首次将代码生成的评估重点从“功能正确性”转向“有机性”，关注代码与项目历史的契合度。
*   **机制设计**：模仿人类工程师的“入职”过程，通过监督对比反思从历史提交中提取隐性知识，比无监督的自我反思更可靠。
*   **防泄露机制**：严格的时间切分设计，有效解决了当前代码生成评估中普遍存在的数据泄露问题。

### 8. 不足与局限

*   **泛化性验证不足**：实验仅在单一内部仓库进行，测试样本量较小（7 个任务），未在多样化的开源 GitHub 仓库上验证其普适性。
*   **评估偏差风险**：主要依赖 LLM 作为评判者，虽然使用了双模型验证，但仍可能存在模型偏见，缺乏更客观的自动化有机性度量标准。
*   **依赖高质量数据**：方法依赖于高质量的提交历史和清晰的 Issue 描述，对于提交历史混乱或缺乏文档的项目可能效果受限。

（完）

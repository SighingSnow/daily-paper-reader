---
title: "ThinkJEPA: Empowering Latent World Models with Large Vision-Language Reasoning Model"
title_zh: ThinkJEPA：利用大型视觉-语言推理模型赋能潜在世界模型
authors: "Haichao Zhang, Yijiang Li, Shwai He, Tushar Nagarajan, Mingfei Chen, Jianglin Lu, Ang Li, Yun Fu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22281v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 视觉语言推理增强的潜在世界模型
tldr: 针对潜在世界模型难以捕捉长时语义、而视觉语言模型不适合作为独立密集预测器的问题，本文提出了 ThinkJEPA 框架。该框架结合了密集帧动态建模与 VLM 的长时语义指导，通过双时间路径设计，实现了细粒度运动捕捉与高层语义推理的融合，显著提升了长时预测的鲁棒性。
selection_source: fresh_fetch
motivation: 现有潜在世界模型难以捕捉长时语义，而视觉语言模型虽具备语义推理能力却受限于计算和输出瓶颈，无法独立进行有效的密集预测。
method: 提出 VLM 引导的 JEPA 框架，利用双时间路径结合密集动态建模与 VLM 长时语义指导，并通过分层金字塔模块实现特征融合。
result: 在手部操作轨迹预测任务中，该方法优于纯 VLM 和 JEPA 基线模型，并展现出更鲁棒的长时推演行为。
conclusion: 结合密集帧建模与 VLM 语义指导能有效提升潜在世界模型的长时预测能力和下游实用性。
---

## 摘要
潜在世界模型（如 V-JEPA2）的最新进展表明，其在从视频观测预测未来世界状态方面具有令人期待的能力。然而，基于短观测窗口的稠密预测限制了时序上下文，并可能使预测器偏向于局部的、低层的外推，导致难以捕捉长时程语义，从而降低了下游实用性。相比之下，视觉-语言模型通过对均匀采样的帧进行推理，提供了强大的语义基础和通用知识，但由于计算驱动的稀疏采样、将细粒度交互状态压缩为面向文本表示的语言输出瓶颈，以及在适应小型动作条件数据集时的数据体制不匹配，它们并不适合作为独立的稠密预测器。我们提出了一个 VLM 引导的 JEPA 风格潜在世界建模框架，该框架通过双时序路径将稠密帧动态建模与长时程语义引导相结合：一条用于捕捉细粒度运动和交互线索的稠密 JEPA 分支，以及一条具有更大时间步长、用于提供丰富知识引导的均匀采样 VLM “思考者”分支。为了有效地迁移 VLM 的渐进式推理信号，我们引入了一个分层金字塔表示提取模块，将多层 VLM 表示聚合为与潜在预测兼容的引导特征。手部操作轨迹预测实验表明，我们的方法优于仅使用 VLM 的强基线和 JEPA 预测器基线，并产生了更鲁棒的长时程推演行为。

## Abstract
Recent progress in latent world models (e.g., V-JEPA2) has shown promising capability in forecasting future world states from video observations. Nevertheless, dense prediction from a short observation window limits temporal context and can bias predictors toward local, low-level extrapolation, making it difficult to capture long-horizon semantics and reducing downstream utility. Vision--language models (VLMs), in contrast, provide strong semantic grounding and general knowledge by reasoning over uniformly sampled frames, but they are not ideal as standalone dense predictors due to compute-driven sparse sampling, a language-output bottleneck that compresses fine-grained interaction states into text-oriented representations, and a data-regime mismatch when adapting to small action-conditioned datasets. We propose a VLM-guided JEPA-style latent world modeling framework that combines dense-frame dynamics modeling with long-horizon semantic guidance via a dual-temporal pathway: a dense JEPA branch for fine-grained motion and interaction cues, and a uniformly sampled VLM \emph{thinker} branch with a larger temporal stride for knowledge-rich guidance. To transfer the VLM's progressive reasoning signals effectively, we introduce a hierarchical pyramid representation extraction module that aggregates multi-layer VLM representations into guidance features compatible with latent prediction. Experiments on hand-manipulation trajectory prediction show that our method outperforms both a strong VLM-only baseline and a JEPA-predictor baseline, and yields more robust long-horizon rollout behavior.

---

## 论文详细总结（自动生成）

# ThinkJEPA：利用大型视觉-语言推理模型赋能潜在世界模型 - 论文总结

## 1. 论文的核心问题与整体含义
*   **研究背景**：潜在世界模型（如 V-JEPA）在视频预测中表现出色，但依赖短观测窗口的密集预测导致其倾向于局部、低层的外推，难以捕捉长时程语义，限制了下游任务的实用性。另一方面，视觉-语言模型（VLM）具备强大的语义理解和通用知识，但受限于计算成本（需稀疏采样）、语言输出瓶颈（丢失细粒度空间信息）以及小数据集适应性问题，无法直接作为独立的密集预测器。
*   **核心问题**：如何结合潜在世界模型的细粒度动态建模能力与 VLM 的长时程语义推理能力，以解决现有方法在长时程预测中语义缺失或物理细节丢失的问题。
*   **整体含义**：本文提出了一种融合框架，旨在通过 VLM 提供的高层语义引导来增强潜在世界模型，使其在保持物理细节预测准确性的同时，具备更强的长时程语义理解能力。

## 2. 论文提出的方法论
*   **核心思想**：提出 **ThinkJEPA** 框架，采用“双时序路径”设计。一条路径保留 JEPA 的密集帧输入以捕捉精细动态；另一条路径利用 VLM 对均匀稀疏采样的帧进行推理，提供长时程语义引导。
*   **关键技术细节**：
    *   **双时序感知场采样**：
        *   **密集分支**：从短视频窗口中密集采样帧，输入 V-JEPA 编码器，提取细粒度的运动和交互特征。
        *   **VLM 思考者分支**：对整个视频片段进行均匀稀疏采样，输入 VLM（Qwen3-VL），获取长时程上下文和高层语义。
    *   **分层金字塔表示提取**：
        *   鉴于 VLM 深层特征偏向语言生成而丢失空间细节，该模块聚合 VLM 视觉编码器的 Token 和语言模型中间层的隐藏状态，形成多深度特征金字塔。
        *   这些特征被投影并池化，生成引导特征，保留了视觉推理线索。
    *   **引导注入机制**：
        *   使用特征线性调制（FiLM）将提取的 VLM 引导特征注入到 JEPA 预测器的每一层中。
        *   公式表示：预测器输出 $\hat{F}_{fut} = g(F_{past}(v_d); \phi(v_u), p)$，其中 $v_d$ 为密集输入，$v_u$ 为均匀采样输入，$\phi(v_u)$ 为 VLM 引导特征，$p$ 为文本提示。
*   **算法流程**：输入视频 -> 分流为密集片段和均匀片段 -> 密集分支提取潜在特征 -> VLM 分支提取金字塔语义特征 -> 通过 FiLM 将语义特征注入潜在预测器 -> 预测未来潜在表示 -> 解码为下游任务（如轨迹预测）。

## 3. 实验设计
*   **数据集**：
    *   **EgoDex**：大规模自我中心视角的灵巧操作数据集，包含 3D 手部姿态标注。
    *   **EgoExo4D**：多视角熟练人类活动数据集，包含自我中心视角视频和 3D 姿态标注。
*   **评测基准**：
    *   **轨迹指标**：ADE（平均位移误差）、FDE（终点位移误差）、Accuracy（误差小于 0.05m 的比例）。
    *   **潜在预测指标**：FD（特征 L2 距离）、SL1（SmoothL1 距离）、CD（余弦距离）。
    *   **长时程推演**：递归推演下的 ADE@H 和 FDE@H。
*   **对比方法**：
    *   **VLM-only Baseline**：使用 Qwen3-VL (Thinking) 提取特征并训练下游头，不使用 JEPA 分支。
    *   **JEPA-only Baseline**：标准的 V-JEPA 预测器，不注入 VLM 引导。
    *   **轨迹预测基线**：Decoder-only / Encoder-decoder 架构结合 BC、DDPM、Flow Matching。
    *   **消融实验**：对比了不同的 VLM Token 来源（Encoder vs AR）、层级选择（最后一层 vs 中间层 vs 金字塔）、时间步长、条件注入方式以及纯提示基线。

## 4. 资源与算力
*   **算力情况**：论文正文及补充材料中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **其他参数**：提到了训练使用 Batch size 为 14（评估为 6），学习率为 $10^{-3}$（预测器为 $10^{-4}$），输入分辨率为 $256 \times 256$。

## 5. 实验数量与充分性
*   **实验数量**：
    *   主实验在 2 个数据集上进行对比。
    *   消融实验涵盖了约 6 组主要变体（Token 来源、层级选择、时间步长、条件机制、直接视觉条件化、纯提示基线）。
    *   包含长时程推演（Rollout）分析。
*   **充分性与客观性**：
    *   实验设计较为充分，不仅对比了强基线，还深入分析了 VLM 不同层特征的作用，验证了“中间层保留更多视觉推理信息”的假设。
    *   **公平性亮点**：对于 VLM-only 基线，作者特意指出直接使用零样本提示效果极差（ADE > 10），为了公平对比，使用了与主方法相同的任务头进行训练，而非简单的零样本测试，这体现了实验设计的严谨性。

## 6. 论文的主要结论与发现
*   **性能提升**：ThinkJEPA 在手部轨迹预测任务上显著优于纯 VLM 和纯 JEPA 基线，证明了结合密集动态建模与语义引导的有效性。
*   **长时程优势**：在递归推演中，随着预测步长增加，ThinkJEPA 相比基线优势扩大，表明 VLM 引导有助于减少误差累积，稳定长时预测。
*   **特征融合发现**：
    *   仅使用 VLM 最后一层特征效果不如金字塔多层特征融合。
    *   VLM 的 Encoder Token 和 AR Token 提供了互补信息。
    *   VLM 不适合作为独立的密集预测器（因稀疏采样和语言瓶颈），更适合作为引导者。

## 7. 优点
*   **架构设计巧妙**：双时序路径设计有效解决了 VLM 计算受限与密集预测需求之间的矛盾，既保留了物理细节又引入了全局语义。
*   **特征利用深入**：提出的分层金字塔提取模块，创新性地利用了 VLM 中间层特征，避免了深层特征过度偏向语言生成而丢失空间信息的问题。
*   **实验分析详实**：对 VLM 在此任务中的局限性（如零样本预测失败）进行了诚实且深入的分析，增强了结论的可信度。

## 8. 不足与局限
*   **场景局限**：实验主要集中在自我中心视角的手部操作轨迹预测，未验证在自动驾驶、开放世界导航等更复杂场景下的泛化能力。
*   **实时性隐忧**：虽然文中使用了“缓存特征”进行实验，但在实际应用中，引入大型 VLM（Qwen3-VL）进行推理会带来显著的计算开销和延迟，论文未讨论实时性问题。
*   **算力透明度**：未公开具体的计算资源消耗，难以评估该方法的训练和推理成本门槛。

（完）

---
title: Structured Latent Dynamics in Wireless CSI via Homomorphic World Models
title_zh: 基于同态世界模型的无线CSI结构化潜在动力学
authors: "Salmane Naoumi, Mehdi Bennis, Marwa Chafii"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20048v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 明确将无线CSI预测建模为世界模型任务，使用JEPA架构
tldr: 本文提出了一种自监督框架，通过将信道状态信息（CSI）的时间演化建模为世界模型任务，学习无线信道的预测性和结构化表示。该方法利用联合嵌入预测架构（JEPA）和李代数同态更新构建潜在空间，在DICHASUS数据集上表现优于基线，生成的信道图支持调度和定位等下游应用。
selection_source: fresh_fetch
motivation: 旨在通过自监督方式学习无线信道的预测性及结构化表示，以解决CSI时间演化建模问题。
method: 利用联合嵌入预测架构（JEPA）构建世界模型，并结合李代数同态更新参数化状态转移，以构建几何一致的结构化潜在空间。
result: 在DICHASUS数据集上的实验表明，该方法在保留拓扑结构和预测未见环境的未来嵌入方面均优于现有强基线。
conclusion: 该方法生成的度量忠实信道图为移动感知调度、定位及无线场景理解等下游应用提供了可扩展的基础。
---

## 摘要
我们引入了一个自监督框架，通过在紧凑的潜在空间中对信道状态信息 (CSI) 的时间演化进行建模，来学习无线信道的预测性和结构化表示。我们的方法将该问题构建为世界建模任务，并利用联合嵌入预测架构 (JEPA) 从CSI轨迹中学习以动作为条件的潜在动力学。为了促进几何一致性和组合性，我们使用源自李代数的同态更新对状态转移进行参数化，从而生成一个反映空间布局和用户运动的结构化潜在空间。在DICHASUS数据集上的评估表明，我们的方法在未见环境中保持拓扑结构和预测未来嵌入方面优于强基线。所得的潜在空间能够生成度量上忠实的信道图，为移动感知调度、定位和无线场景理解等下游应用提供了可扩展的基础。

## Abstract
We introduce a self-supervised framework for learning predictive and structured representations of wireless channels by modeling the temporal evolution of channel state information (CSI) in a compact latent space. Our method casts the problem as a world modeling task and leverages the Joint Embedding Predictive Architecture (JEPA) to learn action-conditioned latent dynamics from CSI trajectories. To promote geometric consistency and compositionality, we parameterize transitions using homomorphic updates derived from Lie algebra, yielding a structured latent space that reflects spatial layout and user motion. Evaluations on the DICHASUS dataset show that our approach outperforms strong baselines in preserving topology and forecasting future embeddings across unseen environments. The resulting latent space enables metrically faithful channel charts, offering a scalable foundation for downstream applications such as mobility-aware scheduling, localization, and wireless scene understanding.

---

## 论文详细总结（自动生成）

# 论文总结：基于同态世界模型的无线CSI结构化潜在动力学

## 1. 论文的核心问题与整体含义
*   **研究背景**：传统的信道图技术通常依赖于静态的流形学习或成对差异性度量，缺乏对时间动态的感知，且需要特定的领域先验知识。虽然已有研究尝试利用世界模型处理无线控制问题，但如何利用世界模型的潜在空间构建具有几何意义的无线环境表示仍是一个开放问题。
*   **核心问题**：如何以自监督的方式从原始信道状态信息（CSI）序列中学习具有预测性、几何一致性和组合性的潜在表示。
*   **整体含义**：本文提出将无线信道演化建模为一个世界模型任务，旨在构建一个“预测性信道图”。该图不仅能反映空间结构，还能在潜在空间中模拟用户运动引起的信道状态演变，为移动感知调度、定位等下游任务提供可扩展的基础。

## 2. 论文提出的方法论
*   **核心思想**：基于联合嵌入预测架构（JEPA）构建自监督世界模型，并引入同态转移算子来约束潜在空间的动力学结构。
*   **关键技术细节**：
    *   **输入表示**：将复数 CSI 张量转换为实数张量（幅度和相位拼接），并辅以用户 2D 速度向量作为控制信号（动作）。
    *   **编码器**：使用 ResNet 风格的卷积网络将高维 CSI 映射为低维潜在向量 $z_t$。
    *   **同态动力学建模**：这是本文的核心创新。不同于通用的神经网络预测器，该方法利用李代数参数化状态转移。
        *   **李代数映射**：通过 MLP 将控制输入 $a_t$ 映射为李代数元素 $\phi_\Theta(a_t)$。
        *   **李群作用**：通过矩阵指数运算 $\rho_\Theta(a_t) = \exp(\phi_\Theta(a_t))$ 得到李群元素。
        *   **状态预测**：未来的潜在状态通过群元素的连续作用（矩阵乘法）计算：$\hat{z}_{t+h} = (\prod \rho_\Theta(a_{t+i})) z_t$。这种设计保证了动力学的组合性和几何一致性。
*   **损失函数**：采用师生架构，目标编码器通过在线编码器的指数移动平均（EMA）更新。总损失函数包含五部分：
    *   **Teacher-forcing Loss ($L_{TF}$)**：对齐每一步的预测与目标嵌入。
    *   **Rollout Loss ($L_{roll}$)**：对齐最终时刻的预测与目标。
    *   **VICReg**：包含方差项和协方差项，防止潜在空间坍塌并解耦维度。
    *   **Inverse Dynamics Modeling Loss ($L_{IDM}$)**：通过冻结的 MLP 从相邻潜在状态预测动作，增强动作感知能力。

## 3. 实验设计
*   **数据集**：使用了 **DICHASUS** 数据集，这是一个包含宽带室内信道测量的数据集。
    *   场景为 L 型工业环境，包含分布式天线阵列。
    *   数据预处理：将子载波压缩至 64 个，输入张量形状为 $(2, 32, 64)$。
*   **实验设置**：
    *   训练集：cf02, cf03, cf07 子集（共 25,149 个样本）。
    *   测试集：cf04, cf05, cf06 子集（用于验证模型在未见轨迹和环境中的泛化能力）。
*   **评价指标**：采用信道图领域的标准指标，包括连续性（CT, Continuity）、可信度（TW, Trustworthiness）、Kruskal 压力（KS）和 Rajski 距离（RD）。
*   **对比方法**：
    *   **MLP Predictor**：直接使用 MLP 预测下一状态。
    *   **FiLM Predictor**：使用特征线性调制层进行状态转移。
    *   **GRU Predictor**：使用门控循环单元建模动力学。

## 4. 资源与算力
*   **算力说明**：文中仅提及使用了纽约大学阿布扎比分校的高性能计算（HPC）资源，但**未明确说明**具体的 GPU 型号、数量或训练时长。

## 5. 实验数量与充分性
*   **实验数量**：
    *   **主实验**：在 3 个测试子集上对比了 4 种方法（含本文方法）的 4 项指标。
    *   **消融实验**：设计了 4 组消融实验，分别验证了 VICReg 和 IDM 损失项的有效性。
    *   **可视化**：提供了训练集和测试集上的潜在轨迹可视化（PCA 投影及 Procrustes 对齐）。
*   **充分性与客观性**：实验设计较为充分。通过在未见过的环境子集上进行测试，客观验证了模型的泛化能力；消融实验清晰地展示了各组件的贡献；对比方法涵盖了前馈、调制和循环神经网络等主流基线，具有公平性。

## 6. 论文的主要结论与发现
*   **性能优越**：提出的同态转移模型在所有评价指标上均优于 MLP、FiLM 和 GRU 基线，证明了在潜在空间引入李群结构的有效性。
*   **泛化能力**：模型在未见过的轨迹和环境中表现出色，说明学习到的动力学具有组合性和几何一致性，而非简单的过拟合。
*   **结构化表示**：消融实验表明，VICReg 正则化和 IDM 损失对于防止表示坍塌和保持局部拓扑结构至关重要。
*   **潜在空间质量**：学习到的潜在空间能够生成度量上忠实的信道图，且仅凭速度序列即可预测未来的信道嵌入轨迹。

## 7. 优点
*   **创新性强**：首次将同态李群动力学引入无线信道的世界模型建模，解决了传统预测器缺乏几何约束的问题。
*   **自监督学习**：无需位置标签或人工设计的相似性度量，实现了端到端的训练。
*   **理论结合**：将表征学习（JEPA）、几何深度学习（李群）与无线通信（CSI 建模）有机结合，提供了坚实的理论基础。

## 8. 不足与局限
*   **环境假设限制**：模型假设环境在轨迹持续时间内是静态的（反射体和散射体固定），未考虑动态障碍物或时变信道的影响。
*   **下游任务未验证**：虽然提到了移动感知调度和定位等应用，但论文仅评估了潜在空间的几何性质，未展示具体的下游任务（如定位精度、波束选择成功率）的实际性能提升。
*   **计算复杂度**：引入矩阵指数运算可能增加计算开销，文中未讨论推理延迟或计算效率。

（完）

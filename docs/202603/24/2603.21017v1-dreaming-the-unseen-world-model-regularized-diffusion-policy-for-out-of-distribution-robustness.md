---
title: "Dreaming the Unseen: World Model-regularized Diffusion Policy for Out-of-Distribution Robustness"
title_zh: 构想未见之物：面向分布外鲁棒性的世界模型正则化扩散策略
authors: "Ziou Hu, Xiangtong Yao, Yuan Meng, Zhenshan Bing, Alois Knoll"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21017v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 用于策略训练的扩散世界模型框架，具备潜在动力学预测能力
tldr: 扩散策略在分布外干扰下容易失效，本文提出Dream Diffusion Policy (DDP)框架，通过共享3D视觉编码器将扩散世界模型融入策略训练。该方法赋予策略鲁棒的状态预测能力，在遇到干扰时能切换至内部“想象”生成轨迹，从而显著提升了机器人在OOD场景下的鲁棒性和成功率。
selection_source: fresh_fetch
motivation: 扩散策略在面临严重的分布外干扰（如物体移位或视觉损坏）时往往会灾难性失败，亟需提升其鲁棒性。
method: 提出Dream Diffusion Policy (DDP)，通过共享3D视觉编码器将扩散世界模型深度整合进策略训练，使其在检测到干扰时能利用内部“想象”的潜在动力学生成轨迹。
result: "实验显示，DDP在MetaWorld的OOD成功率达73.8%，在真实世界严重空间偏移下达83.3%，甚至在完全开环想象的压力测试中仍保持76.7%的成功率。"
conclusion: DDP通过世界模型正则化和“想象”机制，成功解决了扩散策略在分布外干扰下的脆弱性问题，实现了卓越的鲁棒性。
---

## 摘要
扩散策略在视觉-运动控制方面表现优异，但在严重的分布外（OOD）扰动下（例如意外的物体位移或视觉损坏）往往会遭遇灾难性失效。为了解决这一脆弱性，我们提出了 Dream 扩散策略（DDP），这是一个通过共享 3D 视觉编码器将扩散世界模型深度融合到策略训练目标中的框架。这种联合优化赋予了策略鲁棒的状态预测能力。在推理过程中遇到突发的 OOD 异常时，DDP 会检测现实与想象之间的差异，并主动丢弃受损的视觉流。取而代之的是，它依赖其内部“想象”（自回归预测的潜在动力学）安全地绕过干扰，生成想象的轨迹，随后平滑地回归物理现实。广泛的评估证明了 DDP 卓越的韧性。值得注意的是，DDP 在 MetaWorld 上实现了 73.8% 的 OOD 成功率（而无预测想象时仅为 23.9%），并在严重的现实世界空间偏移下实现了 83.3% 的成功率（而无预测想象时仅为 3.3%）。此外，作为压力测试，DDP 即使在初始化后完全依赖开环想象的情况下，仍保持了 76.7% 的现实世界成功率。

## Abstract
Diffusion policies excel at visuomotor control but often fail catastrophically under severe out-of-distribution (OOD) disturbances, such as unexpected object displacements or visual corruptions. To address this vulnerability, we introduce the Dream Diffusion Policy (DDP), a framework that deeply integrates a diffusion world model into the policy's training objective via a shared 3D visual encoder. This co-optimization endows the policy with robust state-prediction capabilities. When encountering sudden OOD anomalies during inference, DDP detects the real-imagination discrepancy and actively abandons the corrupted visual stream. Instead, it relies on its internal "imagination" (autoregressively forecasted latent dynamics) to safely bypass the disruption, generating imagined trajectories before smoothly realigning with physical reality. Extensive evaluations demonstrate DDP's exceptional resilience. Notably, DDP achieves a 73.8% OOD success rate on MetaWorld (vs. 23.9% without predictive imagination) and an 83.3% success rate under severe real-world spatial shifts (vs. 3.3% without predictive imagination). Furthermore, as a stress test, DDP maintains a 76.7% real-world success rate even when relying entirely on open-loop imagination post-initialization.

---

## 论文详细总结（自动生成）

# 论文总结：Dreaming the Unseen: World Model-regularized Diffusion Policy for Out-of-Distribution Robustness

## 1. 论文的核心问题与整体含义
*   **核心问题**：基于扩散模型的视觉-运动策略在面临严重的分布外（OOD）干扰（如物体突然位移、视觉遮挡）时极其脆弱，往往导致任务失败。现有的缓解方法（如域随机化、在线适应）通常引入复杂的流程或覆盖专家技能，无法从根本上解决视觉流受损时的控制问题。
*   **整体含义**：受人类在视觉混乱时依靠“内部直觉”完成动作的启发，本文旨在赋予机器人“想象”能力。通过将世界模型深度整合进策略训练，使机器人在视觉失效时能够切换至内部预测的潜在动力学空间进行推理，从而实现对未见干扰的鲁棒应对。

## 2. 论文提出的方法论
*   **核心思想**：提出 Dream Diffusion Policy (DDP) 框架，通过共享 3D 视觉编码器联合优化扩散策略和扩散世界模型。世界模型不仅作为正则化项提升策略的物理感知能力，更在推理阶段作为“想象引擎”，在 OOD 状态下替代受损的视觉输入。
*   **关键技术细节**：
    *   **共享 3D 编码器**：使用 PointNet 处理点云，MLP 处理本体感知，生成统一的潜在嵌入，同时供策略网络和世界模型使用。
    *   **扩散策略**：基于 DDPM，以历史观测为条件，通过去噪过程生成动作序列。
    *   **扩散世界模型**：以历史观测和计划动作为条件，预测未来的潜在观测状态。
    *   **联合训练目标**：总损失函数为行为克隆损失与世界模型去噪损失的加权和，即 $L_{total} = L_{BC} + \lambda L_{WM}$。
*   **推理流程**：
    1.  **OOD 检测**：计算真实观测编码与预测编码之间的差异 $D_{R-I}$，若超过阈值则判定为 OOD 状态。
    2.  **物体跟踪**：利用外部 6D 姿态估计器获取位移物体的新位置。
    3.  **想象执行**：在 OOD 状态下，丢弃真实视觉流，利用世界模型自回归预测潜在状态，策略基于这些“想象”状态生成动作。
    4.  **物理恢复**：子任务结束时，执行反向位移操作，尝试将环境拉回分布内（ID）状态，若差异降低则切回真实视觉控制。

## 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：MetaWorld（10 个任务，涵盖简单到困难）和 Adroit（3 个高自由度任务）。
    *   **真实世界**：Franka Panda 机械臂平台，包含 Press Button（简单）、Pour Tea（中等）、Stack Blocks（困难）三个任务。
*   **Benchmark**：
    *   **分布内（ID）性能**：标准环境下的成功率。
    *   **分布外（OOD）性能**：在任务执行中途突然移动物体或遮挡摄像头，测试恢复能力。
    *   **开环想象压力测试**：初始化后完全切断视觉输入，仅靠世界模型开环推演。
*   **对比方法**：
    *   **DP3**：3D 扩散策略基线。
    *   **FlowPolicy**：基于流匹配的策略。
    *   **消融实验**：DP3 + Tracking（仅添加跟踪模块无世界模型）、不同比例的想象干预（10%, 50%, 100%）。

## 4. 资源与算力
*   **训练硬件**：文中明确提到使用 **NVIDIA 4090 GPU** 进行模型训练（真实世界实验部分提到训练 2000 轮）。
*   **推理硬件**：使用 **NVIDIA 4070 GPU** 部署 DDP 框架。
*   **训练时长**：文中未明确说明具体的训练小时数，仅提及训练轮数（仿真 500 轮，真实 2000 轮）。
*   **推理速度**：扩散策略推理约 40ms，世界模型推理约 70ms，整体控制频率约为 2Hz。

## 5. 实验数量与充分性
*   **实验数量**：
    *   仿真实验覆盖 13 个任务，每个任务 3 个随机种子，每个种子 100 个回合。
    *   真实世界实验覆盖 3 个不同难度任务，每个任务 10 次试验。
    *   包含多组消融实验：基线对比、跟踪增强对比、想象干预比例分析。
*   **充分性与公平性**：
    *   实验设计较为充分，涵盖了从仿真到真机、从简单操作到精细操作的多维度验证。
    *   对比实验中，基线方法（DP3, FlowPolicy）采用了相同的骨干网络和超参数，保证了公平性。
    *   真实世界实验虽然任务数量有限，但包含了遮挡和位移等极端干扰，具有说服力。

## 6. 论文的主要结论与发现
*   **性能提升显著**：DDP 在 MetaWorld 的 OOD 场景下成功率达到 **73.8%**（对比基线 23.9%）；在真实世界严重空间偏移下成功率达到 **83.3%**（对比基线 3.3%）。
*   **想象能力的有效性**：即使在完全切断视觉输入（100% 开环想象）的压力测试下，DDP 在真实世界仍保持了 **76.7%** 的成功率，证明了世界模型提供的内部动力学预测是可靠且有效的。
*   **机制验证**：消融实验证明，仅靠外部跟踪模块无法解决 OOD 问题，必须结合世界模型的潜在预测能力才能实现鲁棒控制。

## 7. 优点
*   **创新性强**：提出了“想象”机制来应对视觉失效，将世界模型从被动预测转变为主动的推理引擎，思路新颖。
*   **架构优雅**：通过共享编码器实现策略与世界模型的深度耦合，避免了复杂的解耦架构，且无需昂贵的强化学习微调。
*   **鲁棒性极佳**：在极端干扰（如物体被移出训练范围、完全遮挡）下表现出了远超现有方法的恢复能力。

## 8. 不足与局限
*   **依赖外部模块**：方法高度依赖外部的 6D 姿态估计器进行物体跟踪，若跟踪模块失效或漂移，系统可能崩溃。
*   **初始状态限制**：任务必须严格从分布内（ID）状态开始，以便正确锚定初始观测。
*   **低级干扰处理不足**：推理机制难以处理复合型 OOD 事件或低级动作干扰（如夹爪打滑）。
*   **控制频率较低**：受限于双扩散模型架构，系统控制频率仅为 2Hz，可能不适用于需要毫秒级响应的高速动态任务。

（完）

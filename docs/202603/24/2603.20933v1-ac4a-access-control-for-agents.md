---
title: "AC4A: Access Control for Agents"
title_zh: AC4A：面向智能体的访问控制
authors: "Reshabh K Sharma, Dan Grossman"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20933v1"
tags: ["query:agent"]
score: 9.0
evidence: 专为LLM智能体设计的访问控制框架
tldr: 现有的LLM智能体系统采用“全有或全无”的粗粒度访问模式，存在过度授权风险。本文提出AC4A框架，借鉴Unix文件系统模型，为智能体提供细粒度的访问控制。该框架支持对API和网页资源进行层级定义与运行时权限计算，确保智能体仅能访问授权资源，有效解决了过度信任问题，并通过案例研究验证了其实用性。
selection_source: fresh_fetch
motivation: 当前智能体系统缺乏细粒度访问控制，迫使用户授予超出任务需求的权限，存在过度信任风险。
method: 提出AC4A框架，借鉴Unix文件系统权限模型，通过层级资源定义和运行时权限计算实现灵活的访问控制。
result: 通过案例研究展示了AC4A在真实API和网页环境中有效执行权限控制的能力。
conclusion: AC4A为智能体提供了一种实用的细粒度访问控制解决方案，消除了对全权限信任的依赖。
---

## 摘要
大语言模型（LLM）智能体将 LLM 的聊天交互能力与同外部工具及 API 交互的能力相结合。这使其能够执行复杂任务，并自主行动以实现用户目标。然而，当前的智能体系统基于“全有或全无”的原则运行：智能体要么拥有对 API 功能和网页内容的完全访问权限，要么根本没有任何访问权限。这种粗粒度的方法迫使用户不得不信任智能体拥有超出特定任务实际需求的能力。
在本文中，我们介绍了 AC4A，一个面向智能体的访问控制框架。随着智能体变得更加强大和自主，用户需要一种方法来限制这些智能体可以访问的 API 或网页部分，从而无需将 API 或网页允许的所有权限都托付给它们。AC4A 的目标是提供一个定义权限的框架，使智能体仅能访问其被授权访问的资源。AC4A 适用于基于 API 和基于浏览器的智能体。它不规定具体的权限内容，而是提供了一种定义和执行权限的灵活方式，使其适用于现实世界的系统。
AC4A 通过创建授予资源访问权限的机制来工作，并借鉴了现有的访问控制框架（如 Unix 文件系统的访问控制框架）。应用程序将其资源定义为层级结构，并提供一种在运行时计算成功访问资源所需权限的方法。我们通过案例研究展示了 AC4A 在对现实世界 API 和网页执行权限控制方面的实用性。AC4A 的源代码可在 https://github.com/reSHARMA/AC4A 获取。

## Abstract
Large Language Model (LLM) agents combine the chat interaction capabilities of LLMs with the power to interact with external tools and APIs. This enables them to perform complex tasks and act autonomously to achieve user goals. However, current agent systems operate on an all-or-nothing basis: an agent either has full access to an API's capabilities and a web page's content, or it has no access at all. This coarse-grained approach forces users to trust agents with more capabilities than they actually need for a given task.   In this paper, we introduce AC4A, an access control framework for agents. As agents become more capable and autonomous, users need a way to limit what APIs or portions of web pages these agents can access, eliminating the need to trust them with everything an API or web page allows. Our goal with AC4A is to provide a framework for defining permissions that lets agents access only the resources they are authorized to access. AC4A works across both API-based and browser-based agents. It does not prescribe what permissions should be, but offers a flexible way to define and enforce them, making it practical for real-world systems.   AC4A works by creating permissions granting access to resources, drawing inspiration from established access control frameworks like the one for the Unix file system. Applications define their resources as hierarchies and provide a way to compute the necessary permissions at runtime needed for successful resource access. We demonstrate the usefulness of AC4A in enforcing permissions over real-world APIs and web pages through case studies. The source code of AC4A is available at https://github.com/reSHARMA/AC4A

---

## 论文详细总结（自动生成）

# 论文总结：AC4A: Access Control for Agents

## 1. 论文的核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）智能体能力的增强，它们能够自主调用外部 API 和浏览网页来执行复杂任务。然而，现有的智能体系统在权限管理上存在严重缺陷，通常采用“全有或全无”的粗粒度模式。
*   **核心问题**：用户为了完成简单任务（如创建一个日历事件），往往被迫授予智能体超出需求的权限（如修改或删除所有日历事件的权限）。这种过度授权带来了巨大的安全风险，即使用户信任应用程序本身，也不应完全信任智能体的自主行为。
*   **整体含义**：本文旨在填补智能体领域访问控制基础设施的空白，提出一种细粒度的权限控制框架，使用户能够限制智能体仅访问其授权的资源，从而消除对智能体的过度信任。

## 2. 论文提出的方法论
*   **核心思想**：借鉴 Unix 文件系统的权限模型，提出 AC4A 框架。该框架将权限定义为对“资源”执行“动作”的许可，支持应用程序自定义资源层级结构，并在运行时动态计算和检查权限。
*   **资源定义**：
    *   应用程序将资源建模为树状结构，称为“资源类型树”。
    *   资源类型节点通过 `::` 操作符链接，形成层级关系（如 `Year::Month::Day`），父节点权限隐含覆盖子节点权限。
*   **权限表示**：
    *   权限由“资源值规范”和“动作”组成。
    *   资源值规范支持通配符 `?`，表示所有可能的值。
    *   动作由应用程序定义（如 `read`, `write`, `create`），框架本身不赋予其语义。
*   **权限检查算法**：
    *   核心逻辑基于 `resource_difference(Need, Have) -> Remaining` 函数。该函数计算当前已授权资源是否覆盖所需资源，返回未满足的部分。
    *   算法流程：遍历所需动作集合 -> 获取该动作下的已授权资源 -> 迭代调用 `resource_difference` 减去已覆盖部分 -> 若最终剩余需求为空，则允许访问。
*   **执行机制**：
    *   **API 智能体**：应用程序提供 Permission Function，根据 API 端点和参数动态返回所需权限。
    *   **浏览器智能体**：通过配置文件将 CSS 选择器映射到资源值规范。框架在运行时拦截 DOM，对未授权元素注入 CSS 覆盖层进行隐藏。
*   **用户界面与模式**：提供仪表盘用于管理权限，支持手动创建、LLM 辅助生成及自动推断模式，并提供了 Ask、Skip、Infer、YOLO 四种权限处理模式以平衡安全与便利。

## 3. 实验设计
*   **实验场景**：论文设计了两个案例研究来验证框架的有效性。
    1.  **井字棋游戏**：作为一个简化的演示场景，展示了如何保护游戏历史记录资源，验证了 API 和 Web 端的基本拦截功能。
    2.  **航班预订任务**：作为一个真实世界的复杂场景，涉及 Outlook Calendar（日历）、Expedia（航班预订）和 Payment Wallet（支付钱包）三个应用程序。
*   **Benchmark**：本文未使用传统的数据集 Benchmark，而是通过构建具体的应用场景来演示功能。
*   **对比方法**：文中未进行定量的对比实验，但在相关工作部分讨论了与 Progent 和 Conseca 等现有方法的区别，指出 AC4A 是首个统一 API 和浏览器智能体权限执行的框架，且基于资源而非工具调用进行建模。

## 4. 资源与算力
*   **模型使用**：文中提到在原型实现中，底层智能体模型使用了 GPT-5.4-mini。
*   **算力细节**：论文未明确说明具体的 GPU 型号、数量或训练时长。由于 AC4A 是一个访问控制框架，主要涉及逻辑判断和 API 调用拦截，不涉及大规模模型训练，因此未重点汇报算力消耗。

## 5. 实验数量与充分性
*   **实验数量**：实验主要包含两个案例研究，每个案例下设计了多个子场景（如无权限、只读权限、读写权限、跨应用访问等）。
*   **充分性与客观性**：
    *   实验充分展示了框架在 API 和 Web 两种环境下的拦截能力，以及跨应用场景的权限控制能力，定性验证了方法的有效性。
    *   然而，实验规模较小，缺乏大规模自动化测试或定量指标（如拦截延迟、系统开销），主要依赖人工演示和截图说明。对于框架在实际生产环境中的性能影响评估尚不充分。

## 6. 论文的主要结论与发现
*   AC4A 成功实现了对 LLM 智能体的细粒度访问控制，解决了“全有或全无”的过度授权问题。
*   通过资源层级模型，框架能够统一处理 API 调用和浏览器 DOM 操作，证明了资源级权限定义的灵活性和通用性。
*   案例研究表明，智能体可以在受限权限下正常完成任务，同时被有效阻止执行未授权的操作（如读取其他月份日历、未授权支付等）。

## 7. 优点
*   **统一框架**：首次提出了同时支持 API 智能体和浏览器智能体的统一访问控制框架，填补了该领域的空白。
*   **灵活的资源模型**：借鉴 Unix 文件系统的层级模型，允许应用程序自定义资源树和动作，适应性强。
*   **运行时动态检查**：不同于静态权限声明，AC4A 在运行时根据实际参数计算所需权限，实现了更精细的控制。
*   **用户友好**：提供了可视化的权限管理仪表盘，并支持 LLM 辅助生成权限，降低了用户配置门槛。

## 8. 不足与局限
*   **映射维护成本高**：Permission Function（API 端）和 Web Configuration（浏览器端）需要手动编写和维护。若网站 DOM 结构发生变化或 API 更新，配置可能失效，导致过度限制或权限泄露。
*   **浏览器端脆弱性**：对于动态生成 ID 或反爬虫机制的网站，CSS 选择器映射可能不稳定。
*   **功能限制**：当前模型难以表达复杂的条件权限（如“仅当金额小于 200 美元时允许支付”），且缺乏权限生命周期管理（如单次使用后自动失效）。
*   **实验评估局限**：实验主要侧重于功能演示，未对框架引入的系统延迟、对智能体任务成功率的影响进行定量分析。

（完）

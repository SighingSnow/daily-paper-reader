---
title: "Stereo World Model: Camera-Guided Stereo Video Generation"
title_zh: 立体世界模型：相机引导的立体视频生成
authors: "Yang-Tian Sun, Zehuan Huang, Yifan Niu, Lin Ma, Yan-Pei Cao, Yuewen Ma, Xiaojuan Qi"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17375v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 相机条件立体世界模型用于视频生成
tldr: 本文提出了StereoWorld，一种相机引导的立体世界模型，仅在RGB模态下联合学习外观与双目几何。通过引入统一相机帧RoPE和立体感知注意力分解机制，实现了高效的端到端立体视频生成。实验表明，该方法在立体一致性和生成速度上显著优于现有流程，并支持VR渲染及具身智能应用。
selection_source: fresh_fetch
motivation: 为了解决现有单目或RGBD方法在生成立体视频时几何一致性差且效率低的问题。
method: 提出统一相机帧RoPE保持时空一致性，并设计立体感知注意力分解机制，利用极线先验高效捕捉视差对应关系。
result: "相比强基线模型，该方法在立体一致性和视差精度上表现更优，生成速度提升超3倍，视点一致性提升5%。"
conclusion: StereoWorld实现了高效且几何一致的立体视频生成，无需深度估计即可支持VR渲染及具身策略学习。
---

## 摘要
我们提出了 StereoWorld，这是一种相机条件化的立体世界模型，它联合学习外观与双目几何，以实现端到端的立体视频生成。与单目 RGB 或 RGBD 方法不同，StereoWorld 仅在 RGB 模态下运行，同时直接从视差中确立几何信息。为了高效实现一致的立体生成，我们的方法引入了两个关键设计：（1）统一的相机帧 RoPE，通过相机感知的旋转位置编码增强潜在 token，在通过稳定的注意力初始化保留预训练视频先验的同时，实现了相对的、视图和时间一致的条件控制；（2）立体感知的注意力分解，将全 4D 注意力分解为 3D 视图内注意力加上水平行注意力，利用极线先验以显著更低的计算量捕获视差对齐的对应关系。在各项基准测试中，StereoWorld 在立体一致性、视差精度和相机运动保真度方面均优于强大的“先单目后转换”流水线，生成速度提升超过 3 倍，且视点一致性额外提升了 5%。除了基准测试外，StereoWorld 还支持无需深度估计或修复的端到端双目 VR 渲染，通过度量尺度深度定位增强具身策略学习，并兼容长视频蒸馏以实现扩展的交互式立体合成。

## Abstract
We present StereoWorld, a camera-conditioned stereo world model that jointly learns appearance and binocular geometry for end-to-end stereo video generation.Unlike monocular RGB or RGBD approaches, StereoWorld operates exclusively within the RGB modality, while simultaneously grounding geometry directly from disparity. To efficiently achieve consistent stereo generation, our approach introduces two key designs: (1) a unified camera-frame RoPE that augments latent tokens with camera-aware rotary positional encoding, enabling relative, view- and time-consistent conditioning while preserving pretrained video priors via a stable attention initialization; and (2) a stereo-aware attention decomposition that factors full 4D attention into 3D intra-view attention plus horizontal row attention, leveraging the epipolar prior to capture disparity-aligned correspondences with substantially lower compute. Across benchmarks, StereoWorld improves stereo consistency, disparity accuracy, and camera-motion fidelity over strong monocular-then-convert pipelines, achieving more than 3x faster generation with an additional 5% gain in viewpoint consistency. Beyond benchmarks, StereoWorld enables end-to-end binocular VR rendering without depth estimation or inpainting, enhances embodied policy learning through metric-scale depth grounding, and is compatible with long-video distillation for extended interactive stereo synthesis.

---

## 论文详细总结（自动生成）

# 论文总结：Stereo World Model: Camera-Guided Stereo Video Generation

## 1. 论文的核心问题与整体含义
*   **研究背景**：现有的世界模型主要分为单目RGB模型和RGB-D模型。单目模型缺乏显式的几何约束，导致深度模糊和尺度不一致；RGB-D模型虽然引入了深度，但依赖不稳定的深度估计器，且深度图往往存在尺度模糊和跨域不稳定的问题。
*   **核心问题**：如何构建一个能够直接从双目观察中学习几何信息，而非依赖单目运动推断或显式深度预测的世界模型，以解决长序列生成中的几何累积误差问题，并满足VR/AR和具身智能对精确几何和尺度感知的需求。
*   **整体含义**：本文提出了首个相机条件化的立体世界模型，旨在通过联合学习外观和双目几何，实现端到端的立体视频生成，从而在保持视觉质量的同时，提供内在的、度量尺度的几何一致性。

## 2. 论文提出的方法论
*   **核心思想**：基于预训练的视频扩散模型，构建一个端到端的立体视频生成框架。该框架不修改原有的单目视频生成主干，而是通过扩展位置编码和设计高效的注意力机制来引入立体视觉约束。
*   **关键技术一：统一相机帧 RoPE (Unified Camera-Frame RoPE)**
    *   **目的**：在注入相机位姿条件（内参、外参、基线）的同时，最大程度保留预训练模型的视频生成先验。
    *   **实现**：不直接修改原有的旋转位置编码空间，而是扩展潜在Token的维度，增加一个专门用于相机感知的RoPE通道。原有的维度保留用于时空信息，新增维度用于编码相对相机位姿。
    *   **初始化策略**：提出了“Copy Init”策略，将新增维度的权重初始化为时间注意力权重，相比零初始化，这能更稳定地激活相机条件信号。
*   **关键技术二：立体感知注意力分解**
    *   **目的**：解决全4D注意力（时空+双目视图）计算量过大的问题。
    *   **实现**：利用立体视觉中的极线约束，即左右视图的对应点主要位于同一水平行。将全4D注意力分解为两部分：
        1.  **3D视图内注意力**：分别处理左右视图内部的时空关系。
        2.  **水平行注意力**：仅在相同时间步、相同水平行的左右视图Token之间进行跨视图交互。
    *   **效果**：计算复杂度从 $O((2f \cdot h \cdot w)^2)$ 降低至 $O(2 \cdot (f \cdot h \cdot w)^2) + f \cdot h \cdot (2w)^2)$，大幅降低了显存和计算需求。

## 3. 实验设计
*   **数据集**：
    *   **训练集**：混合了多个数据集，包括 Stereo4D (动态真实), TartanAir (静态合成), TartanAirGround (静态合成), DynamicReplica (动态合成), VKitti (动态合成)。
    *   **测试集**：FoundationStereo, Unreal-Stereo4K, TartanAir Testset, Middlebury，涵盖了合成与真实、室内与室外场景。
*   **Benchmark**：
    *   **相机精度**：旋转误差 和 平移误差。
    *   **视图同步性**：匹配像素数、FVD-V、CLIP-V。
    *   **视觉质量**：FID, FVD, CLIP-T, CLIP-F 以及 VBench 指标。
*   **对比方法**：
    *   对比了 SOTA 的相机控制视频生成模型，包括 RGB-D 模型 和 RGB 模型。
    *   由于缺乏同类立体世界模型，基线方法通过“先生成单目视频再利用 StereoCrafter 或 DepthCrafter 转换为立体视频”的流水线进行对比。
    *   同时对比了本文方法的单目版本以验证立体生成的优势。

## 4. 资源与算力
*   **硬件环境**：使用了 24 张 NVIDIA H20 GPU。
*   **训练配置**：训练步数为 20,000 步，Batch size 设为 24，输入视频分辨率为 480x640，帧数为 49 帧。
*   **优化器**：AdamW，学习率为 1e-4。

## 5. 实验数量与充分性
*   **实验数量**：
    *   主实验：在 4 个测试集上对比了 5 个基线模型，评估了 10+ 项指标。
    *   消融实验：验证了相机注入策略（对比 Plücker Ray, PRoPE, Zero Init）和注意力机制（对比 4D Attention）的有效性。
    *   应用实验：展示了在 VR 显示、具身智能（DROID 数据集微调）和长视频蒸馏中的应用效果。
*   **充分性与客观性**：实验设计较为充分。不仅对比了定量指标，还提供了可视化的定性对比（如视差图、轨迹对比）。消融实验详细分析了各模块的贡献。对比方法虽非原生立体模型，但通过构建公平的流水线对比，客观反映了端到端立体生成的优势。

## 6. 论文的主要结论与发现
*   **性能提升**：StereoWorld 在立体一致性、视差精度和相机运动保真度上均优于“先单目后转换”的流水线。相比强基线，生成速度提升超过 3 倍，视点一致性提升约 5%。
*   **几何一致性**：该方法无需显式的深度监督，仅通过双目信号即可生成几何一致、具有度量尺度的立体视频，避免了深度估计带来的误差累积。
*   **效率优势**：提出的注意力分解机制在保持生成质量的同时，显著降低了计算开销，使得立体视频生成变得高效可行。

## 7. 优点
*   **创新性**：提出了首个相机条件化的立体世界模型，填补了该领域的空白。
*   **方法设计巧妙**：通过扩展维度而非修改原有空间的方式注入相机条件，有效保留了预训练模型的强大先验；利用极线约束分解注意力，兼顾了效率与几何特性。
*   **应用价值**：直接输出立体视频，无需后处理即可用于 VR 渲染，且能提供度量尺度深度，对具身智能任务具有重要意义。

## 8. 不足与局限
*   **数据依赖**：训练数据主要依赖静态场景或合成数据，导致模型在生成动态场景（如人物大范围运动）方面能力受限。
*   **长视频生成**：尽管支持长视频蒸馏，但在生成长序列视频时仍存在明显的质量退化问题。
*   **计算成本**：虽然相比 4D 注意力已大幅优化，但立体视频生成的计算量仍高于单目模型，且受限于立体视频数据集的规模，模型扩展性受限。
*   **场景一致性**：文中提到模型目前缺乏显式的场景级记忆约束，在长序列中可能出现空间不一致（如物体凭空出现）的情况。

（完）

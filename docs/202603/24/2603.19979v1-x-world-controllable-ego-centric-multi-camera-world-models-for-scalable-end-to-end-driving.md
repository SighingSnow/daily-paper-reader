---
title: "X-World: Controllable Ego-Centric Multi-Camera World Models for Scalable End-to-End Driving"
title_zh: X-World：面向可扩展端到端驾驶的可控以自车为中心多相机世界模型
authors: "Chaoda Zheng, Sean Li, Jinhao Deng, Zhennan Wang, Shijia Chen, Liqiang Xiao, Ziheng Chi, Hongbin Lin, Kangjie Chen, Boyang Wang, Yu Zhang, Xianming Liu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19979v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 用于自动驾驶的动作条件多摄像头生成式世界模型
tldr: 针对端到端自动驾驶评估成本高、难复现的问题，本文提出了X-World，一种以动作为条件的多摄像头生成式世界模型。该模型能根据历史观测和未来动作生成逼真的多视角视频流，支持对动态物体、静态道路及外观属性的可控编辑。实验证明其具备优异的视角一致性、长期稳定性和可控性，为自动驾驶提供了可扩展的评估基础。
selection_source: fresh_fetch
motivation: 现有的端到端自动驾驶评估依赖昂贵的实地测试，缺乏可扩展性和可复现性，亟需一种可控且逼真的模拟环境。
method: 提出X-World模型，利用多视角潜变量视频生成器，根据历史观测和动作指令生成未来视频，并支持对交通元素和外观的精细控制。
result: 实验表明，该方法生成的多视角视频具有强视角一致性、长期时间稳定性以及高度的动作跟随与场景控制能力。
conclusion: X-World能够生成高质量、可控的多视角驾驶视频，为自动驾驶策略的可扩展、可复现评估提供了实用的基础平台。
---

## 摘要
在自动驾驶的端到端时代，可扩展且可靠的评估变得愈发关键，因为视觉-语言-动作 (VLA) 策略直接将原始传感器流映射为驾驶动作。然而，当前的评估流程仍严重依赖真实道路测试，这种方式成本高昂，场景覆盖范围有限且存在偏差，并难以复现。这些挑战催生了对真实世界模拟器的需求，该模拟器能够在给定动作下生成逼真的未来观测，并在长时间跨度内保持可控性和稳定性。我们提出了 X-World，这是一种以动作为条件的多相机生成式世界模型，可直接在视频空间模拟未来观测。给定同步的多视角相机历史数据和未来动作序列，X-World 能够生成遵循指令动作的未来多相机视频流。为了确保场景推演的可复现性和可编辑性，X-World 进一步支持对动态交通智能体和静态道路元素的可选控制，并保留了用于外观层面控制（如天气和时间段）的文本提示接口。除了世界模拟，X-World 还能通过以外观提示为条件，在保持底层动作和场景动态的同时实现视频风格迁移。X-World 的核心是一个多视角潜视频生成器，旨在显式促进多种控制信号下的跨视角几何一致性和时间连贯性。实验表明，X-World 实现了高质量的多视角视频生成，具有以下特点： 跨相机的强视角一致性， 长时推演中稳定的时间动态，以及 高可控性，表现为严格的动作遵循和对可选场景控制的忠实遵守。这些特性使 X-World 成为可扩展且可复现评估的实用基础。

## Abstract
Scalable and reliable evaluation is increasingly critical in the end-to-end era of autonomous driving, where vision--language--action (VLA) policies directly map raw sensor streams to driving actions. Yet, current evaluation pipelines still rely heavily on real-world road testing, which is costly, biased toward limited scenario coverage, and difficult to reproduce. These challenges motivate a real-world simulator that can generate realistic future observations under proposed actions, while remaining controllable and stable over long horizons. We present X-World, an action-conditioned multi-camera generative world model that simulates future observations directly in video space. Given synchronized multi-view camera history and a future action sequence, X-World generates future multi-camera video streams that follow the commanded actions. To ensure reproducible and editable scene rollouts, X-World further supports optional controls over dynamic traffic agents and static road elements, and retains a text-prompt interface for appearance-level control (e.g., weather and time of day). Beyond world simulation, X-World also enables video style transfer by conditioning on appearance prompts while preserving the underlying action and scene dynamics. At the core of X-World is a multi-view latent video generator designed to explicitly encourage cross-view geometric consistency and temporal coherence under diverse control signals. Experiments show that X-World achieves high-quality multi-view video generation with (i) strong view consistency across cameras, (ii) stable temporal dynamics over long rollouts, and (iii) high controllability with strict action following and faithful adherence to optional scene controls. These properties make X-World a practical foundation for scalable and reproducible evaluation.

---

## 论文详细总结（自动生成）

# 论文总结：X-World

## 1. 论文的核心问题与整体含义
*   **研究背景**：随着端到端自动驾驶（特别是视觉-语言-动作 VLA 模型）的发展，传统的模块化测试方法已不再适用。当前的评估主要依赖昂贵的真实道路测试，存在成本高、场景覆盖有限（长尾问题）、难以复现且存在安全隐患等问题。
*   **核心问题**：缺乏一个可扩展、可靠且可控的“真实世界模拟器”，能够根据驾驶动作生成逼真的未来观测，用于端到端系统的评估与训练。
*   **整体含义**：本文提出了 X-World，旨在构建一个生成式世界模型，直接在视频空间模拟未来多视角观测。该模型不仅能解决评估难题，还能作为闭环仿真环境支持在线强化学习（RL）和数据合成，推动端到端自动驾驶的发展。

## 2. 论文提出的方法论
*   **核心思想**：构建一个以动作为条件的多摄像头生成式世界模型。给定历史多视角视频流和未来动作序列，模型生成符合物理规律和几何一致性的未来视频。
*   **模型架构**：
    *   **基础框架**：基于 WAN 2.2 视频生成模型，采用 3D 因果变分自编码器（VAE）进行时空压缩（16x 空间，4x 时间），并使用 Diffusion Transformer (DiT) 作为核心去噪网络。
    *   **视角-时间自注意力**：设计专门的注意力模块，在时间维度和跨视角维度交替进行特征交互，显式强制多摄像头间的几何一致性和时间连贯性。
    *   **解耦交叉注意力**：针对异构控制信号（动作、动态物体、静态元素、文本），采用解耦的交叉注意力分支，减少不同条件间的干扰，提高可控性。
*   **条件注入机制**：
    *   **自车动作**：将速度、曲率等运动学状态通过 Symlog 归一化和傅里叶编码后，使用自适应层归一化注入。
    *   **动态/静态元素**：通过检测模型提取语义和位置信息，经 MLP 投影后通过交叉注意力注入，支持对交通流和道路拓扑的编辑。
    *   **外观控制**：保留文本条件分支，控制天气、时间等全局外观。
*   **训练流程**：
    *   **第一阶段（双向 I2V 训练）**：基于矫正流目标，训练双向模型以获取准确的可控性初始化。
    *   **第二阶段（因果少步训练）**：将模型转换为因果自回归生成器，支持流式推理。采用“自强迫”策略和分布匹配蒸馏（DMD）损失，解决长时推演中的误差累积问题，并实现少步生成以降低延迟。
*   **生成模式**：支持图像到视频（I2V）、视频到视频（V2V）和条件到视频（C2V，用于风格迁移）三种模式。

## 3. 实验设计
*   **数据集**：使用小鹏汽车精心整理的大规模真实世界驾驶数据集。包含 7 个同步摄像头（360度覆盖）、动态物体轨迹、静态道路元素标注及 VLM 生成的文本描述。视频以 12 FPS 采样，每个样本时长 10 秒。
*   **Benchmark**：文中未使用标准的公开 Benchmark 进行定量对比，而是基于自建数据集进行定性展示和功能性验证。
*   **对比方法**：论文主要展示模型自身的生成效果，未列出具体的基线模型进行定量指标对比。实验侧重于验证模型在可控性、一致性方面的能力。

## 4. 资源与算力
*   **算力细节**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **数据标注资源**：提到投入了大量计算资源用于整个训练数据集的自动标注（包括环境、静态、动态标签等）。

## 5. 实验数量与充分性
*   **实验内容**：
    *   **动作可控性验证**：展示模型能根据不同的驾驶指令（如直行、变道、转弯）生成对应的视频流。
    *   **场景可控性验证**：展示对动态物体（车辆）和静态元素（车道线）的控制能力，验证几何一致性。
    *   **长时推演验证**：展示 24 秒的长视频生成效果，验证时间稳定性。
    *   **多视角一致性**：定性展示不同摄像头间物体和场景的几何对齐。
    *   **外观编辑**：展示通过文本提示改变天气、时间、地域风格的能力。
    *   **应用展示**：展示了在闭环评估（如绕行停车辆、应对鬼探头）和数据合成中的实际应用案例。
*   **充分性与客观性**：实验主要依赖定性展示（可视化图片），缺乏定量指标（如 FVD、FID 或一致性误差的具体数值）。虽然展示的效果图具有很强的说服力，但缺乏与 SOTA 方法的定量对比，使得客观评估略显不足。

## 6. 论文的主要结论与发现
*   X-World 能够生成高质量、多视角一致的未来驾驶视频。
*   模型具备严格的动作跟随能力和精细的场景控制能力（动态/静态元素），支持可复现的仿真推演。
*   通过两阶段训练策略，模型实现了流式、实时的长时推演，解决了传统扩散模型难以用于实时交互的问题。
*   该模型成功应用于 VLA 2.0 系统的闭环评估、在线强化学习训练以及大规模数据增强，证明了其作为“真实世界模拟器”的实用价值。

## 7. 优点
*   **创新性强**：首次提出了支持多视角、多条件控制的流式生成式世界模型，直接面向端到端自动驾驶的评估痛点。
*   **架构设计合理**：视角-时间自注意力和解耦交叉注意力机制有效解决了多视角一致性和多条件控制难题。
*   **实用导向**：不仅关注生成质量，还通过因果化和少步化设计解决了实时交互和长时推演的工程落地问题。
*   **功能全面**：集成了动作控制、场景编辑、外观迁移等多种功能，应用场景广泛。

## 8. 不足与局限
*   **缺乏定量评估**：论文未提供标准的定量评价指标（如视频生成质量评分、一致性误差数值），主要依赖视觉展示，难以精确衡量其相对于其他 SOTA 方法的优势幅度。
*   **算力透明度低**：未披露具体的模型参数量（仅提及基于 WAN 2.2 5B）、训练硬件资源和时间成本，限制了社区对复现难度的评估。
*   **数据偏差风险**：模型依赖大规模真实数据训练，虽然支持编辑，但生成结果可能仍受限于训练数据的分布，对于极端罕见场景的生成能力未做深入探讨。
*   **物理真实性**：虽然强调几何一致性，但生成式模型仍可能存在物理规律违背（如幻觉）的情况，论文未详细讨论此类失败案例的分析。

（完）

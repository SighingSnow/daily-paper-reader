---
title: Coding Agents are Effective Long-Context Processors
title_zh: 代码智能体是有效的长上下文处理器
authors: "Weili Cao, Xunjian Yin, Bhuwan Dhingra, Shuyan Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20432v1"
tags: ["query:agent"]
score: 9.0
evidence: 评估编码智能体作为长上下文处理任务的通用接口
tldr: "现有大语言模型在处理长上下文时性能下降且注意力机制不可解释。本研究提出将长上下文处理外部化，利用编码智能体通过文件系统和原生工具来组织与操作文本。实验表明，该方法在长上下文推理、RAG等任务上平均优于现有SOTA 17.3%，证明了编码智能体是处理长上下文的有效替代方案。"
selection_source: fresh_fetch
motivation: 现有大语言模型在处理长上下文时性能显著下降，且注意力机制难以解释。
method: 利用编码智能体通过文件系统和原生工具显式地组织和操作文本，将长上下文处理外部化。
result: "在长上下文推理等任务中，该方法平均优于现有最先进方法17.3%。"
conclusion: 将长上下文处理委托给编码智能体是语义搜索或上下文窗口扩展的有效替代方案。
---

## 摘要
大语言模型 (LLMs) 在扩展以访问海量上下文方面取得了显著进展。然而，这种访问是通过潜在且不可解释的注意力机制实现的，LLMs 未能有效处理长上下文，随着上下文长度的增加表现出显著的性能下降。在这项工作中，我们研究了是否可以通过允许代码智能体在文件系统中组织文本并使用其原生工具进行操作，从而将长上下文处理从潜在注意力外部化为显式的、可执行的交互。我们评估了现有的前沿代码智能体作为处理长上下文任务的通用接口，任务包括长上下文推理、检索增强生成，以及涉及多达三万亿 token 大规模语料库的开放域问答。在多个基准测试中，这些智能体平均优于已发表的最先进方法 17.3%。我们将这种有效性归因于两个关键因素：原生工具熟练度，这使智能体能够利用可执行代码和终端命令而非被动的语义查询；以及文件系统熟悉度，这允许它们像浏览目录结构一样浏览海量文本语料库。这些发现表明，将长上下文处理委托给代码智能体为语义搜索或上下文窗口扩展提供了一种有效的替代方案，为 LLMs 中的长上下文处理开辟了新的方向。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable progress in scaling to access massive contexts. However, the access is via the latent and uninterpretable attention mechanisms, and LLMs fail to effective process long context, exhibiting significant performance degradation as context length increases. In this work, we study whether long-context processing can be externalized from latent attention into explicit, executable interactions, by allowing coding agents to organize text in file systems and manipulate it using its native tools. We evaluate off-the-shelf frontier coding agents as the general interface for tasks that require processing long contexts, including long-context reasoning, retrieval-augmented generation, and open-domain question answering with large-scale corpus contains up to three trillion tokens. Across multiple benchmarks, these agents outperform published state-of-the-art by 17.3% on average. We attribute this efficacy to two key factors: native tool proficiency, which enables agents to leverage executable code and terminal commands rather than passive semantic queries, and file system familiarity, which allows them to navigate massive text corpora as directory structures. These findings suggest that delegating long-context processing to coding agents offers an effective alternative to semantic search or context window scaling, opening new directions for long-context processing in LLMs.

---

## 论文详细总结（自动生成）

# 论文总结：Coding Agents are Effective Long-Context Processors

## 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLMs）虽然在扩展上下文窗口方面取得了进展，但在处理超长上下文时仍面临严峻挑战。随着上下文长度增加，模型性能显著下降（Context Rot），且注意力机制具有潜在性和不可解释性。
*   **现有局限**：传统的检索增强生成（RAG）方法虽然外部化了上下文访问，但其检索流程固定且浅层，难以支持复杂的迭代式多跳推理。
*   **核心问题**：能否将长上下文处理从模型内部的“潜在注意力”机制外部化为显式、可执行的交互，从而更有效地处理海量文本？
*   **整体含义**：本文提出利用代码智能体来处理长上下文任务，证明了将文本组织为文件系统结构并利用代码执行能力进行操作，是一种优于单纯扩展上下文窗口或传统RAG的有效替代方案。

## 2. 论文提出的方法论
*   **核心思想**：将长上下文处理重构为“文件系统导航与操作”任务。不将海量文本直接输入模型上下文窗口，而是将其组织为文件系统中的文件和目录，委托给代码智能体进行管理。
*   **关键流程**：
    1.  **语料格式化**：对于大规模语料库，将每个文档存储为单独的txt文件并组织在目录中；对于单个长文档，存储为单个txt文件。
    2.  **智能体接口**：使用现成的代码智能体（如 OpenAI Codex, Claude Code），仅提供文件路径和查询任务。
    3.  **自主工具调用**：智能体利用其原生能力，自主决定使用终端命令（如 `grep`, `sed`, `head`）进行搜索和提取，或编写 Python 脚本进行复杂的文本处理和聚合。
    4.  **迭代优化**：智能体根据中间执行结果动态调整搜索策略或修正代码逻辑，实现迭代式推理。
*   **关键能力支撑**：
    *   **原生工具熟练度**：智能体能利用可执行代码和终端命令进行精确操作，而非仅依赖被动的语义查询。
    *   **文件系统熟悉度**：智能体在代码训练中习得了目录结构导航能力，能高效定位和遍历海量文本。

## 3. 实验设计
*   **数据集 / 场景**：覆盖了从 188K 到 3 Trillion（万亿）tokens 的不同规模和类型的任务。
    *   **BrowseComp-Plus**：750M tokens，多跳网络浏览问答。
    *   **Oolong-Syn & Oolong-Real**：536K & 385K tokens，长文档推理与聚合任务。
    *   **LongBench-v2**：188K tokens，多样化长上下文理解任务。
    *   **Natural Questions (NQ)**：3T tokens，大规模开放域问答。
*   **对比方法**：
    *   **LLM Full Context**：GPT-5 直接处理（大语料库采用采样或滑动窗口）。
    *   **RAG**：标准检索增强生成流程。
    *   **ReAct Agent**：配备检索工具的智能体。
    *   **RLM (Recursive Language Models)**：递归语言模型。
    *   **Best Published**：各数据集上已发表的最优结果。
*   **评估指标**：Accuracy（BrowseComp-Plus, LongBench）、Exact Match（NQ）、Score（Oolong）。

## 4. 资源与算力
*   **算力说明**：论文**未明确提及**模型训练所用的 GPU 型号、数量或训练时长。这是因为研究主要基于现成的商业 API 模型（GPT-5, Claude Sonnet 4.5）进行推理评估，而非从头训练模型。
*   **成本分析**：论文提供了详细的推理成本分析。例如，在 BrowseComp-Plus 上，Codex (No Retriever) 的平均查询成本为 $0.703，高于 RAG ($0.111) 但低于 RLM ($0.920)。

## 5. 实验数量与充分性
*   **实验规模**：共涉及 5 个主流基准数据集，跨越 5 个数量级的上下文规模。
*   **消融实验**：
    *   **文件结构影响**：对比了“文件夹结构”与“单文件”组织方式，证明文件夹结构更优。
    *   **检索工具影响**：对比了“无检索器”、“BM25”、“Gemini Embedding”三种配置，发现原生工具往往优于配备外部检索器。
    *   **策略分析**：量化分析了智能体在不同任务中的搜索强度、阅读量和代码生成量。
*   **公平性与客观性**：
    *   对于 NQ 数据集，因计算成本限制，随机采样了 200 个样本并重跑所有基线，保证了对比的公平性。
    *   使用了 GPT-5 作为 Judge 进行自动评估，并采用了标准化的评估协议。
    *   实验设计覆盖了不同类型的任务（检索、推理、聚合），充分验证了方法的泛化能力。

## 6. 论文的主要结论与发现
*   **性能表现**：代码智能体在 5 个基准测试中的 4 个上取得了新的 SOTA，平均优于已发表的最优方法 **17.3%**。
*   **结构优势**：文件系统结构能激发智能体的“索引与切片”策略，比单文件结构性能提升显著（如 BrowseComp-Plus 上 +6% ~ +7%）。
*   **反直觉发现**：为代码智能体配备标准检索工具并不总能提升性能，甚至可能降低性能。原因是检索工具可能抑制了智能体原生的探索和搜索行为。
*   **涌现策略**：智能体能根据任务类型自主涌现出特定策略，如多跳推理时的“迭代查询优化”、分析任务时的“程序化聚合”、阅读理解时的“混合搜索阅读”。

## 7. 优点
*   **创新视角**：首次系统性地将代码智能体作为长上下文处理的通用接口，避开了 LLM 上下文窗口限制和注意力机制的缺陷。
*   **无需微调**：直接利用现成的代码智能体，无需针对特定任务进行架构修改或微调，具有极高的通用性和易用性。
*   **可解释性强**：智能体的操作过程（终端命令、Python 脚本）是完全显式和可解释的，便于调试和分析。
*   **策略灵活性**：智能体能根据任务需求动态调整策略（写代码、搜文件、读文本），优于固定流程的 RAG 或 ReAct。

## 8. 不足与局限
*   **推理成本较高**：相比简单的 RAG 方法，代码智能体的推理成本较高（虽然性能提升显著），可能限制其在极大规模应用中的普及。
*   **检索工具集成问题**：论文发现简单集成检索工具可能干扰智能体原生能力，如何更好地融合检索与原生工具仍需进一步研究。
*   **模型依赖性**：该方法的效果高度依赖于基座模型（如 GPT-5, Claude）的代码执行和推理能力，对于能力较弱的模型效果未知。
*   **应用场景限制**：主要针对文本处理任务，对于非文本数据（如多模态）或非结构化程度极高的数据，文件系统组织方式可能需要额外适配。

（完）

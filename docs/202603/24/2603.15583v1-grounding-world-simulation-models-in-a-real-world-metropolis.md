---
title: Grounding World Simulation Models in a Real-World Metropolis
title_zh: 将世界仿真模型锚定于现实大都市
authors: "Junyoung Seo, Hyunwook Choi, Minkyung Kwon, Jinhyeok Choi, Siyoon Jin, Gayoung Lee, Junho Kim, JoungBin Lee, Geonmo Gu, Dongyoon Han, Sangdoo Yun, Seungryong Kim, Jin-Hwa Kim"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15583v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 基于真实城市的城市级世界模型
tldr: 现有世界模型多生成虚构环境，本文提出基于真实首尔城市的Seoul World Model (SWM)。该模型通过检索增强条件实现自回归视频生成，利用跨时间配对、合成数据集和视图插值解决时空错位和数据稀疏问题，并引入Virtual Lookahead Sink稳定长时生成。实验表明，SWM在生成空间真实、时间一致的长距离城市视频方面优于现有方法。
selection_source: fresh_fetch
motivation: 现有世界模型多生成虚构环境，本文旨在构建能渲染真实城市（首尔）的世界模型。
method: 提出Seoul World Model，通过检索增强条件、跨时间配对、视图插值及Virtual Lookahead Sink技术，解决真实城市场景下的时空错位与数据稀疏挑战。
result: 在首尔、釜山和安娜堡的测试中，SWM在生成空间忠实且时间一致的长时域视频方面优于现有方法。
conclusion: SWM成功实现了基于真实城市环境的长距离、高保真视频生成，支持多样的摄像机运动和文本提示变化。
---

## 摘要
如果世界仿真模型能够渲染的不是虚构的环境，而是现实存在的城市，将会怎样？先前的生成式世界模型通过想象所有内容，合成了视觉上合理但虚构的环境。我们提出了首尔世界模型（SWM），这是一个锚定于现实首尔城市的城市级世界模型。SWM 通过对附近街景图像进行检索增强条件化，来锚定自回归视频生成。然而，这种设计带来了若干挑战，包括检索参考图与动态目标场景之间的时间不对齐、轨迹多样性受限，以及因稀疏间隔的车载拍摄导致的数据稀疏性。我们通过跨时间配对、一个支持多样相机轨迹的大规模合成数据集，以及一个从稀疏街景图像合成连贯训练视频的视图插值流水线，来解决这些挑战。我们进一步引入了虚拟前瞻汇点（Virtual Lookahead Sink），通过将每个片段持续重新锚定到未来位置的检索图像，来稳定长程生成。我们在三个城市：首尔、釜山和安娜堡，将 SWM 与近期的视频世界模型进行了对比评估。SWM 在生成锚定于真实城市环境、长达数百米轨迹的空间保真度高且时间连贯的长程视频方面，优于现有方法，同时支持多样的相机运动和文本提示的场景变化。

## Abstract
What if a world simulation model could render not an imagined environment but a city that actually exists? Prior generative world models synthesize visually plausible yet artificial environments by imagining all content. We present Seoul World Model (SWM), a city-scale world model grounded in the real city of Seoul. SWM anchors autoregressive video generation through retrieval-augmented conditioning on nearby street-view images. However, this design introduces several challenges, including temporal misalignment between retrieved references and the dynamic target scene, limited trajectory diversity and data sparsity from vehicle-mounted captures at sparse intervals. We address these challenges through cross-temporal pairing, a large-scale synthetic dataset enabling diverse camera trajectories, and a view interpolation pipeline that synthesizes coherent training videos from sparse street-view images. We further introduce a Virtual Lookahead Sink to stabilize long-horizon generation by continuously re-grounding each chunk to a retrieved image at a future location. We evaluate SWM against recent video world models across three cities: Seoul, Busan, and Ann Arbor. SWM outperforms existing methods in generating spatially faithful, temporally consistent, long-horizon videos grounded in actual urban environments over trajectories reaching hundreds of meters, while supporting diverse camera movements and text-prompted scenario variations.

---

## 论文详细总结（自动生成）

# 论文总结：将世界仿真模型锚定于现实大都市

## 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视频世界模型通常生成完全虚构的想象环境，缺乏对真实物理世界的对应关系。本文旨在解决如何将世界模型“锚定”于真实存在的城市环境（以首尔为例），使其能够根据用户指定的轨迹和文本提示，生成符合真实城市几何结构与外观的动态视频。
*   **整体含义**：该研究推动了世界模型从“纯生成”向“真实世界仿真”的转变。通过结合大规模街景数据与生成模型，SWM 能够支持城市规划可视化、自动驾驶场景生成以及基于位置的沉浸式探索，具有重要的应用价值。

## 2. 论文提出的方法论
本文提出了 **Seoul World Model (SWM)**，核心思想是利用检索增强的条件化生成，将自回归视频生成过程锚定到真实的街景图像数据库中。

*   **核心流程**：
    1.  用户输入起始位置、相机轨迹和文本提示。
    2.  系统从数据库中检索附近的街景图像作为参考。
    3.  模型基于参考图像、历史帧和条件生成视频片段，并通过自回归方式扩展至长视频。

*   **关键技术细节**：
    *   **跨时间配对**：
        *   **问题**：街景图像是在不同时间拍摄的，包含动态物体（如车辆、行人），若直接作为参考会导致生成内容不一致。
        *   **方法**：训练时强制要求参考图像与目标视频序列来自不同的时间戳。这迫使模型学习忽略瞬态物体，仅关注持久的场景结构（建筑、道路）。
    *   **视图插值流水线**：
        *   **问题**：街景数据空间间隔稀疏，直接训练难以生成连续视频。
        *   **方法**：提出“间歇性冻结帧策略”。针对 3D VAE 的 4 帧时间压缩机制，将关键帧重复 4 次以形成完整的压缩单元，生成后再丢弃冗余帧，从而合成连贯的训练视频。
    *   **虚拟前瞻汇点**：
        *   **问题**：长时自回归生成会导致误差累积和漂移，传统的“首帧锚点”在相机移动较远后失效。
        *   **方法**：在生成当前片段时，检索未来位置的街景图像作为“虚拟汇点”，将其编码为潜在变量并放置在当前时间窗口之后。这为模型提供了一个未来的“目标锚点”，有效抑制了漂移。
    *   **几何与语义参考双路径**：
        *   **几何参考**：利用深度估计将参考图像重投影到目标视角，提供空间布局约束。
        *   **语义参考**：直接注入参考图像的潜在特征，补充外观细节。

## 3. 实验设计
*   **数据集**：
    *   **训练数据**：
        *   **首尔街景数据**：收集了 120 万张全景图，处理后使用 44 万张图像。
        *   **Waymo 开放数据集**：用于增加驾驶场景的多样性。
        *   **CARLA 合成数据**：构建了包含行人、车辆和自由相机轨迹的合成数据集，弥补真实街景轨迹单一的不足。
    *   **测试 Benchmark**：
        *   **Busan-City-Bench**：新构建的釜山城市测试集（模型未见过的城市）。
        *   **Ann-Arbor-City-Bench**：基于 MARS 数据集构建的安娜堡测试集（模型未见过的城市）。
        *   每个测试集包含 30 个序列，每个序列约 365 帧（约 100 米）。
*   **对比方法**：
    *   **动态世界模型**：Aether, DeepVerse, Yume1.5, HY-World1.5, FantasyWorld, Lingbot。
    *   **静态场景生成模型**：GEN3C, VMem（用于补充对比）。
*   **评价指标**：
    *   **视觉质量**：FID, FVD, Image Quality (VBench)。
    *   **相机控制精度**：旋转误差、平移误差。
    *   **3D 一致性**：静态区域的 PSNR 和 LPIPS（通过 SAM3 分割动态物体后计算）。

## 4. 资源与算力
*   **硬件配置**：使用了 24 张 NVIDIA H100 GPU。
*   **训练细节**：
    *   基于 Cosmos-Predict2.5-2B 模型进行微调。
    *   Batch size 为 48。
    *   Teacher-Forcing (TF) 模式训练 10K 次迭代。
    *   Self-Forcing (SF) 模式先进行 6K 步 ODE 初始化，再微调 10K 步。
*   **推理性能**：Self-Forcing (SF) 变体在单张 H100 GPU 上可实现 15.2 fps 的生成速度。

## 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨城市（首尔、釜山、安娜堡）的泛化能力测试。
    *   针对数据构建策略（跨时间配对、合成数据）、条件化路径（几何/语义参考）、注意力汇点设计进行了详细的消融实验。
    *   额外分析了参考图像稀疏度对性能的影响以及长时生成（1460 帧）的稳定性。
*   **评价**：实验设计充分且客观。通过在训练数据中排除测试城市，严格验证了模型的泛化能力；消融实验清晰地验证了各个模块的有效性；对比方法涵盖了当前主流的视频世界模型，具有说服力。

## 6. 论文的主要结论与发现
*   **性能优越性**：SWM 在生成空间保真度高、时间连贯的长程视频方面显著优于现有的世界模型，能够生成长达数百米且符合真实城市布局的视频。
*   **关键模块有效性**：
    *   跨时间配对是模型能够忽略街景中动态物体干扰的关键，去除该策略会导致性能大幅下降。
    *   Virtual Lookahead Sink 有效解决了长时生成中的漂移问题，优于传统的首帧锚点。
    *   合成数据补充了真实街景中缺乏的多样化轨迹（如行人视角），提升了相机控制的鲁棒性。

## 7. 优点
*   **创新性强**：首次提出了基于真实城市的“世界模型”概念，并成功实现了大规模真实环境下的可控视频生成。
*   **针对性强**：针对街景数据稀疏、时间不一致等具体痛点设计了专门的技术方案（如间歇性冻结帧、跨时间配对）。
*   **泛化能力出色**：模型仅在首尔数据上训练，即可在未见过的釜山和安娜堡数据集上取得优异表现，证明了其强大的场景理解与生成能力。

## 8. 不足与局限
*   **数据质量瓶颈**：由于缺乏大规模连续的城市视频数据，训练数据依赖从稀疏街景插值生成的视频，其质量低于真实拍摄视频，限制了生成视频的上限。
*   **动态物体伪影**：论文指出，由于街景元数据的时间噪声，训练数据中偶尔存在物体突变现象，导致模型生成的视频中车辆可能突然出现或消失。
*   **依赖街景覆盖**：模型的生成能力受限于街景数据库的覆盖范围，对于未覆盖区域或街景数据质量差的区域，生成效果可能受限。

（完）

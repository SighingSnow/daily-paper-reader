---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 面向高效机器人策略学习的动作中心世界-动作模型
tldr: 针对现有世界-动作模型推理开销大且表征纠缠的问题，本文提出GigaWorld-Policy。该模型通过因果设计解耦动作预测与视频生成，允许在推理时跳过视频生成，从而实现高效解码。实验表明，该方法比基线Motus快9倍且成功率更高，在RoboTwin 2.0上性能提升显著。
selection_source: fresh_fetch
motivation: 现有世界-动作模型存在推理开销大以及视觉与运动表征纠缠导致预测依赖视频质量的瓶颈。
method: 提出GigaWorld-Policy，通过因果设计解耦动作预测与视频生成，允许推理时跳过视频生成以加速解码。
result: "真实机器人实验表明，该方法比Motus快9倍且成功率提高7%，在RoboTwin 2.0上性能提升95%。"
conclusion: GigaWorld-Policy通过解耦设计与高效推理，显著提升了机器人策略学习的性能与部署速度。
---

## 摘要
从预训练视频生成骨干网络初始化的世界-动作模型（WAM）在机器人策略学习中展示了巨大的潜力。然而，现有方法面临两个阻碍性能与部署的关键瓶颈。首先，对未来视觉动态和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模往往导致视觉与运动表征的纠缠，使得运动预测的准确性严重依赖于未来视频预测的质量。为了解决这些问题，我们提出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它学习 2D 像素-动作动态，同时实现高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练构建为两个耦合组件：模型基于当前观测预测未来动作序列，同时基于预测的动作和同一观测生成未来视频。该策略受动作预测和视频生成的双重监督，提供了更丰富的学习信号，并通过视觉动态约束鼓励生成物理上合理的动作。通过阻止未来视频 token 影响动作 token 的因果设计，显式的未来视频生成在推理阶段变为可选，从而允许在部署期间进行更快的动作预测。为了支持这一范式，我们构建了一个多样化的大规模机器人数据集，用于预训练一个以动作为中心的视频生成模型，随后将其适配为机器人策略学习的骨干网络。真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基线 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

# 论文总结：GigaWorld-Policy

## 1. 核心问题与整体含义
*   **研究背景**：现有的视觉-语言-动作（VLA）模型虽然表现强劲，但面临“监督稀疏”问题，即动作标签稀疏而观测数据高维丰富，导致模型易陷入上下文捷径。引入世界模型（WM）进行视频预测虽能提供密集监督，但现有的世界-动作模型（WAM）在推理时需要迭代采样生成未来视频，导致高延迟，且视频预测误差会传播至动作解码，影响长时域控制。
*   **核心问题**：如何在利用未来视觉动态提供密集监督的同时，消除推理阶段对显式视频生成的依赖，从而实现低延迟、高性能的机器人控制。
*   **整体含义**：本文提出一种“以动作为中心”的世界-动作模型，旨在解耦训练时的密集监督与推理时的生成开销，解决 WAM 模型效率与性能难以兼得的瓶颈。

## 2. 方法论
*   **核心思想**：提出 **GigaWorld-Policy**，一种以动作为中心的 WAM。在训练阶段，模型联合预测动作序列和未来视频，利用视频预测作为辅助监督信号；在推理阶段，通过因果设计使未来视频生成为可选项，允许模型仅解码动作，从而大幅降低延迟。
*   **架构设计**：
    *   基于 5B 参数的扩散 Transformer（Wan 2.2）骨干网络。
    *   **输入 Token 化**：将多视角观测拼接为复合图像，经 VAE 编码为视觉 Token ($T_o, T_f$)；本体状态和动作通过线性投影编码为 Token ($T_s, T_a$)；语言指令通过交叉注意力注入。
    *   **因果自注意力机制**：这是关键设计。将 Token 序列组织为 $[T_o; T_s; T_a; T_f]$，并施加因果掩码：
        *   动作 Token ($T_a$) 仅关注当前观测 ($T_o$) 和状态 ($T_s$)，**不可见**未来视频 Token ($T_f$)。
        *   未来视频 Token ($T_f$) 可关注动作、状态和观测。
        *   这种设计防止了未来信息泄露，保证了推理时可以跳过 $T_f$ 的生成。
*   **训练流程**：
    *   **预训练**：先从大规模网络视频模型初始化，然后在约 10,000 小时的具身数据（机器人视频 + 自我中心人类视频）上进行预训练，注入物理先验。
    *   **后训练**：在目标任务轨迹上微调，对齐图像、语言和动作。
    *   **目标函数**：采用流匹配损失，联合优化动作预测损失 ($\mathcal{L}_{action}$) 和未来视频预测损失 ($\mathcal{L}_{video}$)。
*   **推理流程**：仅采样动作 Token，无需生成视频 Token，实现低延迟闭环控制。

## 3. 实验设计
*   **数据集**：
    *   **预训练数据**：约 10,000 小时，包含 EgoDex, EGO4D（人类视频），Agibot, DROID, Open X-Embodiment（机器人视频）等。
    *   **仿真评测**：**RoboTwin 2.0**，包含 50 个代表性操作任务，分为 Clean（干净场景）和 Randomized（随机化场景）两种设置。
    *   **真实世界评测**：在 AgileX PiPER 6-DoF 机械臂上进行，包含 4 个任务：清理桌面、扫描二维码、堆叠碗、清扫垃圾。
*   **对比方法**：
    *   **VLA 类**：$\pi_0.5$, GigaBrain-0, X-VLA。
    *   **WAM 类**：Motus, Cosmos-Policy。
*   **评测指标**：任务成功率（SR），仿真中为二值指标，真实世界中采用分级评分（如抓取 0.5 分，放置 0.5 分）。

## 4. 资源与算力
*   **训练算力**：论文补充材料提及预训练使用了 **6000 GPU 小时**（未明确具体 GPU 型号）。
*   **推理硬件**：推理延迟测试在 **NVIDIA A100 GPU** 上进行。
*   **模型规模**：骨干网络为 5B（50亿）参数的扩散 Transformer。

## 5. 实验数量与充分性
*   **实验数量**：
    *   仿真实验：在 RoboTwin 2.0 上进行了大规模评测（50 个任务）。
    *   真实世界实验：4 个任务，每个任务 20 次试验。
    *   消融实验：包括预训练策略对比（从头训练 vs 不同预训练组合）、预训练数据量影响、未来帧预测数量 ($\Delta$) 影响、因果掩码 vs 自注意力对比。
*   **充分性与公平性**：
    *   实验设计较为充分，涵盖了仿真与真实环境，对比了 SOTA 的 VLA 和 WAM 方法。
    *   消融实验详细验证了各个组件（如因果掩码、预训练数据）的贡献。
    *   评测指标客观，且在真实世界实验中考虑了多级评分，较为公平。

## 6. 主要结论与发现
*   **效率提升**：GigaWorld-Policy 比 SOTA 的 WAM 方法 Motus 快 **9 倍**（推理时间降至 0.36s），同时任务成功率提高了 **7%**。
*   **性能优越**：在 RoboTwin 2.0 仿真基准上，相比 $\pi_0.5$ 性能提升 **95%**；在真实世界任务中，相比基线方法有显著优势。
*   **数据效率**：仅需 VLA 基线 10% 的数据即可达到其最高性能，证明了世界模型先验的有效性。
*   **设计有效性**：因果掩码设计不仅保证了推理效率，还提升了视频生成的质量（PSNR/SSIM 更高），证明了物理合理的解耦有助于表征学习。

## 7. 优点
*   **架构创新**：巧妙利用因果掩码解耦动作预测与视频生成，成功将 WAM 的训练优势（密集监督）与 VLA 的推理优势（低延迟）结合。
*   **训练策略**：提出了从通用视频模型到具身预训练再到任务微调的课程学习范式，有效利用了大规模异构数据。
*   **综合性能**：在显著降低推理延迟的同时并未牺牲性能，反而因引入视觉动态约束提升了动作的物理合理性。

## 8. 不足与局限
*   **真实世界任务规模**：虽然仿真实验规模较大，但真实世界实验仅包含 4 个任务，任务多样性相对有限，可能无法完全覆盖极端复杂的长时域操作场景。
*   **模型体积**：基于 5B 参数的扩散模型，对部署硬件要求较高（如 A100），可能限制了其在边缘设备或算力受限场景下的应用。
*   **视频生成的必要性**：虽然推理时可跳过，但训练仍依赖高质量的视频预测监督，若视频生成质量不佳（如遮挡严重场景），可能会影响动作学习的质量。

（完）

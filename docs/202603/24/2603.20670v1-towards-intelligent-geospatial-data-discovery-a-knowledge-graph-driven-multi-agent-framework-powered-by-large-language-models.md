---
title: "Towards Intelligent Geospatial Data Discovery: a knowledge graph-driven multi-agent framework powered by large language models"
title_zh: 迈向智能地理空间数据发现：一种由大语言模型赋能的知识图谱驱动多智能体框架
authors: "Ruixiang Liu, Zhenlong Li, Ali Khosravi Kazazi"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20670v1"
tags: ["query:agent"]
score: 9.0
evidence: 智能数据发现的多智能体框架
tldr: 针对地理空间数据异构性强、现有检索方式语义支持不足的问题，本研究提出了一种由大语言模型驱动、基于知识图谱的多智能体框架。该框架通过统一元数据本体和知识图谱实现语义对齐，利用多智能体协作完成意图解析与检索，显著提升了数据发现的准确性与透明度，推动了自主GIS的发展。
selection_source: fresh_fetch
motivation: 现有地理空间数据检索主要依赖关键词搜索，缺乏语义支持且难以捕捉用户意图，导致检索性能不佳。
method: 提出一种基于大语言模型的知识图谱驱动多智能体框架，利用统一本体对齐元数据，并通过多智能体协作实现意图解析与检索。
result: 实验表明该框架在意图匹配准确率、排序质量、召回率及发现透明度上均显著优于传统系统。
conclusion: 该研究推动了地理空间数据发现向语义化与智能化范式转变，为下一代自主空间数据基础设施奠定了基础。
---

## 摘要
地理空间数据在体量、多样性和速度上的快速增长，造就了高度分布式、异构且语义不一致的数据生态系统。现有的数据目录、门户和基础设施在很大程度上仍依赖基于关键词的搜索，语义支持有限，往往无法捕捉用户意图，导致检索性能不佳。为应对这些挑战，本研究提出了一种由大语言模型赋能的知识图谱驱动多智能体框架，用于智能地理空间数据发现。该框架引入统一的地理空间元数据本体作为语义中介层，以对齐跨平台的异构元数据标准，并构建地理空间元数据知识图谱，以显式建模数据集及其多维关系。基于该结构化表示，框架采用多智能体协作架构执行意图解析、知识图谱检索和答案合成，形成了从用户查询到结果的可解释闭环发现过程。代表性用例和性能评估结果表明，与传统系统相比，该框架显著提高了意图匹配准确率、排序质量、召回率和发现透明度。本研究将地理空间数据发现推向更具语义化、意图感知和智能化的范式，为下一代智能自主空间数据基础设施提供了实践基础，并为自主GIS（Autonomous GIS）的宏大愿景做出了贡献。

## Abstract
The rapid growth in the volume, variety, and velocity of geospatial data has created data ecosystems that are highly distributed, heterogeneous, and semantically inconsistent. Existing data catalogs, portals, and infrastructures still rely largely on keyword-based search with limited semantic support, which often fails to capture user intent and leads to weak retrieval performance. To address these challenges, this study proposes a knowledge graph-driven multi-agent framework for intelligent geospatial data discovery, powered by large language models. The framework introduces a unified geospatial metadata ontology as a semantic mediation layer to align heterogeneous metadata standards across platforms and constructs a geospatial metadata knowledge graph to explicitly model datasets and their multidimensional relationships. Building on the structured representation, the framework adopts a multi-agent collaborative architecture to perform intent parsing, knowledge graph retrieval, and answer synthesis, forming an interpretable and closed-loop discovery process from user queries to results. Results from representative use cases and performance evaluation show that the framework substantially improves intent matching accuracy, ranking quality, recall, and discovery transparency compared with traditional systems. This study advances geospatial data discovery toward a more semantic, intent-aware, and intelligent paradigm, providing a practical foundation for next-generation intelligent and autonomous spatial data infrastructures and contributing to the broader vision of Autonomous GIS.

---

## 论文详细总结（自动生成）

以下是对论文《Towards Intelligent Geospatial Data Discovery: a knowledge graph-driven multi-agent framework powered by large language models》的详细总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着地理空间数据在体量、多样性和速度上的快速增长，现有的数据生态系统呈现出高度分布式、异构且语义不一致的特征。
*   **核心问题**：现有的数据目录和门户主要依赖基于关键词的搜索机制，缺乏深度的语义支持，难以准确捕捉用户的真实意图，导致检索性能较弱，用户往往需要通过反复试错才能找到所需数据。
*   **研究动机**：为了解决上述问题，并响应“自主GIS”（Autonomous GIS）中关于数据自动收集与准备的需求，本研究旨在构建一个能够理解自然语言意图、进行语义检索并提供可解释结果的智能数据发现框架。

### 2. 论文提出的方法论
本研究提出了一种由大语言模型（LLM）赋能的知识图谱驱动多智能体框架，核心思想是利用统一本体对齐异构元数据，通过多智能体协作实现从自然语言查询到结构化结果的闭环。

*   **统一地理空间元数据本体**：
    *   设计了一个语义中介层，将STAC、FGDC CSDGM、CKAN等主流元数据标准映射到统一的本体模型。
    *   定义了核心实体（如Dataset, Topic, Space, Time, Organization等）及其语义关系（如HAS_TOPIC, PUBLISHED_BY等），以显式建模数据集的多维特征。
*   **地理空间元数据知识图谱构建**：
    *   **数据摄取与归一化**：解析不同来源的元数据并映射到统一本体。
    *   **实体嵌入与主题生成**：利用嵌入模型计算实体向量，并使用LLM自动生成数据集的主题实体。
    *   **时空增强**：补充缺失的时空元数据。
    *   **索引创建**：建立向量索引、空间索引和时间索引以优化查询。
*   **多智能体协作架构**：
    *   **编排与记忆模块**：负责工作流控制、状态管理和记忆存储（存储用户查询、意图及中间结果）。
    *   **意图解析智能体**：
        *   将自然语言转换为结构化意图表示。
        *   引入置信度评估机制，公式为：$Conf(I) = \alpha \cdot A(I) + \beta \cdot P(I)$，其中$A(I)$为提取准确率，$P(I)$为语义合理性。若置信度低，则触发人机交互进行澄清。
    *   **图检索智能体**：
        *   **实体匹配**：基于向量相似度匹配候选实体。
        *   **硬约束过滤**：强制过滤不满足时空约束的数据集。
        *   **多维相关性评分**：综合主题、空间、时间、组织等维度计算得分，公式为：$Score_{raw}(d) = \sum W_{dim} \cdot S_{dim}(d)$。时空维度采用F1分数衡量重叠度。
    *   **答案合成智能体**：对Top K候选结果进行重排序，生成Top N结果，并生成解释性文本和子图可视化，增强透明度。

### 3. 实验设计
*   **数据集**：集成了6个异构数据目录，共包含264,177个数据集。来源包括Google Earth Engine Proxy for openEO、Microsoft Planetary Computer、DEDL HDA、Paituli STAC、PASDA（FGDC标准）以及Data.gov（CKAN标准）。
*   **Benchmark**：构建了包含100个测试查询的数据集，按约束复杂度分为4个等级（Level 1：仅主题；Level 2：主题+单一时空；Level 3：主题+时空；Level 4：复杂多约束）。
*   **评价指标**：
    *   **EIMR (Exact Intent Match Rate)**：评估意图解析的准确率。
    *   **NDCG@10**：评估前10个结果的排序质量。
    *   **Recall@20**：评估前20个结果的召回率。
*   **对比方法**：
    *   **基线方法**：传统的Data.gov门户搜索。
    *   **消融实验**：对比了完整框架（IGDD-G&A）、仅使用Data.gov源的框架（IGDD(D)-G&A）、移除答案合成智能体的框架（IGDD-G）等变体。

### 4. 资源与算力
*   **硬件环境**：文中仅提及部署在高性能服务器环境，使用Neo4j图数据库存储知识图谱（包含约280万实体和1220万关系）。
*   **模型资源**：
    *   使用OpenAI的**GPT-5.2 API**作为底层LLM服务。
    *   使用OpenAI的**text-embedding-3-large**模型生成实体嵌入向量。
*   **算力细节**：**文中未明确说明具体的GPU型号、数量或训练时长**。由于该框架主要基于推理和检索，文中提到单次完整发现周期（含LLM延迟）约需30-40秒，消耗约11,000 tokens。

### 5. 实验数量与充分性
*   **实验数量**：
    *   **定量评估**：基于100个测试查询进行了性能评估，涵盖了不同复杂度的场景。
    *   **定性用例**：展示了6个代表性用例，包括气候数据发现、美国人口普查数据发现、公共卫生数据发现、卫星影像数据发现、本地数据目录发现以及人机交互与多轮对话演示。
*   **充分性与客观性**：
    *   实验设计较为充分，不仅对比了传统方法，还进行了详细的消融实验以验证各组件贡献。
    *   评估指标涵盖了准确率、排序质量和召回率，较为全面。
    *   采用了人工标注的相关性评分（0-3分）作为基准，保证了评估的客观性。
    *   实验场景覆盖了全球、国家、区域等不同空间尺度以及多源异构数据，具有较好的代表性。

### 6. 论文的主要结论与发现
*   **性能显著提升**：提出的框架在意图匹配准确率上达到99.27%；在排序质量上，NDCG@10达到85.22%，相比Data.gov（43.65%）提升了41.57%；在召回率上，Recall@20达到66.54%，相比Data.gov（34.61%）提升了31.93%。
*   **组件有效性**：消融实验证明，答案合成智能体在提升排序质量方面起到了关键作用（NDCG@10提升了约16.82%）。
*   **复杂查询处理能力**：随着查询复杂度的增加，传统方法的性能急剧下降，而本框架仍能保持较高的性能水平。
*   **透明度与交互性**：框架能够生成匹配理由和子图可视化，提供了可解释的发现过程；人机交互机制有效解决了模糊查询问题。

### 7. 优点
*   **语义对齐能力强**：通过统一本体和知识图谱有效解决了多源异构元数据的语义不一致问题。
*   **架构设计合理**：多智能体分工明确，实现了意图解析、检索、合成的闭环，且具备记忆和状态管理能力。
*   **鲁棒性与交互性**：引入置信度评估和人机交互机制，能够处理模糊或低置信度的查询，提升了系统的鲁棒性。
*   **可解释性**：不仅返回结果，还提供匹配理由和知识图谱子图，增强了用户信任。

### 8. 不足与局限
*   **流程覆盖不全**：目前仅关注数据发现阶段，尚未涵盖数据预处理和分析就绪数据的生成，无法支持完整的自主分析流程。
*   **尺度匹配缺失**：未充分考虑空间和时间分辨率的匹配与尺度兼容性问题，可能导致检索结果在粒度上不符合用户需求。
*   **依赖商业API**：核心推理能力依赖GPT-5.2 API，存在成本较高（约11,000 tokens/query）和延迟问题（30-40秒/query），可能限制其在大规模并发场景下的应用。
*   **评估样本局限**：虽然设计了100个查询，但相对于海量的地理空间数据空间，样本量仍相对有限，可能存在一定的偏差风险。

（完）

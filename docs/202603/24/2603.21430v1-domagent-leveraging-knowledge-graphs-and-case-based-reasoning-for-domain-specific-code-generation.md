---
title: "DomAgent: Leveraging Knowledge Graphs and Case-Based Reasoning for Domain-Specific Code Generation"
title_zh: DomAgent：利用知识图谱与基于案例的推理进行特定领域代码生成
authors: "Shuai Wang, Dhasarathy Parthasarathy, Robert Feldt, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21430v1"
tags: ["query:agent"]
score: 10.0
evidence: 领域特定代码生成的自主编程智能体
tldr: 针对大语言模型在领域特定代码生成中因缺乏专业知识而表现不佳的问题，本文提出了DomAgent框架。该框架通过核心组件DomRetriever，结合知识图谱推理和案例推理，模拟人类学习过程以检索结构化知识和典型案例。实验表明，该方法显著提升了代码生成质量，使小模型在复杂任务中缩小了与大模型的差距。
selection_source: fresh_fetch
motivation: 现有大语言模型因缺乏领域特定知识，在实际软件开发场景中的代码生成成功率较低。
method: 提出DomAgent框架，通过DomRetriever模块结合知识图谱推理与案例推理，实现结构化知识与典型案例的迭代检索与合成。
result: 在DS-1000数据集和实际卡车软件开发任务上的实验显示，该方法显著提升了领域特定代码生成的性能。
conclusion: DomAgent有效弥补了通用模型在专业领域的不足，使小型开源模型在复杂应用中缩小了与大型专有模型的性能差距。
---

## 摘要
大语言模型在代码生成方面展现了卓越的能力。然而，由于大多数LLM基于公共领域语料库训练，将其直接应用于实际软件开发往往成功率较低，因为这些场景通常需要特定领域的知识。特别是，特定领域任务通常需要高度专业化的解决方案，而这些方案在通用LLM的训练数据中往往代表性不足或完全缺失。为了应对这一挑战，我们提出了DomAgent，这是一种自主编程智能体，通过结构化推理和针对性检索使LLM能够生成领域适应的代码，从而弥合这一差距。DomAgent的一个核心组件是DomRetriever，这是一个新颖的检索模块，通过结合概念理解与经验案例，模拟人类学习特定领域知识的方式。该模块动态整合了自顶向下的知识图谱推理与自底向上的基于案例的推理，实现了结构化知识与代表性案例的迭代检索和合成，以确保上下文相关性和广泛的任务覆盖范围。DomRetriever既可以作为DomAgent的一部分运行，也可以独立与任何LLM配合，实现灵活的领域适应。我们在数据科学领域的开放基准数据集（DS-1000）上对DomAgent进行了评估，并将其进一步应用于实际的卡车软件开发任务。实验结果表明，DomAgent显著提升了特定领域的代码生成能力，使小型开源模型在复杂的实际应用中能够大幅缩小与大型专有LLM之间的性能差距。代码已发布于：https://github.com/Wangshuaiia/DomAgent。

## Abstract
Large language models (LLMs) have shown impressive capabilities in code generation. However, because most LLMs are trained on public domain corpora, directly applying them to real-world software development often yields low success rates, as these scenarios frequently require domain-specific knowledge. In particular, domain-specific tasks usually demand highly specialized solutions, which are often underrepresented or entirely absent in the training data of generic LLMs. To address this challenge, we propose DomAgent, an autonomous coding agent that bridges this gap by enabling LLMs to generate domain-adapted code through structured reasoning and targeted retrieval. A core component of DomAgent is DomRetriever, a novel retrieval module that emulates how humans learn domain-specific knowledge, by combining conceptual understanding with experiential examples. It dynamically integrates top-down knowledge-graph reasoning with bottom-up case-based reasoning, enabling iterative retrieval and synthesis of structured knowledge and representative cases to ensure contextual relevance and broad task coverage. DomRetriever can operate as part of DomAgent or independently with any LLM for flexible domain adaptation. We evaluate DomAgent on an open benchmark dataset in the data science domain (DS-1000) and further apply it to real-world truck software development tasks. Experimental results show that DomAgent significantly enhances domain-specific code generation, enabling small open-source models to close much of the performance gap with large proprietary LLMs in complex, real-world applications. The code is available at: https://github.com/Wangshuaiia/DomAgent.

---

## 论文详细总结（自动生成）

以下是对该论文的详细总结：

# 论文总结：DomAgent：利用知识图谱与基于案例的推理进行特定领域代码生成

## 1. 核心问题与整体含义
*   **研究背景**：尽管大语言模型（LLM）在通用代码生成上表现优异，但在面对实际软件开发中的特定领域任务（如数据科学库、工业控制协议）时，由于训练语料中缺乏专业领域知识，其生成成功率往往较低。
*   **核心问题**：如何弥补通用 LLM 在专业领域知识上的缺失，使其能够生成准确、符合特定领域规范的代码。
*   **整体含义**：论文提出了一种名为 **DomAgent** 的自主编程智能体框架，旨在通过模拟人类学习过程（结合概念理解与经验案例），利用知识图谱（KG）和基于案例的推理（CBR）来增强 LLM 的领域适应能力，从而解决通用模型在专业场景下“水土不服”的问题。

## 2. 方法论
*   **核心思想**：DomAgent 的核心在于 **DomRetriever** 模块，它采用“自顶向下”的知识图谱推理与“自底向上”的案例推理相结合的策略，动态检索结构化知识和典型案例，为代码生成提供精准的上下文。
*   **关键技术细节**：
    *   **检索触发机制**：通过微调 LLM 使其能够生成特殊标记（`<search_kg>` 和 `<search_case>`）来主动触发检索工具。
    *   **知识图谱引导的案例选择**：利用知识图谱中实体间的关系来指导案例库的构建与检索，通过分层选择策略确保案例的代表性和多样性。
    *   **代码生成流程**：将原始任务描述 $q$、检索到的精炼领域知识 $\bar{K}$ 以及专门的解决方案 $\hat{c}$ 拼接，输入 LLM 生成最终代码。
        *   公式表达：$\hat{y} = LLM_{gen}(q || \bar{K} || \hat{c})$
*   **智能体训练流程**：
    *   **监督微调（SFT）**：利用强大的 LLM（文中提及如 GPT-5）合成包含推理步骤的长链思维数据，教会模型生成检索触发标记。
    *   **强化学习（RL）**：为了过滤无关信息，使用奖励模型 $f_r$ 评估检索内容与查询的相关性，并采用 GRPO 算法优化模型参数，使其学会审查和筛选知识。
        *   奖励函数：$reward = f_r(q || \bar{K} || \hat{c})$

## 3. 实验设计
*   **数据集与场景**：
    *   **DS-1000**：数据科学领域的开放基准数据集，包含 1000 个任务，覆盖 NumPy、Pandas、PyTorch 等 7 个主流库。
    *   **Truck CAN Signal**：真实世界的卡车软件开发任务，涉及 776 个 CAN 信号的读写操作，跨越 6 个功能域。
*   **评测指标**：Pass@1（代码执行通过率）。
*   **对比方法**：
    *   **原始 LLM**：GPT-4o, GPT-3.5-Turbo, LLaMA 系列, Qwen 系列等。
    *   **编程智能体**：StarCoder, WizardCoder, Magicoder 等专门针对代码微调的模型。
    *   **消融对比**：对比了仅使用知识图谱（+KG）、仅使用案例推理（+CBR）、标准 RAG 流水线以及 DomAgent 完整框架的效果。

## 4. 资源与算力
*   **硬件环境**：实验在 **8 张 NVIDIA A100 80G GPUs** 上进行。
*   **训练配置**：
    *   每个输入查询生成 16 个输出。
    *   训练 2 个 epoch。
    *   Batch size 为 16。
    *   学习率为 1e-6。
*   **数据构建**：构建了 500 个高质量示例用于微调模型学习工具调用；DS-1000 中仅采样 30%（300 例）构建案例库。

## 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 2 个截然不同的领域（数据科学基准与工业嵌入式开发），涉及多个主流开源与闭源模型基座。
*   **消融实验**：
    *   **模块消融**：详细拆解了 KG、CBR 以及 RL 训练策略各自的贡献。
    *   **采样策略消融**：对比了基于 KG 的案例选择与随机采样在不同采样比例下的表现。
    *   **模型组合消融**：测试了“小模型检索 + 大模型生成”的协作模式。
*   **充分性与客观性**：实验设计较为充分，不仅验证了在公开榜单上的表现，还验证了在真实工业场景（卡车 CAN 信号）的落地效果。对比了多种 SOTA 模型，且设置了不同参数量的模型（7B, 8B, 70B）进行对照，具有较好的客观性。

## 6. 主要结论与发现
*   **性能显著提升**：DomAgent 显著提升了领域特定代码生成的质量。在 DS-1000 上，基于 LLaMA3.1-8B 的 DomAgent 超越了所有同量级编程智能体；结合 GPT-4o 生成代码时，Pass@1 达到 58.6%，比原始 GPT-4o 提升了 7.6%。
*   **缩小模型差距**：在 Truck CAN Signal 任务中，基于 Qwen2.5-7B 的 DomAgent 达到了 96.64% 的通过率，几乎追平了增强后的 GPT-4o（98.04%），大幅缩小了开源小模型与闭源大模型在专业领域的差距。
*   **高效案例构建**：基于知识图谱引导的案例选择策略极其高效，仅使用 30% 的案例数据即可达到随机采样 80% 数据的性能水平。
*   **模块有效性**：知识图谱（KG）提供概念支撑，案例推理（CBR）提供实例参考，两者结合优于单一模块；强化学习进一步提升了模型筛选信息的能力。

## 7. 优点
*   **模拟人类学习机制**：创新性地将“概念学习”（KG）与“案例学习”（CBR）结合，符合人类掌握新领域知识的认知规律。
*   **工业落地价值高**：不仅在基准数据集上有效，更在实际的卡车软件开发中验证了有效性，证明了其实际应用潜力。
*   **架构灵活**：DomRetriever 可独立作为检索增强模块配合其他 LLM 使用（如 LLaMA 检索 + GPT-4o 生成），适应性强。
*   **数据效率高**：通过 KG 引导筛选案例，大幅降低了构建案例库所需的数据量和人工成本。

## 8. 不足与局限
*   **依赖外部知识源**：方法的有效性高度依赖于高质量知识图谱的构建和维护，对于缺乏结构化文档的新领域，构建 KG 仍需人工成本。
*   **案例库构建成本**：虽然 KG 引导提高了效率，但仍需初始的案例库支持，对于完全冷启动的领域可能存在挑战。
*   **实验范围**：虽然包含了真实工业场景，但领域主要集中在数据科学库和特定硬件接口（CAN），在其他类型的领域（如前端开发、操作系统内核）的泛化性有待进一步验证。
*   **文中提及的未来工作**：目前尚未引入 AST（抽象语法树）级别的细粒度分析，对代码结构的深层理解仍有提升空间。

（完）

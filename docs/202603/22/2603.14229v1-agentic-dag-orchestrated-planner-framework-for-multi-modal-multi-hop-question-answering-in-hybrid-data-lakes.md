---
title: "Agentic DAG-Orchestrated Planner Framework for Multi-Modal, Multi-Hop Question Answering in Hybrid Data Lakes"
title_zh: 面向混合数据湖多模态多跳问答的代理式DAG编排规划器框架
authors: "Kirushikesh D B, Manish Kesarwani, Nishtha Madaan, Sameep Mehta, Aldrin Dennis, Siddarth Ajay, Rakesh B R, Renu Rajagopal, Sudheesh Kairali"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14229v1"
tags: ["query:agent"]
score: 8.0
evidence: 用于多跳问答的智能体规划器框架
tldr: 针对混合数据湖中多模态、多跳问答的挑战，本文提出了A.DOT框架。该框架将自然语言查询编译为有向无环图（DAG）执行计划，支持跨结构化和非结构化数据的并行检索与推理。通过引入模式感知、高级缓存及验证机制，系统显著提升了准确性与效率，并提供了可验证的证据链。
selection_source: fresh_fetch
motivation: 现有的混合数据湖问答方案缺乏对多跳推理的支持，且检索效率低下，无法有效处理跨结构化与非结构化数据的复杂查询。
method: 提出A.DOT框架，将自然语言查询转化为有向无环图（DAG）执行计划，协调跨异构数据源的并行检索与多跳推理，并结合高级缓存与验证机制。
result: "在基准数据集上，A.DOT在正确率上实现了14.8%的绝对提升，在完整率上实现了10.7%的提升。"
conclusion: 该框架不仅显著提升了问答系统的准确性和延迟表现，还通过生成显式的证据链增强了结果的可验证性与用户信任。
---

## 摘要
企业日益需要针对结合了结构化表格和非结构化文档的混合数据湖进行自然语言（NL）问答。当前部署的解决方案，包括基于RAG的系统，通常依赖于从各个存储中进行暴力检索并进行事后合并。此类方法不仅低效且存在泄漏，更关键的是，它们缺乏对多跳推理的显式支持，即无法将查询分解为可能在结构化与非结构化源之间往返遍历的连续步骤（跳）。我们提出了代理式DAG编排Transformer（A.DOT）规划器，这是一个用于多模态、多跳问答的框架，能将用户NL查询编译为跨越结构化和非结构化存储的有向无环图（DAG）执行计划。该系统将查询分解为可并行的子查询，融合模式感知推理，并在执行前应用结构和语义验证。执行引擎遵循生成的DAG计划，协调跨异构源的并发检索，将中间输出路由至依赖的子查询，并严格按照计划的逻辑依赖关系合并最终结果。结合了释义感知模板匹配的高级缓存机制，使系统能够检测等效查询并重用先前的DAG执行计划以实现快速重新执行，而DataOps系统则负责处理验证反馈或执行错误。该框架不仅改善了准确性与延迟，还生成了显式的证据链，从而支持检索内容的验证、数据血缘的追踪，并增强用户对系统输出的信任。在基准数据集上，A.DOT在正确性上相比基线实现了14.8%的绝对提升，在完整性上实现了10.7%的绝对提升。

## Abstract
Enterprises increasingly need natural language (NL) question answering over hybrid data lakes that combine structured tables and unstructured documents. Current deployed solutions, including RAG-based systems, typically rely on brute-force retrieval from each store and post-hoc merging. Such approaches are inefficient and leaky, and more critically, they lack explicit support for multi-hop reasoning, where a query is decomposed into successive steps (hops) that may traverse back and forth between structured and unstructured sources. We present Agentic DAG-Orchestrated Transformer (A.DOT) Planner, a framework for multi-modal, multi-hop question answering, that compiles user NL queries into directed acyclic graph (DAG) execution plans spanning both structured and unstructured stores. The system decomposes queries into parallelizable sub-queries, incorporates schema-aware reasoning, and applies both structural and semantic validation before execution. The execution engine adheres to the generated DAG plan to coordinate concurrent retrieval across heterogeneous sources, route intermediate outputs to dependent sub-queries, and merge final results in strict accordance with the plan's logical dependencies. Advanced caching mechanisms, incorporating paraphrase-aware template matching, enable the system to detect equivalent queries and reuse prior DAG execution plans for rapid re-execution, while the DataOps System addresses validation feedback or execution errors. The proposed framework not only improves accuracy and latency, but also produces explicit evidence trails, enabling verification of retrieved content, tracing of data lineage, and fostering user trust in the system's outputs. On benchmark dataset, A.DOT achieves 14.8% absolute gain in correctness and 10.7% in completeness over baselines.

---

## 论文详细总结（自动生成）

# 论文总结：面向混合数据湖多模态多跳问答的代理式DAG编排规划器框架

## 1. 论文的核心问题与整体含义
*   **研究背景**：企业数据湖通常包含结构化数据（如关系型数据库表格）和非结构化数据（如文档、向量存储）。现有的自然语言问答（QA）系统（如标准RAG）在处理这类混合数据时，往往采用暴力检索后合并的方式。
*   **核心问题**：现有方法存在三大痛点：
    1.  **效率低下与数据泄漏**：过度检索导致计算开销大且可能暴露不必要信息。
    2.  **缺乏多跳推理支持**：无法有效处理需要在结构化与非结构化数据源之间来回切换的复杂查询。
    3.  **缺乏可解释性**：难以追踪数据血缘和验证答案来源。
*   **整体含义**：本文旨在提出一种能够显式规划、并行执行并具备自我修复能力的框架，以解决混合数据湖中跨模态、多跳推理的复杂问答需求，同时提升系统的可验证性与信任度。

## 2. 论文提出的方法论
本文提出了 **A.DOT (Agentic DAG-Orchestrated Transformer Planner)** 框架，核心思想是将自然语言查询编译为有向无环图（DAG）执行计划。

*   **核心组件与流程**：
    1.  **计划缓存**：
        *   支持精确匹配、模板匹配（带槽位填充）和语义匹配（基于嵌入向量）。
        *   通过复用已验证的DAG计划，减少重复计算，加速查询响应。
    2.  **计划生成器**：
        *   利用单次LLM调用将用户查询分解为原子子查询，构建DAG图。
        *   节点代表子查询（标注了工具类型、输出变量标签等），边代表依赖关系。
    3.  **计划验证器**：
        *   **结构检查**：验证模式合规性、变量引用是否悬空、DAG是否有环。
        *   **语义检查**：利用LLM审计意图是否偏移，确保连接键类型匹配等。
    4.  **DataOps系统**：
        *   包含诊断器、推荐器、修复器和重规划器。
        *   当验证失败或执行出错时，系统能自动诊断根因并进行局部修复或全局重规划，而非简单终止。
    5.  **计划执行器**：
        *   按拓扑排序执行，独立节点并行执行。
        *   **变量绑定机制**：仅在节点间传递必要的最小数据（如ID），而非全量载荷，降低内存压力并减少数据泄漏。
        *   记录每一步的数据血缘，生成证据链。

## 3. 实验设计
*   **数据集**：使用了 **HybridQA** 数据集（开发集，包含3,466个问答对）。这是一个需要跨表格和文本进行多跳推理的基准数据集。
    *   数据处理：将原数据集重构为混合存储架构，结构化部分存入SQL数据库，非结构化部分存入Milvus向量数据库，并建立双向链接。
*   **基线方法**：
    *   **Standard RAG**：标准检索增强生成，无迭代推理。
    *   **ReAct**：交替进行推理与工具调用的代理框架。
    *   **LLM Compiler**：基于DAG的工具调用规划器，但缺乏A.DOT的验证与修复机制。
*   **评估指标**：
    *   **Answer Correctness（答案正确性）**：预测答案与参考答案的事实一致性。
    *   **Answer Completeness（答案完整性）**：答案覆盖所需信息的程度。
    *   评估方式：使用 Mistral Large 2 模型作为裁判。

## 4. 资源与算力
*   **模型资源**：
    *   推理模型：LLaMA-3 70B。
    *   评估模型：Mistral Large 2。
    *   框架：基于 LangGraph 实现。
*   **算力说明**：**论文未明确说明具体的GPU型号、数量或训练时长**。由于该框架主要涉及推理阶段的规划与执行，重点在于框架逻辑而非模型训练，因此未列出训练算力消耗。

## 5. 实验数量与充分性
*   **主实验**：对比了A.DOT与3个基线在HybridQA数据集上的表现。
*   **消融实验**：在500个样本子集上进行了消融研究，分别移除“计划验证器”和“DataOps系统”以验证各组件贡献。
*   **充分性与客观性**：
    *   实验设计较为充分，涵盖了主要性能对比和关键组件的有效性验证。
    *   评估采用了LLM-as-a-Judge方式，相比传统的字符串匹配更能衡量语义正确性，具有一定客观性。
    *   但数据集仅使用了HybridQA的开发集（未使用测试集），且未涉及其他类型的多模态数据集，覆盖面略显单一。

## 6. 论文的主要结论与发现
*   **性能提升**：A.DOT在答案正确性上达到71.0%，相比最强基线提升了14.8%；在完整性上达到73.0%，提升了10.7%。
*   **组件有效性**：消融实验证明，DataOps系统对于从执行错误中恢复至关重要；若缺少验证器，系统容易产生结构错误的计划。
*   **基线对比发现**：
    *   ReAct受限于串行执行和步数限制，容易陷入死循环或提前终止。
    *   LLM Compiler虽然支持并行，但缺乏中间过程的验证与错误修复机制，导致性能不如A.DOT。
*   **应用价值**：该框架已计划部署于IBM Watsonx.data Premium产品中，证明了其实际应用潜力。

## 7. 优点
*   **创新的规划机制**：引入DAG编排，实现了跨异构数据源的并行检索与精确依赖管理，解决了多跳推理难题。
*   **鲁棒性设计**：双重验证（结构+语义）与DataOps修复机制相结合，显著提升了系统应对错误的能力，避免了“一错到底”。
*   **企业级特性**：强调数据血缘追踪、证据链生成和最小化数据传递，非常契合企业对数据安全、合规和可解释性的需求。
*   **高效执行**：通过计划缓存和并行执行策略，有效降低了端到端延迟。

## 8. 不足与局限
*   **数据集局限**：仅在HybridQA单一数据集上验证，该数据集主要涉及表格和文本，未涵盖图像等其他模态，且未使用官方测试集。
*   **错误类型**：论文承认仍存在三类主要错误：中间子查询失败、数据源分配错误（如本应查SQL却查了向量库）、缺乏常识知识。
*   **应用限制**：目前框架主要针对单轮问答，尚未支持多轮对话场景（文中列为未来工作）。
*   **资源依赖**：系统性能高度依赖底层NL2SQL和NL2Vector模块的质量，若底层检索工具失效，整体规划也会受阻。

（完）

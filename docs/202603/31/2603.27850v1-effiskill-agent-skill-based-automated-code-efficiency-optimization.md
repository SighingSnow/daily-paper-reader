---
title: "EffiSkill: Agent Skill Based Automated Code Efficiency Optimization"
title_zh: EffiSkill：基于智能体技能的自动化代码效率优化
authors: "Zimu Wang, Yuling Shi, Mengfan Li, Zijun Liu, Jie M. Zhang, Chengcheng Wan, Xiaodong Gu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27850v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于LLM智能体的代码效率优化框架
tldr: 针对现有LLM代码优化方法缺乏可复用知识的问题，本文提出EffiSkill框架。该框架将重复的代码转换建模为可复用的智能体技能，通过两阶段流程构建技能库并应用于新程序。实验表明，该方法显著提升了优化成功率，证明了机制级技能复用在无执行代码优化中的有效性。
selection_source: fresh_fetch
motivation: 现有代码优化方法未显式提炼可复用的优化知识，限制了模型在单个实例之外的泛化能力。
method: 提出EffiSkill框架，分两阶段进行：先从程序对中挖掘技能构建库，再通过无执行诊断与检索应用技能优化新代码。
result: 在EffiBench-X上的实验显示，EffiSkill的优化成功率比最强基线提高了3.69到12.52个百分点。
conclusion: 机制级技能复用为无执行代码优化提供了有效基础，生成的技能库可作为广泛智能体工作流的可复用资源。
---

## 摘要
代码效率是软件质量的一个基础方面，然而如何利用大语言模型（LLMs）来优化程序仍具挑战性。现有方法尝试了一次性重写、检索样例或基于提示的搜索，但它们并未显式提炼可复用的优化知识，这限制了其在单个实例之外的泛化能力。本文提出了EffiSkill，这是一个代码效率优化框架，旨在为基于LLM的智能体构建一个可移植的优化工具箱。其核心思想是将反复出现的从慢到快的转换建模为可复用的智能体技能，这些技能既捕获了具体的转换机制，也涵盖了高层次的优化策略。EffiSkill采用两阶段设计：第一阶段从大规模慢/快程序对中挖掘操作技能和元技能以构建技能库；第二阶段通过免执行诊断、技能检索、规划组合和候选生成，将该技能库应用于未见过的程序，且无需运行时反馈。在EffiBench-X上的实验结果表明，EffiSkill实现了更高的优化成功率，在不同模型和语言设置下，相比最强基线提升了3.69至12.52个百分点。这些发现表明，机制层面的技能复用为免执行代码优化提供了有益的基础，且生成的技能库可作为更广泛的智能体工作流的可复用资源。

## Abstract
Code efficiency is a fundamental aspect of software quality, yet how to harness large language models (LLMs) to optimize programs remains challenging. Prior approaches have sought for one-shot rewriting, retrieved exemplars, or prompt-based search, but they do not explicitly distill reusable optimization knowledge, which limits generalization beyond individual instances.   In this paper, we present EffiSkill, a framework for code-efficiency optimization that builds a portable optimization toolbox for LLM-based agents. The key idea is to model recurring slow-to-fast transformations as reusable agent skills that capture both concrete transformation mechanisms and higher-level optimization strategies. EffiSkill adopts a two-stage design: Stage I mines Operator and Meta Skills from large-scale slow/fast program pairs to build a skill library; Stage II applies this library to unseen programs through execution-free diagnosis, skill retrieval, plan composition, and candidate generation, without runtime feedback.   Results on EffiBench-X show that EffiSkill achieves higher optimization success rates, improving over the strongest baseline by 3.69 to 12.52 percentage points across model and language settings. These findings suggest that mechanism-level skill reuse provides a useful foundation for execution-free code optimization, and that the resulting skill library can serve as a reusable resource for broader agent workflows.

---

## 论文详细总结（自动生成）

# 论文总结：EffiSkill: Agent Skill Based Automated Code Efficiency Optimization

## 1. 论文的核心问题与整体含义
*   **核心问题**：现有的基于大语言模型（LLM）的代码效率优化方法（如一次性重写、检索样例、基于提示的搜索）主要针对单个实例进行优化，未能显式提炼和复用优化知识。这导致模型在面对新任务时无法利用过往的优化经验，限制了其在单个实例之外的泛化能力。
*   **整体含义**：本文提出将代码优化视为一种“技能”复用的过程，而非简单的实例匹配或黑盒重写。通过显式地将“慢到快”的代码转换模式建模为可复用的智能体技能，旨在构建一个可移植的优化工具箱，使LLM能够像人类专家一样积累并应用优化策略，从而在无需运行时反馈的情况下实现高效的代码优化。

## 2. 论文提出的方法论
本文提出了 **EffiSkill** 框架，采用离线挖掘与在线优化分离的两阶段设计：

*   **核心思想**：将重复出现的代码转换机制建模为“操作技能”和“元技能”，构建技能库。在推理阶段，通过检索和组合这些技能来指导代码生成，实现免执行的优化。

*   **阶段 I：技能挖掘**
    1.  **轨迹提取**：利用 LLM（GPT-5.1）分析大规模慢/快程序对，生成包含问题摘要、慢代码审计、快代码审计和差异摘要的结构化优化轨迹。
    2.  **签名抽象**：将轨迹抽象为紧凑的签名，包含优化类型、复杂度变化、触发条件等关键特征。
    3.  **聚类与蒸馏**：在混合词法-语义空间中对签名进行聚类，将同类转换机制蒸馏为 **操作技能**，记录转换意图、适用信号和预期效果。
    4.  **元技能构建**：构建控制逻辑，指导如何诊断问题、检索操作技能并组合成优化计划。

*   **阶段 II：技能引导的优化**
    该阶段无需执行代码或运行时反馈，流程如下：
    1.  **诊断**：分析输入程序，生成优化简报，识别瓶颈。
    2.  **检索**：根据诊断结果，从技能库中检索相关的操作技能集。
    3.  **规划组合**：利用元技能将检索到的技能组合成多个可行的优化计划。
    4.  **候选生成**：依据优化计划生成优化后的代码候选。

## 3. 实验设计
*   **数据集与场景**：
    *   **挖掘语料库**：使用 PIE（C++）以及 PIE、Mercury、DeepMind Code Contests、CodeParrot APPS 的聚合数据，筛选加速比大于 2 的慢/快程序对。
    *   **评估基准**：使用 **EffiBench-X** 基准测试，包含 Python 和 C++ 子集，共 623 个任务。
*   **对比方法**：
    *   **提示类**：Instruction（直接提示）、CoT（思维链）。
    *   **检索增强类**：RAG、FasterPy。
    *   **进化搜索类**：SBLLM。
    *   **微调类**：EffiCoder。
*   **评估指标**：**OPT@k**，指在前 k 个候选中，至少有一个通过所有测试且运行时间减少至少 10% 的任务比例。

## 4. 资源与算力
*   **执行环境**：文中明确说明了代码执行测试环境为 AMD EPYC 7302 CPU (16 cores, 32 threads, 3.0 GHz) 和 112 GB RAM。
*   **模型资源**：使用了 GPT-5-mini 和 Qwen3-Coder-30B-A3B-Instruct 作为骨干模型。
*   **算力说明**：**文中未明确说明具体的 GPU 型号、数量或训练时长**。由于方法主要涉及 API 调用（如 GPT-5.1 用于挖掘）和推理，未提及大规模模型训练过程。

## 5. 实验数量与充分性
*   **实验组数**：
    *   主实验：在 2 种模型（GPT-5-mini, Qwen3-Coder）和 2 种语言（Python, C++）下对比了 6 种基线方法。
    *   消融实验：设计了 3 组消融实验（移除检索、随机技能、移除多计划）验证各组件贡献。
    *   分析实验：包括技能库分析、案例研究、预算扩展性分析等。
*   **充分性与公平性**：
    *   **充分**：覆盖了多种主流优化范式（提示、检索、搜索、微调）和不同规模的模型，实验维度丰富。
    *   **公平**：所有方法均使用相同的生成预算（k=8），采用相同的公开/私有测试集划分协议，并在相同的骨干模型上进行对比，控制了变量。

## 6. 论文的主要结论与发现
*   **性能提升**：EffiSkill 在所有设置下均取得了最佳性能。相比最强基线，在 GPT-5-mini 上 C++ 提升了 12.03 个百分点，Python 提升了 4.98 个百分点；在 Qwen3-Coder 上提升更为显著。
*   **组件有效性**：消融实验证明，技能检索的相关性和多计划组合的多样性对性能至关重要，移除任一组件都会导致显著性能下降。
*   **跨语言泛化**：方法在 Python 和 C++ 上均有效，且在 C++ 这种对优化更敏感的语言上优势更大。
*   **技能库价值**：学到的技能库包含 29 个操作技能，主要集中在实现与常数因子优化、代数重写等，证明了机制级复用的有效性。

## 7. 优点
*   **创新的知识表示**：将隐式的优化经验显式化为结构化的“技能”，提高了优化过程的可解释性和复用性。
*   **免执行优化**：方法在推理阶段不依赖运行时反馈，适用于无法直接执行代码或缺乏测试环境的场景，具有更高的实用性。
*   **模块化设计**：技能库作为一个独立的工具箱，可以即插即用地集成到不同的 LLM 智能体工作流中。

## 8. 不足与局限
*   **领域局限性**：实验主要基于竞争性编程基准，这类代码结构相对规范且独立，可能无法完全代表工业界复杂软件系统（涉及外部依赖、多文件交互等）的优化场景。
*   **评估指标单一**：主要关注运行效率的提升，未评估生成代码的可读性、可维护性或潜在的副作用。
*   **依赖 LLM 质量**：技能挖掘阶段依赖 GPT-5.1 生成轨迹，若 LLM 对代码差异的理解存在偏差，可能会引入噪声技能。
*   **数据分布风险**：尽管检查了任务重叠，但挖掘语料库和评估基准均来自竞争性编程领域，可能存在分布相似性带来的潜在优势。

（完）

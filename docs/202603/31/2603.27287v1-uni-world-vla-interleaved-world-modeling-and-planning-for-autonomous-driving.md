---
title: "Uni-World VLA: Interleaved World Modeling and Planning for Autonomous Driving"
title_zh: Uni-World VLA：面向自动驾驶的交错世界建模与规划
authors: "Qiqi Liu, Huan Xu, Jingyu Li, Bin Sun, Zhihui Hao, Dangen She, Xiatian Zhu, Li Zhang"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27287v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 统一VLA模型，实现自动驾驶中的交错世界建模与规划
tldr: 针对自动驾驶中现有世界模型先预测后规划导致的开环偏差问题，本文提出了Uni-World VLA模型。该模型将未来帧预测与轨迹规划紧密交错进行，形成闭环交互，并引入单目深度信息增强几何感知。实验表明，该方法在NAVSIM基准上实现了具有竞争力的闭环规划性能和高保真预测，验证了耦合预测与规划的有效性。
selection_source: fresh_fetch
motivation: 现有世界模型方法采用先预测后规划的开环模式，易导致预测与决策过程脱节。
method: 提出Uni-World VLA模型，逐步交替预测未来帧与规划动作，实现世界建模与控制的闭环交互，并引入单目深度信息增强几何感知。
result: 在NAVSIM基准上的实验表明，该方法实现了具有竞争力的闭环规划性能和高保真的未来帧预测。
conclusion: 紧密耦合世界预测与规划是构建可扩展VLA驾驶系统的有效途径。
---

## 摘要
自动驾驶需要对环境的演变进行推理，并据此规划动作。现有的基于世界模型的方法通常先预测未来场景再进行规划，导致开环推演可能偏离实际决策过程。本文提出了 Uni-World VLA，这是一种统一的视觉-语言-动作（VLA）模型，它将未来帧预测与轨迹规划紧密交织。该模型并非在规划前生成完整的世界推演，而是逐步交替预测未来帧和自车动作，使规划决策能够持续基于想象的未来观测进行条件化。这种交错生成机制在世界建模与控制之间形成了闭环交互，从而在动态交通场景中实现更具适应性的决策。此外，我们将单目深度信息融入帧中，为世界建模提供更强的几何线索，改善了长时域场景预测。在 NAVSIM 基准上的实验表明，我们的方法在生成高保真未来帧预测的同时，实现了具有竞争力的闭环规划性能。这些结果表明，紧密耦合世界预测与规划是构建可扩展 VLA 驾驶系统的一个有前景的方向。

## Abstract
Autonomous driving requires reasoning about how the environment evolves and planning actions accordingly. Existing world-model-based approaches typically predict future scenes first and plan afterwards, resulting in open-loop imagination that may drift from the actual decision process. In this paper, we present Uni-World VLA, a unified vision-language-action (VLA) model that tightly interleaves future frame prediction and trajectory planning. Instead of generating a full world rollout before planning, our model alternates between predicting future frames and ego actions step by step, allowing planning decisions to be continuously conditioned on the imagined future observations. This interleaved generation forms a closed-loop interaction between world modeling and control, enabling more adaptive decision-making in dynamic traffic scenarios. In addition, we incorporate monocular depth information into frames to provide stronger geometric cues for world modeling, improving long-horizon scene prediction. Experiments on the NAVSIM benchmark show that our approach achieves competitive closed-loop planning performance while producing high-fidelity future frame predictions. These results demonstrate that tightly coupling world prediction and planning is a promising direction for scalable VLA driving systems.

---

## 论文详细总结（自动生成）

# 论文总结：Uni-World VLA

## 1. 论文的核心问题与整体含义

*   **研究背景**：自动驾驶系统不仅需要理解当前环境，还需推理环境演变并据此规划动作。现有的世界模型方法通常采用“先预测后规划”的范式，即先生成完整的未来场景推演，再基于此进行轨迹规划。
*   **核心问题**：传统的顺序模式存在“开环推演”缺陷。在生成未来推演时，模型假设环境基于初始意图静止演变，忽略了自车动作与环境之间的动态交互。这导致生成的未来场景（特别是长时域预测）可能成为“冻结的幻觉”，无法反映自车在中间时刻的微调（如轻微刹车或转向），从而导致规划偏离实际决策过程。
*   **整体含义**：为了解决预测与规划脱节的问题，论文提出了一种紧密耦合的交互机制，旨在通过闭环反馈让规划决策能实时根据预测的未来观测进行调整，从而在动态交通场景中实现更具适应性的决策。

## 2. 论文提出的方法论

*   **核心思想**：提出了 **Uni-World VLA** 模型，采用“交错世界建模与规划”范式。该模型不再一次性生成完整的未来推演，而是逐步交替预测未来帧和自车动作，形成“预测-规划-预测”的闭环交互。
*   **模型架构**：
    *   基于多模态大语言模型 **Show-o**（骨干为 Phi-1.5）构建统一的自回归架构。
    *   使用 **MagVIT-v2** 作为视觉分词器，将图像编码为离散的视觉 Token。
*   **关键技术细节**：
    *   **交错生成机制**：模型按照时间步交替生成视觉 Token 和动作 Token。公式化表达为：
        *   预测未来帧：$\hat{d}_{t+k} \sim p_\theta(d_{t+k} | \hat{d}_{\le t+k-1}, \hat{a}_{\le t+k-1})$
        *   预测动作：$\hat{a}_{t+k} \sim p_\theta(a_{t+k} | \hat{d}_{\le t+k}, \hat{a}_{\le t+k-1})$
        *   这种机制允许动作预测基于刚刚生成的未来帧，而下一帧的预测又包含了之前的动作信息，实现了闭环交互。
    *   **深度融合策略**：引入 **Depth Anything 3** 提取单目深度图，通过交叉注意力机制将深度特征与历史视觉 Token 融合，为世界建模提供几何线索，改善长时域预测。
    *   **输入 Token 化**：将历史视频流分为高分辨率的“上下文 Token”（提供语义结构）和低分辨率的“动态 Token”（捕捉运动细节），并结合自车状态（速度、加速度等）。
    *   **注意力机制**：采用双向帧内注意力以捕捉空间依赖，同时保持跨时间的因果掩码，确保时间因果性。
*   **训练目标**：
    *   视觉预测损失：采用动态焦点损失，通过加权强调时序变化的图像区域，缓解相邻帧静态偏差。
    *   轨迹预测损失：L1 损失。
    *   总损失：$L = \lambda_1 L_{dyn} + \lambda_2 L_{traj}$。

## 3. 实验设计

*   **数据集与场景**：主要在 **NAVSIM** 数据集上进行实验，这是一个高保真的自动驾驶模拟基准，包含以自我为中心的 RGB 序列和车辆状态信息。部分消融实验补充了 nuPlan 数据集的 10Hz 轨迹。
*   **Benchmark**：NAVSIM 测试集。
*   **评估指标**：
    *   **规划性能**：预测驾驶员模型得分（PDMS），包含五个子指标：无过错碰撞率（NC）、可行驶区域合规性（DAC）、自车进度（EP）、碰撞时间（TTC）和舒适性。
    *   **视频生成质量**：Fréchet Video Distance（FVD），衡量生成视频的分布级真实感。
*   **对比方法**：
    *   **传统端到端方法**：VADv2, UniAD, TransFuser, DiffusionDrive 等。
    *   **世界模型方法**：DrivingGPT, Epona, ImagiDrive, PWM, ResWorld 等。
    *   **视频生成基线**：WoVoGen, DriveDreamer, SVD, GenAD 等。

## 4. 资源与算力

*   **硬件配置**：使用了 **32 张 NVIDIA H20 GPU**。
*   **训练细节**：
    *   总训练轮数为 30 个 epoch（最佳检查点出现在第 16 个 epoch）。
    *   Batch Size 为 3（仅使用前视相机）。
    *   采用两阶段训练策略：第一阶段训练深度特征提取模块（5 epochs），第二阶段进行多模态联合训练（16 epochs）。
*   **未明确说明**：文中未明确提及具体的训练总时长（小时/天）。

## 5. 实验数量与充分性

*   **实验数量**：
    *   **主实验**：在 NAVSIM 测试集上与 SOTA 方法的全面对比（表 1 和表 2）。
    *   **消融实验**：
        *   组件有效性分析（预训练、未来帧建模、深度融合的影响）。
        *   生成方案对比（对比了 5 种不同的帧-动作交错生成策略）。
        *   历史视觉信息的影响（对比不同历史时长和 Token 类型）。
*   **充分性与客观性**：
    *   实验设计较为充分，涵盖了主要组件验证、不同策略对比以及输入配置分析。
    *   评估指标全面，兼顾了规划的安全性和视频生成的质量。
    *   对比方法涵盖了最新的端到端和世界模型方法，具有代表性。虽然 ResWorld 使用了 LiDAR 而本文仅用单目相机，但文中已明确指出输入模态差异，对比相对公平。

## 6. 论文的主要结论与发现

*   **性能表现**：Uni-World VLA 在 NAVSIM 上取得了最高的 PDMS（89.4），优于其他 SOTA 方法，同时在视频生成质量（FVD 141.8）上也具有竞争力。
*   **机制有效性**：交错生成机制（方案 E）比顺序生成或并行生成更有效，证明了闭环交互对规划和预测的双重提升作用。
*   **深度融合收益**：引入单目深度信息显著提升了长时域场景预测的清晰度和几何一致性，同时也改善了规划指标（如 DAC 和 EP）。
*   **核心观点**：紧密耦合世界预测与规划，通过交错生成实现闭环交互，是构建可扩展 VLA 驾驶系统的有效途径。

## 7. 优点

*   **创新性范式**：提出了“交错世界建模与规划”范式，有效解决了传统“先预测后规划”模式下的开环偏差问题，实现了预测与控制的闭环反馈。
*   **几何感知增强**：创新性地将单目深度信息通过交叉注意力融入视觉 Token，弥补了纯 RGB 输入在几何推理上的不足，提升了长时域预测质量。
*   **综合性能优异**：在保证高保真视频生成的同时，显著提升了闭环规划的安全性和效率（PDMS 和 TTC 指标 SOTA）。
*   **设计细节完善**：动态焦点损失和双向帧内注意力机制的设计，有效解决了视频预测中的静态偏差和空间一致性问题。

## 8. 不足与局限

*   **输入模态限制**：仅使用前视单目相机作为输入，未利用多视角相机或 LiDAR 数据，可能在遮挡或深度极端复杂的场景下受限（尽管对比中 ResWorld 用了 LiDAR，本文方法在单目设定下表现优异，但信息源本身仍有局限）。
*   **模拟器验证**：主要实验基于 NAVSIM 模拟器，虽然高保真，但与真实世界驾驶场景仍存在域差距，真实车辆上的部署效果有待验证。
*   **计算资源需求**：模型基于大语言模型（Phi-1.5）和复杂的视频生成架构，训练需要大量 GPU 资源（32 张 H20），推理成本可能较高，对实时性部署提出挑战。
*   **频率匹配问题**：消融实验显示，高频动作预测（10Hz）与低频评估（2Hz）不匹配会导致性能下降，模型最佳性能依赖于特定的频率对齐策略，可能限制了其在不同控制频率下的通用性。

（完）

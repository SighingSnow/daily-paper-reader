---
title: "The Specification Gap: Coordination Failure Under Partial Knowledge in Code Agents"
title_zh: 规格差距：代码智能体在部分知识下的协调失败
authors: Camilo Chacón Sartori
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24284v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于LLM的代码智能体协调与集成研究
tldr: 本文研究了多个LLM代码智能体在规范细节缺失时面临的协调失败问题。通过在51个类生成任务中逐步剥离规范细节，发现双智能体集成准确率显著下降，存在持续的“规范缺口”。研究表明，更丰富的规范是解决协调问题的关键，而非单纯的冲突检测。
selection_source: fresh_fetch
motivation: 旨在探究多个代码智能体在规范隐式或缺失时，如何协调共享内部表示以避免集成失败。
method: 在51个类生成任务中逐步剥离规范细节并引入结构偏差，对比单智能体与双智能体的表现差异。
result: 发现双智能体集成准确率随规范缺失急剧下降，存在显著的协调缺口，且恢复完整规范比冲突检测更能提升性能。
conclusion: 多智能体代码生成应遵循“规范优先”原则，更丰富的规范是解决协调失败的主要机制。
---

## 摘要
当多个基于LLM的代码智能体独立实现同一个类的不同部分时，即便规格说明未明确这些选择，它们也必须就共享的内部表示达成一致。我们通过51个类生成任务研究这一协调问题，逐步将规格细节从完整的文档字符串（L0）剥离至仅剩签名（L3），并引入对立的结构偏差（列表与字典）以对集成进行压力测试。研究得出了三个发现。首先，存在一个持续存在的规格差距：随着细节被移除，双智能体集成准确率从58%降至25%，而单智能体基线的退化则更为平缓（89%至56%），留下了25至39个百分点的协调差距，该差距在两个Claude模型（Sonnet和Haiku）及三次独立运行中保持一致。其次，一种基于AST的冲突检测器在最弱规格级别下无需额外的LLM调用即可达到97%的精确率，然而一项析因恢复实验表明，仅恢复完整规格说明即可达到单智能体的性能上限（89%），而提供冲突报告并未带来可衡量的收益。第三，将该差距分解为协调成本（+16个百分点）和信息不对称（+11个百分点）表明，这两种效应相互独立且近似可加。该差距不仅是隐藏信息的后果，更反映了在缺乏共享决策时生成兼容代码的困难。这些结果支持多智能体代码生成的“规格优先”观点：更丰富的规格说明既是主要的协调机制，也是充分的恢复手段。

## Abstract
When multiple LLM-based code agents independently implement parts of the same class, they must agree on shared internal representations, even when the specification leaves those choices implicit. We study this coordination problem across 51 class-generation tasks, progressively stripping specification detail from full docstrings (L0) to bare signatures (L3), and introducing opposing structural biases (lists vs. dictionaries) to stress-test integration. Three findings emerge. First, a persistent specification gap: two-agent integration accuracy drops from 58% to 25% as detail is removed, while a single-agent baseline degrades more gracefully (89% to 56%), leaving a 25--39 pp coordination gap that is consistent across two Claude models (Sonnet, Haiku) and three independent runs. Second, an AST-based conflict detector achieves 97% precision at the weakest specification level without additional LLM calls, yet a factorial recovery experiment shows that restoring the full specification alone recovers the single-agent ceiling (89%), while providing conflict reports adds no measurable benefit. Third, decomposing the gap into coordination cost (+16 pp) and information asymmetry (+11 pp) suggests that the two effects are independent and approximately additive. The gap is not merely a consequence of hidden information, but reflects the difficulty of producing compatible code without shared decisions. These results support a specification-first view of multi-agent code generation: richer specifications are both the primary coordination mechanism and the sufficient recovery instrument.

---

## 论文详细总结（自动生成）

# 论文总结：The Specification Gap: Coordination Failure Under Partial Knowledge in Code Agents

## 1. 论文的核心问题与整体含义

*   **研究背景**：随着大语言模型（LLM）在单智能体代码生成任务上表现优异，研究重心逐渐转向多智能体协作系统。然而，当多个代码智能体独立实现同一个类的不同方法时，它们必须在无法实时通信的情况下，对共享的内部状态（如数据结构）达成一致。
*   **核心问题**：如果规格说明不够明确，智能体可能会做出互不兼容的设计决策（例如一个用列表存储数据，另一个用字典），导致代码集成失败。这种在“部分知识”下的协调失败被称为“规格差距”。
*   **整体含义**：本文旨在探究规格说明的完整性如何影响多智能体代码生成的协调成功率，并验证“更丰富的规格说明”是否比“事后冲突检测”更能解决集成问题。

## 2. 论文提出的方法论

*   **核心思想**：通过控制变量实验，逐步剥离规格说明中的细节信息，模拟多智能体在不同信息透明度下的协作场景，量化“规格差距”的大小及其成因。
*   **关键技术细节**：
    *   **规格级别分层**：定义了四个递减的规格级别：
        *   **L0**：完整文档字符串，包含数据结构引用和示例。
        *   **L1**：简化文档字符串，保留数据结构引用。
        *   **L2**：移除数据结构引用，仅保留行为描述。
        *   **L3**：仅保留方法签名。
    *   **智能体配置与偏见注入**：
        *   **单智能体**：作为性能上限，可见完整的 `__init__` 方法，无偏见。
        *   **分裂智能体**：两个智能体分别实现类的一部分方法。实验通过提示词注入对立的结构偏见，Agent A 偏好使用列表，Agent B 偏好使用字典，且不可见 `__init__` 方法体，必须自行推断数据结构。
    *   **AST 冲突检测器**：基于抽象语法树（AST）开发静态分析工具，用于在集成前检测两个智能体输出代码之间的类型冲突、状态冲突等。
    *   **差距分解实验**：设计 $2 \times 2$ 析因实验（单/分裂智能体 $\times$ `__init__` 可见/隐藏），将总体性能差距分解为“协调成本”和“信息不对称”两个独立效应。

## 3. 实验设计

*   **数据集**：基于 ClassEval 基准构建的 AmbigClass 数据集，包含 51 个类生成任务（每个类至少包含 3 个方法）。
*   **Benchmark**：自建的 AmbigClass，通过脚本自动生成 L0-L3 四种不同详细程度的规格说明版本，共产生 204 个任务-规格对。
*   **对比方法与条件**：
    *   **基线对比**：单智能体 vs. 分裂智能体在不同规格级别（L0-L3）下的表现。
    *   **恢复实验**：在 L3（最弱规格）下，对比四种合并策略：Blind（无辅助）、Guided（仅提供冲突报告）、Spec-Only（仅提供完整规格）、Resolve（两者皆有）。
    *   **模型**：主要使用 Claude Sonnet 4，并在 Claude Haiku 4.5 上进行了复现验证。

## 4. 资源与算力

*   **算力资源**：论文未提及具体的 GPU 型号或训练时长，因为实验主要涉及 LLM 推理调用而非模型训练。
*   **成本统计**：
    *   主要实验（612 次 LLM 调用 + 408 次测试执行）总成本为 $3.54。
    *   Haiku 模型的复现实验（3 次独立运行）成本分别为 $4.38 和 $4.83。
    *   恢复实验包含 318 次合并运行。

## 5. 实验数量与充分性

*   **实验规模**：
    *   主实验包含 51 个任务 $\times$ 4 个规格级别 $\times$ 3 个条件（Single, Split, Conflicts）。
    *   恢复实验包含 53 个任务 $\times$ 6 个合并条件。
    *   差距分解实验包含 50 个任务 $\times$ 4 个条件。
    *   跨模型复现实验进行了 3 次独立运行以验证稳定性。
*   **充分性与客观性**：
    *   实验设计严谨，采用了控制变量法（剥离规格细节）和析因设计（分解协调成本与信息不对称）。
    *   使用了 Wilcoxon 符号秩检验和 Friedman 检验等统计方法验证显著性，并报告了 Cohen's d 效应量。
    *   通过注入对立偏见（List vs. Dict）来模拟极端的协调冲突，虽然具有一定的人为性，但能有效放大问题以便观察，且论文通过 Init-visibility 实验验证了这种差距不仅仅是偏见注入的产物。

## 6. 论文的主要结论与发现

*   **存在持续的规格差距**：随着规格细节的移除，双智能体集成准确率从 58% 骤降至 25%，而单智能体仅从 89% 降至 56%。两者之间存在 25-39 个百分点的持续“协调税”，即使规格完整（L0）也无法消除。
*   **冲突检测的作用有限**：AST 冲突检测器在规格最弱（L3）时精确率高达 97%，能低成本诊断问题。然而，在恢复实验中，仅提供完整规格说明即可将性能恢复至单智能体水平（89%），而提供冲突报告没有任何帮助（$\Delta = 0$ pp），甚至在有完整规格时反而略微降低性能。
*   **差距成因分解**：性能差距主要由“协调成本”（约 +16pp）和“信息不对称”（约 +11pp）两部分组成，且两者近似独立可加。这证明了差距不仅源于信息缺失，更源于缺乏共享决策机制。
*   **核心观点**：多智能体代码生成应遵循“规格优先”原则。丰富的规格说明是解决协调失败的主要机制，也是充分的恢复手段，优于事后冲突检测。

## 7. 优点

*   **视角新颖**：将经典软件工程中的“规格说明”理论与现代多智能体 AI 系统相结合，指出了多智能体协作中的结构性隐患。
*   **实验设计精巧**：通过“偏见注入”和“规格剥离”成功构建了可复现的协调失败场景；通过 $2 \times 2$ 析因实验巧妙分离了协调成本与信息不对称的影响。
*   **反直觉的发现**：证明了冲突检测虽然能发现问题，但对解决问题（代码合并）无效，这一发现对构建多智能体系统具有重要的指导意义。

## 8. 不足与局限

*   **任务范围受限**：实验仅限于 Python 类生成任务，且冲突类型主要集中在数据结构（List vs. Dict）的选择上，未涉及更复杂的逻辑或架构层面的冲突。
*   **偏见注入的人为性**：虽然论文论证了偏见注入模拟了现实中的风格分歧，但通过 Prompt 强制指定数据结构类型可能比实际开发中的隐性分歧更为极端。
*   **模型覆盖面**：主要测试了 Claude 系列模型，未涵盖 GPT 或开源模型，结论的普适性有待进一步验证。
*   **静态合并策略**：实验中的代码合并主要采用简单的文本拼接或基于规格的重写，未探索更复杂的协商式合并机制。

（完）

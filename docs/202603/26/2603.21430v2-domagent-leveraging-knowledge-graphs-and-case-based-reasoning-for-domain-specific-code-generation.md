---
title: "DomAgent: Leveraging Knowledge Graphs and Case-Based Reasoning for Domain-Specific Code Generation"
title_zh: DomAgent：利用知识图谱与基于案例的推理进行领域特定代码生成
authors: "Shuai Wang, Dhasarathy Parthasarathy, Robert Feldt, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21430v2"
tags: ["query:agent"]
score: 9.0
evidence: 用于领域特定代码生成的自主编程智能体
tldr: 大型语言模型在特定领域代码生成中因缺乏专业知识而表现不佳。本文提出DomAgent，通过DomRetriever模块结合知识图谱推理和基于案例的推理，模拟人类学习过程进行结构化检索与推理。实验表明，该方法显著提升了特定领域代码生成质量，使小模型性能逼近大型专有模型。
selection_source: fresh_fetch
motivation: 通用大型语言模型缺乏领域特定知识，导致在实际软件开发场景中代码生成成功率低。
method: 提出DomAgent框架，利用DomRetriever模块结合知识图谱推理与基于案例的推理，模拟人类学习过程进行动态检索与代码生成。
result: 在数据科学基准和实际卡车软件开发任务中，该方法显著提升了代码生成质量，缩小了开源小模型与闭源大模型的差距。
conclusion: DomAgent有效解决了特定领域代码生成的挑战，证明了结合结构化推理与检索能大幅提升模型表现。
---

## 摘要
大语言模型（LLM）在代码生成方面展现了卓越的能力。然而，由于大多数LLM是在公共领域语料库上训练的，将其直接应用于现实世界的软件开发往往成功率较低，因为这些场景通常需要领域特定知识。特别是，领域特定任务通常需要高度专业化的解决方案，而这些方案在通用LLM的训练数据中往往代表性不足或完全缺失。为了应对这一挑战，我们提出了DomAgent，这是一种自主编码智能体，通过结构化推理和针对性检索使LLM能够生成领域适配的代码，从而弥合这一差距。DomAgent的一个核心组件是DomRetriever，这是一个新颖的检索模块，通过结合概念理解与经验案例，模拟人类学习领域特定知识的方式。它动态整合了自顶向下的知识图谱推理与自底向上的基于案例的推理，实现了结构化知识与代表性案例的迭代检索和综合，以确保上下文相关性和广泛的任务覆盖。DomRetriever可以作为DomAgent的一部分运行，也可以独立地与任何LLM配合使用，以实现灵活的领域适配。我们在数据科学领域的开放基准数据集（DS-1000）上评估了DomAgent，并将其进一步应用于现实世界的卡车软件开发任务。实验结果表明，DomAgent显著提升了领域特定代码生成的能力，使小型开源模型在复杂的现实应用中能够大幅缩小与大型专有LLM之间的性能差距。代码地址：https://github.com/Wangshuaiia/DomAgent。

## Abstract
Large language models (LLMs) have shown impressive capabilities in code generation. However, because most LLMs are trained on public domain corpora, directly applying them to real-world software development often yields low success rates, as these scenarios frequently require domain-specific knowledge. In particular, domain-specific tasks usually demand highly specialized solutions, which are often underrepresented or entirely absent in the training data of generic LLMs. To address this challenge, we propose DomAgent, an autonomous coding agent that bridges this gap by enabling LLMs to generate domain-adapted code through structured reasoning and targeted retrieval. A core component of DomAgent is DomRetriever, a novel retrieval module that emulates how humans learn domain-specific knowledge, by combining conceptual understanding with experiential examples. It dynamically integrates top-down knowledge-graph reasoning with bottom-up case-based reasoning, enabling iterative retrieval and synthesis of structured knowledge and representative cases to ensure contextual relevance and broad task coverage. DomRetriever can operate as part of DomAgent or independently with any LLM for flexible domain adaptation. We evaluate DomAgent on an open benchmark dataset in the data science domain (DS-1000) and further apply it to real-world truck software development tasks. Experimental results show that DomAgent significantly enhances domain-specific code generation, enabling small open-source models to close much of the performance gap with large proprietary LLMs in complex, real-world applications. The code is available at: https://github.com/Wangshuaiia/DomAgent.

---

## 论文详细总结（自动生成）

# 论文总结：DomAgent

## 1. 论文的核心问题与整体含义
*   **核心问题**：大型语言模型（LLM）虽然在通用代码生成上表现优异，但在面对现实世界中高度专业化的领域特定任务（如特定工业API调用、专业库函数使用）时，由于训练语料中缺乏相关领域知识，导致生成成功率较低。
*   **研究动机**：为了弥合通用大模型与领域特定需求之间的鸿沟，论文提出了一种自主编程智能体框架，旨在通过结构化的知识检索与推理，让模型能够像人类专家一样利用领域知识库和过往案例来解决复杂编程任务。
*   **整体含义**：该研究展示了如何通过结合知识图谱（KG）和基于案例的推理（CBR），使参数量较小的开源模型（如7B/8B）在特定领域任务上达到甚至超越大型闭源模型（如GPT-4o）的性能，具有重要的工业应用价值。

## 2. 论文提出的方法论
论文提出了 **DomAgent** 框架，其核心组件为 **DomRetriever**。

*   **核心思想**：模拟人类学习过程，结合“自顶向下”的知识图谱推理（概念理解）与“自底向上”的基于案例推理（经验借鉴），动态检索并过滤信息，生成领域适配的代码。

*   **关键技术细节**：
    *   **代码生成流程**：将原始任务描述 $q$、精炼后的领域知识 $\hat{K}$ 和专用解决方案 $\hat{c}$ 拼接，输入代码生成模型 $LLM_{gen}$ 生成目标代码 $\hat{y}$。
        *   公式：$\hat{y} = LLM_{gen}(q || \hat{K} || \hat{c})$
    *   **智能体训练**：
        *   **监督微调（SFT）**：构建包含特殊标记（`<search_kg>`, `<search_case>`）的长思维链数据，训练LLM学会调用检索工具。
        *   **强化学习（RL）**：使用奖励模型 $f_r$ 评估检索内容与查询的相关性，通过GRPO算法优化模型参数，使其能够自主过滤无关知识。
        *   奖励函数公式：$reward = f_r(q || \hat{K} || \hat{c})$
        *   优化目标：$\theta^* = \arg \max_\theta E_{q, \hat{K}, \hat{c} \sim \pi_\theta} [f_r(q || \hat{K} || \hat{c})]$
    *   **分层案例选择**：利用知识图谱指导案例库构建，通过包重叠关系重排序候选案例，提高案例选取的代表性和多样性。

## 3. 实验设计
*   **数据集与场景**：
    *   **DS-1000**：数据科学领域的开放基准数据集，包含1000个任务，覆盖NumPy、Pandas、PyTorch等7个主流库。
    *   **Truck CAN Signal**：真实世界的卡车软件开发场景，涉及CAN信号的读写任务，涵盖6个功能域共776个信号。
*   **Benchmark与对比方法**：
    *   **基线模型**：Vanilla LLMs（GPT-4o, GPT-3.5, LLaMA-3.1-8B, Qwen-2.5-7B等）。
    *   **编码智能体**：StarCoder, WizardCoder, Magicoder等。
    *   **消融对比**：对比了仅使用知识图谱（+KG）、仅使用案例推理（+CBR）、标准RAG流水线以及DomAgent完整框架的效果。

## 4. 资源与算力
*   **硬件资源**：使用了 **8张 NVIDIA A100 80G GPU**。
*   **训练配置**：
    *   训练轮数：2个 epochs。
    *   Batch size：16。
    *   学习率：1e-6。
    *   每个查询生成16个输出用于强化学习采样。
*   **数据构建**：构建了500个高质量示例用于训练模型调用检索工具；DS-1000案例库仅采样了30%的数据（300例）。

## 5. 实验数量与充分性
*   **实验数量**：
    *   在两个截然不同的领域（数据科学基准、工业卡车软件）进行了完整测试。
    *   进行了详尽的消融实验，分别验证了KG、CBR、RL过滤机制以及案例选择策略的有效性。
    *   对比了不同规模（7B vs 70B vs 闭源）模型作为生成器或检索器的组合效果。
*   **充分性与客观性**：
    *   实验设计较为充分，不仅对比了SOTA模型，还深入分析了各模块贡献。
    *   评估指标统一采用 `pass@1`，客观公平。
    *   特别是在真实工业场景的验证，增强了结论的可信度。

## 6. 论文的主要结论与发现
*   **性能提升显著**：DomAgent显著提升了领域特定代码生成的质量。在DS-1000上，基于LLaMA3.1-8B的DomAgent达到40.5%，优于同尺寸的编码智能体；结合GPT-4o生成代码时，性能达到58.6%，比原生GPT-4o提升7.6%。
*   **缩小模型差距**：在Truck CAN Signal任务中，小参数模型（Qwen2.5-7B）配合DomAgent达到了96.64%的准确率，几乎追平了配合检索器的GPT-4o（98.04%），证明了该方法能有效弥补小模型在领域知识上的短板。
*   **模块有效性**：知识图谱（KG）提供基础概念，案例推理（CBR）提供具体示例，两者结合效果最佳。强化学习（RL）机制能有效过滤噪声，进一步提升检索质量。
*   **高效构建**：基于KG引导的案例选择策略仅需30%的数据即可达到全量数据的性能，大幅降低了案例库构建成本。

## 7. 优点
*   **方法创新**：创新性地将知识图谱推理与案例推理相结合，模拟人类“理论+实践”的学习模式，比单纯的RAG更具解释性和结构性。
*   **工业落地价值高**：在真实卡车软件开发中成功部署并验证，解决了传统LLM在私有API和特定领域代码生成上的痛点。
*   **架构灵活**：DomRetriever可作为独立模块与不同LLM（如GPT-4o, LLaMA-70B）结合，实现“小模型检索+大模型生成”的高效协作模式。

## 8. 不足与局限
*   **依赖外部模型**：训练过程依赖强大的教师模型（文中提及GPT-5）生成推理步骤和奖励信号，存在一定的数据依赖和成本问题。
*   **案例库构建成本**：虽然KG引导提高了效率，但构建高质量的领域知识图谱和初始案例库仍需人工参与（如文中提到手动创建few-shot示例）。
*   **领域迁移成本**：虽然方法通用，但应用到新领域仍需重新构建对应的KG和案例库，并非完全的“零成本”迁移。

（完）

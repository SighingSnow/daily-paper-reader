---
title: "SEVerA: Verified Synthesis of Self-Evolving Agents"
title_zh: SEVerA：自演化智能体的验证综合
authors: "Debangshu Banerjee, Changming Xu, Gagandeep Singh"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25111v1"
tags: ["query:agent"]
score: 9.0
evidence: 自演化LLM智能体与验证合成框架
tldr: 针对自演化LLM智能体缺乏安全与正确性保证的问题，本文提出SEVerA框架。该框架引入形式化守护生成模型（FGGM），通过搜索、验证和学习三阶段流程，将智能体合成转化为约束学习问题。实验结果显示，SEVerA在实现零约束违规的同时性能超越基线，证明了形式化约束既能保证正确性又能引导生成更高质量的智能体。
selection_source: fresh_fetch
motivation: 现有自演化智能体框架缺乏形式化的安全与正确性保证，存在可靠性与安全隐患。
method: 提出SEVerA框架，利用形式化守护生成模型（FGGM）将智能体合成转化为约束学习问题，通过搜索、验证与学习三阶段确保正确性并优化性能。
result: 在Dafny验证等任务中，SEVerA实现了零约束违规，且性能优于无约束及SOTA基线。
conclusion: 形式化行为约束不仅保证了智能体的正确性，还能引导合成出更高质量的智能体。
---

## 摘要
近期进展表明，自演化LLM智能体在程序修复和科学发现等任务上具有有效性。在此范式中，规划器LLM综合出一个调用参数化模型（包括LLM）的智能体程序，随后针对特定任务对这些模型进行调优以提升性能。然而，现有的自演化智能体框架并未提供安全性或正确性的形式化保证。由于此类程序常在未见输入上自主执行，缺乏此类保证引发了可靠性与安全性隐忧。我们将智能体代码生成形式化为约束学习问题，结合了硬性形式规范与捕捉任务效用的软目标。我们引入了形式化守护生成模型（FGGM），允许规划器LLM使用一阶逻辑为每次生成模型调用指定形式化输出契约。每次FGGM调用均将底层模型封装于带有经验证回退机制的拒绝采样器中，确保对于任意输入和参数设置，返回的每个输出均满足契约。基于FGGM，我们提出了SEVerA（自演化验证智能体），这是一个三阶段框架：搜索阶段综合包含FGGM调用的候选参数化程序；验证阶段证明在所有参数值下对硬约束的正确性，从而将问题简化为无约束学习；学习阶段应用可扩展的基于梯度的优化（包括GRPO风格微调），在保持正确性的同时提升软目标。我们在Dafny程序验证、符号数学综合以及符合策略的智能体工具使用（$\tau^2$-bench）任务上对SEVerA进行了评估。在各项任务中，SEVerA实现了零约束违规，且性能优于无约束和SOTA基线，表明形式化行为约束不仅保证了正确性，还能引导综合过程生成更高质量的智能体。

## Abstract
Recent advances have shown the effectiveness of self-evolving LLM agents on tasks such as program repair and scientific discovery. In this paradigm, a planner LLM synthesizes an agent program that invokes parametric models, including LLMs, which are then tuned per task to improve performance. However, existing self-evolving agent frameworks provide no formal guarantees of safety or correctness. Because such programs are often executed autonomously on unseen inputs, this lack of guarantees raises reliability and security concerns. We formulate agentic code generation as a constrained learning problem, combining hard formal specifications with soft objectives capturing task utility. We introduce Formally Guarded Generative Models (FGGM), which allow the planner LLM to specify a formal output contract for each generative model call using first-order logic. Each FGGM call wraps the underlying model in a rejection sampler with a verified fallback, ensuring every returned output satisfies the contract for any input and parameter setting. Building on FGGM, we present SEVerA (Self-Evolving Verified Agents), a three-stage framework: Search synthesizes candidate parametric programs containing FGGM calls; Verification proves correctness with respect to hard constraints for all parameter values, reducing the problem to unconstrained learning; and Learning applies scalable gradient-based optimization, including GRPO-style fine-tuning, to improve the soft objective while preserving correctness. We evaluate SEVerA on Dafny program verification, symbolic math synthesis, and policy-compliant agentic tool use ($τ^2$-bench). Across tasks, SEVerA achieves zero constraint violations while improving performance over unconstrained and SOTA baselines, showing that formal behavioral constraints not only guarantee correctness but also steer synthesis toward higher-quality agents.

---

## 论文详细总结（自动生成）

# 论文总结：SEVerA：自演化智能体的验证综合

## 1. 核心问题与整体含义
- **研究动机**：现有自演化LLM智能体框架（如程序修复、科学发现）缺乏形式化的安全与正确性保证，导致在未见输入上自主执行时存在可靠性与安全隐患（如智能体作弊修改输入程序、违反领域政策等）。
- **整体含义**：论文旨在将**形式化验证**的严格保证与**梯度优化**的性能提升相结合，解决智能体合成中的“安全-性能”平衡难题，实现既可靠又高效的智能体生成。

## 2. 方法论
- **核心思想**：将智能体代码生成形式化为**约束学习问题**，结合硬性形式规范（保证安全）与软性任务目标（提升性能），通过三阶段流程实现验证与优化的统一。
- **关键技术细节**：
  - **形式化守护生成模型（FGGM）**：为每个生成模型调用绑定局部输入-输出契约（一阶逻辑），并封装为拒绝采样器+验证回退机制，确保输出始终满足契约（无论模型参数如何）。
  - **三阶段流程**：
    1. **搜索**：规划器LLM综合候选参数化程序（包含FGGM调用）。
    2. **验证**：使用Dafny内置验证器证明程序对所有参数值满足硬约束，将问题简化为无约束学习。
    3. **学习**：应用梯度优化（如GRPO微调）提升任务性能，同时保持形式正确性。
- **公式/算法流程**：
  - 约束学习问题形式化：`min L(f) s.t. ∀x. Φ(x) ⇒ Ψ(x, f(x))`。
  - FGGM定义：包含标识符、底层模型、类型签名、局部契约、提示程序、回退程序。
  - 拒绝采样机制：采样K次，若均不满足契约则调用回退程序。
  - 学习目标：结合任务损失与局部契约符合损失。

## 3. 实验设计
- **数据集/场景**：
  - **Dafny程序验证**：HumanEvalDafny、DafnyBench（智能体需生成注释使程序通过验证）。
  - **符号数学综合**：GSM-Symbolic（生成符合形式语法的数学表达式）。
  - **智能体工具使用**：τ²-bench（零售与航空领域，需遵守领域政策调用API）。
  - **约束符号回归**：35个合成任务（从噪声数据恢复满足符号约束的函数）。
- **Benchmark**：各任务均有标准评估指标（验证率、准确率、通过率、NMSE）。
- **对比方法**：
  - 无约束自演化框架（仅优化任务损失）。
  - SOTA基线：Agent-C（τ²-bench）、CRANE（GSM-Symbolic）、PySR/LLM-SR（符号回归）。
  - 直接LLM调用（如Claude Sonnet 4.5、Qwen3-8B）。

## 4. 资源与算力
- **明确说明**：实验使用 **NVIDIA A100 GPU（40 GB）**，但未具体说明GPU数量、训练时长等细节。
- **其他资源**：使用Dafny验证器、SMT求解器（Z3）、LLM API调用（Claude、Qwen）。

## 5. 实验数量与充分性
- **实验组数**：
  - 四个不同领域任务，每个任务包含多个数据集/子任务。
  - 消融实验：分析局部契约调优与全局损失调优的贡献（GSM-Symbolic）。
  - 对比实验：与多个基线方法比较。
- **充分性/客观性**：
  - 实验覆盖科学发现、程序验证、数学推理、工具使用等多领域，较全面。
  - 对比方法包括无约束、SOTA及直接LLM调用，公平性较好。
  - 消融实验验证了方法各组件的有效性。
  - 但未报告多次运行统计显著性，可能存在随机性偏差。

## 6. 主要结论与发现
- **核心结论**：SEVerA在所有任务上实现**零约束违规**，同时任务性能优于无约束及SOTA基线。
- **具体发现**：
  - Dafny验证：97.0%验证率（vs. 基线86.9%），零违规。
  - τ²-bench：52.6%通过率（vs. Agent-C 47.3%），零违规。
  - GSM-Symbolic：66.0%准确率（vs. CRANE 44.7%），零违规。
  - 符号回归：NMSE显著低于基线，零违规。
  - 形式约束不仅保证安全，还能**剪枝搜索空间**，引导生成更高质量智能体。

## 7. 优点
- **方法创新**：首次将形式化验证与梯度优化结合，实现自演化智能体的可验证保证。
- **技术设计**：FGGM机制灵活（支持闭源模型）、参数无关正确性保证、局部契约引导调优。
- **实验设计**：覆盖多领域任务，对比全面，消融实验验证组件贡献。
- **结果显著**：零违规同时性能提升，证明约束的“引导”作用。

## 8. 不足与局限
- **实验覆盖**：未报告统计显著性，可能存在随机性偏差；部分任务（如τ²-bench）未进行参数调优。
- **偏差风险**：规划器LLM可能未在搜索预算内找到最优程序；验证器超时可能导致不完整。
- **应用限制**：
  - 资源消耗较大（验证、拒绝采样开销）。
  - 对闭源模型参数调优受限（仅依赖提示调优）。
  - 当前未考虑资源约束（如LLM调用次数、时间预算）。
  - 需用户提供形式规范与回退程序，可能增加使用门槛。

（完）

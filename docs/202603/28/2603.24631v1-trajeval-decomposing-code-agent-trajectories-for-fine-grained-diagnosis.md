---
title: "TRAJEVAL: Decomposing Code Agent Trajectories for Fine-Grained Diagnosis"
title_zh: TRAJEVAL：分解代码智能体轨迹以进行细粒度诊断
authors: "Myeongsoo Kim, Dingmin Wang, Siwei Cui, Farima Farmahinifarahani, Shweta Garg, Baishakhi Ray, Terry Yue Zhuo, Rajdeep Mukherjee, Varun Kumar"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24631v1"
tags: ["query:agent"]
score: 9.0
evidence: 代码智能体轨迹的直接评估框架
tldr: 现有代码智能体评估指标缺乏对失败原因的细粒度分析，本文提出TRAJEVAL框架，将智能体轨迹分解为搜索、读取和编辑三个阶段进行诊断。通过对大量轨迹的分析，发现了普遍的低效性和特定模型的失败模式。该框架不仅能预测模型性能，还能通过实时反馈提升模型表现并降低成本，实现了从结果导向向机制驱动诊断的转变。
selection_source: fresh_fetch
motivation: 现有评估指标仅提供二元结果，无法揭示代码智能体失败的具体位置和原因。
method: 提出TRAJEVAL框架，将智能体轨迹分解为搜索、读取和编辑三个阶段，并计算各阶段的精确率和召回率以进行诊断。
result: 实验发现智能体存在普遍低效性，且诊断结果具有预测性，基于此的实时反馈能显著提升模型性能并降低成本。
conclusion: 该框架成功将智能体评估从结果导向转变为机制驱动的诊断，提供了可操作的改进信号。
---

## 摘要
代码智能体能够自主解决 GitHub 问题，然而当它们失败时，当前的评估方法无法揭示失败发生的位置或原因。诸如 Pass@1 之类的指标将整个执行过程简化为单个二元结果，使得难以确定智能体出错的位置和原因。为了解决这一局限性，我们提出了 TRAJEVAL，这是一个诊断框架，它将智能体轨迹分解为三个可解释的阶段：搜索（文件定位）、阅读（函数理解）和编辑（修改定位）。对于每个阶段，我们通过将其与参考补丁进行对比来计算精确率和召回率。通过分析涵盖三种智能体架构和七个模型的 16,758 条轨迹，我们发现了普遍的低效性（所有智能体检查的函数数量大约是必要数量的 22 倍），但也存在截然不同的失败模式：GPT-5 能够定位相关代码但编辑定位错误，而 Qwen-32B 则完全无法发现文件。我们验证了这些诊断结果具有预测性，模型级别 Pass@1 预测的平均绝对误差（MAE）在 0.87-2.1% 之间；同时也具有可操作性：基于轨迹信号的实时反馈使两个最先进模型的性能提升了 2.2-4.6 个百分点，同时降低了 20-31% 的成本。这些结果表明，我们的框架不仅提供了对智能体行为更细粒度的分析，还将诊断信号转化为了实质的性能提升。更广泛地说，TRAJEVAL 将智能体评估从基于结果的基准测试转变为对智能体成功与失败的机制驱动诊断。

## Abstract
Code agents can autonomously resolve GitHub issues, yet when they fail, current evaluation provides no visibility into where or why. Metrics such as Pass@1 collapse an entire execution into a single binary outcome, making it difficult to identify where and why the agent went wrong. To address this limitation, we introduce TRAJEVAL, a diagnostic framework that decomposes agent trajectories into three interpretable stages: search (file localization), read (function comprehension), and edit (modification targeting). For each stage, we compute precision and recall by comparing against reference patches. Analyzing 16,758 trajectories across three agent architectures and seven models, we find universal inefficiencies (all agents examine approximately 22x more functions than necessary) yet distinct failure modes: GPT-5 locates relevant code but targets edits incorrectly, while Qwen-32B fails at file discovery entirely. We validate that these diagnostics are predictive, achieving model-level Pass@1 prediction within 0.87-2.1% MAE, and actionable: real-time feedback based on trajectory signals improves two state-of-the-art models by 2.2-4.6 percentage points while reducing costs by 20-31%. These results demonstrate that our framework not only provides a more fine-grained analysis of agent behavior, but also translates diagnostic signals into tangible performance gains. More broadly, TRAJEVAL transforms agent evaluation beyond outcome-based benchmarking toward mechanism-driven diagnosis of agent success and failure.

---

## 论文详细总结（自动生成）

# 论文总结：TRAJEVAL

## 1. 论文的核心问题与整体含义
*   **核心问题**：现有的代码智能体评估指标（如 Pass@1）仅提供二元结果（成功/失败），无法揭示智能体在执行过程中失败的具体位置和原因，导致难以进行针对性的优化。
*   **整体含义**：本研究旨在将智能体评估从“结果导向”转变为“机制驱动”，通过细粒度的轨迹诊断，不仅解释智能体为何失败，还提供可操作的信号以提升性能和效率。

## 2. 论文提出的方法论
*   **核心思想**：提出 TRAJEVAL 框架，将智能体的执行轨迹分解为三个可解释的阶段，并与参考补丁定义的“黄金上下文”进行对比。
*   **三阶段分解**：
    1.  **Search（搜索）**：文件级别的定位能力。
    2.  **Read（阅读）**：函数级别的代码理解能力。
    3.  **Edit（编辑）**：修改定位的准确性。
*   **黄金上下文**：
    *   **Tier 0（核心）**：参考补丁中实际修改的文件和函数，用于计算 Recall。
    *   **Tier 1（扩展）**：包含 Tier 0 及其结构依赖（如导入、父类），用于计算 Precision，以区分有意义的探索和冗余浏览。
*   **指标计算**：对每个阶段计算精确率和召回率。
    *   Search Precision/Recall：$P_s = |F^* \cap F_T| / |F_T|$, $R_s = |F^* \cap F_T| / |F^*|$
    *   Read Precision/Recall：$P_r = |H^* \cap H_T| / |H_T|$, $R_r = |H^* \cap H_T| / |H^*|$
    *   Edit Precision/Recall：$P_e = |H^* \cap \hat{H}_P| / |\hat{H}_P|$, $R_e = |H^* \cap \hat{H}_P| / |H^*|$
    *   其中 $F^*$ 和 $H^*$ 为黄金文件/函数集，$F_T$ 和 $H_T$ 为轨迹中查看的文件/函数集。
*   **预测与干预**：使用逻辑回归模型基于上述 6 个特征预测任务成功率；设计了实时反馈机制，当智能体接触到黄金上下文时给予确认信号。

## 3. 实验设计
*   **数据集**：
    *   **SWE-bench Verified**：包含 500 个 Python GitHub 问题。
    *   **PolyBench Verified**：包含 382 个多语言实例，覆盖 Python, Java, JavaScript, TypeScript。
*   **智能体架构**：SWE-Agent, OpenHands, LiveSWEAgent（覆盖了不同的工具接口设计）。
*   **模型**：GPT-5, GPT-5-mini, Qwen3-8B, Qwen3-32B, Qwen3-235B, Qwen3-Coder-30B, Qwen3-Coder-480B。
*   **评估基准**：
    *   **预测准确性**：以 Pass@1 为真值，验证轨迹特征预测模型性能的 MAE 和 Spearman 相关系数。
    *   **分布偏移测试**：实例留出、仓库留出、语言留出。
    *   **干预效果**：对比有无实时反馈下的 Pass@1 和 Token 消耗。

## 4. 资源与算力
*   **算力说明**：文中明确指出，轨迹特征提取算法复杂度为 $O(|T|)$，在 CPU 上仅需几分钟即可完成分析。
*   **主要成本**：主要的计算成本在于智能体的执行过程，这部分工作在研究之前已完成，论文未详细列出具体的 GPU 型号或训练时长。

## 5. 实验数量与充分性
*   **实验规模**：共分析了 16,758 条轨迹，涵盖 3 种架构、7 个模型、2 个基准测试。
*   **充分性与客观性**：
    *   实验设计非常充分，不仅验证了预测能力，还测试了跨仓库、跨语言的泛化能力。
    *   对比了不同架构和规模的模型，结论具有普遍性。
    *   进行了干预实验，验证了方法的实用性。
    *   实验设置客观，使用了标准的留出法和交叉验证。

## 6. 论文的主要结论与发现
*   **普遍低效性**：所有智能体都存在严重的过度探索，检查的函数数量是必要数量的 22 倍（Read Precision 仅 4-5%）。
*   **Recall 决定成败**：Precision 与成功率相关性弱，而 Recall（尤其是 Edit Recall）与任务成功强相关。
*   **模型特异性失败模式**：
    *   GPT-5：能定位代码但编辑定位错误。
    *   Qwen-32B：完全无法发现相关文件。
*   **高预测精度**：轨迹特征能高精度预测模型级 Pass@1（MAE 在 0.87-2.1% 之间），且在分布偏移下保持高排序相关性（$\rho \ge 0.886$）。
*   **干预有效性**：基于轨迹信号的实时反馈使 SOTA 模型性能提升 2.2-4.6 个百分点，同时降低成本 20-31%。

## 7. 优点
*   **细粒度诊断**：首次将代码智能体的黑盒执行过程分解为可量化的阶段指标，提供了比 Pass@1 更深入的洞察。
*   **实用性强**：证明了诊断信号可以直接转化为性能提升（通过实时反馈），解决了“评估无法指导优化”的痛点。
*   **架构无关性**：方法不依赖特定的智能体工具接口，适用于 SWE-Agent、OpenHands 等多种架构。
*   **发现深刻**：揭示了“过度探索”的普遍现象，并指出 Recall 比 Precision 更重要，这对未来智能体设计有指导意义。

## 8. 不足与局限
*   **依赖参考补丁**：Golden Context 的定义依赖于参考补丁，这在实际部署（无标准答案）时不可用，需要依赖预测器或检索近似。
*   **函数提取局限**：函数提取依赖 tree-sitter 和正则表达式，可能无法处理复杂的嵌套函数或元编程结构。
*   **干预实验限制**：干预实验使用了 Oracle（即已知正确答案）来提供反馈，且反馈信号较粗糙，模型覆盖范围有限（仅测试了 2 个模型）。
*   **任务范围**：主要关注 Bug 修复任务，对于其他类型的任务（如性能优化、安全漏洞修复）的适用性有待验证。

（完）

---
title: "From Static Templates to Dynamic Runtime Graphs: A Survey of Workflow Optimization for LLM Agents"
title_zh: 从静态模板到动态运行时图：LLM 智能体工作流优化综述
authors: "Ling Yue, Kushal Raj Bhandari, Ching-Yun Ko, Dhaval Patel, Shuxin Lin, Nianjun Zhou, Jianxi Gao, Pin-Yu Chen, Shaowu Pan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22386v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM智能体工作流与框架综述
tldr: 该综述探讨了基于大语言模型（LLM）的智能体工作流优化。文章将工作流视为智能体计算图，依据结构确定时机将其分为静态与动态方法，并从优化时机、对象及评估信号三个维度梳理文献。此外，研究区分了模板、实例图与执行轨迹，提出了结合图属性与任务指标的结构感知评估视角，为该领域提供了统一框架与评估标准。
selection_source: fresh_fetch
motivation: 旨在解决基于LLM的系统工作流日益复杂且缺乏统一优化框架的问题。
method: 将工作流定义为智能体计算图，依据结构确定时机、优化对象及评估信号三个维度对现有文献进行分类综述。
result: 构建了区分模板、实例图与执行轨迹的清晰词汇表，并提出了结合图属性与任务指标的结构感知评估视角。
conclusion: 为LLM智能体工作流优化领域提供了统一框架、可比视角及可复现的评估标准。
---

## 摘要
基于大语言模型（LLM）的系统通过构建可执行工作流来解决任务正变得日益流行，这些工作流交织了 LLM 调用、信息检索、工具使用、代码执行、记忆更新和验证。本综述回顾了设计与优化此类工作流的近期方法，我们将这些工作流视为智能体计算图。我们根据工作流结构的确定时机对文献进行梳理，其中结构是指存在哪些组件或智能体、它们如何相互依赖以及信息如何在它们之间流动。这一视角区分了静态方法与动态方法：前者在部署前固定可复用的工作流框架，后者则在执行前或执行过程中为特定运行选择、生成或修订工作流。我们进一步从三个维度梳理现有工作：结构确定的时机、工作流优化的部分，以及指导优化的评估信号（例如，任务指标、验证器信号、偏好或轨迹衍生的反馈）。我们还区分了可复用工作流模板、特定运行的实例化图和执行轨迹，将可复用的设计选择与特定运行中实际部署的结构以及实际运行时行为区分开来。最后，我们概述了一种结构感知的评估视角，通过图层级属性、执行成本、鲁棒性以及跨输入的结构变化来补充下游任务指标。我们的目标是为 LLM 智能体工作流优化领域的未来工作提供清晰的术语、用于定位新方法的统一框架、对现有文献更具可比性的视角，以及更具可复现性的评估标准。

## Abstract
Large language model (LLM)-based systems are becoming increasingly popular for solving tasks by constructing executable workflows that interleave LLM calls, information retrieval, tool use, code execution, memory updates, and verification. This survey reviews recent methods for designing and optimizing such workflows, which we treat as agentic computation graphs (ACGs). We organize the literature based on when workflow structure is determined, where structure refers to which components or agents are present, how they depend on each other, and how information flows between them. This lens distinguishes static methods, which fix a reusable workflow scaffold before deployment, from dynamic methods, which select, generate, or revise the workflow for a particular run before or during execution. We further organize prior work along three dimensions: when structure is determined, what part of the workflow is optimized, and which evaluation signals guide optimization (e.g., task metrics, verifier signals, preferences, or trace-derived feedback). We also distinguish reusable workflow templates, run-specific realized graphs, and execution traces, separating reusable design choices from the structures actually deployed in a given run and from realized runtime behavior. Finally, we outline a structure-aware evaluation perspective that complements downstream task metrics with graph-level properties, execution cost, robustness, and structural variation across inputs. Our goal is to provide a clear vocabulary, a unified framework for positioning new methods, a more comparable view of existing body of literature, and a more reproducible evaluation standard for future work in workflow optimizations for LLM agents.

---

## 论文详细总结（自动生成）

# 论文总结：从静态模板到动态运行时图——LLM 智能体工作流优化综述

## 1. 论文的核心问题与整体含义
*   **研究背景**：基于大语言模型（LLM）的系统正从简单的问答机器人演变为复杂的智能体系统，这些系统通过构建可执行工作流来协调 LLM 调用、工具使用、检索和验证等步骤。
*   **核心问题**：目前文献中缺乏统一的框架来定义和优化工作流结构。现有的研究往往将工作流结构视为既定的工程实现细节，而非可优化的核心对象。这导致不同方法（如静态模板优化与动态运行时生成）之间难以比较，且缺乏对质量与成本权衡的系统性分析。
*   **整体含义**：本综述旨在填补这一空白，提出将工作流视为“智能体计算图”，强调结构本身应成为优化的第一类对象。通过区分静态与动态优化方法，为该领域提供了统一的术语、分类标准和评估协议。

## 2. 论文提出的方法论
本综述的核心贡献在于提出了一个概念框架和分类体系，而非单一的算法模型。

*   **核心思想**：将 LLM 智能体工作流抽象为**智能体计算图**，并引入三个核心对象的区分：**模板**（可复用的设计规范）、**实例化图**（特定运行时的实际结构）和**执行轨迹**（运行时的行为记录）。
*   **分类维度**：
    *   **结构确定时机**：区分静态方法（部署前固定结构）与动态方法（运行时确定结构）。
    *   **图确定时间（GDT）**：离线、执行前、执行中。
    *   **图可塑性模式（GPM）**：无、选择、生成、编辑。
*   **优化目标与信号**：
    *   **优化层级**：节点级（如提示词优化）、图级（拓扑结构优化）、联合优化。
    *   **反馈信号**：任务指标、验证器信号、偏好排序、轨迹衍生的文本反馈。
*   **质量-成本权衡公式**：
    论文形式化了优化目标，即在给定输入分布下，寻找最优的实例化图 $G_{run}$ 以最大化期望收益并最小化成本：
    $$ \max_{\bar{G}} \mathbb{E}_{x \sim D} [\mathbb{E}_{G_{run}|x} [\mathbb{E}_{\tau|G_{run},x} [R(\tau; x) - \lambda C(\tau)]]] $$
    其中 $R$ 为任务质量得分，$C$ 为执行成本，$\lambda$ 为权衡系数。

## 3. 实验设计
作为一篇综述论文，本文主要对现有文献进行系统性梳理和分类，而非进行具体的模型实验。

*   **文献范围**：调研了 77 篇相关论文，分为核心方法（39篇）、相关方法（7篇）和背景资源（31篇）。
*   **评估资产梳理**：整理了 27 项与工作流相关的评估资产，包括：
    *   **工作流生成基准**：如 WorFBench, FlowBench，用于评估结构生成的有效性。
    *   **交互式工具使用基准**：如 GAIA, SWE-bench, Terminal-Bench，用于端到端验证。
*   **方法对比**：通过提出的“比较卡片”，对比了静态方法（如 AFlow, ADAS）与动态方法（如 DyFlow, AgentConductor）在 GDT、GPM、优化层级和反馈机制上的异同。

## 4. 资源与算力
*   **文中未明确说明**。由于本文为综述性质，主要工作在于理论框架构建和文献分析，未涉及具体的模型训练或大规模算力消耗，因此文中未提及 GPU 型号、数量或训练时长等算力细节。

## 5. 实验数量与充分性
*   **综述性质**：本文未进行传统的消融实验或对比实验，而是通过构建分类体系对现有文献进行了结构化分析。
*   **分析充分性**：
    *   **覆盖面广**：涵盖了从静态模板搜索到动态运行时编辑的多种方法，分类标准清晰。
    *   **客观性**：指出了当前评估标准的不足（如仅关注任务成功率而忽略图属性和成本），并提出了改进建议。
    *   **案例丰富**：通过具体案例（如固定流水线 vs. 查询条件生成器）解释了模板、实例化图与轨迹的区别，论证充分。

## 6. 论文的主要结论与发现
*   **静态 vs. 动态**：静态优化适用于环境稳定、验证器可靠的场景；动态优化适用于任务异构性强、环境交互多的场景。
*   **优化层级**：图级优化（改变拓扑结构）往往比单纯的节点级优化（如提示词调优）具有更高的杠杆效应，能解决结构性缺陷。
*   **验证器的重要性**：廉价且有语义意义的验证器（如单元测试、格式检查）对工作流优化至关重要，能显著提升搜索效率。
*   **评估标准缺失**：当前研究多关注下游任务指标，忽视了图属性（如深度、宽度）、执行成本和鲁棒性，导致结果难以复现和比较。

## 7. 优点
*   **统一框架**：首次提出了 ACG 这一统一抽象，解决了术语混乱问题，将流水线、多智能体系统等纳入同一框架。
*   **分类精细**：引入 GDT 和 GPM 维度，有效区分了“离线训练的生成器”与“运行时选择”等模糊概念。
*   **实用建议**：提出了“最低报告协议”，建议论文报告工作流结构、成本、图属性等细节，对提升领域研究质量具有指导意义。
*   **结构感知评估**：倡导将工作流结构作为一等公民进行评估，补充了传统的任务指标评估。

## 8. 不足与局限
*   **理论空白**：论文指出目前尚缺乏关于“何时需要动态生成”、“样本复杂度如何随结构可塑性变化”的理论指导。
*   **结构归因困难**：现有方法难以准确判断性能提升是源于结构改变、提示词优化还是单纯的计算量增加。
*   **基准测试局限**：现有的基准测试大多不是为结构感知评估设计的，缺乏标准化的结构对比和成本控制协议。
*   **应用限制**：动态方法虽然灵活，但引入了验证、停止策略和预算控制等额外的系统复杂性，在实际部署中面临挑战。

（完）

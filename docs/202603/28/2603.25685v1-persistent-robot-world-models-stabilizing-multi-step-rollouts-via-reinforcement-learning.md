---
title: "Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning"
title_zh: 持久化机器人世界模型：通过强化学习稳定多步推演
authors: "Jai Bardhan, Patrik Drozdik, Josef Sivic, Vladimir Petrik"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25685v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 基于RL自回归稳定的机器人世界模型
tldr: 针对机器人世界模型在自回归多步推演中误差累积和画质退化的问题，本文提出了一种强化学习后训练方案。该方法通过在自身推演上训练、对比多候选未来以及设计多视角视觉保真度奖励，显著提升了推演稳定性。实验表明，该方法在DROID数据集上达到SOTA，大幅提升了视频预测质量。
selection_source: fresh_fetch
motivation: 现有的机器人世界模型在自回归多步推演中存在误差累积和视觉质量快速退化的问题。
method: 提出一种强化学习后训练方案，通过在模型自身的自回归推演上训练并引入多视角视觉保真度奖励，以稳定多步预测。
result: "在DROID数据集上实现了新的SOTA，各项指标显著优于最强基线，并在人类盲测中获得80%的偏好率。"
conclusion: 该方法有效解决了世界模型长期推演的不稳定性问题，显著提升了生成视频的保真度。
---

## 摘要
动作条件的机器人世界模型在给定机器人动作序列时生成被操作场景的未来视频帧，为模拟那些难以用传统物理引擎建模的任务提供了一种有前景的替代方案。然而，这些模型针对短期预测进行了优化，在自回归部署时会失效：每个预测片段作为下一片段的上下文反馈，导致误差累积和视觉质量的迅速退化。我们通过以下贡献解决了这一问题。首先，我们引入了一种强化学习（RL）后训练方案，在世界模型自身的自回归推演上对其进行训练，而非基于真实历史数据。我们通过将近期用于扩散模型的对比强化学习目标适配至我们的设定来实现这一点，并证明了其收敛性保证完全适用。其次，我们设计了一种训练协议，从同一推演状态生成并比较多个候选的变长未来序列，从而强化高保真预测优于低保真预测。第三，我们开发了高效的多视角视觉保真度奖励，该奖励结合了跨相机视角的互补感知指标，并在片段层级聚合，以提供稠密、低方差的训练信号。第四，我们展示了本方法在DROID数据集上确立了推演保真度的新的最先进水平，在所有指标上均优于最强基线（例如，外部相机上的LPIPS降低了14%，腕部相机上的SSIM提高了9.1%），赢得了98%的成对比较，并在盲测人类研究中获得了80%的偏好率。

## Abstract
Action-conditioned robot world models generate future video frames of the manipulated scene given a robot action sequence, offering a promising alternative for simulating tasks that are difficult to model with traditional physics engines. However, these models are optimized for short-term prediction and break down when deployed autoregressively: each predicted clip feeds back as context for the next, causing errors to compound and visual quality to rapidly degrade. We address this through the following contributions. First, we introduce a reinforcement learning (RL) post-training scheme that trains the world model on its own autoregressive rollouts rather than on ground-truth histories. We achieve this by adapting a recent contrastive RL objective for diffusion models to our setting and show that its convergence guarantees carry over exactly. Second, we design a training protocol that generates and compares multiple candidate variable-length futures from the same rollout state, reinforcing higher-fidelity predictions over lower-fidelity ones. Third, we develop efficient, multi-view visual fidelity rewards that combine complementary perceptual metrics across camera views and are aggregated at the clip level for dense, low-variance training signal. Fourth, we show that our approach establishes a new state-of-the-art for rollout fidelity on the DROID dataset, outperforming the strongest baseline on all metrics (e.g., LPIPS reduced by 14% on external cameras, SSIM improved by 9.1% on the wrist camera), winning 98% of paired comparisons, and achieving an 80% preference rate in a blind human study.

---

## 论文详细总结（自动生成）

# 论文总结：持久化机器人世界模型

## 1. 论文的核心问题与整体含义
*   **核心问题**：现有的动作条件机器人世界模型在自回归多步推演中存在严重的误差累积问题。由于模型在训练时基于真实的“教师强制”历史帧，而在推理时必须基于自身生成的“不完美”历史帧，这种分布不匹配导致视觉质量迅速退化，物体结构崩塌，限制了其作为长期模拟器的实用性。
*   **整体含义**：本研究旨在通过强化学习（RL）后训练方案，弥合训练与推理之间的“闭环差距”。其核心思想是让模型在训练阶段就暴露在自身的生成错误中，通过优化长期推演的视觉保真度，使世界模型具备“持久性”，从而能够生成稳定、连贯的长时域视频预测。

## 2. 论文提出的方法论
*   **核心思想**：提出一种基于对比去噪的在线 RL 后训练框架。不再依赖真实历史进行监督学习，而是直接在模型自身的自回归推演上进行训练，利用奖励信号来强化高质量的预测，惩罚低质量的预测。
*   **关键技术细节**：
    *   **对比去噪目标**：针对扩散模型难以直接应用策略梯度的难题，借鉴并适配了 DiffusionNFT 方法。通过构建隐式的正负样本对，避免了反向传播去噪过程的高昂计算成本。
    *   **变长前缀分支策略**：针对自回归视频生成缺乏固定 Prompt 的问题，设计了从共享历史状态分支生成多个候选未来的训练协议。通过随机采样前缀长度，使模型同时适应早期轻微误差和后期严重误差的分布。
    *   **多视角视觉奖励**：设计了结合 LPIPS、SSIM 和 PSNR 的多视角奖励函数，并在组内进行归一化，以提供稠密且低方差的训练信号。
*   **公式与算法流程**：
    *   **正负样本构建**：给定当前模型预测 $\hat{x}_{0,\theta}$ 和参考模型预测 $\hat{x}^{old}_0$，构建正负样本：
        $$ \hat{x}^+_0 = (1-\beta)\hat{x}^{old}_0 + \beta\hat{x}_{0,\theta}, \quad \hat{x}^-_0 = (1+\beta)\hat{x}^{old}_0 - \beta\hat{x}_{0,\theta} $$
    *   **损失函数**：基于奖励权重 $r$ 的加权损失：
        $$ L(\theta) = \mathbb{E}[r \|\hat{x}^+_0 - x_0\|^2 + (1-r) \|\hat{x}^-_0 - x_0\|^2] $$
    *   **流程**：S1 生成共享前缀 -> S2 分支 K 个候选续写 -> S3 计算并归一化奖励 -> S4 更新模型权重。

## 3. 实验设计
*   **数据集**：使用了大规模机器人操作数据集 **DROID**，包含多样化的桌面操作任务和三视角（两个外部视角，一个腕部视角）视频数据。
*   **Benchmark**：主要评估指标为 14 步（约 11 秒）自回归推演的视觉质量，包括 **SSIM**（结构相似性）、**PSNR**（峰值信噪比）和 **LPIPS**（感知相似度）。
*   **对比方法**：对比了当前最强的基线模型 **Ctrl-World**，以及其他先进方法 **WPE** 和 **IRASim**。
*   **额外评估**：
    *   **物体/机器人中心评估**：利用 RoboEngine 分割前景，评估任务关键区域的保真度。
    *   **人类偏好研究**：进行盲测成对比较，评估感知真实感。
    *   **策略评估相关性**：验证世界模型推演的任务进度与真实环境的相关性。

## 4. 资源与算力
*   **硬件配置**：使用了 **8 张 NVIDIA H200 GPU**。
*   **训练时长**：训练了 **3 天**。
*   **训练细节**：共进行 8000 步训练，Batch size 为 64，组大小 K=16。仅微调 LoRA 适配器和动作编码器，冻结主干网络。

## 5. 实验数量与充分性
*   **实验数量**：实验设计非常详尽。
    *   **主实验**：在 DROID 验证集上进行了全面的定量对比。
    *   **消融实验**：深入分析了学习率、奖励函数组合、前缀采样策略、组大小、预测视野、视角权重、KL 正则化及 EMA 策略等 8 个关键因素。
    *   **人类研究**：涉及 8 名专家用户，提供了统计显著性分析。
    *   **下游任务验证**：验证了模型作为策略评估器的有效性。
*   **充分性与客观性**：实验覆盖了定量指标、定性展示、人类主观评估和下游应用验证，且消融实验细致，充分证明了各组件的有效性，评估标准客观公正。

## 6. 论文的主要结论与发现
*   **性能提升**：该方法在 DROID 数据集上确立了新的 SOTA，外部相机 LPIPS 降低 14%，腕部相机 SSIM 提高 9.1%。
*   **稳定性增强**：显著减缓了自回归推演中的误差累积速度，能够维持更长时间的结构完整性和空间一致性。
*   **人类偏好**：在盲测中获得了 80% 的偏好率，并在 98% 的成对比较中胜出。
*   **任务相关性**：改进主要集中在物体和机器人等动态区域，而非背景，证明其捕捉到了任务相关的动力学特征。

## 7. 优点
*   **问题针对性强**：精准定位并解决了世界模型自回归推演中的“暴露偏差”问题。
*   **方法创新**：成功将对比 RL 目标适配到 $x_0$-prediction 扩散模型，并设计了巧妙的变长前缀分支训练协议，无需反向传播去噪过程，计算高效。
*   **奖励设计实用**：利用现成的感知指标构建奖励，无需额外的人工标注或复杂的物理仿真器。
*   **验证全面**：从像素级指标到人类感知再到策略评估，全方位验证了方法的有效性。

## 8. 不足与局限
*   **计算开销**：训练过程中需要为每个样本分支生成 K=16 个候选续写，相比标准监督微调增加了计算开销。
*   **奖励局限性**：目前的奖励函数仅基于视觉保真度，未显式引入物理约束或几何一致性，可能导致生成的视频虽然视觉逼真但物理上不一定完全合理。
*   **数据集局限**：主要在 DROID 数据集上验证，对于更复杂或非结构化环境的泛化能力有待进一步考察。

（完）

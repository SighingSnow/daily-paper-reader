---
title: "CaP-X: A Framework for Benchmarking and Improving Coding Agents for Robot Manipulation"
title_zh: CaP-X：面向机器人操作的编码智能体评测与改进框架
authors: "Max Fu, Justin Yu, Karim El-Refai, Ethan Kou, Haoru Xue, Huang Huang, Wenli Xiao, Guanzhi Wang, Fei-Fei Li, Guanya Shi, Jiajun Wu, Shankar Sastry, Yuke Zhu, Ken Goldberg, Linxi \"Jim\" Fan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22435v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于机器人操作编码智能体基准测试的框架
tldr: 该论文提出了CaP-X框架，旨在系统研究机器人操作中的Code-as-Policy智能体。核心包含交互环境CaP-Gym和基准CaP-Bench，揭示了模型对人工抽象的依赖，并发现通过测试时计算扩展可提升鲁棒性。基于此提出的CaP-Agent0和CaP-RL方法，在仿真和真机实验中实现了接近人类水平的可靠性。
selection_source: fresh_fetch
motivation: 尽管“代码即策略”能补充视觉-语言-动作方法，但其在具身操作中作为自主控制器的有效性尚未得到充分探索。
method: 提出了包含交互环境CaP-Gym和评测基准CaP-Bench的CaP-X框架，并基于评测发现提出了免训练框架CaP-Agent0和强化学习方法CaP-RL。
result: 评测显示模型性能依赖人工抽象，但通过扩展测试时计算可缓解该问题，最终在多项任务中恢复了接近人类水平的可靠性。
conclusion: CaP-X为推进具身编程智能体提供了一个原则性的、开放获取的平台。
---

## 摘要
“代码即策略”探讨了可执行代码如何补充数据密集型的视觉-语言-动作（VLA）方法，然而其作为具身操作自主控制器的有效性仍未得到充分探索。我们提出了 CaP-X，一个用于系统研究机器人操作中“代码即策略”智能体的开放获取框架。其核心是 CaP-Gym，这是一个交互式环境，智能体通过合成并执行由感知和控制原语组成的程序来控制机器人。在此基础上，CaP-Bench 在不同的抽象层级、交互程度和感知接地水平上，对前沿的语言模型和视觉-语言模型进行了评估。针对 12 个模型的测试表明，CaP-Bench 揭示了一致的趋势：性能随着人工构建的抽象而提升，但随着这些先验信息的移除而下降，暴露了对设计者搭建的脚手架的依赖。同时，我们观察到这一差距可以通过扩展智能体测试时计算来缓解——通过多轮交互、结构化执行反馈、视觉差分、自动技能合成和集成推理——即使智能体在低级原语上操作，也能显著提升鲁棒性。这些发现使我们能够构建 CaP-Agent0，这是一个免训练框架，在仿真和真实具身的多项操作任务中恢复了人类水平的可靠性。我们进一步引入了 CaP-RL，展示了具有可验证奖励的强化学习能提高成功率，并以极小的差距实现从仿真到真实（sim2real）的迁移。总之，CaP-X 为推进具身编码智能体提供了一个有原则的、开放获取的平台。

## Abstract
"Code-as-Policy" considers how executable code can complement data-intensive Vision-Language-Action (VLA) methods, yet their effectiveness as autonomous controllers for embodied manipulation remains underexplored. We present CaP-X, an open-access framework for systematically studying Code-as-Policy agents in robot manipulation. At its core is CaP-Gym, an interactive environment in which agents control robots by synthesizing and executing programs that compose perception and control primitives. Building on this foundation, CaP-Bench evaluates frontier language and vision-language models across varying levels of abstraction, interaction, and perceptual grounding. Across 12 models, CaP-Bench reveals a consistent trend: performance improves with human-crafted abstractions but degrades as these priors are removed, exposing a dependence on designer scaffolding. At the same time, we observe that this gap can be mitigated through scaling agentic test-time computation--through multi-turn interaction, structured execution feedback, visual differencing, automatic skill synthesis, and ensembled reasoning--substantially improves robustness even when agents operate over low-level primitives. These findings allow us to derive CaP-Agent0, a training-free framework that recovers human-level reliability on several manipulation tasks in simulation and on real embodiments. We further introduce CaP-RL, showing reinforcement learning with verifiable rewards improves success rates and transfers from sim2real with minimal gap. Together, CaP-X provides a principled, open-access platform for advancing embodied coding agents.

---

## 论文详细总结（自动生成）

# 论文总结：CaP-X：面向机器人操作的编码智能体评测与改进框架

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：尽管“代码即策略”作为一种新兴范式，被认为能补充数据密集型的视觉-语言-动作（VLA）方法，但其作为机器人自主控制器的真实有效性、能力边界及改进路径尚未得到系统性的探索和量化评估。
- **研究背景**：
    - **经典控制范式**：依赖人工编写程序，具有强可解释性和几何精度保证，但扩展性差，难以适应开放环境。
    - **VLA范式**：从大规模模仿数据中学习，在接触丰富任务上表现优异，但缺乏可解释性，泛化能力受限，且依赖大量数据收集。
    - **编码智能体**：现代大语言模型（LLM）具备代码合成与调试能力，有望替代人类工程师，成为连接上述两种范式的桥梁。然而，现有研究多依赖高级、任务特定的原语，掩盖了智能体本身的推理能力，未能揭示其在更接近真实编程接口（低级原语）上的真实表现。
- **整体含义**：本研究旨在填补这一空白，通过构建一个统一的评测与改进框架，系统性地揭示编码智能体在机器人操作中的真实能力、依赖关系，并探索通过测试时计算扩展和强化学习来提升其鲁棒性与可靠性的有效路径。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：构建一个原则性、开放获取的平台，用于系统评测和改进面向机器人操作的编码智能体。核心是区分智能体能力与人工设计“脚手架”的贡献，并探索通过智能体自身机制（如多轮交互、技能合成）来弥补低级原语操作中的不足。
- **关键技术细节与流程**：
    - **CaP-Gym（交互环境）**：
        - **架构**：分层控制框架，绑定底层物理仿真器/真机循环与上层代码执行器循环，采用REPL范式。
        - **原语设计**：
            - **感知原语**：模块化服务，如SAM3（语言条件分割）、Molmo 2（开放词汇指向）、OpenCV/Open3D等。
            - **控制原语**：调用运动规划器或逆运动学求解器（如PyRoki），处理碰撞检查、可达性约束，使智能体在任务空间推理。
    - **CaP-Bench（评测基准）**：
        - **评测维度**：抽象层级、时间交互、感知接地。
        - **层级设计**：
            - **单轮（S1-S4）**：S1/S2使用高级原语（S1为特权状态，S2为真实感知）；S3/S4使用低级原语（S3含示例，S4无示例）。
            - **多轮（M1-M4）**：M1仅文本反馈；M2直接多模态视觉输入；M3引入视觉差分模块（VDM）将视觉转为文本描述；M4结合VDM与低级原语。
    - **CaP-Agent0（免训练改进框架）**：
        - **多轮视觉差分（VDM）**：将视觉观察转化为结构化自然语言，提供场景描述和变化差异，解决跨模态对齐问题。
        - **自动合成技能库**：从成功执行轨迹中提取函数定义，经LLM分析识别任务无关逻辑，形成可复用技能库，减轻低级逻辑负担。
        - **并行推理**：并发采样多个候选代码方案（单模型或多模型集成），由中心智能体综合成最终代码，增加探索多样性。
    - **CaP-RL（强化学习方法）**：
        - **方法**：使用可验证奖励的强化学习（RLVR），采用GRPO算法对编码智能体进行后训练。
        - **流程**：在特权状态API（S1层级）上训练以稳定收敛，优化智能体对抽象感知API的推理策略，实现零样本sim2real迁移。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **数据集/场景**：
    - **CaP-Gym**：整合了187个任务，源自RoboSuite、LIBERO-PRO和BEHAVIOR三个标准机器人操作仿真器，涵盖桌面、双臂和移动操作。
    - **CaP-Bench核心任务**：选取7个代表性任务进行主要分析：Cube Lift、Cube Stack、Spill Wipe、Peg Insertion、Cube Re-stack、Two-Arm Lift、Two-Arm Handover。
    - **扩展评估**：在LIBERO-PRO（30个任务）和BEHAVIOR（2个长视野移动操作任务）上验证CaP-Agent0。
- **Benchmark**：CaP-Bench，系统评测智能体在不同抽象层级、交互模式和感知接地下的表现，并与人类专家编写的参考方案进行对比。
- **对比方法**：
    - **模型对比**：评估了12个前沿模型，包括闭源模型（Gemini-3-Pro、GPT o1/o4-mini/5.1/5.2、Claude Haiku 4.5/Opus 4.5）和开源模型（GPT-OSS-20B/120B、Qwen3 235B、Qwen-2.5-Coder-7B-Instruct、Kimi K2、DeepSeek-V3.1）。
    - **方法对比**：在LIBERO-PRO上，将CaP-Agent0与VLA方法（OpenVLA、π0、π0.5）进行对比。
    - **基准对比**：所有评测均与人类专家在相同环境和原语下的表现进行对比。

## 4. 资源与算力
- **论文未明确说明**：文中未提及具体的GPU型号、数量、训练时长等算力细节。
- **部分提及**：CaP-RL部分提到对Qwen2.5-Coder-7B-Instruct模型进行了50轮迭代的后训练，但未给出计算资源消耗的具体数据。

## 5. 实验数量与充分性
- **实验数量**：
    - **CaP-Bench核心评测**：每个任务进行100次试验，共7个任务，覆盖12个模型，在不同层级（S1-S4, M1-M4）下进行。
    - **CaP-Agent0消融实验**：在7个任务上，逐步添加VDM、技能库、并行推理组件，验证各部分贡献。
    - **CaP-RL实验**：在仿真（100次试验）和真机（25次试验）上验证后训练效果。
    - **扩展评估**：LIBERO-PRO每个任务50次试验；BEHAVIOR每个任务25次试验。
- **充分性与客观性**：
    - **充分**：实验覆盖了从单轮到多轮、从高级到低级原语、从仿真到真机的多种场景，并进行了详细的消融研究，验证了方法各组件的有效性。
    - **客观公平**：评测协议统一（Zero-Shot Pass@1），所有模型和人类专家在相同环境、相同原语接口下进行对比，确保了公平性。多轮交互中环境不重置，模拟真实连续操作场景。

## 6. 论文的主要结论与发现
- **发现1**：前沿模型与人类专家在单轮、零样本评估中存在显著差距，即使是最强的模型也未达到人类专家的成功率。
- **发现2**：性能随原语抽象层级提高而单调提升，表明现有“代码即策略”工作的高性能很大程度上依赖于人工设计的高级原语（“脚手架”），这限制了表达力和泛化性。
- **发现3**：多轮交互和视觉接地能显著改善性能。文本执行反馈（M1）有效；直接输入原始图像（M2）反而降低性能；将视觉转化为结构化文本的VDM（M3）效果最佳。
- **发现4**：通过扩展测试时计算（多轮交互、VDM、技能合成、并行推理），智能体在低级原语上的操作（M4）可以超越高级原语的单轮表现（S2），甚至达到高级原语多轮表现（M3），证明了鲁棒性可以在运行时合成。
- **发现5**：CaP-Agent0在4/7的CaP-Bench任务上达到或超越人类专家水平，并在LIBERO-PRO和BEHAVIOR上表现出优于或可比于VLA方法的性能，且对指令扰动更鲁棒。
- **发现6**：CaP-RL后训练能显著提升基础模型的代码编译率和任务成功率，并实现极小差距的sim2real迁移，证明了直接优化编码智能体策略的有效性。

## 7. 优点：方法或实验设计上有哪些亮点
- **框架系统性**：CaP-X提供了一个从环境、基准到改进方法的完整闭环，首次系统性地量化了编码智能体在不同抽象层级下的真实能力。
- **基准设计精细**：CaP-Bench通过分层设计（S1-S4, M1-M4），巧妙地分离了规划能力、感知噪声、交互能力和视觉接地能力的影响，揭示了智能体对“脚手架”的依赖。
- **改进方法创新且有效**：CaP-Agent0提出的VDM、自动技能库和并行推理均为免训练方法，直接利用智能体自身能力弥补低级原语操作的不足，效果显著。CaP-RL展示了强化学习直接优化编码智能体的潜力。
- **真机验证充分**：不仅在仿真中验证，还在Franka Panda和AgiBot G1等真实机器人上进行了零样本部署，展示了方法的实用性和泛化能力。
- **开源开放**：框架和基准设计为开放获取，为社区提供了标准化的评测和改进平台。

## 8. 不足与局限
- **任务类型局限**：论文指出，程序控制对需要紧密视觉伺服和连续反馈的接触丰富行为（如精细插入、倾倒）仍显脆弱。未来需探索混合CaP-VLA策略。
- **感知与控制模块依赖**：CaP-Agent0的成功依赖于底层感知模块（如SAM3、Molmo）和控制模块（如运动规划器）的可靠性。文中案例显示，感知失败（如分割错误）或缺乏碰撞感知的运动规划会导致任务失败。
- **实验覆盖范围**：虽然任务多样，但核心评测的7个任务仍以桌面操作为主。BEHAVIOR的移动操作实验规模较小（2个任务）。
- **真机实验规模**：真机实验主要展示了定性案例和有限的成功率统计，大规模、多样化的真机评测有待进一步开展。
- **强化学习训练设置**：CaP-RL在特权状态（S1）上训练以避免噪声，这可能与真实非特权场景存在差异，尽管论文展示了良好的迁移效果，但该假设的普适性需更多验证。

（完）

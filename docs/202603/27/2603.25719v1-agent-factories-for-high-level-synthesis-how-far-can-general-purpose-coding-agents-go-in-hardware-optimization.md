---
title: "Agent Factories for High Level Synthesis: How Far Can General-Purpose Coding Agents Go in Hardware Optimization?"
title_zh: 用于高层次综合的智能体工厂：通用编程智能体在硬件优化中能走多远？
authors: "Abhishek Bhandwaldar, Mihir Choudhury, Ruchir Puri, Akash Srivastava"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25719v1"
tags: ["query:agent"]
score: 9.0
evidence: 智能体工厂框架，构建和协调多个自主编码智能体进行硬件优化
tldr: 本研究探讨了通用编码智能体在高层综合（HLS）硬件优化中的潜力，提出了一种名为“智能体工厂”的两阶段流水线。该方法通过分解子内核、整数线性规划配置及多智能体协同探索，实现了无需特定硬件训练的自动优化。实验显示，扩展智能体数量可获得平均8.27倍的加速，证明了智能体扩展在HLS优化中的有效性。
selection_source: fresh_fetch
motivation: 探究无需特定硬件训练的通用编码智能体在高层综合硬件优化中能达到何种程度。
method: 提出两阶段“智能体工厂”流水线，结合子内核分解、整数线性规划配置与多智能体协同探索跨函数优化。
result: 在12个内核测试中，扩展至10个智能体实现了平均8.27倍加速，部分高难度基准测试加速超过20倍。
conclusion: 通用编码智能体能有效重发现已知优化模式，验证了智能体扩展是HLS优化的实用且有效的新途径。
---

## 摘要
我们开展了一项实证研究，探讨通用编程智能体——在未接受特定硬件训练的情况下——能在多大程度上根据高层算法规约优化硬件设计。我们引入了“智能体工厂”，这是一个构建并协调多个自主优化智能体的两阶段流水线。在第一阶段，流水线将设计分解为子内核，利用pragma指令和代码级变换独立优化每个子内核，并构建整数线性规划（ILP）模型，在面积约束下整合出全局有潜力的配置。在第二阶段，它在排名靠前的ILP解上启动$N$个专家智能体，每个智能体探索子内核分解无法涵盖的跨函数优化，如pragma重组、循环融合和内存重构。我们使用Claude Code (Opus 4.5/4.6) 和 AMD Vitis HLS，在来自HLS-Eval和Rodinia-HLS的12个内核上评估了该方法。将智能体数量从1扩展到10，相比基线实现了平均$8.27\times$的加速，在较难的基准测试上提升更大：streamcluster超过$20\times$，kmeans达到约$10\times$。在所有基准测试中，智能体在未接受特定领域训练的情况下成功复现了已知的硬件优化模式，且最佳设计往往并非源自排名靠前的ILP候选解，这表明全局优化能够发现子内核搜索所遗漏的改进空间。这些结果确立了智能体扩展作为HLS优化中一个实用且有效的方向。

## Abstract
We present an empirical study of how far general-purpose coding agents -- without hardware-specific training -- can optimize hardware designs from high-level algorithmic specifications. We introduce an agent factory, a two-stage pipeline that constructs and coordinates multiple autonomous optimization agents.   In Stage~1, the pipeline decomposes a design into sub-kernels, independently optimizes each using pragma and code-level transformations, and formulates an Integer Linear Program (ILP) to assemble globally promising configurations under an area constraint. In Stage~2, it launches $N$ expert agents over the top ILP solutions, each exploring cross-function optimizations such as pragma recombination, loop fusion, and memory restructuring that are not captured by sub-kernel decomposition.   We evaluate the approach on 12 kernels from HLS-Eval and Rodinia-HLS using Claude Code (Opus~4.5/4.6) with AMD Vitis HLS. Scaling from 1 to 10 agents yields a mean $8.27\times$ speedup over baseline, with larger gains on harder benchmarks: streamcluster exceeds $20\times$ and kmeans reaches approximately $10\times$. Across benchmarks, agents consistently rediscover known hardware optimization patterns without domain-specific training, and the best designs often do not originate from top-ranked ILP candidates, indicating that global optimization exposes improvements missed by sub-kernel search. These results establish agent scaling as a practical and effective axis for HLS optimization.

---

## 论文详细总结（自动生成）

以下是对论文《Agent Factories for High Level Synthesis: How Far Can General-Purpose Coding Agents Go in Hardware Optimization?》的详细总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：高层次综合（HLS）旨在将C/C++算法转化为硬件描述，但现有工具仍需大量专家手动插入pragma指令和重构代码以优化性能（延迟与面积）。现有的自动化方法多局限于预定义参数空间的搜索，无法进行开放式的代码重构或算法变换。
*   **核心问题**：探究**未经特定硬件训练的通用编程智能体**，在仅给定源代码和综合工具访问权限的情况下，能在多大程度上优化硬件设计。
*   **整体含义**：本研究旨在验证“智能体扩展”作为一种新的计算范式在硬件设计空间探索（DSE）中的有效性，即通过增加推理时的计算资源（智能体数量）来提升优化质量，从而减少对领域专家的依赖。

### 2. 论文提出的方法论
*   **核心思想**：提出“智能体工厂”框架，采用**两阶段流水线**策略。第一阶段通过分解问题降低搜索复杂度，利用整数线性规划（ILP）筛选候选解；第二阶段通过多智能体并行探索实现全局优化。
*   **关键技术细节与流程**：
    *   **阶段一：子内核优化与ILP选择**
        *   **分解**：协调智能体分析调用图，将设计分解为子函数。
        *   **变体生成**：为每个子函数生成$M=7$个优化变体（包括基线、保守策略、流水线、激进展开、数组分割等）。
        *   **评估**：对变体进行正确性检查和HLS综合，获取延迟$L$和面积$A$。
        *   **ILP建模**：构建整数线性规划模型，目标是在满足全局面积约束$A \le A_{budget}$的前提下最小化总延迟$L_{total}$。求解器输出Top-N个最优配置方案。
    *   **阶段二：全局优化与智能体扩展**
        *   **初始化**：基于阶段一的Top-N解，启动$N$个并行的探索智能体。
        *   **全局探索**：每个智能体独立进行跨函数边界的优化，路径包括：Pragma重组、代码重构（如循环融合）、内存优化、计算优化。
        *   **输出**：迭代搜索后，从所有智能体发现的有效设计中选出满足约束且延迟最低的设计$D^*$。

### 3. 实验设计
*   **数据集/Benchmark**：共选取**12个内核**。
    *   6个来自 **HLS-Eval**（AES, DES, KMP, NW, PRESENT, SHA256）。
    *   6个来自 **Rodinia-HLS**（lavamd, kmeans, hotspot, leukocyte, cfd, streamcluster）。
*   **实验场景**：主要针对FPGA平台（使用AMD Vitis HLS工具链），部分实验扩展至ASIC逻辑综合验证（使用ABC工具）。
*   **对比方法**：
    *   **基线**：对于HLS-Eval，采用有界穷举搜索作为基线；对于Rodinia-HLS，使用现有的优化参考实现。
    *   **变量控制**：主要变量为智能体数量$N \in \{1, 2, 4, 8, 10\}$，用于验证“智能体扩展”对性能提升的影响。

### 4. 资源与算力
*   **模型资源**：使用 **Claude Code (Opus 4.5/4.6)** 作为底层大语言模型。
*   **算力消耗**：文中未提及具体的GPU型号或物理集群配置，但详细统计了**Token消耗**：
    *   每次运行的中位数消耗为 **5.82M tokens**。
    *   平均消耗为 **7.67M tokens**。
    *   最大消耗可达 **45.33M tokens**。
*   **说明**：研究重点在于推理时的计算扩展，而非模型训练。

### 5. 实验数量与充分性
*   **实验数量**：
    *   在12个基准测试上进行评估。
    *   每个配置重复运行**5次**取平均值。
    *   设置了5组不同的智能体数量（$N=1, 2, 4, 8, 10$）进行对比。
    *   包含了对FPGA和ASIC相关性的验证实验。
*   **充分性与客观性评价**：
    *   **充分性**：对于一项初步探索性研究，实验设置较为全面，涵盖了不同类型的计算负载，并进行了多次重复实验以减少随机性。
    *   **客观性局限**：作者明确指出基线并非最先进的DSE框架（如AutoDSE），而是受限的穷举搜索；且仅使用了单一模型家族，可能存在模型偏好偏差。

### 6. 论文的主要结论与发现
*   **智能体扩展有效**：将智能体数量从1增加到10，相比基线实现了平均 **8.27倍** 的加速。在复杂任务上提升显著，如`streamcluster`加速超过20倍，`kmeans`约10倍。
*   **全局优化优于局部组合**：最佳设计往往不源自ILP排名最高的解，证明跨函数的全局优化（阶段二）能发现子内核独立优化（阶段一）无法触及的改进空间。
*   **自主发现优化模式**：智能体在无特定训练下，自主学会了硬件优化专家知识，如使用`ARRAY_PARTITION`解决内存瓶颈，以及在存在循环依赖时避免盲目使用`PIPELINE`。
*   **ASIC相关性**：HLS预估面积与ASIC逻辑综合面积在多数内核上呈强相关性，验证了方法在ASIC流程的潜在价值。

### 7. 优点
*   **方法创新**：首次提出将“智能体工厂”和多智能体扩展机制应用于HLS设计空间探索，突破了传统参数搜索的限制，支持代码重构。
*   **流程设计合理**：两阶段设计巧妙结合了确定性算法（ILP）与启发式搜索（LLM智能体），既保证了基础解的质量，又保留了探索更优解的可能性。
*   **实证价值高**：证明了通用大模型无需微调即可掌握复杂的硬件优化规律，为自动化硬件设计提供了新的基线。

### 8. 不足与局限
*   **基线对比不足**：未与当前最先进的自动化DSE框架（如AutoDSE）进行直接对比，仅使用了受限的穷举搜索作为基线，难以衡量其相对于SOTA方法的绝对优势。
*   **模型与工具单一**：实验仅基于Claude Opus系列模型和AMD Vitis HLS工具，结论的泛化性有待验证。
*   **Benchmark规模有限**：12个内核规模较小，未涉及大规模真实工业级设计，可能无法完全反映复杂设计中的挑战。
*   **成本问题**：Token消耗较高（单次运行数百万级），对于复杂设计可能面临成本效益比的挑战。

（完）

---
title: "SlopCodeBench: Benchmarking How Coding Agents Degrade Over Long-Horizon Iterative Tasks"
title_zh: SlopCodeBench：评估编码智能体在长视界迭代任务中的退化情况
authors: "Gabriel Orlanski, Devjeet Roy, Alexander Yun, Changho Shin, Alex Gu, Albert Ge, Dyah Adila, Frederic Sala, Aws Albarghouthi"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24755v1"
tags: ["query:agent"]
score: 10.0
evidence: 评估代码智能体的基准
tldr: 现有基准多评估单次任务，忽略了软件开发的迭代特性。本文提出SlopCodeBench基准，通过93个检查点评估智能体在迭代扩展任务中的代码质量退化。研究发现，智能体代码的冗长度和结构侵蚀随迭代显著增加，质量远逊于人类代码，且提示干预无法阻止退化，揭示了当前智能体缺乏迭代开发所需的设计纪律。
selection_source: fresh_fetch
motivation: 现有基准多关注单次通过率，无法真实衡量代码在长期迭代扩展中的质量退化问题。
method: 提出SlopCodeBench基准，让智能体在演变规范下迭代扩展代码，并跟踪冗长度和结构侵蚀两个质量指标。
result: 实验显示智能体代码质量随迭代稳步下降，冗长度和结构侵蚀显著增加，且远差于人类代码。
conclusion: 通过率基准低估了扩展鲁棒性，当前智能体缺乏迭代软件开发所需的设计纪律。
---

## 摘要
软件开发是迭代的，然而现有的编码智能体基准测试绝大多数仅针对完整规范评估一次性解决方案。代码可以通过测试套件，但可能变得越来越难以扩展。近期的迭代基准测试试图弥合这一差距，但对智能体的设计决策限制过严，无法如实衡量代码质量如何影响未来的扩展。我们提出了 SlopCodeBench，这是一个包含 20 个问题和 93 个检查点的语言无关基准测试；在该测试中，智能体在不断演进的规范下反复扩展其先前的解决方案，这些规范强制要求进行架构决策，但不规定内部结构。我们追踪两个轨迹层面的质量信号：冗长度（冗余或重复代码的比例）和结构侵蚀（集中高复杂度函数中的复杂度质量份额）。在 11 个模型中，没有任何智能体能端到端地解决任何问题；最高检查点解决率为 17.2%。质量稳步退化：80% 的轨迹中侵蚀上升，89.8% 的轨迹中冗长度上升。与 48 个开源 Python 代码库相比，智能体生成的代码冗长度高出 2.2 倍，且侵蚀程度明显更高。对其中 20 个代码库的长期追踪显示，人类编写的代码保持平稳，而智能体生成的代码随每次迭代而恶化。一项提示干预研究表明，初始质量可以得到改善，但这并不能阻止退化。这些结果表明，通过率基准测试系统性地低估了扩展鲁棒性，且当前的智能体缺乏迭代软件开发所需的设计纪律。

## Abstract
Software development is iterative, yet agentic coding benchmarks overwhelmingly evaluate single-shot solutions against complete specifications. Code can pass the test suite but become progressively harder to extend. Recent iterative benchmarks attempt to close this gap, but constrain the agent's design decisions too tightly to faithfully measure how code quality shapes future extensions. We introduce SlopCodeBench, a language-agnostic benchmark comprising 20 problems and 93 checkpoints, in which agents repeatedly extend their own prior solutions under evolving specifications that force architectural decisions without prescribing internal structure. We track two trajectory-level quality signals: verbosity, the fraction of redundant or duplicated code, and structural erosion, the share of complexity mass concentrated in high-complexity functions. No agent solves any problem end-to-end across 11 models; the highest checkpoint solve rate is 17.2%. Quality degrades steadily: erosion rises in 80% of trajectories and verbosity in 89.8%. Against 48 open-source Python repositories, agent code is 2.2x more verbose and markedly more eroded. Tracking 20 of those repositories over time shows that human code stays flat, while agent code deteriorates with each iteration. A prompt-intervention study shows that initial quality can be improved, but it does not halt degradation. These results demonstrate that pass-rate benchmarks systematically undermeasure extension robustness, and that current agents lack the design discipline iterative software development demands.

---

## 论文详细总结（自动生成）

以下是对论文《SlopCodeBench: Benchmarking How Coding Agents Degrade Over Long-Horizon Iterative Tasks》的详细总结：

### 1. 论文的核心问题与整体含义

*   **研究背景**：软件开发本质上是一个迭代过程，需求不断变更，代码需要反复扩展。然而，现有的编码智能体基准测试（如 SWE-Bench）主要评估“一次性”解决方案，即针对完整规范生成代码并通过测试。这种方式忽略了代码在长期迭代中的可维护性。
*   **核心问题**：代码可以通过测试但变得难以扩展。智能体在反复修改自身代码时，往往会产生低质量、高冗余的代码（俗称“slop”），导致维护成本增加。现有基准无法衡量这种随时间推移的“结构退化”现象。
*   **整体含义**：研究揭示了当前大模型智能体在长期迭代任务中的致命缺陷——缺乏“设计纪律”。即使初始代码质量尚可，随着迭代进行，代码质量会系统性恶化，且仅靠提示词干预无法阻止这一趋势。

### 2. 论文提出的方法论

*   **核心思想**：提出 **SlopCodeBench (SCBench)**，一个语言无关的基准测试，模拟真实的软件迭代场景。智能体必须基于自己上一轮的代码实现新功能，而非每次从零开始或基于标准答案修改。
*   **关键设计原则**：
    *   **无内部接口规定**：仅指定外部行为（CLI/API），不限制内部实现，迫使智能体自行承担架构决策的后果。
    *   **隐藏测试套件**：智能体只能看到规范文本，无法通过测试反馈来“作弊”或获取架构提示。
    *   **黑盒与语言无关**：通过标准输入输出评估，理论上支持任何编程语言（本文实验聚焦 Python）。
*   **质量评估指标**：除了正确性，论文定义了两个轨迹层面的质量信号：
    *   **冗长度**：衡量冗余或重复代码的比例。
        *   *计算方法*：结合 AST-Grep 规则检测到的冗余模式行数与克隆代码行数，除以总代码行数（LOC）。
    *   **结构侵蚀**：衡量复杂度是否集中在少数高复杂度函数中。
        *   *计算方法*：定义函数 $f$ 的质量为 `mass(f) = CC(f) × √SLOC(f)`，其中 CC 为圈复杂度，SLOC 为源代码行数。侵蚀度为高复杂度函数（CC>10）的质量总和占总质量的比例。

### 3. 实验设计

*   **数据集 / 场景**：
    *   **SlopCodeBench**：包含 20 个问题，共 93 个检查点。每个问题从基础功能开始，逐步增加新需求（如从仅支持 Python 扩展到多语言支持、增加 AST 匹配等）。
    *   **对比基准**：选取 48 个开源 Python 仓库作为人类代码基线，用于校准智能体代码的质量指标。
*   **评估对象**：
    *   **模型**：11 个前沿模型，包括 Claude Sonnet 4.5/4.6、Claude Opus 4.5/4.6、GPT 5.1-5.4 系列、GLM 4.7。
    *   **工具**：使用各模型原生的 CLI 工具（如 Claude Code, Codex CLI）进行评估，而非通用框架，以模拟真实开发者工作流。
*   **对比方法**：
    *   **提示干预策略**：对比了三种提示策略——Baseline（仅要求解决任务）、Anti-Slop（明确禁止冗余模式）、Plan-First（要求先规划后编码）。

### 4. 资源与算力

*   **算力说明**：论文未明确提及训练所需的 GPU 型号或数量（因为这是评估性研究，不涉及模型训练）。
*   **推理成本**：详细记录了 API 调用成本。例如，Opus 4.6 的平均成本为每检查点 3.47 美元，总成本约 322.97 美元；GPT 5.2 总成本约 422.72 美元。实验设置了每个检查点 2 小时的挂钟时间限制。

### 5. 实验数量与充分性

*   **实验规模**：
    *   主实验涵盖了 11 个模型在 20 个问题上的完整轨迹评估。
    *   对 48 个人类仓库进行了静态分析，并对其中 20 个进行了时间序列分析（共 568 个提交点）。
    *   进行了提示干预的消融实验，对比了不同策略对代码质量和成本的影响。
*   **充分性与客观性**：
    *   实验设计较为充分，不仅评估了通过率，还引入了定量的质量指标（冗长度、侵蚀度）。
    *   通过与人类仓库的对比，客观展示了智能体代码与人类维护代码在演化趋势上的巨大差异。
    *   评估了多种模型和提示策略，结论具有普遍性。

### 6. 论文的主要结论与发现

*   **通过率极低**：没有任何智能体能端到端解决所有问题，最高严格通过率仅为 17.2%（Opus 4.6）。
*   **质量系统性退化**：
    *   80% 的轨迹中结构侵蚀上升，89.8% 的轨迹中冗长度上升。
    *   智能体代码比人类代码冗长度高 2.2 倍，侵蚀程度显著更高。
    *   随着迭代进行，人类代码质量指标趋于平稳，而智能体代码持续恶化。
*   **提示干预的局限性**：虽然“Anti-Slop”等提示能降低初始代码的冗长度和侵蚀度，但无法改变随迭代退化的斜率。更糟糕的是，追求高质量代码反而增加了成本（如 GPT 5.4 成本增加 47.9%），且并未显著提升通过率。
*   **早期决策的复利效应**：早期糟糕的架构决策（如硬编码逻辑）会导致后续修改成本剧增，形成“技术债务”。

### 7. 优点

*   **视角创新**：首次系统性地量化评估了智能体在长视界迭代任务中的代码质量退化问题，填补了现有基准仅关注单次通过率的空白。
*   **指标设计科学**：提出的“结构侵蚀”和“冗长度”指标，有效捕捉了代码腐烂的特征，且与软件工程中的技术债务概念相呼应。
*   **实验设置真实**：强制智能体基于自身历史代码修改，而非基于标准答案，真实模拟了软件维护场景。

### 8. 不足与局限

*   **语言覆盖限制**：虽然基准设计为语言无关，但当前实验仅报告了 Python 轨迹的结果，未验证在其他语言（如 Java, C++）上的表现。
*   **工具依赖性**：实验依赖特定版本的 CLI 工具（如 Claude Code, Codex），不同版本的 Harness 可能会影响结果稳定性（论文附录中也有提及版本敏感性）。
*   **样本量限制**：受限于高昂的 API 成本和时间消耗，部分实验可能样本量有限（如提示干预实验仅在部分模型上进行）。
*   **评估范围**：仅关注了代码结构和复杂度，未涉及安全性漏洞、内存泄漏等其他维度的质量评估。

（完）

---
title: "DIAL: Decoupling Intent and Action via Latent World Modeling for End-to-End VLA"
title_zh: DIAL：通过潜在世界建模解耦意图与动作以实现端到端 VLA
authors: "Yi Chen, Yuying Ge, Hui Zhou, Mingyu Ding, Yixiao Ge, Xihui Liu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29844v1"
tags: ["query:agent"]
score: 9.0
evidence: 具有潜在世界建模的视觉-语言-动作智能体用于决策
tldr: 针对现有VLA模型忽视VLM高级决策潜力的问题，本文提出DIAL框架，通过潜在世界建模解耦意图与动作。该框架利用VLM生成潜在意图瓶颈，指导轻量级策略执行低级动作。实验表明，DIAL仅需十分之一演示数据即可达到SOTA性能，并在真实人形机器人上实现了鲁棒的零样本泛化。
selection_source: fresh_fetch
motivation: 现有端到端VLA模型直接映射特征到低级动作，导致VLM高级决策潜力未被充分利用且训练不稳定。
method: 提出DIAL框架，利用基于VLM的System-2进行潜在世界建模生成意图瓶颈，并通过轻量级System-1策略将其解码为精确动作。
result: 在RoboCasa基准测试中达到SOTA性能，所需演示数据减少10倍，并在真实人形机器人上实现了零样本泛化。
conclusion: DIAL成功通过解耦意图与动作连接了高级决策与低级执行，显著提升了VLA模型的效率、稳定性与泛化能力。
---

## 摘要
预训练视觉-语言模型显著加速了视觉-语言-动作 (VLA) 模型的发展。然而，大多数现有的端到端 VLA 主要将 VLM 视为多模态编码器，直接将视觉-语言特征映射到底层动作。这种范式未充分利用 VLM 在高层决策中的潜力，并引入了训练不稳定性，经常导致其丰富的语义表示退化。为了解决这些局限性，我们提出了 DIAL，这是一个通过可微潜在意图瓶颈连接高层决策与底层运动执行的框架。具体而言，基于 VLM 的系统 2 (System-2) 通过在 VLM 的原生特征空间内合成潜在视觉预见来执行潜在世界建模；这种预见显式编码了意图，并作为结构性瓶颈。随后，一个轻量级的系统 1 (System-1) 策略通过潜在逆动力学，将预测的意图与当前观测一起解码为精确的机器人动作。为了确保优化稳定性，我们采用了一种两阶段训练范式：一个解耦的预热阶段，其中系统 2 学习预测潜在未来，而系统 1 在统一特征空间内的真实未来引导下学习运动控制，随后进行无缝的端到端联合优化。这使得动作感知梯度能够以受控的方式优化 VLM 主干网络，从而保留预训练知识。在 RoboCasa GR1 桌面基准上的大量实验表明，DIAL 确立了新的最先进水平，且使用的演示数据量比先前方法少 10 倍的情况下实现了优越的性能。此外，通过利用异构人类演示，DIAL 学习了基于物理的操纵先验，并在人形机器人的真实世界部署中，对未见物体和新颖构型表现出鲁棒的零样本泛化能力。

## Abstract
The development of Vision-Language-Action (VLA) models has been significantly accelerated by pre-trained Vision-Language Models (VLMs). However, most existing end-to-end VLAs treat the VLM primarily as a multimodal encoder, directly mapping vision-language features to low-level actions. This paradigm underutilizes the VLM's potential in high-level decision making and introduces training instability, frequently degrading its rich semantic representations. To address these limitations, we introduce DIAL, a framework bridging high-level decision making and low-level motor execution through a differentiable latent intent bottleneck. Specifically, a VLM-based System-2 performs latent world modeling by synthesizing latent visual foresight within the VLM's native feature space; this foresight explicitly encodes intent and serves as the structural bottleneck. A lightweight System-1 policy then decodes this predicted intent together with the current observation into precise robot actions via latent inverse dynamics. To ensure optimization stability, we employ a two-stage training paradigm: a decoupled warmup phase where System-2 learns to predict latent futures while System-1 learns motor control under ground-truth future guidance within a unified feature space, followed by seamless end-to-end joint optimization. This enables action-aware gradients to refine the VLM backbone in a controlled manner, preserving pre-trained knowledge. Extensive experiments on the RoboCasa GR1 Tabletop benchmark show that DIAL establishes a new state-of-the-art, achieving superior performance with 10x fewer demonstrations than prior methods. Furthermore, by leveraging heterogeneous human demonstrations, DIAL learns physically grounded manipulation priors and exhibits robust zero-shot generalization to unseen objects and novel configurations during real-world deployment on a humanoid robot.

---

## 论文详细总结（自动生成）

# DIAL：通过潜在世界建模解耦意图与动作以实现端到端 VLA

## 1. 论文的核心问题与整体含义
*   **研究背景**：预训练视觉-语言模型（VLM）为通用机器人策略提供了强大的语义基础。然而，现有的端到端视觉-语言-动作（VLA）模型通常将 VLM 仅作为特征编码器，直接将特征映射为低级动作。
*   **核心问题**：这种直接映射的方式存在两大缺陷：
    1.  **潜力未充分利用**：忽视了 VLM 在高级决策和推理方面的潜力。
    2.  **训练不稳定**：低级动作监督容易导致 VLM 丰富的语义表示崩塌，且容易陷入捷径学习，即模型仅依赖表面相关性而非真正的物理理解。
*   **整体含义**：本文旨在解决如何设计一种架构，既能保留 VLM 的高级认知推理能力，又能实现精确的低级运动控制，从而在保证训练稳定性的同时提升数据效率和泛化能力。

## 2. 论文提出的方法论
*   **核心思想**：受认知科学“双系统理论”启发，提出 DIAL 框架。通过引入“潜在意图瓶颈”，将高级决策（System-2）与低级执行（System-1）解耦。
*   **架构设计**：
    *   **System-2 (VLM)**：作为“大脑”，负责潜在世界建模。它不直接输出动作，而是在 VLM 原生的 ViT 特征空间中预测未来的视觉状态，即“潜在视觉预见”。这作为显式的意图表示。
    *   **System-1 (Policy)**：作为“小脑”，是一个轻量级的流匹配策略。它作为潜在逆动力学模型，接收当前观测和 System-2 预测的潜在意图，推断出达到该意图所需的精确动作。
    *   **结构性瓶颈**：System-1 必须依赖 System-2 的预测意图来生成动作，这种强制依赖防止了模型绕过推理过程直接拟合动作，解决了捷径学习问题。
*   **训练流程**：采用“解耦预热 -> 端到端联合优化”的两阶段范式。
    *   **阶段 1（解耦预热）**：System-2 学习预测真实未来特征（$L_{world}$），System-1 学习在真实未来特征引导下生成动作（$L_{fm}$）。两者独立训练，防止初期梯度干扰和表示崩塌。
    *   **阶段 2（端到端训练）**：将两者连接，System-1 依赖 System-2 的预测输出。动作生成的梯度可回传至 VLM，使其意图变得“动作感知”，同时保留预训练知识。
*   **关键公式**：
    *   世界建模损失：$L_{world} = \|x_t - Enc_{ViT}(o_{t+H})\|_2^2$，其中 $x_t$ 为预测意图，$o_{t+H}$ 为未来观测。
    *   动作生成损失：基于流匹配的 $L_{fm}$，用于训练 System-1。

## 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：RoboCasa GR1 Tabletop 基准，包含 24 个桌面任务（18 个抓取放置任务，6 个关节操作任务）。
    *   **真实世界**：IRON-R01-1.11 人形机器人，执行 Pick & Place 和 Pouring 任务。
    *   **异构数据**：引入 EgoDex 人类演示数据集进行跨具身学习。
*   **Benchmark**：
    *   **性能对比**：在 Full Data（24k 轨迹）和 Few-Shot（2.4k 轨迹）设置下对比。
    *   **泛化性测试**：未见外观、未见组合、未见物体类型。
*   **对比方法**：
    *   **传统策略**：Diffusion Policy, UWM, FLARE。
    *   **VLA 模型**：GR00T-N1.6, GR00T-Qwen3, $\pi$-Qwen3, FAST-Qwen3, OFT-Qwen3。
    *   **消融变体**：GR00T-Qwen2.5 (Frozen/FT), GR00T + FLARE/SEER (辅助任务对比), DIAL-DINO (特征空间一致性验证)。

## 4. 资源与算力
*   **说明**：论文中未明确提及具体的 GPU 型号、数量或训练时长等算力消耗细节。

## 5. 实验数量与充分性
*   **实验数量**：
    *   **仿真实验**：包含全量数据与少样本数据下的性能对比，以及 3 种 OOD（分布外）泛化测试。
    *   **真机实验**：包含 2 种任务类型、3 种泛化场景（组合泛化、干扰鲁棒性、实例级迁移）。
    *   **消融实验**：详细验证了世界建模目标、接口设计（瓶颈 vs 辅助）、特征对齐（原生 ViT vs DINO）、解耦预热策略以及人类数据的作用。
    *   **可视化分析**：通过 PCA 可视化潜在意图特征，验证模型是否学到了有语义意义的未来预测。
*   **充分性与客观性**：实验设计非常充分且严谨。不仅对比了 SOTA 方法，还构建了基于相同骨干网络的公平消融实验，深入分析了各组件的贡献。真机部署进一步验证了实用性。

## 6. 论文的主要结论与发现
*   **性能突破**：DIAL 在 RoboCasa 基准上达到了 SOTA 性能，且仅需先前方法 10% 的演示数据（10x 数据效率）。
*   **架构优势**：相比于将未来预测作为辅助任务（如 SEER, FLARE），DIAL 的结构性瓶颈设计强制动作依赖于预测意图，有效避免了捷径学习，显著提升了性能。
*   **训练稳定性**：解耦预热阶段对于防止 VLM 语义崩塌至关重要，移除该阶段会导致真机性能大幅下降。
*   **泛化能力**：利用异构人类数据预训练，DIAL 能有效学习物理先验，显著提升了对未见物体和场景的零样本泛化能力。
*   **特征一致性**：System-2 和 System-1 必须共享统一的特征空间（原生 ViT），跨特征空间（如 DINO）会导致性能下降。

## 7. 优点
*   **创新架构**：巧妙利用“潜在意图瓶颈”连接 VLM 的推理能力与策略的控制能力，解决了端到端 VLA 中语义崩塌的痛点。
*   **训练策略**：提出的两阶段训练范式有效平衡了 VLM 的知识保留与动作感知能力的注入。
*   **高效与实用**：极高的数据效率（10x 减少）和强大的真机泛化能力，展示了其在实际机器人应用中的巨大潜力。
*   **理论解释性强**：通过可视化证明了潜在意图确实编码了任务相关的未来状态变化，而非黑盒特征。

## 8. 不足与局限
*   **算力信息缺失**：论文未公开具体的计算资源消耗，难以评估其训练成本。
*   **模型规模限制**：System-1 目前使用较小的 DiT 骨干，未来可能需要扩展以处理更复杂的任务。
*   **视觉编码器冻结**：为了保证特征一致性，ViT 编码器目前是冻结的，可能限制了视觉感知的进一步微调潜力。
*   **数据依赖性**：虽然利用人类数据提升了泛化性，但对于缺乏对应人类演示数据的任务类型（如仿真中的关节操作任务），性能提升不明显，表明跨形态迁移仍受限于数据覆盖。

（完）

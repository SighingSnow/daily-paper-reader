---
title: "HippoCamp: Benchmarking Contextual Agents on Personal Computers"
title_zh: HippoCamp：个人计算机上的上下文感知智能体基准测试
authors: "Zhe Yang, Shulin Tian, Kairui Hu, Shuai Liu, Hoang-Nhat Nguyen, Yichi Zhang, Zujin Guo, Mengying Yu, Zinan Zhang, Jingkang Yang, Chen Change Loy, Ziwei Liu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01221v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于评估智能体在个人文件上执行搜索的基准；评估搜索和推理能力
tldr: "本文提出了HippoCamp基准，旨在评估个人电脑环境下的多模态文件管理智能体。该基准构建了基于真实用户画像的设备级文件系统（42.4GB数据），包含581个QA对及大量标注轨迹。实验显示最先进模型在用户画像构建上准确率仅48.3%，揭示了当前智能体在长周期检索和跨模态推理上的显著局限，为下一代个人AI助手提供了评估基础。"
selection_source: fresh_fetch
motivation: 现有智能体基准缺乏对个人电脑环境下以用户为中心的多模态文件管理能力的评估。
method: 构建了包含42.4GB真实文件和581个QA对的HippoCamp基准，模拟设备级文件系统以评估智能体的搜索、感知和推理能力。
result: "实验显示最先进模型在用户画像构建上准确率仅48.3%，多模态感知和证据定位是主要瓶颈。"
conclusion: HippoCamp揭示了当前智能体在现实用户环境中的关键局限，为开发下一代个人AI助手奠定了基础。
---

## 摘要
我们提出了 HippoCamp，这是一个旨在评估智能体多模态文件管理能力的新基准。与专注于通用场景下的网络交互、工具使用或软件自动化等任务的现有智能体基准不同，HippoCamp 在以用户为中心的环境中评估智能体，以建模个体用户画像并搜索海量个人文件进行上下文感知推理。我们的基准基于涵盖多种模态的真实世界画像，实例化了设备级文件系统，包含超过 2000 个真实文件，总计 42.4 GB 数据。在原始文件的基础上，我们构建了 581 个问答对，以评估智能体在搜索、证据感知和多步推理方面的能力。为了促进细粒度分析，我们提供了 46.1K 条密集标注的结构化轨迹，用于分步失败诊断。我们在 HippoCamp 上评估了广泛的最新多模态大语言模型（MLLM）和智能体方法。我们的综合实验揭示了显著的性能差距：即使是最先进的商业模型在用户画像任务中也仅达到 48.3% 的准确率，特别是在密集个人文件系统内的长程检索和跨模态推理方面表现不佳。此外，我们的分步失败诊断将多模态感知和证据定位确定为主要瓶颈。最终，HippoCamp 揭示了当前智能体在现实以用户为中心的环境中的关键局限性，并为开发下一代个人 AI 助手提供了坚实的基础。

## Abstract
We present HippoCamp, a new benchmark designed to evaluate agents' capabilities on multimodal file management. Unlike existing agent benchmarks that focus on tasks like web interaction, tool use, or software automation in generic settings, HippoCamp evaluates agents in user-centric environments to model individual user profiles and search massive personal files for context-aware reasoning. Our benchmark instantiates device-scale file systems over real-world profiles spanning diverse modalities, comprising 42.4 GB of data across over 2K real-world files. Building upon the raw files, we construct 581 QA pairs to assess agents' capabilities in search, evidence perception, and multi-step reasoning. To facilitate fine-grained analysis, we provide 46.1K densely annotated structured trajectories for step-wise failure diagnosis. We evaluate a wide range of state-of-the-art multimodal large language models (MLLMs) and agentic methods on HippoCamp. Our comprehensive experiments reveal a significant performance gap: even the most advanced commercial models achieve only 48.3% accuracy in user profiling, struggling particularly with long-horizon retrieval and cross-modal reasoning within dense personal file systems. Furthermore, our step-wise failure diagnosis identifies multimodal perception and evidence grounding as the primary bottlenecks. Ultimately, HippoCamp exposes the critical limitations of current agents in realistic, user-centric environments and provides a robust foundation for developing next-generation personal AI assistants.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的智能体基准测试（如WebShop、GAIA等）主要关注网络交互、工具使用或软件自动化等通用场景，缺乏对**个人电脑环境下以用户为中心的多模态文件管理能力**的评估。真实个人环境包含海量、异构、长期演化的多模态文件，智能体需要在此环境中进行搜索、感知和推理，以提供个性化服务，这构成了一个未被充分评估的关键挑战。
*   **整体含义**：HippoCamp旨在填补这一空白，通过构建一个模拟真实个人文件系统的基准，系统性地评估智能体在**搜索、多模态证据感知、跨文件跨时间推理**等方面的能力，从而揭示当前智能体在构建“个性化记忆”方面的关键局限，为下一代个人AI助手的发展提供诊断基础和评估标准。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
*   **核心思想**：构建一个**设备级、多模态、以用户为中心的文件系统基准**，通过两类互补任务（事实保留与用户画像）来全面评估智能体的个性化记忆能力。
*   **关键技术细节**：
    *   **数据集构建**：基于对100多名真实用户的访谈与文件收集，聚合形成三个典型用户画像（学生/内容创作者、法律从业者、金融分析师），每个画像包含一个完整的、异构的文件系统（总计42.4 GB，超2000个文件，涵盖文本、文档、图像、视频、音频5种模态）。
    *   **任务设计**：
        *   **事实保留**：要求智能体检索并推理出文件中蕴含的具体、可验证的事实信息（如“找到符合签证要求的照片”）。
        *   **用户画像**：要求智能体从分散、弱信号的多模态证据中聚合、推断出用户的偏好、行为模式、日程安排等高层特征（如“我通常如何管理压力”）。
    *   **标注流程**：采用人机协作流程，为每个QA问题生成**结构化轨迹**，包含：
        *   **最小支持文件集**：回答问题所需的最少文件列表。
        *   **细粒度证据定位**：将证据定位到文件内的具体位置（如PDF页码、视频时间戳、图像区域）。
        *   **分步推理轨迹**：将解决问题的过程分解为搜索、感知、推理等步骤，并关联到具体证据。
        *   **能力标签**：标注每个步骤所需的智能体能力（如语义搜索、文档理解、视频定位、验证等）。
    *   **原子单元**：为统一评估不同模态的证据定位粒度，定义了“原子单元”（如文本的token span、文档的页、图像的patch、视频的帧），用于细粒度诊断。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
*   **数据集/场景**：使用论文构建的 **HippoCamp基准**，包含三个用户画像的文件系统环境，共581个QA问题（521个事实保留，60个用户画像）。
*   **Benchmark**：HippoCamp本身即为benchmark，其评估框架包含：
    *   **答案质量评估**：使用LLM作为评判者，对模型生成的答案进行语义匹配评分（0-5分）和正确性判断。
    *   **证据检索质量评估**：计算模型检索到的文件与标注的最小支持文件集之间的F1、召回率、命中率。
    *   **能力分解评估**：基于标注的能力标签，分项评估智能体在搜索、感知、推理各阶段的表现。
*   **对比方法**：评估了三大类方法：
    *   **RAG方法**：Standard RAG、Self-RAG。
    *   **搜索智能体方法**：ReAct (基于Qwen3-30B-A3B和Gemini-2.5-flash)、Search-R1。
    *   **自主智能体系统**：Terminal Agent (基于Qwen3-VL-8B-Instruct、Gemini-2.5-flash、GPT-5.2)、ChatGPT Agent Mode。
    *   所有方法均在**严格隔离的画像环境**中运行，仅能访问对应画像的本地文件系统，禁止外部网络访问。

### 4. 资源与算力
*   **论文未明确说明**所使用的具体GPU型号、数量、训练时长等算力细节。
*   实验主要涉及对多种现成模型和智能体系统的**推理评估**，可能依赖云端API（如ChatGPT Agent Mode）或本地计算资源（如Terminal Agent在Docker环境中的运行），但具体硬件配置未披露。

### 5. 实验数量与充分性
*   **实验数量**：评估了**9种不同的方法**（涵盖不同架构和模型后端），并进行了多维度分析：
    *   主要结果分析（按画像和任务类型）。
    *   能力分解分析（搜索、感知、推理）。
    *   系统性失败模式分析（检索错配、证据回避、幻觉、实体误归因等）。
    *   难度与性能相关性分析。
*   **充分性与客观性**：
    *   **充分**：实验覆盖了从简单RAG到复杂自主智能体的主流范式，并提供了细粒度的诊断分析，能够揭示不同方法的瓶颈所在。
    *   **客观与公平**：设计了统一的评估框架，控制了预算、随机性，并采用LLM评判与人工审计结合的方式。然而，商业API（如ChatGPT Agent Mode）的内部非确定性可能影响结果稳定性，论文对此进行了说明。

### 6. 论文的主要结论与发现
*   **性能差距显著**：即使最先进的商业智能体，在用户画像任务上的准确率也仅达**48.3%**，在事实保留任务上为**62.8%**，远低于人类水平。
*   **瓶颈不在检索**：主要瓶颈在于**检索后的处理流程**，即多模态证据感知、证据定位、跨文件整合与验证。许多方法能检索到相关文件，但无法正确理解、整合并基于证据生成答案。
*   **任务难度差异**：用户画像任务比事实保留任务更难，因为它需要从弱信号、分散证据中进行长周期聚合和抽象推理。
*   **失败模式多样**：包括检索错配（检索到无关文件）、证据回避（给出通用建议而非基于证据）、证据幻觉（虚构文件路径）、实体误归因（将用户行为归因于宠物等）。
*   **成功模式**：ChatGPT Agent Mode通过**迭代式文件系统探索**（反复列出目录、读取文件、修正假设）取得了最佳表现，表明迭代探索和纠错能力至关重要。

### 7. 优点：方法或实验设计上有哪些亮点
*   **真实性与规模**：构建了基于真实用户画像、设备级规模（42.4GB）的多模态文件系统，高度模拟了个人电脑环境的复杂性和异构性。
*   **任务设计贴近需求**：任务（事实保留与用户画像）直接反映了个人数字助理的真实使用场景，超越了简单的检索或问答。
*   **标注精细且可诊断**：提供了46.1K条结构化轨迹和细粒度证据定位，支持对智能体失败原因进行分步、分能力的精准诊断，而非仅给出一个总分。
*   **评估全面**：不仅评估答案正确性，还评估证据检索质量和能力分解，揭示了“检索好但答案错”等深层问题。

### 8. 不足与局限
*   **数据集规模相对较小**：581个QA问题对于大规模评估可能仍显不足，可能限制结论的泛化性。
*   **依赖LLM评判**：答案质量评估依赖LLM作为评判者，尽管有人工审计，但仍可能引入评判偏差或对某些答案风格偏好。
*   **商业API评估受限**：对ChatGPT Agent Mode等商业产品的评估受其平台限制，无法完全控制其内部工具和流程，与本地Docker环境的对比不完全公平。
*   **未涉及训练或微调**：论文仅评估了现有模型的推理能力，未探讨如何针对此类任务进行训练或微调，未提供改进方案的具体实现。
*   **隐私与伦理考量**：尽管进行了匿名化处理，构建基于真实用户文件的基准仍涉及隐私敏感领域，其数据治理和伦理审查细节需持续关注。

（完）

---
title: Enhancing Policy Learning with World-Action Model
title_zh: 利用世界-动作模型增强策略学习
authors: "Yuci Han, Alper Yilmaz"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28955v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 用于策略学习的世界-动作模型
tldr: "本文针对传统世界模型忽略动作相关结构的问题，提出了世界-动作模型（WAM）。该模型通过引入逆向动力学目标，联合推理视觉观察与动作，从而学习对控制至关重要的表征。在CALVIN基准测试中，WAM显著提升了行为克隆的成功率，并在PPO微调后实现了92.8%的平均成功率，训练步数减少8.7倍，有效增强了策略学习效果。"
selection_source: fresh_fetch
motivation: 传统世界模型仅通过图像预测训练，难以捕捉对下游控制至关重要的动作相关结构。
method: 提出世界-动作模型（WAM），在DreamerV2中引入逆向动力学目标，通过预测潜在状态转移中的动作来学习包含动作相关结构的表征。
result: "在CALVIN基准测试中，WAM将行为克隆成功率提升至71.2%，经PPO微调后平均成功率达92.8%，且训练步数减少8.7倍。"
conclusion: WAM在不改变策略架构的前提下，显著提升了策略学习的性能与效率，证明了动作正则化世界模型的有效性。
---

## 摘要
本文提出了世界-动作模型（WAM），这是一种动作正则化的世界模型，能够对未来视觉观测和驱动状态转移的动作进行联合推理。与仅通过图像预测训练的传统世界模型不同，WAM 将逆动力学目标引入 DreamerV2，通过潜在状态转移预测动作，从而鼓励学习到的表征捕捉对下游控制至关重要的动作相关结构。我们在来自 CALVIN 基准的八个操作任务上评估了 WAM 对策略学习的增强效果。我们首先在世界模型潜在空间上通过行为克隆预训练扩散策略，然后在冻结的世界模型内利用基于模型的 PPO 对其进行优化。在不修改策略架构或训练流程的情况下，WAM 将 DreamerV2 和 DiWA 基线的平均行为克隆成功率从 59.4% 提升至 71.2%。经过 PPO 微调后，WAM 实现了 92.8% 的平均成功率，而基线为 79.8%，其中两个任务达到了 100%，且训练步数减少了 8.7 倍。

## Abstract
This paper presents the World-Action Model (WAM), an action-regularized world model that jointly reasons over future visual observations and the actions that drive state transitions. Unlike conventional world models trained solely via image prediction, WAM incorporates an inverse dynamics objective into DreamerV2 that predicts actions from latent state transitions, encouraging the learned representations to capture action-relevant structure critical for downstream control. We evaluate WAM on enhancing policy learning across eight manipulation tasks from the CALVIN benchmark. We first pretrain a diffusion policy via behavioral cloning on world model latents, then refine it with model-based PPO inside the frozen world model. Without modifying the policy architecture or training procedure, WAM improves average behavioral cloning success from 59.4% to 71.2% over DreamerV2 and DiWA baselines. After PPO fine-tuning, WAM achieves 92.8% average success versus 79.8% for the baseline, with two tasks reaching 100%, using 8.7x fewer training steps.

---

## 论文详细总结（自动生成）

# 论文总结：利用世界-动作模型增强策略学习

## 1. 论文的核心问题与整体含义
*   **研究背景**：世界模型通过在潜在空间中模拟环境动态，为机器人策略学习提供了样本高效的解决方案。然而，传统世界模型（如DreamerV2）仅通过图像重建任务进行训练，其潜在表征主要优化于像素预测，缺乏显式的约束来编码对下游控制至关重要的“动作相关结构”。
*   **核心问题**：传统世界模型在编码观测时，往往忽略了状态转移是由动作驱动的这一因果关系。这种不对称性导致潜在状态虽然能很好地预测未来画面，但可能丢失了对于策略决策（如机械臂精确操作）至关重要的细粒度控制信息。
*   **整体含义**：本文旨在通过引入动作预测作为辅助任务，强制世界模型的编码器捕捉环境对动作的响应机制，从而学习到更具控制价值的表征，进而提升下游模仿学习和强化学习策略的性能与数据效率。

## 2. 论文提出的方法论
*   **核心思想**：提出世界-动作模型，在DreamerV2架构基础上增加一个“逆向动力学头”。该头部通过预测连续状态转移之间的动作，对编码器进行正则化，使其生成的表征不仅包含视觉信息，还包含动作相关的因果结构。
*   **关键技术细节**：
    *   **架构设计**：WAM基于RSSM（循环状态空间模型），包含双流CNN编码器、RSSM核心和解码器。
    *   **逆向动力学头**：不同于直接使用RSSM的输出特征，WAM在编码器嵌入层面操作。具体而言，它将当前时刻和下一时刻的编码器嵌入（$e_t$ 和 $e_{t+1}$）拼接，通过一个三层MLP预测采取的动作 $\hat{a}_t$。
    *   **级联效应**：这是设计的核心。由于RSSM的特征 $f_t$ 已经包含了历史动作信息，若在此处预测动作会过于简单。通过在编码器层面施加动作预测损失，动作感知结构被迫融入后验 $z_t$，并通过KL散度传递至先验 $\hat{z}_t$，从而在想象轨迹生成时也能保持动作相关性。
*   **训练目标**：模型通过端到端优化以下损失函数：
    $$ L_{WAM} = \lambda_{KL} L_{KL} + \lambda_{img} L_{recon} + \lambda_{act} L_{action} $$
    其中，$L_{action}$ 采用L1损失衡量预测动作与真实动作的差异。
*   **策略学习流程**：
    1.  **预训练**：在CALVIN数据集上训练WAM。
    2.  **行为克隆（BC）**：冻结WAM，提取潜在特征训练扩散策略。
    3.  **在线微调**：在冻结的WAM潜在空间内，使用DPPO（基于扩散策略的PPO）进行强化学习微调，无需真实环境交互。

## 3. 实验设计
*   **数据集与场景**：使用 **CALVIN** 基准测试，这是一个包含7自由度Franka机械臂的桌面操作环境。实验使用了该数据集中的环境D，包含约6小时的遥操作数据（约50万次转移）。
*   **Benchmark任务**：选取了8个操作任务进行评估，包括开关抽屉、左右移动滑块、开关灯泡和开关LED。
*   **对比方法**：
    *   **世界模型质量对比**：基线为 **DreamerV2**，评估指标包括PSNR、SSIM、LPIPS和FVD。
    *   **策略学习对比**：基线为 **DiWA**（基于DreamerV2的扩散策略方法）。WAM与DiWA使用完全相同的策略架构和训练超参数，仅替换了提供特征的世界模型，确保对比公平。

## 4. 资源与算力
*   **文中未明确说明**具体的硬件配置（如GPU型号、数量）或物理训练时长。
*   仅提及训练批次大小为500，学习率为 $3 \times 10^{-4}$，以及WAM训练了230K梯度步数，相比基线的2M步数大幅减少。

## 5. 实验数量与充分性
*   **实验数量**：
    *   **世界模型生成质量**：在验证集上随机采样100个序列进行50步开环想象，定量对比4种指标。
    *   **策略学习**：在8个任务上进行行为克隆（BC）测试，每个任务评估29个保留初始配置；随后进行PPO微调实验，共运行800次迭代。
*   **充分性与客观性**：
    *   实验设计较为充分，涵盖了从表征质量（视频预测指标）到下游任务性能（成功率）的全方位评估。
    *   对比实验控制了变量（策略架构、训练流程一致），证明了性能提升确实来源于改进的世界模型表征。
    *   不仅报告了平均成功率，还展示了具体的任务表现和学习曲线，结果具有说服力。

## 6. 论文的主要结论与发现
*   **表征质量提升**：WAM在所有视频预测指标（PSNR, SSIM, LPIPS, FVD）上均优于DreamerV2，证明动作正则化有助于学习更准确的动态模型。
*   **策略性能显著提升**：
    *   在行为克隆阶段，WAM将平均成功率从DiWA的45.8%提升至61.7%。
    *   在PPO微调后，WAM达到92.8%的平均成功率，显著高于基线的79.8%，且有两个任务达到100%成功率。
*   **数据效率极高**：WAM仅需23万次梯度更新，是基线（200万次）的约1/8.7，却取得了更优的性能。

## 7. 优点
*   **方法简洁有效**：仅通过增加一个轻量级的逆向动力学头，未改变主体架构，即显著提升了性能。
*   **理论解释性强**：利用“级联效应”解释了如何将编码器层面的动作信息传递至先验分布，解决了潜在空间想象中动作信息缺失的问题。
*   **通用性好**：该方法作为一个插件式的改进，可无缝集成到现有的基于世界模型的策略学习流程中（如DiWA），无需修改策略网络结构。

## 8. 不足与局限
*   **实验环境单一**：仅在CALVIN仿真基准上进行了测试，未涉及真实机器人环境或其他类型的仿真环境，泛化性有待验证。
*   **超参数敏感性**：文中提到需要仔细调整损失系数（如 $\lambda_{act}=1000.0$）以平衡重建与动作预测，这可能增加了调参难度。
*   **依赖奖励模型重训练**：由于WAM改变了潜在空间结构，原有的奖励分类器无法直接使用，需要基于WAM特征重新训练奖励模型，这在迁移到新任务时可能增加额外开销。

（完）

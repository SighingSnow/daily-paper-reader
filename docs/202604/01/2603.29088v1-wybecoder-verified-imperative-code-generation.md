---
title: "WybeCoder: Verified Imperative Code Generation"
title_zh: WybeCoder：经验证的命令式代码生成
authors: "Fabian Gloeckle, Mantas Baksys, Darius Feher, Kunhao Zheng, Amaury Hayat, Sean B. Holden, Gabriel Synnaeve, Peter O'Hearn"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29088v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于验证代码生成的智能体代码验证框架
tldr: 针对软件验证滞后于代码生成的问题，本文提出WybeCoder框架，实现边生成边证明的开发模式。该框架结合自动验证条件生成、SMT求解器和Lean交互式证明，使代码、不变量和证明协同演化。实验显示其在复杂算法上表现优异，显著超越了之前的评估结果，为构建大规模验证命令式代码数据集开辟了新路径。
selection_source: fresh_fetch
motivation: 为了解决软件验证领域相对于代码生成和定理证明进展滞后的问题。
method: 提出WybeCoder智能体框架，结合验证条件生成、SMT求解器与Lean交互式证明，实现代码、不变量与证明的协同演化。
result: "该方法在Verina和Clever基准测试中分别解决了74%和62%的任务，显著超越以往工作并在复杂算法上表现出持续的性能提升。"
conclusion: WybeCoder成功实现了高效的验证式命令代码生成，为自动化构建大规模验证代码数据集铺平了道路。
---

## 摘要
大语言模型（LLMs）的最新进展推动了自动代码生成和形式化定理证明的发展，但软件验证领域尚未取得同样的进步。为了弥合这一差距，我们提出了 WybeCoder，这是一个智能体代码验证框架，能够实现“边生成边证明”的开发模式，使代码、不变式和证明得以协同演化。它基于一个近期提出的框架，该框架将自动验证条件生成和 SMT 求解器与 Lean 中的交互式证明相结合。为了进行系统性评估，我们将 Lean 中用于功能验证的两个基准测试 Verina 和 Clever 转换为等效的命令式代码规范。在堆排序等复杂算法上，我们通过扩展方法规模观察到了一致的性能提升，合成了数十个有效的不变式并解决了数十个子目标，生成了数百行经过验证的代码，克服了先前工作中报告的平台期。我们的最佳系统在适中的计算预算下解决了 74% 的 Verina 任务和 62% 的 Clever 任务，显著超越了先前的评估结果，为自动构建大规模验证命令式代码数据集铺平了道路。

## Abstract
Recent progress in large language models (LLMs) has advanced automatic code generation and formal theorem proving, yet software verification has not seen the same improvement. To address this gap, we propose WybeCoder, an agentic code verification framework that enables prove-as-you-generate development where code, invariants, and proofs co-evolve. It builds on a recent framework that combines automatic verification condition generation and SMT solvers with interactive proofs in Lean. To enable systematic evaluation, we translate two benchmarks for functional verification in Lean, Verina and Clever, to equivalent imperative code specifications. On complex algorithms such as Heapsort, we observe consistent performance improvements by scaling our approach, synthesizing dozens of valid invariants and dispatching of dozens of subgoals, resulting in hundreds of lines of verified code, overcoming plateaus reported in previous works. Our best system solves 74% of Verina tasks and 62% of Clever tasks at moderate compute budgets, significantly surpassing previous evaluations and paving a path to automated construction of large-scale datasets of verified imperative code.

---

## 论文详细总结（自动生成）

# WybeCoder：经验证的命令式代码生成 论文总结

## 1. 论文的核心问题与整体含义
*   **研究动机**：大语言模型（LLMs）在代码生成和形式化定理证明领域取得了显著进展（如达到竞赛级水平），但**软件验证领域的发展相对滞后**。现有工作要么专注于函数式代码的交互式验证（忽略了主流的命令式编程），要么专注于命令式代码的“自动-主动”验证（如Dafny，依赖SMT求解器，未能充分利用LLM作为定理证明器的潜力）。
*   **整体含义**：本文旨在弥合这一差距，提出一个能够**自动化生成并验证命令式代码**的框架，实现“边生成边证明”的开发模式，使代码、不变式和证明能够协同演化，从而推动可验证软件的规模化生成。

## 2. 论文提出的方法论
*   **核心思想**：提出WybeCoder智能体框架，采用**混合验证策略**，结合了“自动-主动”验证（自动生成验证条件并使用SMT求解器）与交互式定理证明（在Lean中手动证明）的优势。框架支持代码、不变式和证明的迭代协同生成与修正。
*   **关键技术细节**：
    *   **基础框架**：基于Loom/Velvet系统，这是一个嵌入在Lean中的命令式程序验证框架。它能将带有不变式注释的命令式代码转换为证明义务，并尝试用CVC5求解器自动解决，失败时可回退到Lean的手动证明。
    *   **智能体架构**：
        *   **顺序智能体**：作为基础构建块，交替进行代码生成和接收Lean编译器反馈，直至验证成功或预算耗尽。
        *   **子目标分解多智能体系统**：核心创新。将验证义务分解为独立的子目标，并行派遣多个证明智能体处理。若所有子目标成功，则重构完整证明；若某子目标不可证，则根据具体冲突驱动地修改方法实现。通过命名方案实现证明在实现修改间的传递，保留部分进度。
    *   **防止规范泄漏**：针对基准测试中存在的“规范泄漏”（规范本身包含可计算函数，使任务 trivial）和“函数泄漏”（在命令式任务中使用函数式实现作弊）问题，提出统一策略：允许函数式规范，但强制要求命令式实现，并通过LLM评估器检查实现的“命令式程度”（关键路径上仅允许常数时间操作）。

## 3. 实验设计
*   **数据集/场景**：将两个现有的Lean函数式验证基准测试 **Verina** 和 **Clever** 翻译为等效的命令式代码规范（分别称为Verina-Loom和Clever-Loom），用于评估命令式代码的合成与验证。
*   **Benchmark**：
    *   **Verina**：包含189个编程挑战，评估代码、规范和证明生成。
    *   **Clever**：包含161个问题，要求生成匹配隐藏真值的规范和可证明正确的实现。
    *   **定性评估**：手动审查了排序算法（选择排序、堆排序、快速排序等）的验证成功与失败案例，以分析系统极限。
*   **对比方法**：
    *   **基线**：DS Prover V2 7B、COPRA (Claude 3.7)。
    *   **本文方法**：顺序智能体、子目标分解多智能体。
    *   **模型对比**：GPT-5、Gemini 3 Pro、Claude 4.5 Opus、Claude 4.5 Sonnet。

## 4. 资源与算力
*   **算力说明**：论文**未明确说明**所使用的GPU型号、数量或训练时长。实验主要关注**推理计算预算**，以LLM API调用次数或“轮次×智能体数量”来衡量。例如，在Verina上，最佳系统（Claude 4.5 Opus，顺序智能体）使用32轮×16智能体的预算达到74.1%解决率。堆排序验证使用了数百个LLM调用（215个子智能体用于heapify等）。

## 5. 实验数量与充分性
*   **实验数量**：
    *   在两个基准测试上进行了全面的定量评估，报告了不同模型、不同智能体架构（顺序 vs. 子目标分解）、不同计算预算下的解决率。
    *   进行了多组对比分析：单智能体 vs. 多智能体、顺序计算 vs. 并行计算、不同模型的推理扩展性对比。
    *   包含消融实验：如评估防止规范泄漏措施的效果（无措施时GPT-5在Verina上达75.1%，加入“命令式程度”评估后降至51.9%，但实现正确）。
    *   进行了详细的定性分析，手动审查了复杂算法（如堆排序）的验证过程与失败原因。
*   **充分性与公平性**：实验设计较为充分，覆盖了多种配置和模型。通过翻译基准测试并控制泄漏问题，力求评估的客观与公平。但需注意，Clever-Loom的结果因翻译方法不同，与原始Clever结果不可直接比较。

## 6. 论文的主要结论与发现
*   **性能突破**：WybeCoder最佳系统在Verina上解决74%任务，在Clever-Loom上解决62%任务，显著超越先前报告的基线（如Verina上先前最佳为18%）。
*   **混合验证有效**：混合验证框架是软件验证智能体的有效组成部分，能在低推理预算下大幅提升评估分数。
*   **性能持续扩展**：性能随推理计算预算的增加而持续提升，未观察到平台期，能处理数百行代码和数十个子目标。
*   **复杂算法验证**：成功验证了堆排序等复杂算法，证明了系统处理需要深度洞察的算法的能力。
*   **架构依赖模型**：最优推理机制（顺序 vs. 子目标分解）取决于具体模型，如Gemini-3受益于多智能体，而Claude 4.5 Opus在顺序智能体上表现更好。

## 7. 优点
*   **方法创新**：首次将混合验证框架应用于LLM驱动的命令式代码生成，结合了SMT求解器的自动化与Lean交互式证明的灵活性。
*   **智能体设计**：子目标分解多智能体系统设计巧妙，能并行处理证明任务，并通过冲突驱动修改实现，提高了效率和可解释性。
*   **评估严谨**：识别并解决了基准测试中的规范泄漏问题，通过强制命令式实现确保了评估任务的难度和真实性。
*   **结果显著**：在基准测试上取得了大幅性能提升，并展示了在复杂、现实算法上的验证能力，超越了先前工作的局限。

## 8. 不足与局限
*   **框架依赖**：依赖实验性的、正在开发的Loom/Velvet软件包，结果可能随框架成熟而变化。
*   **语言限制**：生成的命令式代码针对Lean（托管内存），尚未覆盖C++或Rust等生产语言及其内存模型（如分离逻辑）。
*   **上游任务未自动化**：规范工程和子方法分解（如排序算法评估中）仍需人工参与，是当前流程的瓶颈。
*   **模型可复现性**：主要实验使用闭源前沿模型（通过API），开源模型表现较差，可能限制结果的完全可复现性。
*   **效率问题**：堆排序验证过程显示了一定程度的低效（使用了大量子智能体），生成的证明未优化长度或可维护性。

（完）

---
title: "SkillReducer: Optimizing LLM Agent Skills for Token Efficiency"
title_zh: SkillReducer：优化 LLM 智能体技能以提升 Token 效率
authors: "Yudong Gao, Zongjie Li, Yuanyuanyuan, Zimo Ji, Pingchuan Ma, Shuai Wang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29919v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于LLM的代码智能体技能优化框架
tldr: 针对LLM编码智能体中技能注入导致的高昂成本和注意力分散问题，本文提出了SkillReducer框架。该框架通过两阶段优化，分别压缩路由描述和重构技能主体，实现了显著的Token压缩。实验表明，该方法在提升功能质量的同时降低了成本，验证了“少即是多”的效果，并具有良好的泛化能力。
selection_source: fresh_fetch
motivation: 现有LLM智能体的技能指令集存在大量冗余和非可操作内容，导致高昂的Token成本和注意力稀释。
method: 提出SkillReducer两阶段框架，通过对抗性增量调试优化路由层，并利用分类驱动和渐进式披露重构技能主体。
result: "实验显示该方法实现了48%描述压缩和39%主体压缩，同时功能质量提升了2.8%。"
conclusion: 研究揭示了“少即是多”效应，去除非必要内容能有效减少上下文干扰，且该方法在多模型上具有高保留率。
---

## 摘要
基于 LLM 的代码智能体依赖于技能（skills），即扩展智能体能力的预封装指令集，然而注入上下文窗口的技能内容中的每一个 Token 都会带来金钱成本和注意力分散。为了解该问题的严重性，我们对 55,315 个公开可用的技能进行了大规模实证研究，发现了系统性的低效问题：26.4% 的技能完全缺乏路由描述，超过 60% 的主体内容不可操作，且参考文件每次调用可注入数万个 Token。受这些发现启发，我们提出了 SkillReducer，这是一个两阶段优化框架。第一阶段通过压缩冗长描述并利用对抗性增量调试生成缺失描述，来优化路由层。第二阶段通过分类法驱动的分类和渐进式披露重构技能主体，将可操作的核心规则与按需加载的补充内容分离，并通过保真度检查和自纠正反馈循环进行验证。在 600 个技能和 SkillsBench 基准上的评估表明，SkillReducer 实现了 48% 的描述压缩和 39% 的主体压缩，同时将功能质量提高了 2.8%，揭示了“少即是多”效应，即移除非必要内容可减少上下文窗口中的干扰。这些优势在来自四个系列的五个模型中表现出迁移性（平均保持率为 0.965），并可泛化至独立的智能体框架。

## Abstract
LLM-based coding agents rely on \emph{skills}, pre-packaged instruction sets that extend agent capabilities, yet every token of skill content injected into the context window incurs both monetary cost and attention dilution. To understand the severity of this problem, we conduct a large-scale empirical study of 55,315 publicly available skills and find systemic inefficiencies: 26.4\% lack routing descriptions entirely, over 60\% of body content is non-actionable, and reference files can inject tens of thousands of tokens per invocation. Motivated by these findings, we present \textsc{SkillReducer}, a two-stage optimization framework. Stage~1 optimizes the routing layer by compressing verbose descriptions and generating missing ones via adversarial delta debugging. Stage~2 restructures skill bodies through taxonomy-driven classification and progressive disclosure, separating actionable core rules from supplementary content loaded on demand, validated by faithfulness checks and a self-correcting feedback loop. Evaluated on 600 skills and the SkillsBench benchmark, \textsc{SkillReducer} achieves 48\% description compression and 39\% body compression while improving functional quality by 2.8\%, revealing a \emph{less-is-more} effect where removing non-essential content reduces distraction in the context window. These benefits transfer across five models from four families with a mean retention of 0.965, and generalize to an independent agent framework.

---

## 论文详细总结（自动生成）

# 论文总结：SkillReducer：优化 LLM 智能体技能以提升 Token 效率

## 1. 论文的核心问题与整体含义
*   **研究背景**：当前基于大语言模型（LLM）的代码智能体（如 Claude Code, Cursor）广泛依赖“技能”来扩展能力。技能是预封装的指令集，包含描述（用于路由）和主体（用于注入上下文）。
*   **核心问题**：技能内容的注入会消耗大量 Token，导致高昂的 API 成本和上下文窗口中的注意力分散。论文将这种现象定义为“技能膨胀”。
*   **实证发现**：通过对 55,315 个公开技能的大规模实证研究发现系统性低效问题：26.4% 的技能缺乏路由描述，超过 60% 的主体内容为非可操作信息（如背景、示例），且参考文件常包含大量无关 Token。
*   **整体含义**：现有的技能设计缺乏关注点分离，导致智能体在处理任务时加载了大量冗余信息。优化技能结构、移除非必要内容不仅能降低成本，还能减少对模型的干扰，提升任务表现。

## 2. 论文提出的方法论
论文提出了 **SkillReducer**，一个两阶段的技能优化框架，旨在通过“去膨胀”减少 Token 消耗并保持功能质量。

### 核心思想
借鉴软件工程中的“去膨胀”和“程序切片”思想，将技能内容分离为“核心规则”（必须加载）和“补充内容”（按需加载），实现结构感知的压缩。

### 关键技术细节
#### 第一阶段：路由层优化
*   **目标**：解决描述缺失或冗长导致的路由失败或 Token 浪费问题。
*   **技术流程**：
    1.  **语义分割**：将描述分解为语义单元。
    2.  **对抗性增量调试**：构建包含目标技能、干扰项和对抗性技能（LLM 生成的易混淆技能）的候选池。使用 DDMIN 算法寻找满足路由正确性的最小描述子集。
    3.  **真实环境验证**：将压缩后的描述部署到真实智能体环境中测试，若路由失败则通过“选择性恢复”机制回退部分内容。

#### 第二阶段：主体重构与渐进式披露
*   **目标**：分离核心规则与非必要内容，实现按需加载。
*   **技术流程**：
    1.  **分类法驱动分类**：利用 LLM 将技能主体内容分为五类：核心规则、背景、示例、模板、冗余。
    2.  **类型特定压缩**：核心规则精简保留，背景、示例、模板转为独立的参考模块，冗余内容丢弃。
    3.  **跨文件去重**：检测并移除主体与外部参考文件之间的重叠内容。
    4.  **质量门控与反馈循环**：
        *   **Gate 1 (保真度检查)**：验证压缩后的内容是否保留了所有关键操作概念。
        *   **Gate 2 (任务评估)**：生成测试任务，对比压缩前后技能的任务完成率。若性能下降，通过反馈循环将必要的非核心项提升回核心模块。

### 公式与算法逻辑
*   **优化目标**：最小化 Token 成本 $Cost(s') \ll Cost(s)$。
*   **约束条件**：
    1.  **路由等价性**：压缩后的技能 $s'$ 必须被相同的查询触发。
    2.  **功能保持性**：智能体使用 $s'$ 的任务表现不低于使用 $s$。
*   **反馈收敛性**：核心项集合 $I_{core}$ 在反馈循环中单调递增，保证算法在有限步内终止。

## 3. 实验设计
### 数据集与场景
*   **实证分析数据集**：包含 55,315 个公开技能，用于分析现状。
*   **评估数据集**：精选 600 个技能，来源包括 SkillHub（官方）、Community（社区）和 Wild（GitHub 爬取）。
*   **外部基准**：SkillsBench（包含 87 个任务，具有确定性验证器）。

### 对比方法
在相同 Token 预算下，对比了以下基线方法：
*   **LLMLingua**：基于困惑度的 Token 剪枝方法。
*   **LLM Direct**：直接使用 LLM 进行压缩。
*   **Truncation**：简单截断。
*   **Random Sentence**：随机移除句子。

### 评估条件
*   **Condition D**：无技能（下界）。
*   **Condition A**：原始技能（基线）。
*   **Condition C**：SkillReducer 压缩后的技能。

## 4. 资源与算力
*   **模型使用**：
    *   压缩与分类：DeepSeek-V3。
    *   模拟路由 Oracle：DeepSeek-R1。
    *   任务生成与评估：Qwen3.5（开源模型，防止信息泄露）。
    *   真实环境验证：Claude Code CLI。
*   **成本分析**：论文未提及具体的 GPU 型号或训练时长（因为这是推理优化任务而非模型训练）。文中指出压缩 600 个技能的 API 调用成本约为 $14-18 美元（使用低成本开源模型），属于一次性投入，可在后续几百次调用中摊销。

## 5. 实验数量与充分性
### 实验规模
*   **大规模分析**：分析了 55,315 个技能，揭示了普遍存在的问题。
*   **主实验**：在 600 个技能上进行了完整的压缩与评估，包含 3000 个任务。
*   **消融实验**：在 50 个技能上测试了不同组件（如分类、去重、反馈循环）的贡献。
*   **泛化实验**：在 5 个不同模型家族的模型和 1 个独立智能体框架上验证了迁移性。

### 充分性与客观性
*   **评估指标混合**：结合了确定性代码执行（52.3%）和 LLM 评判（47.7%），并验证了 LLM 评判与确定性验证器的一致性（Cohen's $\kappa=0.939$），减少了评估偏差。
*   **防止泄露**：压缩模型与评估模型分离，且使用独立的 API Key 和会话。
*   **实验设计合理**：涵盖了不同来源的技能，且通过“选择性恢复”机制保证了路由功能的 100% 保持率，实验设计较为严谨。

## 6. 论文的主要结论与发现
*   **压缩效果显著**：实现了 48% 的描述压缩率和 39% 的主体压缩率，端到端节省约 26.8% 的 Token。
*   **“少即是多”效应**：压缩后的技能在功能质量上反而提升了 2.8%，表明移除非必要内容减少了上下文干扰，提升了智能体表现。
*   **优于基线**：在相同 Token 预算下，SkillReducer 的任务保持率显著优于 LLMLingua 等方法。
*   **高保留率**：跨模型平均保留率为 0.965，跨框架保留率为 0.944，证明了方法的鲁棒性。
*   **失败原因分析**：仅 4.7% 的技能出现真正的压缩失败，主要原因是“示例即规范”（规则隐含在示例中），大部分性能下降归因于技能过时。

## 7. 优点
*   **结构感知的压缩策略**：不同于通用的文本压缩，该方法识别并区分了技能中的“核心规则”与“补充内容”，保留了关键信息。
*   **引入软件工程经典技术**：创造性地将 Delta Debugging 用于路由描述最小化，将 Program Slicing 思想用于内容分离。
*   **闭环质量保证**：设计了“模拟 Oracle + 真实验证”的双重路由检查和“任务评估 + 反馈循环”的功能检查，确保压缩不损害功能。
*   **实证驱动**：基于大规模实证研究设计方法，针对性强。

## 8. 不足与局限
*   **平台依赖性**：实验主要基于 Anthropic 提出的技能协议，未在 Cursor 或 Windsurf 等其他主流平台上验证。
*   **分类法的局限**：当技能作者将规则隐含在示例中时，分类器可能将其误判为非核心内容移除，导致功能受损。
*   **评估偏差风险**：Gate 2 既作为优化信号（反馈循环）又作为评估标准，可能存在过拟合风险（尽管 SkillsBench 外部验证缓解了此疑虑）。
*   **技能过时问题**：研究发现 10.7% 的技能已过时（即使不压缩也无法触发），这属于生态系统问题，非方法本身能解决。

（完）

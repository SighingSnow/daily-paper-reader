---
title: "AutoWorld: Scaling Multi-Agent Traffic Simulation with Self-Supervised World Models"
title_zh: AutoWorld：利用自监督世界模型扩展多智能体交通仿真
authors: "Mozhgan Pourkeshavatz, Tianran Liu, Nicholas Rhinehart"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28963v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 基于自监督世界模型的多智能体交通仿真
tldr: 针对现有多智能体交通仿真依赖昂贵标注数据的问题，本文提出AutoWorld框架，利用未标注LiDAR数据通过自监督世界模型进行学习。该方法构建从粗到细的预测场景上下文，结合级联DPP框架和运动感知潜在监督目标，实现了无需额外标注即可扩展仿真真实性，在WOSAC基准测试中排名第一。
selection_source: fresh_fetch
motivation: 现有交通仿真依赖昂贵的监督学习，难以利用海量未标注数据提升性能。
method: 提出AutoWorld框架，利用未标注LiDAR数据学习世界模型，构建从粗到细的预测场景上下文，并结合级联DPP框架与运动感知潜在监督目标。
result: AutoWorld在WOSAC基准排行榜上排名第一，且仿真性能随未标注数据的加入持续提升。
conclusion: 该方法证明了利用未标注数据扩展交通仿真真实性的可行性，无需额外标注成本。
---

## 摘要
多智能体交通仿真对于开发和测试自动驾驶系统至关重要。近期的数据驱动仿真器已取得了令人瞩目的成果，但严重依赖基于标注轨迹或语义标注的监督学习，这使得扩展其性能的成本高昂。与此同时，海量的未标注传感器数据可以被大规模收集，但现有的交通仿真框架基本未利用这些数据。这引出了一个关键问题：一种方法如何利用未标注数据来提升交通仿真性能？在这项工作中，我们提出了AutoWorld，这是一种交通仿真框架，它采用了从LiDAR数据的未标注占据表示中学习到的世界模型。给定世界模型样本，AutoWorld构建了一个由粗到细的预测性场景上下文，作为多智能体运动生成模型的输入。为了提升样本多样性，AutoWorld使用级联行列式点过程框架来指导世界模型和运动模型的采样过程。此外，我们设计了一个运动感知潜在监督目标，增强了AutoWorld对场景动态的表示能力。在WOSAC基准上的实验表明，根据主要的真实性元度量，AutoWorld在排行榜上排名第一。我们进一步表明，随着纳入未标注LiDAR数据，仿真性能持续提升，并通过消融实验研究了每个组件的有效性。我们的方法为在无需额外标注的情况下扩展交通仿真真实性铺平了道路。我们的项目页面包含额外的可视化和发布的代码。

## Abstract
Multi-agent traffic simulation is central to developing and testing autonomous driving systems. Recent data-driven simulators have achieved promising results, but rely heavily on supervised learning from labeled trajectories or semantic annotations, making it costly to scale their performance. Meanwhile, large amounts of unlabeled sensor data can be collected at scale but remain largely unused by existing traffic simulation frameworks. This raises a key question: How can a method harness unlabeled data to improve traffic simulation performance? In this work, we propose AutoWorld, a traffic simulation framework that employs a world model learned from unlabeled occupancy representations of LiDAR data. Given world model samples, AutoWorld constructs a coarse-to-fine predictive scene context as input to a multi-agent motion generation model. To promote sample diversity, AutoWorld uses a cascaded Determinantal Point Process framework to guide the sampling processes of both the world model and the motion model. Furthermore, we designed a motion-aware latent supervision objective that enhances AutoWorld's representation of scene dynamics. Experiments on the WOSAC benchmark show that AutoWorld ranks first on the leaderboard according to the primary Realism Meta Metric (RMM). We further show that simulation performance consistently improves with the inclusion of unlabeled LiDAR data, and study the efficacy of each component with ablations. Our method paves the way for scaling traffic simulation realism without additional labeling. Our project page contains additional visualizations and released code.

---

## 论文详细总结（自动生成）

# 论文总结：AutoWorld：利用自监督世界模型扩展多智能体交通仿真

## 1. 论文的核心问题与整体含义

*   **核心问题**：现有的多智能体交通仿真方法严重依赖昂贵的监督学习（需要大量标注轨迹或语义信息），难以利用海量易获取的未标注传感器数据，导致性能扩展成本高昂。
*   **研究动机**：如何有效利用未标注数据来提升交通仿真的真实性和扩展性？
*   **整体含义**：本文提出了一种新的范式，即通过自监督学习从未标注的LiDAR数据中学习世界模型，将其作为行为生成的基础，从而打破对标注数据的依赖，实现低成本、可扩展的高真实性交通仿真。

## 2. 论文提出的方法论

AutoWorld 是一个两阶段的交通仿真框架，核心在于利用未标注数据学习世界模型并指导运动生成。

*   **核心思想**：
    *   **阶段一（自监督世界模型）**：利用未标注的LiDAR序列，通过预测未来的占据网格来学习场景动态。
    *   **阶段二（条件运动生成）**：基于扩散模型，以世界模型预测的未来场景上下文为条件，生成多智能体轨迹。

*   **关键技术细节**：
    *   **运动感知潜在监督**：
        *   针对未标注占据网格中静态背景占主导、动态目标稀疏的问题，设计了加权损失函数。
        *   通过计算相邻帧占据变化生成运动权重图 $W_t$，对动态变化区域赋予更高权重，从而在无语义标签的情况下增强模型对动态元素的感知。
    *   **世界模型条件注入**：
        *   世界模型预测未来的潜在占据序列。
        *   构建“预测场景上下文”：通过多头交叉注意力（MHCA）池化机制，将预测的未来占据序列聚合为一个全局上下文向量。
        *   形成“从粗到细”的引导：在扩散模型生成轨迹时，同时注入全局上下文（粗粒度）和单步占据特征（细粒度）。
    *   **级联潜在多样性策略**：
        *   在推理阶段，利用行列式点过程（DPP）在两个层级促进多样性，无需额外训练。
        *   **层级一（场景级）**：在世界模型采样未来占据时，利用DPP引导，生成多样化的场景假设。
        *   **层级二（行为级）**：在运动生成阶段，利用质量加权的DPP引导采样，确保生成的轨迹既多样又符合物理约束（如无碰撞、可行驶）。

## 3. 实验设计

*   **数据集**：
    *   **Waymo Open Dataset (WOD)**：用于训练世界模型（使用未标注的LiDAR数据）。
    *   **Waymo Open Motion Dataset (WOMD)**：用于训练运动生成模块（使用标注的轨迹数据）。
*   **Benchmark**：
    *   **WOSAC (Waymo Open Sim Agents Challenge)**：这是评估交通仿真真实性的标准基准。
*   **评价指标**：
    *   **主指标**：真实性元度量，综合了运动学、交互和地图合规性。
    *   **辅助指标**：minADE（最小平均位移误差）。
*   **对比方法**：
    *   与当前SOTA方法对比，包括：InfGen, LLM2AD, SMART-tiny, UniMM, UniMotion, TrajTok, CAT-K, RLFTSim, SMART-R1, DecompGAIL 等。

## 4. 资源与算力

*   **算力说明**：文中未明确列出具体的GPU型号、数量或训练时长。
*   **相关支持**：致谢部分提到研究得到了加拿大数字研究联盟、NVIDIA学术资助计划和Google TPU研究云的支持，暗示使用了相关的高性能计算资源。

## 5. 实验数量与充分性

*   **实验数量**：
    *   **主实验**：在WOSAC排行榜上的性能对比（表1）。
    *   **消融实验**：
        *   世界模型有效性验证（表2）：对比有无世界模型、不同采样策略（IID vs DPP）及采样预算分配。
        *   运动感知监督有效性（表3）：对比语义监督、均匀监督与本文提出的运动感知监督。
        *   数据扩展性实验（图4）：验证随未标注数据量增加性能的变化。
        *   预测场景上下文有效性（表5）：验证从粗到细引导的作用。
    *   **定性分析**：可视化展示了多模态行为覆盖能力。
*   **充分性与公平性**：
    *   实验设计非常充分，涵盖了模块有效性、数据扩展性、核心组件贡献度等多个维度。
    *   对比实验基于公开的标准基准（WOSAC），对比方法均为当前最先进模型，评价客观公平。

## 6. 论文的主要结论与发现

*   **性能表现**：AutoWorld 在 WOSAC 排行榜上排名第一（RMM 0.7865），证明了其优越的真实性。
*   **数据扩展性**：仿真性能随着未标注 LiDAR 数据量的增加而持续提升，验证了利用未标注数据扩展仿真性能的可行性。
*   **模块有效性**：
    *   运动感知监督能有效提升动态区域的预测精度，缩小与语义监督的差距。
    *   级联 DPP 采样策略能有效利用世界模型的潜在空间，在保证真实性的前提下显著提升多样性。
    *   世界模型提供的预测场景上下文对提升交互质量至关重要。

## 7. 优点

*   **创新的数据利用范式**：首次提出利用未标注 LiDAR 数据通过世界模型来增强交通仿真，降低了对昂贵标注数据的依赖。
*   **巧妙的设计**：
    *   “运动感知潜在监督”解决了非语义占据网格中动态元素被淹没的问题。
    *   “级联 DPP”策略在推理阶段优雅地解决了多模态生成中多样性与真实性的平衡问题。
*   **综合性能强**：在运动学合理性、交互质量和地图合规性三个维度上取得了最佳平衡，而非单纯优化某一指标。

## 8. 不足与局限

*   **算力细节缺失**：论文未公开具体的计算资源消耗和训练时间，可能影响复现的难易度评估。
*   **数据模态限制**：方法依赖于 LiDAR 数据构建占据网格，对于仅有视觉（摄像头）数据的场景可能不直接适用。
*   **复杂路径覆盖**：定性分析显示，对于部分复杂的拓扑路径（如长距离变道），模型的多模态覆盖仍有提升空间。
*   **两阶段训练复杂度**：框架包含世界模型预训练和运动模型训练两个阶段，相比端到端方法流程较长。

（完）

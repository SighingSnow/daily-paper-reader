---
title: "HCLSM: Hierarchical Causal Latent State Machines for Object-Centric World Modeling"
title_zh: HCLSM：用于以物体为中心的世界建模的分层因果潜在状态机
authors: "Jaber Jaber, Osama Jaber"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29090v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 用于以物体为中心的视频预测的世界模型架构
tldr: 针对现有世界模型存在对象纠缠、忽略因果结构及单一时间尺度的问题，本文提出HCLSM架构。该模型结合对象中心分解、分层时间动态引擎与因果结构学习，通过两阶段训练协议优化。在PushT基准测试中实现了高精度的下一状态预测，并展示了有效的空间分解与事件边界学习能力，同时通过定制Triton内核大幅提升了计算效率。
selection_source: fresh_fetch
motivation: 现有世界模型受限于扁平潜表示，导致对象纠缠、忽略因果结构且缺乏多尺度时间动态建模能力。
method: 提出HCLSM架构，结合Slot Attention进行对象中心分解，利用三级分层引擎处理多尺度动态，并通过图神经网络学习因果结构。
result: 在PushT基准测试中，模型实现了0.008的下一状态预测MSE，展现出良好的空间分解效果，且定制Triton内核实现了38倍加速。
conclusion: HCLSM通过分层因果潜状态机有效解决了世界模型中的对象解耦与多尺度动态建模难题，显著提升了预测精度与计算效率。
---

## 摘要
从视频中预测未来状态的世界模型仍然受限于扁平的潜在表示，这些表示纠缠了物体、忽略了因果结构，并将时间动态压缩为单一尺度。我们提出了 HCLSM，这是一种基于三个相互关联的原则运行的世界模型架构：通过带有空间广播解码的槽注意力实现以物体为中心的分解；通过结合了用于连续物理的选择性状态空间模型、用于离散事件的稀疏 Transformer 和用于抽象目标的压缩 Transformer 的三级引擎实现分层时间动态；以及通过图神经网络交互模式进行因果结构学习。HCLSM 引入了两阶段训练协议，其中空间重建在动态预测开始之前迫使槽特化。我们在 Open X-Embodiment 数据集的 PushT 机器人操作基准上训练了一个 6800 万参数的模型，实现了 0.008 的 MSE 下一状态预测损失，并涌现出空间分解（SBD 损失：0.0075）和学习到的事件边界。用于 SSM 扫描的定制 Triton 内核相比顺序 PyTorch 实现了 38 倍的加速。完整系统包含 8,478 行 Python 代码，跨越 51 个模块，并配有 171 个单元测试。代码：https://github.com/rightnow-ai/hclsm

## Abstract
World models that predict future states from video remain limited by flat latent representations that entangle objects, ignore causal structure, and collapse temporal dynamics into a single scale. We present HCLSM, a world model architecture that operates on three interconnected principles: object-centric decomposition via slot attention with spatial broadcast decoding, hierarchical temporal dynamics through a three-level engine combining selective state space models for continuous physics, sparse transformers for discrete events, and compressed transformers for abstract goals, and causal structure learning through graph neural network interaction patterns. HCLSM introduces a two-stage training protocol where spatial reconstruction forces slot specialization before dynamics prediction begins. We train a 68M-parameter model on the PushT robotic manipulation benchmark from the Open X-Embodiment dataset, achieving 0.008 MSE next-state prediction loss with emerging spatial decomposition (SBD loss: 0.0075) and learned event boundaries. A custom Triton kernel for the SSM scan delivers 38x speedup over sequential PyTorch. The full system spans 8,478 lines of Python across 51 modules with 171 unit tests. Code: https://github.com/rightnow-ai/hclsm

---

## 论文详细总结（自动生成）

# 论文总结：HCLSM：用于以物体为中心的世界建模的分层因果潜在状态机

## 1. 论文的核心问题与整体含义
*   **研究背景**：当前的视频世界模型（如V-JEPA、DreamerV3）主要依赖扁平的潜在表示，存在三大局限：将不同物体纠缠在一起无法分离、忽略物体间的因果结构、将不同时间尺度的动态压缩在单一尺度下处理。
*   **核心问题**：如何构建一个能够像人类一样感知世界的世界模型？即能够分离独立物体、处理多尺度时间动态（连续物理运动与离散事件）并推理因果关系。
*   **整体含义**：本文提出HCLSM架构，旨在通过引入结构化的归纳偏置（物体中心、分层时间、因果图），解决现有模型缺乏结构化知识的问题，使其具备更强的规划与反事实推理能力。

## 2. 论文提出的方法论
*   **核心思想**：提出“结构先于预测”的原则，通过两阶段训练协议，先迫使模型学会物体分解，再学习动态预测，避免模型走捷径使用分布式编码。
*   **架构设计（五层结构）**：
    1.  **感知层**：使用ViT编码器提取图像块特征。
    2.  **物体分解层**：利用Slot Attention机制将特征分解为多个槽位，结合空间广播解码器（SBD）重建冻结的ViT特征（非像素），迫使每个槽位专精于特定空间区域。
    3.  **分层动态层（三级引擎）**：
        *   **Level 0**：选择性状态空间模型（SSM，类似Mamba），处理帧级别的连续物理动态。
        *   **Level 1**：稀疏Transformer，仅在检测到事件边界时触发，处理离散事件。
        *   **Level 2**：目标压缩Transformer，处理抽象的长程目标规划。
    4.  **因果结构层**：通过图神经网络（GNN）学习物体间的交互权重，并引入NOTEARS约束以尝试学习有向无环图（DAG）。
    5.  **持续记忆层**：结合Hopfield网络与EWC（弹性权重巩固）。
*   **训练协议**：
    *   **Stage 1（前40%步数）**：仅启用空间重建损失（SBD Loss），强制槽位竞争空间所有权，实现物体解耦。
    *   **Stage 2（后60%步数）**：加入动态预测损失（JEPA Loss），在已解耦的物体上学习时间动态。
*   **工程优化**：开发了定制的Triton内核用于SSM扫描算子，实现了并行计算加速。

## 3. 实验设计
*   **数据集**：Open X-Embodiment数据集中的PushT机器人操作任务（206个片段，25,650帧）。
*   **Benchmark**：下一状态预测MSE、空间分解质量（SBD Loss）、槽位多样性损失。
*   **对比方法**：
    *   主要进行了消融实验，对比了“无SBD模块”与“两阶段训练（HCLSM完整版）”的效果。
    *   在理论层面（Table 1）与V-JEPA 2、DreamerV3、SlotFormer等现有模型进行了特性对比（物体中心、分层、因果、SSM等维度的定性比较），但未提供这些基线模型在PushT上的复现数据。

## 4. 资源与算力
*   **硬件环境**：NVIDIA H100 80GB GPU。
*   **模型规模**：HCLSM Small（68M参数）。
*   **训练时长**：50K步，单次运行约6小时。
*   **其他配置**：Batch size为4，使用bfloat16混合精度训练。

## 5. 实验数量与充分性
*   **实验数量**：相对较少。主要定量结果基于PushT单一任务。
*   **消融实验**：仅展示了有无空间广播解码器（SBD）的对比，验证了两阶段训练的必要性。
*   **充分性与客观性评价**：
    *   **不足**：实验覆盖面较窄，未在多物体复杂场景或大规模数据集上验证。
    *   **稳定性问题**：实验中提到因数值溢出问题，4次运行中仅有2次成功存活，且Base（262M）和Large（3B）版本模型均因梯度爆炸未能成功训练，这限制了结论的普适性。
    *   **因果验证缺失**：虽然架构包含因果学习模块，但实验部分承认显式的因果邻接矩阵学习失败（权重塌缩为零），未进行真正的因果推理验证。

## 6. 论文的主要结论与发现
*   **两阶段训练的有效性**：如果不先进行空间重建训练，模型会倾向于使用所有槽位进行分布式编码，导致预测Loss更低但丧失物体分解能力；两阶段训练成功实现了空间分解。
*   **性能表现**：在PushT任务上实现了0.008的下一状态预测MSE，并能自动学习到事件边界（如机器人与物体接触瞬间）。
*   **计算效率**：定制的Triton SSM内核相比PyTorch顺序实现实现了38倍的加速，显著消除了计算瓶颈。

## 7. 优点
*   **架构创新**：首次尝试将物体中心分解、分层时间动态（SSM+Transformer）和因果结构学习统一在单一可微架构中。
*   **训练策略洞察**：深刻指出了“预测任务主导导致分解失败”的问题，并提出有效的两阶段训练解决方案。
*   **工程落地**：提供了完整的开源代码（8478行代码，171个单元测试）及高效的算子优化，具有很高的复现价值。

## 8. 不足与局限
*   **槽位管理缺陷**：模型无法自动“杀死”无用槽位，导致32个槽位全部存活，单一物体被拆分到约10个槽位中，未能实现理想的“一物体一槽位”。
*   **因果学习失效**：显式的因果图学习模块在训练中权重塌缩，未能学到有效的因果边，仅依靠GNN隐式交互，未验证是否符合真实因果逻辑。
*   **模型稳定性差**：对随机种子敏感，大模型（Base/Large）无法训练，bfloat16精度下频繁出现NaN梯度，表明模型在数值稳定性上存在严重问题。
*   **应用限制**：仅在简单的PushT任务（3个物体）上验证，未涉及多物体、复杂交互的真实场景。

（完）

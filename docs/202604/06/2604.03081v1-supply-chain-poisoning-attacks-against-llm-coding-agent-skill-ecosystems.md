---
title: Supply-Chain Poisoning Attacks Against LLM Coding Agent Skill Ecosystems
title_zh: 针对 LLM 编码智能体技能生态系统的供应链投毒攻击
authors: "Yubin Qu, Yi Liu, Tongcheng Geng, Gelei Deng, Yuekang Li, Leo Yu Zhang, Ying Zhang, Lei Ma"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03081v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM代码智能体技能生态系统的安全分析
tldr: "本文揭示了LLM编码代理技能生态系统的供应链安全风险。研究者提出DDIPE攻击方法，将恶意载荷隐藏于技能文档的代码示例中，利用代理复用示例的特性实现无感攻击。实验显示该方法在强防御下仍有最高33.5%的绕过率，成功暴露了现有安全审查的漏洞，并推动了相关平台的修复。"
selection_source: fresh_fetch
motivation: 针对LLM编码代理第三方技能缺乏安全审查的现状，研究旨在探索通过供应链投毒直接劫持代理操作空间的攻击可行性。
method: 提出文档驱动的隐式载荷执行（DDIPE）方法，将恶意逻辑嵌入技能文档的代码示例中，利用代理复用示例的特性实现无显式指令的攻击。
result: "实验表明，DDIPE在强防御下实现了11.6%至33.5%的绕过率，显著高于显式指令攻击，且部分恶意技能成功逃避了静态检测与安全对齐。"
conclusion: 研究证实了LLM代理技能生态系统的供应链安全漏洞，通过负责任披露促成了多个漏洞的确认与修复。
---

## 摘要
基于 LLM 的编码智能体通过开放市场分发的第三方智能体技能扩展其能力，而这些市场无需强制安全审查。与传统软件包不同，这些技能作为拥有系统级权限的操作指令执行，因此单个恶意技能即可攻陷主机。尽管存在现有的安全防护措施，先前的工作尚未研究供应链攻击是否能直接劫持智能体的动作空间，例如文件写入、Shell 命令和网络请求。我们提出了文档驱动的隐式载荷执行（DDIPE），该方法将恶意逻辑嵌入技能文档内的代码示例和配置模板中。由于智能体在正常任务期间会复用这些示例，载荷无需显式提示即可执行。利用 LLM 驱动的流水线，我们从涵盖 15 个 MITRE ATTACK 类别的 81 个种子中生成了 1,070 个对抗性技能。在四个框架和五个模型中，DDIPE 实现了 11.6% 至 33.5% 的绕过率，而显式指令攻击在强防御下的绕过率为 0%。静态分析检测出了大多数情况，但仍有 2.5% 同时规避了检测和对齐。负责任披露促成了四个漏洞的确认和两个修复。

## Abstract
LLM-based coding agents extend their capabilities via third-party agent skills distributed through open marketplaces without mandatory security review. Unlike traditional packages, these skills are executed as operational directives with system-level privileges, so a single malicious skill can compromise the host. Prior work has not examined whether supply-chain attacks can directly hijack an agent's action space, such as file writes, shell commands, and network requests, despite existing safeguards. We introduce Document-Driven Implicit Payload Execution (DDIPE), which embeds malicious logic in code examples and configuration templates within skill documentation. Because agents reuse these examples during normal tasks, the payload executes without explicit prompts. Using an LLM-driven pipeline, we generate 1,070 adversarial skills from 81 seeds across 15 MITRE ATTACK categories. Across four frameworks and five models, DDIPE achieves 11.6% to 33.5% bypass rates, while explicit instruction attacks achieve 0% under strong defenses. Static analysis detects most cases, but 2.5% evade both detection and alignment. Responsible disclosure led to four confirmed vulnerabilities and two fixes.

---

## 论文详细总结（自动生成）

# 论文总结：针对 LLM 编码智能体技能生态系统的供应链投毒攻击

## 1. 论文的核心问题与整体含义
*   **研究背景**：当前基于大语言模型（LLM）的编码智能体广泛依赖第三方“技能”来扩展能力，这些技能通常来自开放市场且缺乏强制性的安全审查。与传统软件包不同，这些技能作为操作指令执行，拥有系统级权限（如文件读写、Shell 命令、网络请求），一旦被污染可能直接攻陷主机。
*   **核心问题**：现有的安全研究多关注文本生成层面的污染或工具选择偏差，尚未深入探讨供应链攻击是否能绕过现有的模型安全对齐和框架架构防御，直接劫持智能体的“动作空间”。
*   **整体含义**：本研究揭示了 LLM 智能体技能生态系统中存在的深层供应链安全隐患，证明了攻击者可以通过伪装的恶意技能，在无显式指令的情况下诱导智能体执行恶意操作，这对智能体生态的安全性提出了严峻挑战。

## 2. 论文提出的方法论
*   **核心思想**：提出了 **文档驱动的隐式载荷执行（DDIPE）**。该方法利用编码智能体倾向于将技能文档中的代码示例视为“可信参考实现”的特性，将恶意逻辑嵌入到文档的代码示例或配置模板中。智能体在执行正常任务时会复用这些示例，从而在无显式恶意提示的情况下触发载荷执行。
*   **关键技术细节**：
    *   **载荷嵌入策略**：
        *   **代码示例投毒**：在 Markdown 代码块中穿插恶意代码。
        *   **配置模板投毒**：在 YAML/JSON 等部署模板中植入后门。
    *   **伪装技术**：包括功能合规伪装（将数据窃取伪装成遥测）、静默异常抑制（使用 try/except 隐藏错误）和虚假权威暗示（使用看似内部的域名）。
*   **自动化生成流水线**：
    *   基于 **种子-变异-验证** 的循环流程。
    *   **攻击分类体系**：基于 MITRE ATT&CK 构建了包含 15 个类别的攻击分类（如反向 Shell、供应链投毒、凭证窃取等）。
    *   **生成过程**：从 81 个专家构建的种子出发，利用 LLM（Claude Opus 4.6）进行种子扩展和伪装变异，最终生成 1,070 个对抗性技能样本。

## 3. 实验设计
*   **数据集**：通过自动化流水线生成的 **1,070 个对抗性技能样本**，覆盖 15 个 MITRE ATT&CK 攻击类别。
*   **实验场景与对象**：
    *   **智能体框架**：Claude Code、OpenHands、Codex、Gemini CLI（代表了不同的防御架构，如多层审查、仅模型对齐、沙箱隔离等）。
    *   **底层模型**：Claude Sonnet 4.6、GLM-4.7、MiniMax-M2.5、GPT-5.4、Gemini 2.5 Pro。
    *   共构建了 **8 种智能体-模型组合配置** 进行测试。
*   **Benchmark 与对比方法**：
    *   **基线方法**：显式指令注入，即直接在文本中包含恶意指令。
    *   **评估指标**：绕过率（BR, Bypass Rate）和直接执行率（DER, Direct Execution Rate）。
    *   **评估方式**：采用 LLM-as-a-Judge 自动评估结合人工审查（10% 样本，Cohen’s κ = 0.88）。

## 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号、数量或训练时长。实验主要涉及通过 API 调用不同的 LLM 服务（如 OpenRouter、各模型官方 API）进行推理和生成，属于黑盒测试场景，不涉及本地模型训练。

## 5. 实验数量与充分性
*   **实验规模**：对 8 种配置分别测试了全部 1,070 个样本，实验量较大。
*   **充分性与公平性**：
    *   **覆盖面广**：涵盖了不同防御强度的框架和不同对齐强度的模型，能够反映真实生态的多样性。
    *   **对比客观**：设置了显式指令注入作为基线，且在相同条件下对比了有无架构防御的差异（如 Claude Code vs OpenHands），控制变量较为严谨。
    *   **验证充分**：不仅进行了自动化评估，还进行了人工抽样验证，并通过负责任披露在真实生产环境中验证了漏洞的有效性。

## 6. 论文的主要结论与发现
*   **攻击有效性显著**：DDIPE 在所有测试配置中均实现了攻击成功，直接执行率（DER）在 2.3% 至 27.1% 之间，而显式指令攻击在强防御下成功率为 0%。
*   **防御层的非对称交互**：模型对齐与框架架构防御之间存在非对称关系。强对齐模型（如 Sonnet 4.6）对架构防御依赖较低，而弱对齐模型（如 GLM-4.7）在移除架构防御后攻击成功率激增 11.3 倍。
*   **跨模型防御多样性有效**：不同模型对攻击的识别存在差异，仅有 1.6% 的载荷能同时绕过所有三个测试模型。这表明部署异构模型作为安全屏障是有效的防御策略。
*   **真实漏洞确认**：通过负责任披露，确认了 4 个生产环境中的安全漏洞，并促成了 2 个修复。

## 7. 优点
*   **揭示了新攻击面**：首次深入研究了针对智能体“动作空间”的供应链攻击，超越了传统的文本污染范畴。
*   **方法隐蔽性强**：DDIPE 利用智能体复用代码示例的特性，无需显式指令即可触发，难以被常规安全审查发现。
*   **自动化程度高**：提出的生成流水线能够从少量种子大规模生成多样化的攻击样本，覆盖了 MITRE ATT&CK 的多个战术阶段。
*   **实证价值高**：实验不仅停留在理论层面，还通过真实披露验证了漏洞的存在，推动了厂商修复。

## 8. 不足与局限
*   **实验覆盖限制**：未覆盖 Llama 生态或 Cursor 等其他流行的编码工具，且假设恶意技能已被检索和加载，未评估检索阶段的概率。
*   **部分配置错误率高**：部分测试配置（如 OpenHands + MiniMax）出现了较高的运行错误率（>60%），可能影响统计结果的稳健性。
*   **检测手段单一**：针对静态分析的防御评估仅使用了 SkillScan 工具，未涉及动态沙箱或基于 LLM 的审计方法。
*   **生成模型偏差**：攻击样本由 Claude Opus 生成，可能存在针对同系列模型的过拟合风险，尽管实验结果显示 Sonnet 表现出了较好的防御能力。

（完）

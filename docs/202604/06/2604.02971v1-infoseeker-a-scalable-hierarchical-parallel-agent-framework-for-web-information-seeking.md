---
title: "InfoSeeker: A Scalable Hierarchical Parallel Agent Framework for Web Information Seeking"
title_zh: InfoSeeker：一种用于网络信息检索的可扩展分层并行智能体框架
authors: "Ka Yiu Lee, Yuxuan Huang, Zhiyuan He, Huichi Zhou, Weilin Luo, Kun Shao, Meng Fang, Jun Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02971v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于网络信息搜索的分层智能体框架
tldr: 现有智能体搜索系统在大规模信息综合中面临上下文饱和与高延迟问题。本文提出InfoSeeker分层并行框架，通过管理者层隔离上下文、工作者层并行执行来提升效率。实验显示该方法实现了3-5倍加速，并在相关基准测试中取得了优异效果。
selection_source: fresh_fetch
motivation: 现有智能体搜索系统在大规模信息综合场景中面临上下文饱和、错误传播和高延迟的挑战。
method: 提出InfoSeeker分层并行框架，通过管理者层隔离上下文防止错误传播，利用工作者层并行加速任务执行。
result: "实验表明该方法实现了3-5倍的速度提升，并在WideSearch-en和BrowseComp-zh基准上分别取得了8.4%的成功率和52.9%的准确率。"
conclusion: InfoSeeker有效解决了数据密集型场景下的性能瓶颈，实现了高效且可扩展的网络信息搜索。
---

## 摘要
近期的智能体搜索系统通过强调深度多步推理取得了显著进展。然而，这种关注往往忽视了大规模信息综合的挑战，即智能体必须聚合来自多个来源的大量异构证据。因此，大多数现有的大语言模型智能体系统在数据密集型场景下面临严重局限，包括上下文饱和、级联错误传播以及高昂的端到端延迟。为了应对这些挑战，我们提出了 InfoSeeker，这是一个基于近可分解性原则的分层框架，包含一个战略性的 Host、多个 Manager 和并行的 Worker。通过在 Manager 层利用聚合与反思机制，我们的框架实施了严格的上下文隔离，以防止饱和和错误传播。同时，Worker 层的并行性加快了整体任务执行速度，缓解了显著的延迟问题。我们在两个互补基准上的评估展示了效率（3-5倍加速）和有效性，在 WideSearch-en 上实现了 8.4% 的成功率，在 BrowseComp-zh 上实现了 52.9% 的准确率。代码已发布于 https://github.com/agent-on-the-fly/InfoSeeker

## Abstract
Recent agentic search systems have made substantial progress by emphasising deep, multi-step reasoning. However, this focus often overlooks the challenges of wide-scale information synthesis, where agents must aggregate large volumes of heterogeneous evidence across many sources. As a result, most existing large language model agent systems face severe limitations in data-intensive settings, including context saturation, cascading error propagation, and high end-to-end latency. To address these challenges, we present \framework, a hierarchical framework based on principle of near-decomposability, containing a strategic \textit{Host}, multiple \textit{Managers} and parallel \textit{Workers}. By leveraging aggregation and reflection mechanisms at the Manager layer, our framework enforces strict context isolation to prevent saturation and error propagation. Simultaneously, the parallelism in worker layer accelerates the speed of overall task execution, mitigating the significant latency. Our evaluation on two complementary benchmarks demonstrates both efficiency ($ 3-5 \times$ speed-up) and effectiveness, achieving a $8.4\%$ success rate on WideSearch-en and $52.9\%$ accuracy on BrowseComp-zh. The code is released at https://github.com/agent-on-the-fly/InfoSeeker

---

## 论文详细总结（自动生成）

# 论文总结：InfoSeeker

## 1. 论文的核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）的发展，网络搜索正从简单的检索转向自主智能体搜索。现有的系统（如 ReAct 风格）主要关注“深度”多步推理，但在处理需要聚合大量异构信息的“广度”任务时表现不佳。
*   **核心问题**：在数据密集型的大规模信息综合场景中，现有系统面临三大瓶颈：
    1.  **上下文饱和**：长程任务导致上下文窗口溢出。
    2.  **级联错误传播**：顺序执行中早期错误会累积放大。
    3.  **高延迟**：串行执行导致端到端响应时间过长。
*   **整体含义**：单纯扩大上下文窗口或模型规模无法根本解决问题，需要一种可扩展的系统架构来解耦推理深度与执行宽度，以实现高效、准确的大规模信息检索。

## 2. 论文提出的方法论
*   **核心思想**：基于“近可分解性”原则，提出 **InfoSeeker** 分层并行框架，将系统分为三层：战略层、管理层和执行层，通过上下文隔离和并行执行解决上述瓶颈。
*   **架构设计**：
    *   **Host Agent（战略层）**：负责高层规划。维护压缩的全局状态，生成高层指令 $q_t$，并选择对应的 Manager。Host 仅接收 Manager 的摘要反馈 $y_t$，不接触底层工具细节，从而限制上下文增长。
    *   **Manager Agents（管理层）**：负责领域特定的任务分解与聚合。将高层指令 $q_t$ 分解为 $P_t$ 个可并行的子任务 $\{q_t^k\}$，分发给 Workers 执行，并负责验证、反思和聚合结果。
    *   **Worker Agents（执行层）**：负责原子工具调用。通过模型上下文协议（MCP）并行执行子任务，保留完整执行轨迹在本地，仅返回最终结果给 Manager。
*   **并行与调度机制**：
    *   采用 MapReduce 模式：Map 阶段分解任务，Worker 层并行执行，Reduce 阶段聚合结果。
    *   理论上，并行执行时间 $T_{par} = \max(\Delta(q_t^k))$，远小于串行时间 $T_{seq} = \sum \Delta(q_t^k)$。
*   **可扩展性**：各层解耦，新增 Manager 或 Worker 无需修改 Host 逻辑，支持模块化扩展。

## 3. 实验设计
*   **数据集**：
    *   **WideSearch-en**：侧重“广度”信息综合。要求智能体从大量网页中提取实体并填充完整表格，评估指标包括成功率、行级 F1 和项级 F1。
    *   **BrowseComp-zh**：侧重“深度”推理与中文环境。包含 289 个专家构建的问题，评估智能体在中文互联网环境下的多跳推理能力，指标为准确率。
*   **对比方法**：
    *   **单智能体基线**：Claude Sonnet 4 (Thinking), Gemini 2.5 Pro, OpenAI o3-high, DeepSeek-R1 等。
    *   **端到端商业系统**：Gemini Deep Research, OpenAI Deep Research。
    *   **多智能体框架**：WebSailor, BrowseMaster 等。
*   **实现细节**：Host 和 Manager 使用 **GPT-5.1**，Worker 使用 **GPT-5-mini** 以平衡性能与成本。工具集成包括 Firecrawl（搜索）和 Playwright（浏览）。

## 4. 资源与算力
*   **算力资源**：论文主要关注推理框架的设计，**未提及具体的训练算力（如 GPU 型号、数量、训练时长）**。
*   **推理成本**：文中报告了 API 调用成本，WideSearch-en 平均每任务约 $2.00，BrowseComp-zh 约 $1.00。通过将繁重的执行任务委托给轻量级 Worker 模型，实现了成本效益。

## 5. 实验数量与充分性
*   **实验规模**：
    *   在两个互补基准上进行了全面评估，覆盖了英文和中文、广度综合与深度推理场景。
    *   对比了 10 余种基线模型和系统，涵盖了当前最强的商业模型和开源框架。
    *   进行了消融实验，分析了 Worker 池大小对延迟的影响（从 1 个增加到 17 个，速度提升 5.7 倍）。
    *   提供了详细的案例分析和失败案例分析。
*   **充分性与公平性**：
    *   实验设计较为充分，不仅对比了最终性能，还对比了时间效率。
    *   为了公平对比，附录中提供了使用相同工具和骨干模型的单智能体基线实验，证明了性能提升源于架构设计而非单纯的模型能力。

## 6. 论文的主要结论与发现
*   **性能提升**：InfoSeeker 在 WideSearch-en 上实现了 8.38% 的成功率（相比最佳基线提升 64%），在 BrowseComp-zh 上实现了 52.9% 的准确率（超越最佳基线 6.4 个百分点）。
*   **效率优势**：相比 OpenAI Deep Research 和 Gemini Deep Research，InfoSeeker 实现了 **3-5 倍** 的推理加速。
*   **关键发现**：
    *   分层架构有效隔离了上下文，防止了长程任务中的信息饱和。
    *   Worker 层的大规模并行执行显著降低了延迟。
    *   即使使用相同骨干模型，分层架构也显著优于单智能体系统。

## 7. 优点
*   **架构创新**：提出了“近可分解”的分层架构，巧妙解决了大规模信息检索中“深度推理”与“广度综合”难以兼得的矛盾。
*   **效率与效果兼顾**：不仅大幅提升了任务成功率，还通过并行机制显著降低了端到端延迟，解决了实际应用中的痛点。
*   **模块化与可扩展**：设计具有良好的扩展性，Manager 和 Worker 可根据需求灵活配置，且上下文隔离机制增强了系统的鲁棒性。

## 8. 不足与局限
*   **依赖闭源模型**：实验主要基于 GPT-5.1 和 GPT-5-mini，虽然效果优异，但对 API 的可用性、速率限制和成本有较强依赖，限制了在资源受限环境下的部署。
*   **极端场景限制**：失败案例分析显示，在面对极高数据量（如需生成数百行表格）的任务时，仍可能触及 Token 限制导致截断；在处理特定实体消歧任务时存在误判风险。
*   **自动化程度**：目前的任务分解和协调策略主要依赖骨干模型的上下文学习能力，未来可能需要引入强化学习等机制进一步优化调度策略。

（完）

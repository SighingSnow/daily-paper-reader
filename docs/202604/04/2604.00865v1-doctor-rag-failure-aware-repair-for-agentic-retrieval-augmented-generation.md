---
title: "Doctor-RAG: Failure-Aware Repair for Agentic Retrieval-Augmented Generation"
title_zh: Doctor-RAG：面向智能体检索增强生成的失败感知修复
authors: "Shuguang Jiao, Chengkai Huang, Shuhan Qi, Xuan Wang, Yifan Li, Lina Yao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00865v1"
tags: ["query:agent"]
score: 8.0
evidence: 智能体RAG框架用于多跳问答与故障修复
tldr: Agentic RAG在长推理中易出错，现有修复方法成本高。本文提出Doctor-RAG框架，通过显式错误定位和前缀复用实现低成本干预。该方法分两阶段：先定位轨迹中最早故障点，再进行局部修复。实验表明，该方法显著提升了答案准确率并大幅降低了推理token消耗。
selection_source: fresh_fetch
motivation: 现有Agentic RAG在推理失败时通常停止或全流程重跑，导致计算开销大且推理冗余。
method: 提出Doctor-RAG框架，通过轨迹级故障诊断定位错误点，并复用有效前缀进行工具条件下的局部修复。
result: 在多跳问答基准测试中，该方法显著提高了答案准确率，同时大幅减少了推理token消耗。
conclusion: Doctor-RAG通过精准的错误定位和最小化干预，实现了高效且准确的Agentic RAG修复。
---

## 摘要
智能体检索增强生成已成为多跳问答和复杂知识推理广泛采用的范式，其中检索与推理在推理阶段交织进行。随着推理轨迹变长，失败情况愈发普遍。现有方法通常通过止步于诊断分析或重新运行整个检索-推理流水线来处理此类失败，这导致了巨大的计算开销和冗余推理。本文提出了 Doctor-RAG (DR-RAG)，这是一个统一的诊断与修复框架，通过显式错误定位和前缀复用来纠正 Agentic RAG 中的失败，实现最小成本干预。DR-RAG 将失败处理分解为两个连续阶段： 轨迹级失败诊断与定位，将错误归因于覆盖门控分类体系，并识别推理轨迹中最早的失败点； 基于工具条件的局部修复，仅在诊断出的失败点进行干预，同时最大程度复用已验证的推理前缀和检索证据。通过显式分离错误归因与纠正，DR-RAG 实现了精确的错误定位，从而避免了昂贵的全流水线重跑，实现了针对性、高效的修复。我们在三个多跳问答基准、多个 Agentic RAG 基线及不同的骨干模型上对 DR-RAG 进行了评估。实验结果表明，与基于重跑的修复策略相比，DR-RAG 显著提高了回答准确率，同时大幅降低了推理 token 消耗。

## Abstract
Agentic Retrieval-Augmented Generation (Agentic RAG) has become a widely adopted paradigm for multi-hop question answering and complex knowledge reasoning, where retrieval and reasoning are interleaved at inference time. As reasoning trajectories grow longer, failures become increasingly common. Existing approaches typically address such failures by either stopping at diagnostic analysis or rerunning the entire retrieval-reasoning pipeline, which leads to substantial computational overhead and redundant reasoning. In this paper, we propose Doctor-RAG (DR-RAG), a unified diagnose-and-repair framework that corrects failures in Agentic RAG through explicit error localization and prefix reuse, enabling minimal-cost intervention. DR-RAG decomposes failure handling into two consecutive stages: (i) trajectory-level failure diagnosis and localization, which attributes errors to a coverage-gated taxonomy and identifies the earliest failure point in the reasoning trajectory; and (ii) tool-conditioned local repair, which intervenes only at the diagnosed failure point while maximally reusing validated reasoning prefixes and retrieved evidence. By explicitly separating error attribution from correction, DR-RAG enables precise error localization, thereby avoiding expensive full-pipeline reruns and enabling targeted, efficient repair. We evaluate DR-RAG across three multi-hop question answering benchmarks, multiple agentic RAG baselines, and different backbone models. Experimental results demonstrate that DR-RAG substantially improves answer accuracy while significantly reducing reasoning token consumption compared to rerun-based repair strategies.

---

## 论文详细总结（自动生成）

# 论文总结：Doctor-RAG

## 1. 核心问题与整体含义
*   **研究背景**：智能体检索增强生成在处理多跳问答和复杂推理任务时，随着推理链的延长，失败（如幻觉、检索错误、逻辑错误）变得日益频繁。
*   **核心问题**：现有的处理方法存在明显缺陷。一类方法仅止步于错误诊断，无法自动修复；另一类方法（如重跑策略）在发现错误后丢弃整个推理轨迹重新开始，导致巨大的计算开销和冗余推理。
*   **研究动机**：如何以最小的成本对 Agentic RAG 的失败轨迹进行精准定位和高效修复，避免全流程重跑带来的资源浪费，是本文试图解决的核心问题。

## 2. 方法论
本文提出了 **Doctor-RAG (DR-RAG)** 框架，核心思想是将失败处理建模为“诊断-修复”的闭环过程，通过显式错误定位和前缀复用实现最小成本干预。

### 核心流程
框架主要包含两个连续阶段：

#### (1) 轨迹级失败诊断与定位
该阶段旨在确定“为什么错”和“错在哪里”。
*   **覆盖门控分类体系**：首先评估检索文档是否足以回答问题（即覆盖率 Cov(y)）。
    *   **全证据覆盖 (Cov=1)**：错误归因于模型内部（如格式错误、推理逻辑错误）。
    *   **部分证据覆盖 (Cov<1)**：错误归因于外部检索或推理引导错误（如检索器失败、搜索错误）。
*   **错误定位**：在确定错误类型后，模块会识别轨迹中最早的失败点 $k^\dagger$。公式定义为 $k^\dagger = \min \{k | \rho^{(k)} \text{ is classified as erroneous}\}$。在此之前的轨迹被视为有效前缀，之后的部分被丢弃。

#### (2) 基于工具条件的局部修复
该阶段根据诊断结果选择特定的修复算子，仅在失败点进行干预，并复用有效前缀。
*   **格式错误修复**：仅重写最终答案，不改变推理过程。
*   **推理逻辑错误修复**：保留检索到的文档，截断轨迹至失败点，基于已有证据重新推理。
*   **检索器失败修复**：保留前缀，重写查询并扩大检索范围（如增加 top-k），重新生成后续步骤。
*   **搜索错误修复**：截断轨迹，重新规划解决方案路径，允许推理和检索路径分叉。

## 3. 实验设计
*   **数据集**：使用了三个多跳问答基准数据集：**HotpotQA**、**2WikiMultihopQA (2Wiki)** 和 **MuSiQue**。
*   **基线方法**：
    *   **Agentic RAG 基线**：ReAct、Search-o1、Search-R1。
    *   **修复策略对比**：
        *   **Rerun**：丢弃失败轨迹，全流程重新生成。
        *   **Step-wise Retry**：逐步验证并重试，但无错误分类和精准定位。
        *   **RAG-Critic**：诊断后重规划并重跑，但未复用有效前缀。
*   **骨干模型**：Qwen3-8B、LLaMA-3.1-8B-Instruct、Qwen3-4B。

## 4. 资源与算力
*   **硬件环境**：实验在 **4 张 NVIDIA RTX Pro 6000 GPU** 上进行。
*   **推理框架**：使用 vLLM 进行推理加速。
*   **训练情况**：论文主要关注推理阶段的修复框架，未提及具体的模型训练时长或微调过程，属于即插即用的推理框架。

## 5. 实验数量与充分性
*   **实验规模**：实验设计较为充分。
    *   **主实验**：在 3 个数据集、3 个基线模型、3 个骨干模型上进行了全面对比。
    *   **消融实验**：验证了错误分类体系和错误定位模块各自的作用。
    *   **效率分析**：对比了 Token 消耗和推理时间。
    *   **诊断可靠性分析**：评估了自动诊断模块与人工标注的一致性。
    *   **Oracle 上限分析**：假设诊断完全正确时的修复上限。
*   **客观性与公平性**：所有方法在相同的骨干模型和检索设置下进行对比，评估指标包括 EM、F1、ROUGE-L 以及 Token 成本，评价标准客观。

## 6. 主要结论与发现
*   **性能提升**：DR-RAG 在所有数据集和基线上均显著提高了答案准确率（例如在 HotpotQA 上 EM 提升高达 +25.8%）。
*   **效率优化**：相比全流程重跑策略，DR-RAG 大幅降低了 Token 消耗（平均减少约 26.8%）和推理时间。
*   **诊断有效性**：自动诊断模块的准确率约为 60%，虽然存在误诊，但足以指导修复并带来显著增益。
*   **修复难度差异**：在全证据覆盖下的修复成功率高于部分覆盖情况；复杂多跳问题的修复难度较大。

## 7. 优点
*   **创新性视角**：首次将 Agentic RAG 的失败修复建模为“最小成本干预”问题，打破了“出错即重跑”的传统思维。
*   **细粒度控制**：通过覆盖门控分类体系和轨迹级定位，实现了对错误的精准归因和局部干预。
*   **资源节约**：前缀复用机制有效避免了冗余计算，显著提升了推理效率。

## 8. 不足与局限
*   **诊断准确率瓶颈**：自动诊断模块的准确率约为 60%，存在误诊风险，可能引导错误的修复策略。
*   **复杂场景修复受限**：在检索证据不足（Cov<1）或问题极其复杂（如 MuSiQue 数据集）的情况下，局部修复的成功率相对较低。
*   **依赖 LLM 能力**：诊断和修复过程严重依赖骨干模型的能力，较小参数量的模型（如 4B 模型）在规划和推理上的短板可能限制修复效果。

（完）

---
title: "Nomad: Autonomous Exploration and Discovery"
title_zh: Nomad：自主探索与发现
authors: "Bokang Jia, Samta Kamboj, Satheesh Katipomu, Seung Hun Han, Neha Sengupta, Andrew Jackson"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29353v2"
tags: ["query:agent"]
score: 8.0
evidence: 探索智能体具备文档搜索、网页搜索和数据库工具能力
tldr: 现有的查询驱动系统受限于人类预设，难以全面探索数据洞察。本文提出 Nomad 系统，采用探索优先架构，通过构建探索地图和智能体自主生成并验证假设。实验表明，该系统生成的报告在可信度、质量和多样性上均优于基线，实现了从被动回答问题到主动发现有价值问题的转变。
selection_source: fresh_fetch
motivation: 现有的查询驱动系统受限于人类预设，难以覆盖更广泛的洞察空间。
method: 提出探索优先架构，通过构建探索地图和智能体自主生成假设，并经独立验证后生成报告。
result: 实验表明，该系统生成的报告在可信度、质量和多样性方面均优于基线模型。
conclusion: Nomad 迈出了关键一步，使系统不仅能回答问题，更能自主发现有价值的探索方向。
---

## 摘要
我们介绍了 Nomad，这是一个用于自主数据探索和洞察发现的系统。面对文档、数据库或其他数据源的语料库，用户往往不清楚所有可能被探索的问题、假设或联系。因此，查询驱动的问答系统和提示驱动的深度研究系统仍然受限于人类的框架，往往无法覆盖更广阔的洞察空间。Nomad 通过探索优先架构解决了这一问题。它在领域内构建显式的探索图，并系统地遍历该图以平衡广度与深度。它生成并筛选假设，并利用能够使用文档搜索、网络搜索和数据库工具的探索智能体对这些假设进行调查。候选洞察随后由独立的验证器进行核查，之后进入报告生成流程，产出带有引用的报告和更高层级的元报告。我们还提出了一个针对自主发现系统的综合评估框架，用于衡量可信度、报告质量和多样性。利用选定的联合国（UN）和世界卫生组织（WHO）报告语料库，我们展示了 Nomad 生成的报告比基线更可信、质量更高，且在多次运行中能产生更多样化的洞察。Nomad 是迈向自主系统的一步，这些系统不仅能回答用户问题或进行定向研究，还能发现哪些问题、研究方向和洞察值得呈现。

## Abstract
We introduce Nomad, a system for autonomous data exploration and insight discovery. Given a corpus of documents, databases, or other data sources, users rarely know the full set of questions, hypotheses, or connections that could be explored. As a result, query-driven question answering and prompt-driven deep-research systems remain limited by human framing and often fail to cover the broader insight space. Nomad addresses this problem with an exploration-first architecture. It constructs an explicit Exploration Map over the domain and systematically traverses it to balance breadth and depth. It generates and selects hypotheses and investigates them with an explorer agent that can use document search, web search, and database tools. Candidate insights are then checked by an independent verifier before entering a reporting pipeline that produces cited reports and higher-level meta-reports. We also present a comprehensive evaluation framework for autonomous discovery systems that measures trustworthiness, report quality, and diversity. Using a corpus of selected UN and WHO reports, we show that Nomad produces more trustworthy and higher-quality reports than baselines, while also producing more diverse insights over several runs. Nomad is a step toward autonomous systems that not only answer user questions or conduct directed research, but also discover which questions, research directions, and insights are worth surfacing in the first place.

---

## 论文详细总结（自动生成）

# 论文《Nomad: Autonomous Exploration and Discovery》详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的查询驱动问答系统和提示驱动的深度研究系统，其探索范围受限于用户预先设定的任务、查询或浏览目标。用户往往无法预知数据语料库中所有潜在的问题、假设或隐藏联系，导致从数据中提取的价值被分析师的初始框架所限制，而非数据本身蕴含的信息。
*   **整体含义**：论文旨在解决“自主发现”问题，即构建一个能够自主探索数据语料库、扩展候选研究方向、并产出多个独特、有证据支持的洞察的系统。这标志着从“回答用户问题”到“自主发现值得探索的问题”的转变。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
*   **核心思想**：提出 **“探索优先”架构**。系统首先构建一个覆盖领域的 **“探索地图”**，并系统性地遍历它以平衡探索的广度与深度，而非直接响应单一查询。
*   **关键技术细节**：
    *   **探索地图构建**：
        *   **自上而下**：当无语料库时，根据用户目标直接生成主题树。
        *   **自下而上**：当有语料库时，先从文档中提取概念并构建概念层，再聚类生成主题树。关键步骤是评估概念的“洞察潜力”，为有潜力的概念生成初始假设。
        *   **地图扩展**：支持通过网页搜索动态更新地图，以纳入最新信息。
    *   **主题选择与假设生成**：采用广度优先遍历主题树，优先探索较少涉足的分支。在叶节点生成多个假设，并根据相关性、影响力和多样性评分选择最佳假设进行探索。
    *   **探索者-验证者循环**：
        *   **探索者**：一个ReAct风格的智能体，使用文档搜索、网页搜索和数据库工具，迭代收集证据、修正假设，最终提交候选洞察。
        *   **验证者**：一个独立的智能体，将洞察分解为原子子主张，使用相同工具独立验证每个子主张，并给出评分和反馈。只有通过验证的洞察才进入报告流程。
    *   **报告生成与合成**：
        *   **单报告生成**：基于验证后的洞察和证据，生成包含大纲、标题、摘要、各章节内容、图表和引用审计的结构化报告。
        *   **元报告生成**：定期将多个报告的洞察进行主题聚类和合成，生成更高层次的跨报告分析。

## 3. 实验设计：数据集、场景、基准与对比方法
*   **数据集/场景**：使用公开的 **联合国（UN）和世界卫生组织（WHO）政策与统计报告语料库**，涵盖健康、气候、发展、贸易等多个领域。实验设置了两种场景：
    *   **自下而上**：系统拥有语料库，进行基于数据的探索。
    *   **自上而下**：系统仅拥有研究目标，进行基于目标的探索。
*   **基准**：
    *   **GPT Researcher**：开源的深度研究工具。
    *   **o3-deep-research**：OpenAI的商业深度研究API（通过Azure OpenAI访问）。
*   **对比方法**：将Nomad与上述两个基准系统在相同目标/语料库下生成的报告进行对比。

## 4. 资源与算力
*   **论文未明确说明**所使用的具体GPU型号、数量、训练时长或总算力消耗。
*   仅在实验设置中提及使用了 **azure-o3模型**（用于假设生成、探索者和报告生成）和 **Llama-3.3-70B-Instruct模型**（用于验证者，因其幻觉风险较低）。GPT Researcher使用了azure-gpt-4o-mini、azure-gpt-4o和azure-o4-mini等模型。

## 5. 实验数量与充分性
*   **实验数量**：在两种场景下，分别对比了三个系统。评估维度全面，包括：
    *   **可信度**：数值接地、事实性。
    *   **质量**：分析质量、原创性、覆盖度、可操作性、呈现质量。
    *   **独特性**：报告内部章节间的信息重复度。
    *   **多样性**：多次运行生成的报告间的主题差异度。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了核心评估维度，并展示了Nomad在多数指标上的优势，特别是可信度和多样性。实验设计相对充分。
    *   **客观性**：评估框架设计详细，部分指标（如数值接地）采用规则方法，减少主观性。质量评估采用基于属性的二分判断，比直接评分更稳定。
    *   **公平性**：为不同系统配置了适合其任务难度的模型（如Nomad的验证者使用较弱但更可靠的模型）。但在自上而下场景中，o3-deep-research被提供了语料库摘要，而其他系统直接访问原文，这可能存在一定不公平。
    *   **缺失**：论文未进行详细的消融实验（如单独评估探索地图或验证者的贡献），作者在“未来工作”中承认了这一点。

## 6. 论文的主要结论与发现
*   **主要结论**：Nomad系统在自主数据探索和洞察发现任务上表现优异。
*   **具体发现**：
    *   **可信度更高**：Nomad报告的数值接地率和事实性得分显著高于基线，尤其在自下而上场景中优势明显。这得益于其结构化的引用管理系统和独立的验证者。
    *   **质量更优**：Nomad报告在分析质量、可操作性和原创性上优于基线，但覆盖度略低于o3-deep-research（后者倾向于在单一报告中覆盖所有方面）。
    *   **多样性更强**：Nomad在多次运行中生成的报告主题多样性远高于基线（约5倍于GPT Researcher，2倍于o3-deep-research），这是其探索优先架构的直接成果。
    *   **独特性不足**：Nomad报告内部章节的独特性得分低于基线，表明存在一定内容重复，这是报告生成流程的一个弱点。

## 7. 优点：方法或实验设计上的亮点
*   **架构创新**：明确提出“探索优先”范式，将研究目标从“回答问题”提升到“发现问题”，具有前瞻性。
*   **结构化探索**：探索地图的设计巧妙地平衡了广度与深度，有效避免了LLM智能体陷入高概率路径的重复探索。
*   **可信度保障**：探索者与验证者分离的设计，将验证嵌入发现流程本身，而非事后检查，显著提升了最终洞察的可信度。
*   **评估框架全面**：提出了针对自主发现系统的多维度评估框架，特别是引入了“多样性”和“数值接地”等关键指标，为该领域提供了有价值的评估基准。

## 8. 不足与局限
*   **实验覆盖不全**：缺乏对系统核心组件（如探索地图、验证者）的消融实验，无法量化各组件的独立贡献。
*   **算力信息缺失**：未报告系统运行的具体算力消耗和成本，这对于评估其实用性至关重要。
*   **报告内部重复**：报告生成流程导致章节间存在信息重复，独特性得分较低，需改进。
*   **验证者模型选择**：为降低幻觉风险，验证者使用了推理能力相对较弱的模型，可能影响对复杂逻辑主张的验证深度。
*   **应用限制**：当前系统主要针对文本数据，未来需扩展至多模态数据（图像、视频等）。系统在高度特定、明确的问题上可能不如传统深度研究工具高效。

（完）

---
title: "ActionParty: Multi-Subject Action Binding in Generative Video Games"
title_zh: ActionParty：生成式视频游戏中的多主体动作绑定
authors: "Alexander Pondaven, Ziyi Wu, Igor Gilitschenski, Philip Torr, Sergey Tulyakov, Fabio Pizzati, Aliaksandr Siarohin"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02330v1"
tags: ["query:wmodel"]
score: 8.0
evidence: 用于生成式视频游戏的多主体世界模型
tldr: 针对现有视频扩散模型难以同时控制多个智能体并实现动作绑定的问题，本文提出了ActionParty模型。该方法引入主体状态标记和空间偏置机制，解耦了全局渲染与个体动作更新。这是首个能在46种环境中同时控制多达7名玩家的视频世界模型，显著提升了动作跟随准确率和身份一致性，实现了复杂交互下的稳健自回归跟踪。
selection_source: fresh_fetch
motivation: 现有视频扩散模型局限于单智能体设置，难以解决多主体场景下的动作绑定问题。
method: 提出ActionParty模型，引入主体状态标记并利用空间偏置机制，解耦全局视频渲染与个体主体的动作控制更新。
result: 在Melting Pot基准上实现了首个可同时控制多达7名玩家的视频世界模型，显著提升了动作跟随准确率和身份一致性。
conclusion: 该方法有效解决了多主体动作绑定问题，在复杂交互场景中实现了稳健的主体跟踪与控制。
---

## 摘要
视频扩散领域的最新进展使得开发能够模拟交互环境的“世界模型”成为可能。然而，这些模型主要局限于单智能体设置，无法在场景中同时控制多个智能体。在本文中，我们解决了现有视频扩散模型中动作绑定的基本问题，即难以将特定动作与其对应的主体相关联。为此，我们提出了 ActionParty，一种用于生成式视频游戏的动作可控多主体世界模型。该模型引入了主体状态 token，即持续捕捉场景中每个主体状态的潜变量。通过利用空间偏置机制对状态 token 和视频潜表示进行联合建模，我们将全局视频帧渲染与单个动作控制的主体更新解耦。我们在 Melting Pot 基准上评估了 ActionParty，展示了首个能够在 46 种不同环境中同时控制多达七名玩家的视频世界模型。结果表明，该方法在动作跟随准确率和身份一致性方面有显著提升，同时能够在复杂的交互过程中实现对主体的鲁棒自回归跟踪。

## Abstract
Recent advances in video diffusion have enabled the development of "world models" capable of simulating interactive environments. However, these models are largely restricted to single-agent settings, failing to control multiple agents simultaneously in a scene. In this work, we tackle a fundamental issue of action binding in existing video diffusion models, which struggle to associate specific actions with their corresponding subjects. For this purpose, we propose ActionParty, an action controllable multi-subject world model for generative video games. It introduces subject state tokens, i.e. latent variables that persistently capture the state of each subject in the scene. By jointly modeling state tokens and video latents with a spatial biasing mechanism, we disentangle global video frame rendering from individual action-controlled subject updates. We evaluate ActionParty on the Melting Pot benchmark, demonstrating the first video world model capable of controlling up to seven players simultaneously across 46 diverse environments. Our results show significant improvements in action-following accuracy and identity consistency, while enabling robust autoregressive tracking of subjects through complex interactions.

---

## 论文详细总结（自动生成）

# 论文总结：ActionParty: Multi-Subject Action Binding in Generative Video Games

## 1. 论文的核心问题与整体含义
*   **研究背景**：视频扩散模型正在向“世界模型”演进，旨在模拟交互式环境。然而，现有的模型（如 Genie, GameNGen）大多局限于**单智能体**设置，无法在同一个场景中同时控制多个智能体。
*   **核心问题**：**动作绑定失效**。当尝试通过文本指令控制多个主体时，现有的视频生成模型无法准确地将特定动作关联到正确的主体上。例如，指令“红色三角形向右，蓝色方块向上”，模型往往会混淆，导致动作错位或属性混合。
*   **研究意义**：本文旨在解决多主体场景下的动作控制难题，使生成式视频游戏能够模拟复杂的多智能体交互，这对于自动驾驶模拟、机器人仿真及通用世界模型的构建具有重要意义。

## 2. 论文提出的方法论
*   **核心思想**：提出 **ActionParty** 模型，通过引入显式的**主体状态标记**来解耦全局视频渲染与个体主体的动作控制。模型不再仅依赖像素空间的隐式推断，而是显式维护每个主体的状态，并通过注意力机制实现动作与主体的精确绑定。
*   **关键技术细节**：
    *   **主体状态标记 ($z_t$)**：引入一组潜变量来持续捕捉场景中每个主体的状态。在本文的 2D 游戏设置中，状态被定义为 2D 坐标 $(h, w)$。这些标记与视频 token 拼接，共同输入扩散 Transformer (DiT) 进行去噪。
    *   **更新与渲染范式**：
        *   **状态更新**：在交叉注意力层中，设计掩码机制，强制主体 $i$ 的状态标记 $z_i$ 仅关注其对应的动作嵌入 $a_i$，从而阻断无关动作的干扰，实现严格的动作-主体绑定。
        *   **渲染**：在自注意力层中，允许主体标记读取视频 token（感知环境），同时允许视频 token 关注主体标记（合成画面），但禁止主体标记之间的相互通信，防止状态混淆。
    *   **RoPE 空间偏置**：利用 3D 旋转位置嵌入，根据主体在上一时刻的坐标位置，对主体状态标记施加空间偏置。这使得模型能够将抽象的状态标记锚定到视频帧中具体的像素区域，解决了“谁在哪里”的问题。
*   **算法流程**：模型采用自回归方式生成视频。给定初始帧和初始状态，模型联合预测下一帧视频 $x_{t+1}$ 和更新后的主体状态 $z_{t+1}$。

## 3. 实验设计
*   **数据集**：使用了 **Melting Pot** 基准数据集，包含 **46 种** 不同的 2D 多智能体游戏环境。这些环境具有不同的视觉风格和规则，主体数量从 2 到 7 个不等。
*   **评估指标**：
    *   **动作绑定指标**：移动准确率（MA，衡量动作是否正确执行）、效果准确率（EA，衡量交互动作如“射击”的效果）、主体保留率（SP）、检测率（DR）。
    *   **视觉质量指标**：PSNR, LPIPS, FVD。
*   **对比方法**：
    *   **Zero-shot I2V**：使用更大的 Wan2.1-14B 模型，仅通过文本描述主体位置和动作进行生成。
    *   **Text-Action**：使用相同的骨干网络，但仅通过文本条件进行训练，无显式状态标记。
    *   **Pretrained AR**：仅在游戏视频上预训练，无动作控制。

## 4. 资源与算力
*   **模型基础**：基于 Wan2.1-1.3B（一种开源的视频扩散 Transformer）进行微调。
*   **训练细节**：
    *   预训练阶段：在所有游戏上训练 22.5k 步。
    *   微调阶段：训练 65k 步。
    *   批量大小：64。
    *   序列长度：$T=5$（对应 4 个动作步）。
*   **算力说明**：**论文未明确提及具体的 GPU 型号、数量或总训练时长**。

## 5. 实验数量与充分性
*   **实验数量**：
    *   主实验对比了 3 种基线方法在 46 种环境下的表现。
    *   提供了详细的消融实验，验证了自注意力掩码（MSA）、交叉注意力掩码（MCA）和 RoPE 偏置机制各自的作用。
    *   分析了不同主体数量（稀疏、密集、拥挤）下的性能变化。
    *   展示了长序列自回归生成的稳定性分析。
*   **充分性与客观性**：实验设计较为充分。不仅对比了零样本的大模型，还对比了同等骨干网络的文本控制模型，比较公平。评估指标涵盖了动作执行的准确性和视觉质量，且针对“动作绑定”这一核心难点设计了专门的定量指标（MA, EA），具有说服力。

## 6. 论文的主要结论与发现
*   **性能提升**：ActionParty 是首个能够在 46 种不同环境中同时控制多达 7 个主体的视频世界模型。在动作跟随准确率（MA）上，相比最强基线提升了约 5 倍（0.779 vs 0.158）。
*   **解决绑定问题**：显式的主体状态标记和注意力掩码机制有效解决了多主体场景下的动作混淆问题，而 RoPE 偏置机制显著提升了主体定位的准确性。
*   **鲁棒性**：模型在主体数量增加时仍能保持较高的控制精度，且在长序列自回归生成中表现出良好的稳定性。

## 7. 优点
*   **创新性强**：首次在视频扩散模型中引入“主体状态标记”概念，巧妙地将游戏引擎的“状态更新-渲染”逻辑融入生成模型架构中。
*   **架构设计精巧**：通过注意力掩码和 RoPE 偏置，在不大幅增加计算开销（仅增加约 6% 的 token 数量）的情况下，实现了复杂的多主体解耦控制。
*   **泛化能力**：单一模型即可泛化到 46 种风格迥异的游戏环境，证明了方法的通用性。

## 8. 不足与局限
*   **环境限制**：实验主要基于 2D 网格游戏环境，主体状态简化为 2D 坐标。对于更复杂的 3D 场景或非刚性主体的适用性尚未验证。
*   **状态漂移**：论文提到在长序列生成中，预测的坐标 $z_t$ 可能会因误差累积而偏离真实位置，虽然 RoPE 缓解了这一问题，但并未完全消除。
*   **实时性**：模型目前尚未达到实时生成的速度，限制了其在实际交互式游戏引擎中的直接应用。
*   **资源细节缺失**：论文未公开具体的计算资源消耗数据，难以评估其训练成本。

（完）

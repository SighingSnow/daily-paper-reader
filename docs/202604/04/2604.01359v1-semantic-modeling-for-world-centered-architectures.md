---
title: Semantic Modeling for World-Centered Architectures
title_zh: 以世界为中心的架构的语义建模
authors: "Andrei Mantsivoda, Darya Gavrilina"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01359v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 引入以世界为中心的多智能体系统与共享世界模型
tldr: 针对传统智能体中心架构在结构化领域中的局限，本文提出了世界中心多智能体系统（WMAS）。该架构通过共享的显式世界模型替代孤立的本地表示，利用语义模型作为数学形式化工具，确保了系统的语义一致性、可解释性与长期稳定性，并推出了Ontobox平台作为具体实现。
selection_source: fresh_fetch
motivation: 为了解决传统智能体中心架构在结构化领域中缺乏语义一致性和可解释性的问题。
method: 提出世界中心多智能体系统（WMAS），利用语义模型数学形式化方法构建共享世界模型以协调智能体行为。
result: 实现了基于共享世界模型的全局一致性与可验证行为，并推出了Ontobox平台。
conclusion: 世界中心架构通过显式共享世界表示，有效保障了复杂系统的语义一致性与长期稳定性。
---

## 摘要
我们引入了以世界为中心的多智能体系统（WMAS）作为传统以智能体为中心的架构的替代方案，论证了诸如企业和制度系统等结构化领域需要共享的、显式的世界表征，以确保语义一致性、可解释性和长期稳定性。我们沿着本体显式性、规范性等维度对世界进行分类。在WMAS中，学习和协调基于共享的世界模型而非孤立的智能体局部表征进行，从而实现全局一致性和可验证的系统行为。我们提出语义模型作为表征此类世界的数学形式体系。最后，我们展示了Ontobox平台作为WMAS的一种实现。

## Abstract
We introduce world-centered multi-agent systems (WMAS) as an alternative to traditional agent-centered architectures, arguing that structured domains such as enterprises and institutional systems require a shared, explicit world representation to ensure semantic consistency, explainability, and long-term stability. We classify worlds along dimensions including ontological explicitness, normativity, etc. In WMAS, learning and coordination operate over a shared world model rather than isolated agent-local representations, enabling global consistency and verifiable system behavior. We propose semantic models as a mathematical formalism for representing such worlds. Finally, we present the Ontobox platform as a realization of WMAS.

---

## 论文详细总结（自动生成）

# 论文总结：以世界为中心的架构的语义建模

## 1. 论文的核心问题与整体含义
*   **研究背景**：传统的多智能体系统（MAS）通常采用“以智能体为中心”的架构，即每个智能体维护各自独立的环境模型。这种范式在感知、探索等非结构化场景中表现良好，但在企业、制度系统等结构化领域中，容易导致模型发散、语义不一致、系统行为不可解释且难以验证。
*   **核心问题**：如何在结构化领域（如企业管理、金融系统）中构建能够保证全局语义一致性、可解释性及长期稳定性的多智能体系统？
*   **整体含义**：论文提出了一种范式转换，从“以智能体为中心”转向“以世界为中心”。作者论证了在特定结构化领域中，构建一个共享的、显式的“世界模型”作为智能体交互、学习和协调的基础，是解决上述问题的关键。

## 2. 论文提出的方法论
*   **核心思想**：提出世界中心多智能体系统（WMAS）。在该架构中，“世界”而非智能体成为主要的表示和学习载体。智能体从属于共享的世界模型，通过受控接口进行观察和行动。
*   **世界的形式化定义**：
    *   将世界定义为六元组 $W = (E, R, S, A, T, C)$，其中 $E$ 为实体集，$R$ 为关系集，$S$ 为状态空间，$A$ 为允许动作，$T$ 为转换函数，$C$ 为约束或规范。
*   **世界分类维度**：提出了六个正交维度来评估世界属性，以确定WMAS的适用性：
    *   本体显式性、结构稳定性、规范性、状态可观测性、语义野心（增长的有界性）、感知-深思比率。
    *   结论：WMAS最适用于高显式性、高稳定性、强规范性、深思主导的制度/企业世界。
*   **语义模型架构**：作为WMAS的核心数学形式化工具，采用双层结构：
    *   **基础语义层**：基于对象本体，表示事实、实体、关系及操作状态，提供共享的“单一事实来源”。
    *   **因果知识层**：基于逻辑概率推理，包含显式的概率因果关系，用于预测和决策支持。
*   **语义机器学习（SML）**：
    *   连接两层的机制。SML根据基础层的状态变化，增量式地更新因果知识层。
    *   学习过程是世界级的（非智能体局部），确保了知识的全局一致性和可验证性。

## 3. 实验设计
*   **验证场景**：论文并未采用传统的算法Benchmark数据集，而是基于Ontobox平台进行了实际应用场景的实现与验证。提到的具体场景包括：
    *   私人诊所管理系统。
    *   汽车贷款管理系统。
    *   水培农场自动控制系统。
*   **对比方法**：本文主要是一篇架构与理论框架论文，未设置具体的基线算法进行量化对比。其对比对象是概念上的“传统智能体中心架构”。
*   **验证目标**：证明WMAS架构在现实复杂系统中的可行性，验证语义模型作为共享上下文的有效性。

## 4. 资源与算力
*   **算力说明**：文中未提及具体的硬件配置（如GPU型号、数量）、训练时长或算力消耗细节。这符合该类架构性论文的特点，重点在于逻辑框架和系统实现，而非大规模计算训练。

## 5. 实验数量与充分性
*   **实验数量**：论文列举了三个不同领域的应用案例（医疗、金融、农业），展示了架构的通用性。
*   **充分性与客观性**：
    *   作为架构性论文，通过实际平台部署证明“存在性证明”是充分的。
    *   然而，缺乏定量的性能指标（如响应时间、吞吐量、准确性提升的具体数值）和消融实验。实验部分主要侧重于定性描述和功能实现，缺乏对系统效率或与传统方法在量化指标上的客观对比。

## 6. 论文的主要结论与发现
*   **架构适用性**：WMAS并非通用解决方案，它是制度性、规范性、语义有界世界的最佳架构选择，但不适用于感知主导或本体未知的开放世界。
*   **语义一致性**：通过共享的显式世界模型，WMAS成功解决了智能体局部模型发散和语义碎片化问题。
*   **可解释性与集成**：语义模型提供了“玻璃盒”式的透明度，支持异构智能体（包括LLM）的集成，能够有效缓解LLM的幻觉问题并提供可验证的推理依据。

## 7. 优点
*   **范式创新**：清晰地界定了“世界中心”与“智能体中心”架构的适用边界，为结构化领域的AI系统设计提供了理论指导。
*   **双层模型设计**：将事实状态（基础层）与因果知识（学习层）分离并通过SML耦合，既保证了操作的正确性，又具备适应环境变化的能力。
*   **可解释性与可信度**：强调显式本体和因果知识，使得系统决策过程可追溯、可验证，这对企业级应用至关重要。
*   **LLM集成优势**：提出了将LLM作为受限推理器集成到语义环境中的方法，利用世界模型约束LLM的输出，提高了系统的可靠性。

## 8. 不足与局限
*   **应用范围受限**：作者明确承认该方法不适用于非结构化、感知主导（如自动驾驶）或快速变化的领域。
*   **初始构建成本高**：构建对象本体和定义术语需要大量的领域专家知识，初期开发复杂度高于端到端的学习管道。
*   **反应性不足**：SML的增量学习机制适合分析和管理任务，但无法提供高频控制所需的瞬时反应能力。
*   **缺乏量化评估**：实验部分主要展示可行性，缺乏对系统性能（如延迟、并发处理能力）的量化分析，难以评估其在超大规模数据下的表现。

（完）

---
title: Toward Consistent World Models with Multi-Token Prediction and Latent Semantic Enhancement
title_zh: 通过多词元预测与潜在语义增强迈向一致的世界模型
authors: "Qimin Zhong, Hao Liao, Haiming Qin, Mingyang Zhou, Rui Mao, Wei Chen, Naipeng Chao"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06155v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 多token预测的一致世界模型
tldr: 本文针对大语言模型构建连贯世界模型的问题，分析了多词元预测（MTP）的梯度归纳偏置及其存在的结构性幻觉缺陷。作者提出了潜在语义增强MTP（LSE-MTP），通过锚定真实隐藏状态轨迹弥合离散词元与连续状态表征的差距。实验证明该方法有效减少了幻觉，提升了表征对齐与鲁棒性。
selection_source: fresh_fetch
motivation: 传统单步预测难以构建连贯世界模型，而标准多步预测虽能促进收敛却存在结构性幻觉问题。
method: 提出潜在语义增强多词元预测方法（LSE-MTP），通过锚定真实隐藏状态轨迹弥合离散词元与连续状态表征的差距。
result: 实验表明该方法有效增强了表征对齐，显著减少了结构性幻觉并提升了抗干扰鲁棒性。
conclusion: LSE-MTP有效解决了离散监督导致的潜在空间捷径问题，成功构建了更一致且鲁棒的世界模型。
---

## 摘要
大型语言模型是否发展出了连贯的内部世界模型仍是一个核心争议。虽然传统的下一词元预测（NTP）专注于单步前瞻监督，但多词元预测（MTP）在学习更具结构化的表示方面已展现出潜力。在这项工作中，我们提供了一个理论视角来分析MTP的梯度归纳偏置，并有实证证据支持，表明MTP通过梯度耦合诱导表示收缩性，从而促进向内部信念状态的收敛。然而，我们揭示出标准的MTP经常遭受结构性幻觉的困扰，其中离散词元监督在潜在空间中鼓励违反环境约束的非法捷径。为了解决这个问题，我们提出了一种新方法——潜在语义增强MTP（LSE-MTP），该方法将预测锚定于真实的隐状态轨迹。在合成图和真实世界曼哈顿出租车行程数据上的实验表明，LSE-MTP有效地弥合了离散词元与连续状态表示之间的差距，增强了表示对齐，减少了结构性幻觉，并提高了对扰动的鲁棒性。

## Abstract
Whether Large Language Models (LLMs) develop coherent internal world models remains a core debate. While conventional Next-Token Prediction (NTP) focuses on one-step-ahead supervision, Multi-Token Prediction (MTP) has shown promise in learning more structured representations. In this work, we provide a theoretical perspective analyzing the gradient inductive bias of MTP, supported by empirical evidence, showing that MTP promotes the convergence toward internal belief states by inducing representational contractivity via gradient coupling. However, we reveal that standard MTP often suffers from structural hallucinations, where discrete token supervision encourages illegal shortcuts in latent space that violate environmental constraints. To address this, we propose a novel method Latent Semantic Enhancement MTP (LSE-MTP), which anchors predictions to ground-truth hidden state trajectories. Experiments on synthetic graphs and real-world Manhattan Taxi Ride show that LSE-MTP effectively bridges the gap between discrete tokens and continuous state representations, enhancing representation alignment, reducing structural hallucinations, and improving robustness to perturbations.

---

## 论文详细总结（自动生成）

# 论文总结：通过多词元预测与潜在语义增强迈向一致的世界模型

## 1. 论文的核心问题与整体含义
*   **研究背景**：大型语言模型（LLM）是否具备连贯的内部世界模型是当前核心争议。传统的下一词元预测（NTP）仅关注局部单步预测，难以捕捉全局结构或长程动态；多词元预测（MTP）虽能促进结构化表示，但存在“结构性幻觉”问题。
*   **核心问题**：标准的 MTP 在训练时仅关注离散的目标词元，容易在潜在空间中诱导出违反环境约束的“非法捷径”，导致模型虽然预测准确，但内部状态演变过程不合法。
*   **整体含义**：本研究旨在弥合离散词元监督与连续状态表示之间的鸿沟，通过理论分析与改进方法，使模型不仅能预测未来，还能构建符合物理/逻辑约束的一致性世界模型。

## 2. 论文提出的方法论
*   **核心思想**：提出**潜在语义增强 MTP（LSE-MTP）**。该方法在标准 MTP 的基础上，引入了潜在一致性约束和语义锚定，强制模型的多步预测表示与真实的中间状态轨迹对齐，从而避免非法捷径。
*   **关键技术细节**：
    *   **架构**：基于 Transformer 骨干网络，引入特定视界的过渡层来预测未来 $K$ 步的表示。
    *   **损失函数**：总损失由三部分组成：
        1.  **多步交叉熵损失 ($L_{ce}$)**：标准的词元预测损失，预测未来 $K$ 个词元。
        2.  **潜在一致性损失 ($L_{latent}$)**：将预测的 $k$ 步潜在表示 $\hat{h}_{n,k}$ 与真实的未来骨干隐藏状态 $h_{n+k-1}$ 对齐（使用 MSE）。这利用了教师强制提供的真实轨迹作为锚点。
        3.  **语义锚定损失 ($L_{semantic}$)**：将预测的潜在表示与目标词元的嵌入 $E(u_{n+k})$ 对齐，起到正则化作用。
    *   **公式**：$L_{total} = L_{ce} + \lambda_l L_{latent} + \lambda_s L_{semantic}$。
    *   **推理阶段**：训练完成后，丢弃过渡层和辅助损失，仅保留骨干网络进行标准的自回归解码，不增加推理延迟。

## 3. 实验设计
*   **数据集/场景**：
    *   **合成图**：Erdős–Rényi (ER) 随机图和城市街道图 (USG)，用于可控的理论验证和表示分析。
    *   **真实世界数据**：曼哈顿出租车行程数据，街道网络被抽象为具有拓扑约束的图，要求模型生成合法路径。
*   **Benchmark（评估指标）**：
    *   **有效轨迹率**：生成的路径是否满足图的拓扑约束并到达终点。
    *   **表示对齐与压缩**：测量具有相同未来的状态在潜在空间中的相似度。
    *   **非法捷径概率 (ISP)**：模型分配给非法动作的概率，用于衡量结构性幻觉。
    *   **鲁棒性**：在生成过程中注入随机扰动，测试路径规划的恢复能力。
*   **对比方法**：
    *   **NTP (1TP)**：标准的下一词元预测模型。
    *   **标准 MTP**：不同预测视界 ($K=2,3,4$) 的多词元预测模型。
    *   **LSE-MTP**：本文提出的改进方法。

## 4. 资源与算力
*   **硬件信息**：文中在附录 D 提到使用 **NVIDIA GeForce RTX 3090 Ti** 显卡测量了训练吞吐量。
*   **训练细节**：合成图实验训练 20,000 次迭代；曼哈顿数据集训练 30 个 Epoch。模型参数量为 6 层、6 头、120 维（合成图）或 12 层、12 头、768 维（真实数据）。
*   **说明**：论文未明确说明主要实验所用的 GPU 总数量及总训练时长，仅提供了效率对比数据。

## 5. 实验数量与充分性
*   **实验数量**：
    *   **理论验证**：构建线性模型验证梯度耦合机制。
    *   **合成实验**：在 ER 和 USG 图上进行了多组实验，分析表示对齐、信念压缩和非法捷径概率。
    *   **真实数据实验**：在曼哈顿出租车数据上评估了 7 种不同的指标。
    *   **消融实验**：在附录 E 中详细分析了超参数 ($\lambda_l, \lambda_s$) 和预测视界 $K$ 的影响。
*   **充分性与客观性**：实验设计较为充分，从理论推导、线性模型验证、合成图分析到真实场景应用，逻辑链条完整。评估指标不仅包含准确率，还深入分析了潜在空间的几何特性（如 ISP、信念压缩），客观地揭示了 MTP 的优缺点及 LSE-MTP 的改进效果。

## 6. 论文的主要结论与发现
*   **MTP 的双刃剑效应**：理论分析证明 MTP 通过梯度耦合诱导表示收缩，有助于形成统一的信念状态；但同时也发现 MTP 存在“目的论偏差”，即为了匹配未来目标而忽略中间过程的合法性，导致结构性幻觉。
*   **LSE-MTP 的有效性**：通过将预测锚定到真实的隐藏状态轨迹，LSE-MTP 显著降低了非法捷径概率（ISP），增强了潜在空间的结构一致性。
*   **性能提升**：在真实世界任务中，LSE-MTP 提高了路径规划的有效性和对扰动的鲁棒性，且计算成本相比标准 MTP 几乎没有增加。

## 7. 优点
*   **理论深度**：利用神经正切核（NTK）和梯度流分析揭示了 MTP 诱导表示收缩和结构性幻觉的数学机制，解释性强。
*   **方法轻量**：LSE-MTP 仅在训练时引入额外的 MSE 损失，推理时无额外开销，具有很高的实用价值。
*   **评估视角独特**：不仅关注预测准确率，还引入了“非法捷径概率”和“信念压缩”等指标，深入量化了世界模型的内部一致性。

## 8. 不足与局限
*   **应用范围限制**：实验主要集中在结构化的图导航和路径规划任务，尚未在开放式自然语言处理（NLP）任务（如叙事理解、代码生成）中验证其效果。
*   **对比方法局限**：主要对比了 NTP 和标准 MTP，未系统性地与其他增强潜在一致性的方法（如强化学习目标、对比学习）进行对比。
*   **理论假设**：理论分析基于线性化近似（懒惰训练），可能无法完全反映大规模 Transformer 在非线性动态下的复杂行为。

（完）

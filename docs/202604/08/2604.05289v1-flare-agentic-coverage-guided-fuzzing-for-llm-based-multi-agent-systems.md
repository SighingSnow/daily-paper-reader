---
title: "FLARE: Agentic Coverage-Guided Fuzzing for LLM-Based Multi-Agent Systems"
title_zh: FLARE：面向基于大语言模型的多智能体系统的覆盖率引导模糊测试
authors: "Mingxuan Hui, Xinyue Li, Lu Wang, Chengcheng Wan, Yifan Wang, Yimian Wang, Feiyue Song, Beining Shi, Yixi Li, Yaxiao Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05289v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM多智能体系统的测试框架
tldr: 针对多智能体LLM系统（MAS）因非确定性行为易发故障且难以测试的问题，本文提出了FLARE框架。该框架从源代码提取规范，利用覆盖引导模糊测试技术自动生成测试用例并判断正确性。实验结果显示，FLARE显著提升了覆盖率，并成功发现了56个此前未知的系统故障，为MAS的可靠性测试提供了有效解决方案。
selection_source: fresh_fetch
motivation: 现有多智能体LLM系统因非确定性行为和复杂交互易发生故障，且传统测试技术因缺乏规范和语义判断能力而难以有效检测。
method: 提出FLARE框架，通过从源代码提取规范构建测试预言机，并执行覆盖引导模糊测试来暴露系统故障。
result: "在16个开源应用上的评估显示，FLARE的智能体间和内覆盖率分别达96.9%和91.1%，优于基线，并发现56个未知故障。"
conclusion: FLARE有效解决了MAS测试难题，显著提升了覆盖率并发现了大量实际故障，证明了其有效性。
---

## 摘要
多智能体大语言模型系统（MAS）已被用于通过将任务分解为子任务来自动化复杂的人类工作流。然而，由于大语言模型智能体的非确定性行为以及智能体之间错综复杂的交互，MAS应用经常遇到故障，包括无限循环和工具调用失败。由于缺乏大语言模型智能体规约、MAS巨大的行为空间以及基于语义的正确性判断，传统软件测试技术在检测此类故障方面效果不佳。本文提出了FLARE，一个专为MAS定制的新型测试框架。FLARE以MAS的源代码为输入，并从智能体定义中提取规约和行为空间。基于这些规约，FLARE构建测试预言，并执行覆盖率引导的模糊测试以暴露故障。随后，它分析执行日志以判断每个测试是否通过，并生成故障报告。我们在16个多样化的开源应用上的评估表明，FLARE实现了96.9%的智能体间覆盖率和91.1%的智能体内覆盖率，分别比基线高出9.5%和1.0%。FLARE还发现了56个MAS特有的前所未知的故障。

## Abstract
Multi-Agent LLM Systems (MAS) have been adopted to automate complex human workflows by breaking down tasks into subtasks. However, due to the non-deterministic behavior of LLM agents and the intricate interactions between agents, MAS applications frequently encounter failures, including infinite loops and failed tool invocations. Traditional software testing techniques are ineffective in detecting such failures due to the lack of LLM agent specification, the large behavioral space of MAS, and semantic-based correctness judgment.   This paper presents FLARE, a novel testing framework tailored for MAS. FLARE takes the source code of MAS as input and extracts specifications and behavioral spaces from agent definitions. Based on these specifications, FLARE builds test oracles and conducts coverage-guided fuzzing to expose failures. It then analyzes execution logs to judge whether each test has passed and generates failure reports. Our evaluation on 16 diverse open-source applications demonstrates that FLARE achieves 96.9% inter-agent coverage and 91.1% intra-agent coverage, outperforming baselines by 9.5% and 1.0%. FLARE also uncovers 56 previously unknown failures unique to MAS.

---

## 论文详细总结（自动生成）

# 论文总结：FLARE：面向基于大语言模型的多智能体系统的覆盖率引导模糊测试

## 1. 论文的核心问题与整体含义
*   **研究背景**：基于大语言模型的多智能体系统（MAS）通过将复杂任务分解为子任务，实现了工作流的自动化。然而，由于LLM的非确定性本质以及智能体间复杂的交互，MAS应用极易发生故障（如无限循环、工具调用失败等）。
*   **核心问题**：传统软件测试技术无法有效检测MAS故障，主要面临三大挑战：缺乏明确的LLM智能体规范、MAS行为空间巨大且隐式、以及需要基于语义的正确性判断而非简单的代码逻辑判断。
*   **整体含义**：本研究旨在解决MAS可靠性测试的空白，提出一种能够自动提取规范、量化行为空间并智能判断语义正确性的测试框架，以保障MAS系统的稳定运行。

## 2. 论文提出的方法论
*   **核心思想**：提出FLARE框架，首个针对MAS的功能测试框架。该框架通过静态分析提取规范，利用覆盖率引导的模糊测试探索状态空间，并结合双智能体验证机制识别故障。
*   **关键技术细节**：
    *   **规范提取**：定义MAS规范包含四个核心部分（智能体关系、终止模式、任务执行、工具调用）。利用Specification Agent从源代码（提示词、配置文件等）中提取结构化规范。
    *   **行为空间建模**：构建两个正交维度的行为空间：
        *   **智能体内行为空间**：基于提示词和工具描述分割原子任务，并包含异常行为（如空输出、死循环）。
        *   **执行路径空间**：枚举智能体间所有合法的交互序列，区分工作流模式和自由交互模式。
    *   **覆盖率引导模糊测试**：
        *   **种子变异**：设计了针对MAS特性的变异策略，包括配置变异（调整温度、模型类型）和序列变异（调整智能体初始顺序）。
        *   **覆盖率反馈**：定义了智能体内行为覆盖率（AAC）和智能体间行为覆盖率（RAC），利用LLM将执行日志语义映射到行为ID，以此引导测试方向。
    *   **故障识别**：构建测试预言机检测四类故障（任务执行、工具调用、智能体关系、系统终止）。采用Failure Agent和Judge Agent的双智能体交互机制，对日志进行语义分析并交叉验证，减少幻觉误报。

## 3. 实验设计
*   **数据集/场景**：基于最流行的MAS开发框架AutoGen，从GitHub选取了16个开源MAS应用，涵盖报告生成、数据分析、角色扮演、代码生成、视频制作等多种任务场景。
*   **Benchmark**：由于该领域缺乏现成基准，作者自建了包含上述16个应用的测试基准。
*   **对比方法**：对比了三种基线方法：
    *   **LLM-Fuzzer**：针对LLM安全的模糊测试工具，适配为变异用户输入。
    *   **PythonFuzz**：传统的覆盖率引导模糊测试工具（AFL移植版），关注代码覆盖。
    *   **Frelatage**：针对Python结构化数据的覆盖率模糊测试工具。

## 4. 资源与算力
*   论文中未明确说明具体的GPU型号、数量或训练时长。实验主要涉及LLM推理调用（后端使用GPT-4.1和Gemini-3.0-Pro），未提及大规模模型训练过程。

## 5. 实验数量与充分性
*   **实验数量**：
    *   主实验：在16个应用上评估覆盖率与故障发现能力。
    *   规范准确性评估：人工评估64个规范组件，计算Kappa系数。
    *   消融/对比实验：对比不同LLM后端（GPT-4.1 vs Gemini-3.0-Pro）的影响。
*   **充分性与客观性**：
    *   **充分**：实验涵盖了多种应用类型和评估指标（覆盖率、故障数、规范准确性）。
    *   **客观**：采用双盲人工评估验证规范提取的准确性；对比了不同类型的基线方法以证明方法的有效性。
    *   **公平**：虽然基线方法并非专为MAS设计，但作者进行了合理适配，并明确指出了基线在语义理解上的局限性，对比结果具有说服力。

## 6. 论文的主要结论与发现
*   **覆盖率提升**：FLARE实现了96.9%的智能体间覆盖率（RAC）和91.1%的智能体内覆盖率（AAC），显著优于基线方法（分别提升9.5%和1.0%）。
*   **故障发现**：成功发现了61个独特的故障，其中56个为MAS特有的功能故障（如任务偏离、死循环），而基线主要只能发现少量崩溃。
*   **故障类型分析**：揭示了MAS的主要故障根源，包括提示词偏离、工具执行错误、违反交互顺序以及终止条件违规等。
*   **模型鲁棒性**：FLARE在不同LLM后端下表现稳定，证明了其方法不依赖于特定模型的先验知识。

## 7. 优点
*   **创新性强**：首次提出了针对MAS的功能测试框架，定义了全新的MAS覆盖率指标（AAC, RAC）。
*   **自动化程度高**：实现了从规范提取、测试生成到故障判断的全流程自动化，有效利用LLM解决了语义理解难题。
*   **针对性强**：变异策略和预言机设计紧扣MAS的交互特性和非确定性特点，而非简单移植传统测试方法。
*   **验证机制完善**：双智能体验证机制有效降低了LLM生成规范和判断故障时的幻觉风险。

## 8. 不足与局限
*   **外部效度限制**：基准测试仅包含16个开源应用，可能无法完全覆盖工业级复杂系统的特性。
*   **框架依赖**：当前实现主要针对AutoGen框架，迁移到其他框架（如LangChain）需要调整领域知识和提示词模板。
*   **LLM依赖风险**：方法的有效性依赖于后端LLM的推理能力，虽然实验显示GPT和Gemini表现接近，但在较弱模型上的效果未验证。
*   **规范准确性**：虽然准确率很高，但仍存在少量规范提取错误（64个中有3个偏差），完全自动化仍需人工介入校验。

（完）

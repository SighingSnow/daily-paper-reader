---
title: "Squeez: Task-Conditioned Tool-Output Pruning for Coding Agents"
title_zh: Squeez：面向代码智能体的任务条件化工具输出剪枝
authors: Ádám Kovács
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04979v1"
tags: ["query:agent"]
score: 9.0
evidence: 代码智能体的工具输出剪枝
tldr: "编码智能体常处理冗长的工具输出，其中大部分内容无关。本文提出Squeez方法，通过任务条件化剪枝提取关键证据块。作者构建了包含上万个示例的基准，并微调Qwen 3.5 2B模型。实验显示，该方法在移除92%输入token的同时保持了高召回率，显著优于大型零样本模型和启发式基线，有效提升了智能体效率。"
selection_source: fresh_fetch
motivation: 编码智能体在处理长工具输出时效率低下，因为只有极少部分内容对下一步决策有用。
method: 提出任务条件化剪枝任务，构建大规模基准数据集，并利用LoRA微调Qwen 3.5 2B模型以提取最小证据块。
result: "模型在移除92%输入token的情况下达到0.86召回率和0.80 F1分数，显著优于大型零样本模型和启发式基线。"
conclusion: 该方法能高效压缩工具输出，大幅减少无关信息干扰，显著提升编码智能体的处理效率。
---

## 摘要
代码智能体反复处理冗长的工具观测结果，尽管其中仅有一小部分对下一步至关重要。我们研究了任务条件化的工具输出剪枝：给定一个聚焦查询和一个工具输出，返回智能体下一步应检查的最小逐字证据块。我们引入了一个包含 11,477 个示例的基准测试，该基准基于 SWE-bench 仓库交互和多生态系统合成工具输出构建，并包含一个 618 个示例的人工精选测试集。我们使用 LoRA 微调了 Qwen 3.5 2B 模型，并将其与更大的零样本模型及启发式剪枝基线进行了比较。我们的模型在移除 92% 输入 token 的同时达到了 0.86 的召回率和 0.80 的 F1 值，其召回率高出零样本 Qwen 3.5 35B A3B 11 个百分点，并显著优于所有启发式基线。

## Abstract
Coding agents repeatedly consume long tool observations even though only a small fraction of each observation matters for the next step. We study task-conditioned tool-output pruning: given a focused query and one tool output, return the smallest verbatim evidence block the agent should inspect next. We introduce a benchmark of 11,477 examples built from SWE-bench repository interactions and synthetic multi-ecosystem tool outputs, with a manually curated 618-example test set. We fine-tune Qwen 3.5 2B with LoRA and compare it against larger zero-shot models and heuristic pruning baselines. Our model reaches 0.86 recall and 0.80 F1 while removing 92% of input tokens, outperforming zero-shot Qwen 3.5 35B A3B by 11 recall points and all heuristic baselines by a wide margin.

---

## 论文详细总结（自动生成）

# 论文总结：Squeez: Task-Conditioned Tool-Output Pruning for Coding Agents

## 1. 论文的核心问题与整体含义
*   **研究背景**：代码智能体在执行任务时，需要处理大量的工具输出（如文件读取、grep 结果、堆栈跟踪、构建日志等）。这些输出通常非常冗长，但其中只有极小一部分内容对智能体的下一步决策真正有用。
*   **核心问题**：如何从冗长的、混合格式的工具输出中，高效地提取出与当前任务查询相关的最小证据块，以减少上下文噪音，提升智能体效率。
*   **整体含义**：现有的通用提示词压缩或文档检索方法不适用于混合格式的工具输出。本文提出了一种“任务条件化工具输出剪枝”任务，旨在通过轻量级模型在智能体读取输出前进行预处理，保留关键信息，大幅降低 Token 消耗。

## 2. 论文提出的方法论
*   **核心思想**：将剪枝问题定义为“任务条件化的证据提取”。给定一个聚焦查询和一段原始工具输出，模型需要返回一个或多个连续的文本片段，这些片段是智能体下一步应检查的最小逐字证据。
*   **模型架构**：选用 **Qwen 3.5 2B** 作为基础模型，使用 **LoRA** 进行微调。选择 2B 小模型是为了在智能体工作流中实现低成本、高效率的部署。
*   **算法流程**：
    1.  **输入构建**：将“查询”与“原始工具输出”拼接作为输入。
    2.  **训练目标**：模型被训练生成被 `<relevant lines>` 标签包裹的逐字提取文本。
    3.  **数据构建**：利用大型模型作为教师，首先根据背景任务编写具体的提取查询，然后在编号的工具输出中标注相关行范围，最后将标注映射回原始文本。
    4.  **负样本处理**：专门构建了不匹配的查询-输出对，训练模型在无相关内容时输出空结果，避免幻觉。

## 3. 实验设计
*   **数据集**：
    *   **来源**：包含 11,477 个示例，分为两部分：(1) **SWE-bench 衍生数据**（9,205 条），来自真实 Python 仓库交互；(2) **合成多生态系统数据**（1,697 条正例 + 575 条负例），覆盖 TypeScript, Go, Rust, Java, Docker, K8s 等环境。
    *   **测试集**：包含 618 个人工精选的高质量示例，确保评估的准确性。
*   **Benchmark**：自建 Benchmark，涵盖 27 种工具类型（如 read file, grep, git log, pytest, docker build 等）。
*   **对比方法**：
    *   **生成式基线**：Qwen 3.5 35B A3B（零样本）、Kimi K2（零样本）、Qwen 3.5 2B Base（未微调）。
    *   **启发式基线**：BM25、First-N（保留前 10%）、Last-N（保留后 10%）、Random（随机保留 10%）。
*   **评估指标**：主要关注 **Recall（召回率）** 和 **F1 分数**，同时报告压缩率。F1 计算采用了模糊匹配策略以容忍微小的格式差异。

## 4. 资源与算力
*   **硬件资源**：使用 **单块 NVIDIA A100 80GB** GPU 进行训练。
*   **训练配置**：训练了 3 个 Epoch，最大序列长度设为 20,000，Batch Size 为 8（梯度累积为 4），学习率为 2e-4。
*   **推理部署**：模型可通过 vLLM 服务部署，或作为 CLI 工具直接处理管道输出。

## 5. 实验数量与充分性
*   **实验规模**：主要实验在一个 618 样本的测试集上进行，对比了 7 种基线方法。此外，还进行了定性分析（展示具体案例）和错误分析。
*   **充分性与客观性**：
    *   **数据多样性**：数据集覆盖了真实开发场景中的多种工具和编程语言，不仅限于 Python，具有较好的代表性。
    *   **评估严谨性**：测试集经过人工筛选，排除了重复或标注错误的样本。
    *   **对比公平性**：所有生成式模型使用相同的提示词，启发式基线调整参数以保持相近的压缩率（约 10% 保留率），对比维度清晰。
    *   **不足**：未进行详细的消融实验（如不同 LoRA rank 或基础模型大小的对比），主要聚焦于与大型零样本模型的性能对比。

## 6. 论文的主要结论与发现
*   **性能表现**：微调后的 Squeez-2B 模型在移除 **92%** 输入 Token 的情况下，达到了 **0.86 的召回率**和 **0.80 的 F1 值**。
*   **对比优势**：
    *   相比零样本的 Qwen 3.5 35B A3B，召回率高出 **11 个百分点**。
    *   相比启发式方法（如 BM25），优势巨大（BM25 召回率仅 0.22）。
*   **关键发现**：
    *   大型零样本模型容易在重复的日志或 Git 历史中选择语义相邻但错误的块，或者无法正确输出“空结果”。
    *   小模型通过特定任务的监督学习，可以学会精准的结构化提取策略，比通用大模型更高效、更准确。

## 7. 优点
*   **高效实用**：仅需 2B 参数即可超越 35B 模型，且能去除 90% 以上的噪音，极大降低了下游智能体的上下文负担。
*   **任务针对性**：专注于混合格式的工具输出（代码、日志、Shell 混合），填补了通用文本压缩方法在智能体场景下的空白。
*   **数据贡献**：构建并开源了包含 1.1 万+ 样例、覆盖 27 种工具的高质量基准数据集，推动了该领域的研究。
*   **易于集成**：提供了 CLI 工具和 vLLM 服务方案，可无缝插入现有的智能体架构中。

## 8. 不足与局限
*   **评估范围**：仅评估了单步工具输出的剪枝质量，未验证其对端到端任务完成率（如 Bug 修复成功率）的实际影响。
*   **指标局限**：使用 Token 重叠度作为指标，可能无法完全捕捉“有用性”（例如，提取出的证据虽然重叠度高但不足以解决问题）。
*   **数据偏差**：部分工具类型（如 grep 和 lint 输出）在数据集中仍存在噪声。
*   **依赖教师模型**：数据构建依赖 GPT-OSS-120B 生成查询和标注，可能继承教师模型的偏差或错误。

（完）

---
title: "StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing"
title_zh: StarVLA：一个用于视觉-语言-动作模型开发的类乐高式代码库
authors: StarVLA Community
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05014v1"
tags: ["query:agent"]
score: 9.0
evidence: 视觉-语言-动作模型开发框架
tldr: 针对视觉-语言-动作（VLA）领域架构和评估协议碎片化的问题，本文提出了StarVLA，一个类似乐高积木的开源代码库。它采用模块化架构支持多种主干网络与动作头，提供统一的训练策略与评估接口，涵盖主流仿真基准与真机部署。实验表明，该框架在极少工程量下即可达到或超越现有方法，显著降低了VLA研究的门槛。
selection_source: fresh_fetch
motivation: 现有VLA方法因架构、代码库和评估协议不兼容而严重碎片化，阻碍了原则性比较与复现。
method: 提出StarVLA代码库，通过模块化主干-动作头架构、统一训练策略及集成多基准的评估接口实现灵活开发。
result: 该框架提供的简单训练配方在多个基准测试中仅需极少数据工程即可匹配或超越现有方法。
conclusion: StarVLA作为目前最全面的开源VLA框架之一，有效降低了复现现有方法与开发新原型的门槛。
---

## 摘要
构建通用具身智能体需要整合感知、语言理解和动作，这是基于多模态基础模型（包括视觉-语言模型和世界模型的最新进展）的视觉-语言-动作 (VLA) 方法所解决的核心能力。尽管进展迅速，VLA 方法在架构、代码库和评估协议方面仍然存在碎片化问题，阻碍了系统性的比较和可复现性。我们提出了 StarVLA，一个用于 VLA 研究的开源代码库。StarVLA 从三个方面解决了这些挑战。首先，它提供了一种模块化的骨干网络-动作头架构，支持 VLM 骨干网络（如 Qwen-VL）和世界模型骨干网络（如 Cosmos）以及代表性的动作解码范式，所有这些均在共享抽象下实现，骨干网络和动作头均可独立替换。其次，它提供了可复用的训练策略，包括跨具身学习和多模态协同训练，这些策略在支持的范式中均可一致应用。第三，它通过统一的评估接口集成了主要基准测试，包括 LIBERO、SimplerEnv、RoboTwin 2.0、RoboCasa-GR1 和 BEHAVIOR-1K，支持仿真和真实机器人部署。StarVLA 还提供了简单、完全可复现的单一基准测试训练方案，尽管数据工程量极少，但在使用 VLM 和世界模型骨干网络的多个基准测试中，其表现已媲美或超越了现有方法。据我们所知，StarVLA 是目前最全面的开源 VLA 框架之一，我们期望它能降低复现现有方法和开发新方法原型的门槛。StarVLA 正处于积极维护和扩展中；随着项目的演进，我们将更新本报告。代码和文档可在 https://github.com/starVLA/starVLA 获取。

## Abstract
Building generalist embodied agents requires integrating perception, language understanding, and action, which are core capabilities addressed by Vision-Language-Action (VLA) approaches based on multimodal foundation models, including recent advances in vision-language models and world models. Despite rapid progress, VLA methods remain fragmented across incompatible architectures, codebases, and evaluation protocols, hindering principled comparison and reproducibility. We present StarVLA, an open-source codebase for VLA research. StarVLA addresses these challenges in three aspects. First, it provides a modular backbone--action-head architecture that supports both VLM backbones (e.g., Qwen-VL) and world-model backbones (e.g., Cosmos) alongside representative action-decoding paradigms, all under a shared abstraction in which backbone and action head can each be swapped independently. Second, it provides reusable training strategies, including cross-embodiment learning and multimodal co-training, that apply consistently across supported paradigms. Third, it integrates major benchmarks, including LIBERO, SimplerEnv, RoboTwin~2.0, RoboCasa-GR1, and BEHAVIOR-1K, through a unified evaluation interface that supports both simulation and real-robot deployment. StarVLA also ships simple, fully reproducible single-benchmark training recipes that, despite minimal data engineering, already match or surpass prior methods on multiple benchmarks with both VLM and world-model backbones. To our best knowledge, StarVLA is one of the most comprehensive open-source VLA frameworks available, and we expect it to lower the barrier for reproducing existing methods and prototyping new ones. StarVLA is being actively maintained and expanded; we will update this report as the project evolves. The code and documentation are available at https://github.com/starVLA/starVLA.

---

## 论文详细总结（自动生成）

以下是对论文《StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing》的详细总结：

## 1. 论文的核心问题与整体含义

*   **研究背景**：构建通用具身智能体需要整合感知、语言理解和动作能力。尽管基于多模态基础模型的视觉-语言-动作（VLA）方法取得了快速进展，但该领域目前面临严重的碎片化问题。
*   **核心问题**：现有的 VLA 方法在架构设计、代码库实现和评估协议上互不兼容，导致难以进行原则性的比较和复现。这种碎片化阻碍了研究者理解不同设计选择之间的权衡，也限制了新方法的快速原型开发。
*   **整体含义**：为了解决上述问题，论文提出了 StarVLA，一个统一、模块化、开源的 VLA 研究框架。其核心意义在于提供标准化的抽象和接口，将 VLM-based 和 World-model-based 两大流派纳入同一框架，降低研究门槛，促进 VLA 领域的系统性探索。

## 2. 论文提出的方法论

*   **核心思想**：采用“乐高积木”式的模块化设计理念，将 VLA 系统解耦为“骨干网络”和“动作头”两个独立组件，通过统一的接口契约进行连接。
*   **关键技术细节**：
    *   **统一策略视角**：将 VLA 系统建模为一个策略 $\pi$，输入为多模态观测历史和语言指令，输出为动作序列和可选的辅助输出。训练目标统一为 $L = L_{action} + L_{aux}$，不同的范式对应不同的辅助损失。
    *   **模块化架构**：
        *   **骨干网络**：支持 VLM（如 Qwen-VL）和世界模型（如 Cosmos-Predict2）作为即插即用的主干，负责编码场景和指令。
        *   **动作头**：实现了四种代表性的动作解码范式，均可独立替换：
            *   **StarVLA-FAST**：自回归离散动作标记化。
            *   **StarVLA-OFT**：并行连续动作回归。
            *   **StarVLA-$\pi$**：基于流匹配的去噪生成。
            *   **StarVLA-GR00T**：双系统推理架构。
    *   **统一 I/O 接口**：定义了标准化的 `forward` 和 `predict_action` 接口，输入为原始环境观测数据，输出为动作，消除了预处理流程的差异。
    *   **训练与评估流程**：
        *   支持监督学习、多模态协同训练和跨具身协同训练。
        *   采用服务器-客户端架构进行评估，模型作为服务端，基准测试环境作为客户端，实现了仿真与真机部署的统一接口。

## 3. 实验设计

*   **数据集与场景**：集成了 5 个主流仿真基准测试，涵盖桌面操作、双臂协作和长时序任务：
    *   **LIBERO**：语言条件下的桌面操作，包含空间、物体、目标和长时序四个套件。
    *   **SimplerEnv**：作为真实机器人评估代理的仿真环境，包含 WidowX 和 Google Robot 设置。
    *   **RoboTwin 2.0**：大规模双臂操作基准，包含 50 个任务。
    *   **RoboCasa-GR1**：人形机器人桌面操作任务。
    *   **BEHAVIOR-1K**：以人为中心的日常活动基准。
*   **对比方法**：与当前主流的 VLA 模型进行了对比，包括 OpenVLA, OpenVLA-OFT, $\pi_0$, $\pi_{0.5}$, GR00T N1.5/N1.6, RT-1, RT-2, Octo, CogAct, SpatialVLA, Magma 等。
*   **实验设置**：
    *   **单基准训练**：在每个基准上独立训练专家模型，提供可复现的基线。
    *   **多模态协同训练**：混合机器人数据与视觉-语言数据（如 RefCOCO），验证对感知能力的保持。
    *   **跨基准通用模型**：在所有基准数据集上联合训练一个通用模型，测试泛化能力。

## 4. 资源与算力

*   **硬件配置**：实验主要使用 NVIDIA A100 (80GB) GPU。
*   **具体算力消耗**：
    *   **LIBERO 实验**：使用 8 张 A100 GPU，总批次大小为 128，训练 30K 步。
    *   **SimplerEnv 实验**：使用 16 张 A100 GPU，总批次大小为 256，训练 100K 步。
    *   **RoboCasa-GR1 实验**：使用 16 张 A100 GPU，训练 100K 步。
    *   **RoboTwin 2.0 实验**：使用 48 张 A100 GPU，总批次大小为 192，训练 150K 步。
*   **扩展性测试**：进行了多节点扩展效率测试，测试规模从 8 GPU 扩展至 256 GPU，展示了在 256 GPU 规模下仍能保持约 79% 的并行效率。

## 5. 实验数量与充分性

*   **实验数量**：实验涵盖了 5 个主要基准测试，每个基准下测试了 4 种不同的架构变体（FAST, OFT, $\pi$, GR00T），并对比了至少 5-10 种现有方法。此外，还进行了多模态协同训练和跨基准训练的额外实验。
*   **充分性与客观性**：
    *   **充分性**：实验设计非常全面，不仅验证了不同架构在不同任务上的表现，还验证了框架在不同骨干网络（VLM vs World Model）下的通用性。算力效率分析也为用户提供了实用的扩展指南。
    *   **客观性**：论文提供了完全可复现的训练配方和评估脚本，且在对比中明确指出了是否为自身复现还是引用原论文数据。单基准训练中避免了复杂的预训练技巧，提供了干净的基线。

## 6. 论文的主要结论与发现

*   **框架有效性**：StarVLA 能够以极少的数据工程量，在多个基准测试上达到或超越现有 SOTA 方法的性能。例如在 LIBERO 上，StarVLA-OFT 仅用 30K 步（约 10 个 Epoch）即达到了 96.6% 的平均成功率，优于 OpenVLA-OFT 的 97.1%（需 175K 步）。
*   **架构权衡**：不同的动作头在不同任务上表现各异。例如，在 RoboCasa-GR1 上，连续动作变体（OFT, $\pi$, GR00T）显著优于离散动作变体（FAST）。
*   **协同训练的重要性**：纯动作训练会导致 VLM 骨干网络的感知能力迅速退化（灾难性遗忘）。引入多模态协同训练（如混合 RefCOCO 数据）能有效保持空间感知能力，从而提升操作成功率。
*   **通用模型可行性**：跨基准联合训练的通用模型在大多数基准上表现出了与专家模型相当甚至更好的性能，证明了统一框架下训练通用 VLA 模型的潜力。

## 7. 优点

*   **高度模块化**：实现了骨干网络与动作头的解耦，支持像搭积木一样灵活组合不同组件。
*   **统一标准**：提供了统一的 I/O 接口和评估协议，解决了领域内的碎片化问题，极大降低了复现和比较的难度。
*   **全面集成**：集成了当前主流的 VLM 和 World Model 骨干，以及多种动作解码范式，覆盖了 VLA 领域的主要技术路线。
*   **开源与可复现**：提供了详细的训练配方、代码和文档，且实验设置透明，为社区提供了强有力的基线工具。

## 8. 不足与局限

*   **真机实验细节较少**：虽然框架支持真机部署，但论文主要展示的是仿真环境下的结果，真实物理环境下的验证细节相对较少。
*   **强化学习部分未完成**：论文提到强化学习（RL）微调功能正在集成中，尚未在当前版本中展示相关实验结果。
*   **对比方法的公平性细节**：虽然尽力保持公平，但不同方法可能基于不同的预训练数据或超参数设置，完全的“苹果对苹果”比较在多模型集成中仍存在挑战。
*   **部分基准性能波动**：在 SimplerEnv 等基准上，不同方法的性能差异受随机性影响较大，且某些任务（如物体放置）对所有方法仍具挑战性。

（完）

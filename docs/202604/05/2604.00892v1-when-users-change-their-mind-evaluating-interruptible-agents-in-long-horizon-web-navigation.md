---
title: "When Users Change Their Mind: Evaluating Interruptible Agents in Long-Horizon Web Navigation"
title_zh: 当用户改变主意：长程网页导航中可中断智能体的评估
authors: "Henry Peng Zou, Chunyu Miao, Wei-Chieh Huang, Yankai Chen, Yue Zhou, Hanrong Zhang, Yaozu Wu, Liancheng Fang, Zhengyao Gu, Zhen Zhang, Kening Zheng, Fangxin Wang, Yi Nian, Shanghao Li, Wenzhe Fan, Langzhou He, Weizhi Zhang, Xue Liu, Philip S. Yu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00892v1"
tags: ["query:agent"]
score: 8.0
evidence: 处理中断的LLM网页导航智能体
tldr: 随着LLM智能体转向长周期任务，处理用户中途改变需求的中断至关重要。本文首次系统研究长周期Web导航中的可中断智能体，形式化了三种中断类型并推出InterruptBench基准。实验显示，现有强大LLM在适应意图更新和恢复任务方面仍面临巨大挑战。
selection_source: fresh_fetch
motivation: 现有基准假设智能体行为不中断，无法满足实际部署中用户在长周期任务执行期间修改需求或目标的现实要求。
method: 作者形式化了三种中断类型，构建了基于WebArena-Lite的InterruptBench基准，并在统一框架下评估了六种LLM的表现。
result: 评估结果显示，即使是强大的大规模LLM，在长周期任务中有效且高效地适应用户中断仍然非常困难。
conclusion: 在长周期智能体任务中有效处理用户中断仍是当前LLM面临的重大挑战，亟需进一步研究。
---

## 摘要
随着 LLM 智能体从短程、静态的问题求解过渡到在动态环境中执行复杂的长程任务，在任务执行过程中处理用户中断（例如添加需求或修改目标）的能力，正成为实际部署的核心要求。然而，现有的基准测试大多假设智能体行为是不间断的，或者仅在短程、无约束的语言任务中研究中断。本文首次对长程、环境落地的网页导航任务中的可中断智能体进行了系统研究，在这些任务中，动作会引起持久的状态变化。我们形式化定义了三种现实的中断类型，包括添加、修改和撤回，并介绍了 InterruptBench，这是一个源自 WebArena-Lite 的基准测试，它在严格的语义约束下合成了高质量的中断场景。利用统一的中断模拟框架，我们在单轮和多轮中断设置下评估了六种强大的 LLM 骨干网络，分析了它们适应更新意图的有效性以及从任务中途变更中恢复的效率。我们的结果表明，对于强大的大规模 LLM 而言，在长程智能体任务中有效且高效地处理用户中断仍然具有挑战性。代码和数据集可在 https://github.com/HenryPengZou/InterruptBench 获取。

## Abstract
As LLM agents transition from short, static problem solving to executing complex, long-horizon tasks in dynamic environments, the ability to handle user interruptions, such as adding requirement or revising goals, during mid-task execution is becoming a core requirement for realistic deployment. However, existing benchmarks largely assume uninterrupted agent behavior or study interruptions only in short, unconstrained language tasks. In this paper, we present the first systematic study of interruptible agents in long-horizon, environmentally grounded web navigation tasks, where actions induce persistent state changes. We formalize three realistic interruption types, including addition, revision, and retraction, and introduce InterruptBench, a benchmark derived from WebArena-Lite that synthesizes high-quality interruption scenarios under strict semantic constraints. Using a unified interruption simulation framework, we evaluate six strong LLM backbones across single- and multi-turn interruption settings, analyzing both their effectiveness in adapting to updated intents and their efficiency in recovering from mid-task changes. Our results show that handling user interruptions effectively and efficiently during long-horizon agentic tasks remains challenging for powerful large-scale LLMs. Code and dataset are available at https://github.com/HenryPengZou/InterruptBench.

---

## 论文详细总结（自动生成）

# 论文总结：当用户改变主意：长程网页导航中可中断智能体的评估

## 1. 论文的核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）智能体从短程、静态任务转向长程、动态环境下的复杂任务执行，用户在任务中途改变主意（如添加需求、修正错误、撤回指令）成为现实部署中的常态。
*   **核心问题**：现有的基准测试大多假设智能体执行过程是不间断的，或者仅在短程、无环境约束的语言任务中研究中断，缺乏对长程、环境落地任务中智能体处理用户中断能力的系统评估。
*   **整体含义**：本文旨在填补这一空白，首次系统研究了在动作会引起持久状态变化的网页导航任务中，智能体如何应对用户的中途干预，揭示了当前LLM在动态意图适应方面的局限性。

## 2. 论文提出的方法论
*   **核心思想**：构建一个基于轨迹的中断模拟与评估框架，形式化定义三种中断类型，并在真实网页环境中评估智能体的适应能力。
*   **中断类型定义**：
    *   **添加**：用户初始查询不完整，后续补充新需求。
    *   **修改**：用户初始查询有误，后续发出修正指令。
    *   **撤回**：用户初始查询包含多余约束，后续撤回部分细节。
*   **数据合成流程**：
    *   基于WebArena-Lite任务，利用LLM（Claude-Opus-4.5）合成初始查询和中断消息。
    *   **严格约束**：保证合成数据与原任务真值一致、每条中断消息必不可少、语气自然真实。
*   **轨迹落地模拟**：
    *   首先生成无中断的完整轨迹。
    *   在轨迹执行的特定进度点（默认为60%）注入中断消息。
    *   智能体需基于当前环境状态和更新后的意图继续执行，支持单轮和多轮中断设置。

## 3. 实验设计
*   **数据集与场景**：基于 **WebArena-Lite** 构建，包含165个人工验证的任务，覆盖社交媒体、协作编程、电商CMS、地图、在线购物5个领域。
*   **Benchmark**：提出了 **InterruptBench**，这是首个针对长程、环境落地任务中可中断智能体的基准测试。
*   **对比方法（模型）**：在WebAgent-R1脚手架下评估了6种主流LLM：
    *   闭源模型：Claude-Haiku-4.5, Claude-Sonnet-4.5, Claude-Opus-4.5。
    *   开源/开放权重模型：Qwen3-235B-A22B, DeepSeek-V3.1, Mistral-Large-3。
*   **评估指标**：
    *   **成功率（SR）**：任务是否完成。
    *   **预算受限成功率 SR(k)**：在接收中断后的k步动作内成功的概率。
    *   **效率**：Token消耗量和动作数量。
    *   **多轮适应性**：连续多次中断下的表现。

## 4. 资源与算力
*   **算力说明**：文中未明确提及具体的GPU型号、数量或训练时长。由于本研究主要侧重于基准构建和推理评估，未涉及模型训练过程，因此未列出大规模算力消耗细节。

## 5. 实验数量与充分性
*   **实验规模**：
    *   覆盖了3种中断类型（添加、修改、撤回）及多轮中断场景。
    *   评估了6个模型在单轮和多轮设置下的表现。
    *   进行了消融实验，分析了不同中断位置（20%, 40%, 60%, 80%）对性能的影响。
    *   详细统计了成对结果（如基线成功但中断失败S/F、基线失败但中断成功F/S等）及效率开销。
*   **充分性与公平性**：实验设计较为充分，不仅关注最终成功率，还深入分析了适应效率（Token/动作开销）和失败模式。所有模型均在统一的WebAgent-R1框架和相同的提示格式下进行测试，保证了对比的公平性。

## 6. 论文的主要结论与发现
*   **挑战巨大**：即使是强大的大规模LLM，在长程任务中有效且高效地处理用户中断仍极具挑战性。
*   **适应性差异**：
    *   在“修改”场景中，若不接收中断信息，基线成功率接近零，说明必须正确整合新信息。
    *   在“撤回”场景中，基线成功率较高，中断主要帮助提前完成任务。
*   **效率问题**：处理中断的主要开销在于Token消耗（思考成本）而非动作数量。失败案例（F/F）往往消耗更多资源，表现出“无效的坚持”。
*   **多轮中断表现**：更多的中断信息通常有助于提高成功率，但开源模型表现不稳定，甚至出现性能倒退。
*   **失败模式**：智能体常未能协调UI状态与更新后的意图（如未清除旧输入、未重新计算路径），导致结果基于过时状态。

## 7. 优点
*   **创新性**：首个针对长程、环境落地任务中可中断智能体的系统研究，填补了重要空白。
*   **现实意义**：定义的中断类型（添加、修改、撤回）高度符合真实人机交互场景。
*   **评估体系完善**：提出了轨迹落地的模拟方法和多维度的评估指标（成功率、预算受限成功率、效率），不仅看结果还看过程开销。
*   **深入分析**：通过成对结果分析（S/F, F/S等）揭示了模型在恢复能力和抗干扰能力上的差异。

## 8. 不足与局限
*   **场景局限**：实验仅限于WebArena环境，未涉及其他领域（如操作系统控制、代码生成等）。
*   **数据合成**：中断数据由LLM合成，尽管有人工审核，但仍可能与真实用户的自发行为存在细微差异。
*   **缺乏改进方案**：本文主要作为评估和诊断研究，未提出具体的算法改进或训练策略来解决发现的问题。
*   **模型范围**：虽然涵盖了主流模型，但随着新模型发布速度极快，评估列表可能很快需要更新。

（完）

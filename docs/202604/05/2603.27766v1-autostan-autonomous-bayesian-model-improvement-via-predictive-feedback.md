---
title: "AutoStan: Autonomous Bayesian Model Improvement via Predictive Feedback"
title_zh: AutoStan：基于预测反馈的自主贝叶斯模型改进
authors: Oliver Dürr
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27766v1"
tags: ["query:agent"]
score: 9.0
evidence: CLI代码智能体自主构建并改进贝叶斯模型
tldr: 本文提出了AutoStan框架，这是一个命令行编码智能体，能够自主构建并迭代改进Stan贝叶斯模型。该系统通过编写代码、执行MCMC采样，并利用保留数据的预测密度和采样器诊断作为反馈信号来优化模型。实验表明，AutoStan在多个数据集上成功发现了复杂的模型结构，性能媲美甚至超越SOTA黑盒方法，且保持可解释性，是首个实现CLI智能体自主改进贝叶斯模型的研究。
selection_source: fresh_fetch
motivation: 旨在探索CLI编码智能体是否能自主编写并迭代改进贝叶斯模型代码，无需特定领域指令或复杂的搜索算法。
method: 智能体通过循环编写Stan代码并执行MCMC采样，利用保留数据的负对数预测密度和采样器诊断作为反馈信号来决定模型的迭代方向。
result: 在五个数据集的测试中，AutoStan成功发现了稳健回归、分层模型等复杂结构，性能匹配或超越SOTA黑盒方法且保持可解释性。
conclusion: 这是首个证明CLI编码智能体无需搜索算法或特定指令即可自主编写并改进Stan代码的研究。
---

## 摘要
我们提出了AutoStan，这是一个框架，其中命令行界面（CLI）代码智能体能够自主构建并迭代改进用Stan编写的贝叶斯模型。该智能体在循环中运行，编写Stan模型文件，执行MCMC采样，然后基于两个互补的反馈信号决定保留还是回滚每次更改：留出数据上的负对数预测密度（NLPD）以及采样器自身的诊断信息（发散、R-hat、有效样本大小）。我们在五个具有不同建模结构的数据集上评估了AutoStan。在一个带有异常值的合成回归数据集上，该智能体从朴素线性回归逐步发展为具有Student-t鲁棒性、非线性异方差结构和显式污染混合的模型，其表现匹配或优于最先进的黑盒方法TabPFN，同时保持完全可解释性。在另外四个实验中，相同的机制发现了分层部分池化、具有相关随机效应的变斜率模型以及足球的泊松攻防模型。该过程不需要搜索算法、评判模块或特定领域的指令。据我们所知，这是首个证明CLI代码智能体能够针对各种贝叶斯建模问题自主编写并迭代改进Stan代码的研究。

## Abstract
We present AutoStan, a framework in which a command-line interface (CLI) coding agent autonomously builds and iteratively improves Bayesian models written in Stan. The agent operates in a loop, writing a Stan model file, executing MCMC sampling, then deciding whether to keep or revert each change based on two complementary feedback signals: the negative log predictive density (NLPD) on held-out data and the sampler's own diagnostics (divergences, R-hat, effective sample size). We evaluate AutoStan on five datasets with diverse modeling structures. On a synthetic regression dataset with outliers, the agent progresses from naive linear regression to a model with Student-t robustness, nonlinear heteroscedastic structure, and an explicit contamination mixture, matching or outperforming TabPFN, a state-of-the-art black-box method, while remaining fully interpretable. Across four additional experiments, the same mechanism discovers hierarchical partial pooling, varying-slope models with correlated random effects, and a Poisson attack/defense model for soccer. No search algorithm, critic module, or domain-specific instructions are needed. This is, to our knowledge, the first demonstration that a CLI coding agent can autonomously write and iteratively improve Stan code for diverse Bayesian modeling problems.

---

## 论文详细总结（自动生成）

以下是对论文《AutoStan: Autonomous Bayesian Model Improvement via Predictive Feedback》的详细总结：

### 1. 论文的核心问题与整体含义

*   **研究背景**：贝叶斯建模虽然在理论上简洁优雅，但在实际操作中（如使用Stan）非常繁琐。从业者需要反复监控MCMC诊断信息（如发散、R-hat）、调整先验、重参数化模型，这是一个需要大量人工干预的“保姆式”过程。
*   **核心问题**：现代的命令行界面（CLI）编码智能体能否自动化这一繁琐的循环？能否仅凭单一的标量奖励信号和采样器诊断，自主编写并迭代改进贝叶斯模型代码？
*   **研究动机**：受Karpathy的`autoresearch`项目启发，作者试图将“通过验证集反馈自主优化代码”的思想引入贝叶斯建模领域。由于贝叶斯推断拥有NLPD（负对数预测密度）这种原则性的、模型无关的评价指标，使其成为自主优化的理想场景。

### 2. 论文提出的方法论

*   **核心思想**：构建一个名为**AutoStan**的框架，利用通用的CLI编码智能体（如Claude Code）在循环中自主编写、修改Stan模型文件。该框架不依赖特定的搜索算法或领域专家模块，而是依靠反馈信号驱动。
*   **反馈信号**：
    1.  **NLPD (Negative Log Predictive Density)**：基于留出测试集计算，作为主要的奖励信号。NLPD是严格的评分规则，能惩罚预测错误和不确定性校准不良，引导模型生成校准良好的预测分布。
    2.  **MCMC诊断信息**：包括发散、R-hat、有效样本大小（ESS）。这些信号不直接用于评分，但告知智能体模型是否“损坏”（如混合不良），从而指导修复（如重参数化）。
*   **工作流程**：
    1.  **初始化**：智能体读取通用的贝叶斯工作流指令（`program.md`）和数据集描述（`dataset.md`）。
    2.  **探索与编码**：智能体自主检查训练数据统计特征，编写初始的`model.stan`文件。
    3.  **执行与评估**：智能体调用评估脚本（`evaluate.py`），该脚本在后台编译模型、运行MCMC采样并计算NLPD。测试数据通过文件权限保护，智能体不可见。
    4.  **决策与迭代**：智能体读取NLPD和诊断结果。若NLPD降低且诊断良好，则保留修改；否则回滚。智能体据此决定下一步的修改策略（如改变似然函数、增加层级结构等）。
    5.  **终止条件**：连续3次迭代无改进或总迭代次数达到20次。

### 3. 实验设计

*   **数据集**：共使用5个数据集，涵盖多种统计建模场景：
    1.  **合成回归数据集（含异常值）**：分为大数据集（n=500训练）和小数据集（n=68训练）。包含非线性均值、异方差噪声及约6%的异常值。
    2.  **分层模型（小/大）**：基于经典的Eight Schools结构生成的合成数据，变量名匿名化。
    3.  **变斜率模型**：包含相关随机截距和斜率的合成数据。
    4.  **德甲足球数据**：真实数据集，预测比赛比分，包含领域语义标签（如主队、进球数）。
*   **Benchmark与对比方法**：
    *   **Oracle（下界）**：基于真实数据生成过程构建的模型，代表理论上的最优性能。
    *   **TabPFN**：当前最先进的黑盒表格数据预测方法（基于Transformer预训练），用于回归任务的对比。
    *   **Baseline**：智能体生成的初始简单模型（如线性回归）。

### 4. 资源与算力

*   **硬件环境**：所有实验均在MacBook Pro（Apple M2 Pro, 10核CPU, 16 GB RAM）上交互运行。
*   **计算消耗**：
    *   未提及GPU使用，MCMC采样主要依赖CPU多核并行。
    *   单次迭代时间从几秒（小模型）到约50分钟（大型回归模型，需30,000次采样以降低蒙特卡洛噪声）不等。
    *   Token消耗：总共生成约220,000个输出tokens，读取约2000万个缓存tokens。

### 5. 实验数量与充分性

*   **实验数量**：共进行了6组实验（5个数据集，其中回归数据集分大小两组）。
*   **充分性与客观性**：
    *   实验覆盖了回归、分层、多级模型和计数数据等多种范式，展示了方法的通用性。
    *   使用了合成数据（可控变量、匿名化处理）和真实数据，对比了SOTA黑盒方法和Oracle下界，评价体系较为客观。
    *   **局限性**：作为一个探索性研究，样本量相对有限，未进行大规模基准测试。部分实验（如变斜率模型）中智能体发现了测试集中不存在的结构，暗示存在轻微的测试集过拟合风险。

### 6. 论文的主要结论与发现

*   **可行性验证**：这是首个证明CLI编码智能体能够自主编写并迭代改进Stan代码的研究。
*   **结构发现能力**：智能体能自主发现复杂的统计结构，包括Student-t鲁棒性处理、非线性异方差、污染混合模型、分层部分池化、相关随机效应以及足球领域的泊松攻防模型。
*   **性能表现**：在回归任务中，AutoStan生成的模型性能匹配甚至超越SOTA黑盒方法TabPFN，且生成的Stan代码具有完全的可解释性。
*   **双重反馈机制**：NLPD引导模型改进方向，MCMC诊断指导模型修复（如解决标签切换问题），两者缺一不可。
*   **自适应能力**：智能体能根据数据规模调整模型复杂度（小数据集倾向于简单模型，大数据集能识别更精细的结构），并能根据变量名是否具有语义自动选择建模策略。

### 7. 优点

*   **极简框架**：无需复杂的搜索算法、专门的评判模块或特定领域的指令，仅依靠通用智能体和评分规则即可工作。
*   **可解释性**：输出结果是标准的Stan代码，人类专家可以阅读、审核和修改，这是相比TabPFN等黑盒模型的核心优势。
*   **自主性与智能**：智能体展现了惊人的统计推理能力，如自主进行EDA、根据诊断信息修复模型病理（如固定参数解决标签切换）、进行消融实验验证假设等。

### 8. 不足与局限

*   **过拟合风险**：在固定测试集上反复迭代优化可能导致轻微过拟合（如Hierarchical Large数据集中模型表现优于Oracle），作者建议未来可通过交叉验证缓解。
*   **先验与似然的混淆**：智能体可能利用训练数据的EDA结果来设定先验，这在严格贝叶斯推断中是不规范的（先验不应依赖数据），虽然对预测任务有利，但可能影响推断的有效性。
*   **记忆污染风险**：尽管使用了匿名化合成数据，LLM仍可能从训练语料中“记忆”了类似的教科书问题，难以完全剥离纯粹的统计推理与记忆匹配。
*   **计算成本**：迭代式MCMC采样对于复杂模型耗时较长，且依赖大上下文窗口的LLM，成本较高。

（完）

---
title: "AgentOpt v0.1 Technical Report: Client-Side Optimization for LLM-Based Agent"
title_zh: AgentOpt v0.1 技术报告：基于大语言模型的智能体客户端优化
authors: "Wenyue Hua, Sripad Karne, Qian Xie, Armaan Agrawal, Nikos Pagonas, Kostis Kaffes, Tianyi Peng"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06296v1"
tags: ["query:agent"]
score: 8.0
evidence: LLM智能体优化框架含代码智能体
tldr: 现有AI智能体研究多关注服务端效率，忽视了客户端资源分配问题。本文提出AgentOpt，首个框架无关的Python包，专注于客户端智能体优化。它通过实现八种搜索算法解决多步流水线中的模型选择问题，在保证准确率的同时显著降低成本和评估预算，填补了客户端优化的空白。
selection_source: fresh_fetch
motivation: 现有研究主要关注服务端效率，但随着用户组合本地工具和多样化模型，客户端的资源分配优化问题亟待解决。
method: 提出AgentOpt框架，通过实现包括Arm Elimination和贝叶斯优化在内的八种搜索算法，高效探索多步流水线中的模型组合空间以优化资源分配。
result: "实验显示最佳与最差模型组合的成本差距可达32倍，且AgentOpt能在保持近最优准确率的同时，相比暴力搜索减少24-67%的评估预算。"
conclusion: AgentOpt作为首个客户端优化工具，有效解决了智能体流水线中的模型选择与成本效益问题，具有重要的实践意义。
---

## 摘要
AI 智能体正日益广泛地部署于现实应用中，包括 Manus、OpenClaw 和编程智能体等系统。现有研究主要关注\emph{服务端}效率，提出了缓存、推测执行、流量调度和负载均衡等方法以降低服务智能体工作负载的成本。然而，随着用户日益通过组合本地工具、远程 API 和多样化模型来构建智能体，客户端也出现了同等重要的优化问题。客户端优化探讨开发者应如何在满足特定应用的质量、成本和延迟约束的前提下，将可用资源（包括模型选择、本地工具和 API 预算）分配至流水线的各个阶段。由于这些目标取决于任务和部署环境，仅靠服务端系统无法确定。我们推出了 AgentOpt，这是首个用于客户端智能体优化的框架无关 Python 包。我们首先研究模型选择，这是多步智能体流水线中一个高影响力的优化手段。给定一个流水线和一个小的评估集，目标是为流水线角色找到最具成本效益的模型分配方案。该问题在实践中具有重要意义：在我们的实验中，在准确率相当的情况下，最佳与最差模型组合之间的成本差距可达 13--32 倍。为了高效探索呈指数级增长的组合空间，AgentOpt 实现了八种搜索算法，包括臂消除（Arm Elimination）、Epsilon-LUCB、阈值连续消除（Threshold Successive Elimination）和贝叶斯优化。在四个基准测试中，臂消除算法在四分之三的任务上实现了接近最优的准确率，同时相比暴力搜索减少了 24--67% 的评估预算。代码和基准测试结果可在 https://agentoptimizer.github.io/agentopt/ 获取。

## Abstract
AI agents are increasingly deployed in real-world applications, including systems such as Manus, OpenClaw, and coding agents. Existing research has primarily focused on \emph{server-side} efficiency, proposing methods such as caching, speculative execution, traffic scheduling, and load balancing to reduce the cost of serving agentic workloads. However, as users increasingly construct agents by composing local tools, remote APIs, and diverse models, an equally important optimization problem arises on the client side. Client-side optimization asks how developers should allocate the resources available to them, including model choice, local tools, and API budget across pipeline stages, subject to application-specific quality, cost, and latency constraints. Because these objectives depend on the task and deployment setting, they cannot be determined by server-side systems alone. We introduce AgentOpt, the first framework-agnostic Python package for client-side agent optimization. We first study model selection, a high-impact optimization lever in multi-step agent pipelines. Given a pipeline and a small evaluation set, the goal is to find the most cost-effective assignment of models to pipeline roles. This problem is consequential in practice: at matched accuracy, the cost gap between the best and worst model combinations can reach 13--32$\times$ in our experiments. To efficiently explore the exponentially growing combination space, AgentOpt implements eight search algorithms, including Arm Elimination, Epsilon-LUCB, Threshold Successive Elimination, and Bayesian Optimization. Across four benchmarks, Arm Elimination recovers near-optimal accuracy while reducing evaluation budget by 24--67\% relative to brute-force search on three of four tasks. Code and benchmark results available at https://agentoptimizer.github.io/agentopt/.

---

## 论文详细总结（自动生成）

# 论文总结：AgentOpt v0.1 技术报告

## 1. 论文的核心问题与整体含义
*   **研究背景**：随着AI智能体应用的普及，现有研究主要集中在服务端优化（如缓存、调度、负载均衡），忽视了客户端开发者在构建智能体流水线时面临的资源分配问题。
*   **核心问题**：在多步智能体流水线中，开发者如何根据特定的质量、成本和延迟约束，为不同角色（如规划器、求解器）选择最优的模型组合。
*   **研究动机**：
    *   **成本差距巨大**：实验发现，在准确率相当的情况下，最佳与最差模型组合的成本差距可达13至32倍。
    *   **反直觉现象**：最强的单一模型（如Claude Opus 4.6）在特定角色（如规划器）上可能表现最差，因为它可能绕过下游工具调用，导致流水线失效。因此，模型选择必须基于组合层面而非单一模型能力。

## 2. 论文提出的方法论
*   **核心思想**：提出 **AgentOpt**，首个框架无关的Python客户端优化包。将模型选择问题建模为黑盒组合优化问题，目标是在巨大的组合空间中寻找帕累托最优解。
*   **关键技术细节**：
    *   **非侵入式拦截**：通过修补 `httpx.Client.send` 和 `httpx.AsyncClient.send` 在HTTP传输层拦截LLM调用，无需修改现有框架代码即可监控和替换模型。
    *   **归因机制**：利用Python的 `contextvars` 将拦截的调用归属到特定的数据点和模型组合，实现精确的指标统计。
    *   **缓存与并行**：支持HTTP响应缓存以减少重复调用开销，并采用双层并发控制（组合级和数据点级）以加速评估。
*   **算法流程**：
    *   实现了8种搜索算法，包括暴力搜索、随机搜索、爬山算法、贝叶斯优化以及基于多臂老虎机的算法。
    *   **Arm Elimination（臂消除算法）**：作为默认推荐算法。流程如下：
        1.  初始化所有候选模型组合。
        2.  分批次在数据集上评估组合性能。
        3.  计算每个组合的置信区间。
        4.  淘汰置信区间上界低于当前最优组合置信区间下界的组合。
        5.  重复上述过程直至预算耗尽或仅剩最优解。

## 3. 实验设计
*   **数据集与场景**：
    *   **HotpotQA**：多跳问答，采用两阶段流水线（规划器+求解器），共81种模型组合。
    *   **GPQA Diamond**：研究生级别科学多选题，单模型代理，共9种组合。
    *   **MathQA**：数学推理，采用两阶段流水线（回答者+评论家），共81种组合。
    *   **BFCL v3 Multi-Turn**：多轮函数调用，单模型代理，共9种组合。
*   **Benchmark与对比方法**：
    *   对比了8种搜索算法：Brute-force（基准）、Random Search、Arm Elimination、Epsilon-LUCB、Threshold Successive Elimination、Hill Climbing、Bayesian Optimization、LM Proposal。
    *   模型池包含9个主流LLM（如Claude Opus 4.6, GPT-oss系列, Qwen3, Ministral等）。

## 4. 资源与算力
*   **算力资源**：论文**未明确说明**使用的GPU型号、数量或训练时长。
*   **成本消耗**：实验主要基于API调用（通过AWS Bedrock），以美元衡量成本。例如，BFCL基准测试的暴力搜索成本为$84.80，而优化算法显著降低了这一成本。

## 5. 实验数量与充分性
*   **实验规模**：在4个不同类型的基准测试上进行了实验，涵盖了单阶段和多阶段流水线结构。每种搜索算法均运行了50个随机种子取平均值，确保了结果的统计稳定性。
*   **充分性与公平性**：
    *   实验设计较为充分，不仅对比了算法效率，还深入分析了模型组合的反直觉现象（如Opus作为规划器的失效）。
    *   所有算法在同一执行底座上运行，评估指标统一（准确率、延迟、成本），对比公平。

## 6. 论文的主要结论与发现
*   **模型组合至关重要**：最佳组合往往不是最强单模型的简单堆叠。例如在HotpotQA中，最便宜的模型作为规划器搭配最强模型作为求解器取得了最佳效果。
*   **优化效果显著**：Arm Elimination算法在3/4的任务上相比暴力搜索减少了24-67%的评估预算，同时保持了接近最优的准确率。
*   **算法表现差异**：贝叶斯优化在平滑空间表现好，爬山算法在特定场景有效但在复杂空间易陷入局部最优，基于LLM先验的LM Proposal方法表现最差，证明了实证评估的必要性。

## 7. 优点
*   **视角新颖**：首次系统性地提出并定义了“客户端智能体优化”问题，填补了现有服务端优化视角的空白。
*   **工程价值高**：提出的AgentOpt框架实现了框架无关和非侵入式设计，极大降低了开发者的集成成本。
*   **洞察深刻**：通过实验揭示了流水线中模型角色的“错位”效应，证明了端到端组合优化的必要性。

## 8. 不足与局限
*   **搜索空间限制**：实验中最大的组合空间仅为81（$9^2$），对于更复杂的智能体流水线（如包含更多角色或工具链），搜索空间的爆炸问题可能更具挑战性，现有算法的可扩展性有待进一步验证。
*   **模型池规模**：仅选用了9个模型进行测试，未涵盖更多样化的开源或闭源模型，可能影响结论的普适性。
*   **算法依赖性**：部分算法（如Hill Climbing）对初始点敏感，LM Proposal方法表现不佳，说明在缺乏先验知识时，现有启发式方法仍有局限。

（完）

---
title: "Gym-Anything: Turn any Software into an Agent Environment"
title_zh: Gym-Anything：将任意软件转化为智能体环境
authors: "Pranjal Aggarwal, Graham Neubig, Sean Welleck"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06126v1"
tags: ["query:agent"]
score: 9.0
evidence: 包含编码代理和审计代理的框架用于环境创建
tldr: 针对当前计算机使用智能体环境构建成本高、范围窄的问题，本文提出Gym-Anything框架，利用多智能体协作将任意软件转化为交互环境。基于此构建了包含200款软件、超1万任务的CUA-World基准，涵盖长程任务。实验表明，通过轨迹蒸馏和测试时审计反馈，显著提升了模型性能。
selection_source: fresh_fetch
motivation: 现有计算机使用智能体研究局限于短期任务和少量软件，主要因为构建复杂软件环境耗时费力且难以扩展。
method: 提出Gym-Anything框架，将环境创建视为多智能体任务，通过编码智能体配置软件并生成证据，再由独立审计智能体验证质量。
result: 构建了涵盖200款软件和超1万任务的CUA-World基准，通过轨迹蒸馏和测试时审计反馈，显著提升了模型在长程任务上的表现。
conclusion: 该研究成功实现了软件环境的自动化构建，开源了大规模高价值基准，推动了现实计算机使用智能体的发展。
---

## 摘要
计算机使用智能体有望协助广泛的数字经济活动。然而，当前的研究主要集中在经济价值有限的软件集上的短视界任务，例如基本的电子商务和操作系统配置任务。一个关键原因是，为复杂软件创建环境需要大量的时间和人力，因此不具备可扩展性。为了解决这一问题，我们提出了 Gym-Anything，这是一个将任意软件转化为交互式计算机使用环境的框架。我们将环境创建本身构建为一个多智能体任务：一个编码智能体编写设置脚本、下载真实世界数据并配置软件，同时生成正确设置的证据。随后，一个独立的审计智能体根据质量检查清单验证环境设置的证据。利用基于美国 GDP 数据的具有经济价值的职业分类体系，我们将此流程应用于具有广泛职业覆盖范围的 200 个软件应用程序。结果是 CUA-World，这是一个包含超过 10K 个长视界任务的集合，涵盖从医学、天文学到工程学和企业系统等领域，每个任务都配置了真实数据以及训练和测试划分。CUA-World 还包含 CUA-World-Long，这是一个具有挑战性的长视界基准测试，其任务通常需要超过 500 个步骤，远超现有的基准测试。将训练集中的成功轨迹蒸馏到一个 2B 视觉-语言模型中，其表现优于参数量为其两倍的模型。我们还在测试时应用了相同的审计原则：一个独立的 VLM 审查已完成的轨迹，并对剩余工作提供反馈，将 Gemini-3-Flash 在 CUA-World-Long 上的表现从 11.5% 提升至 14.0%。我们发布所有代码、基础设施和基准数据，以促进未来关于真实计算机使用智能体的研究。

## Abstract
Computer-use agents hold the promise of assisting in a wide range of digital economic activities. However, current research has largely focused on short-horizon tasks over a limited set of software with limited economic value, such as basic e-commerce and OS-configuration tasks. A key reason is that creating environments for complex software requires significant time and human effort, and therefore does not scale. To address this, we introduce Gym-Anything, a framework for converting any software into an interactive computer-use environment. We frame environment creation itself as a multi-agent task: a coding agent writes setup scripts, downloads real-world data, and configures the software, while producing evidence of correct setup. An independent audit agent then verifies evidence for the environment setup against a quality checklist. Using a taxonomy of economically valuable occupations grounded in U.S. GDP data, we apply this pipeline to 200 software applications with broad occupational coverage. The result is CUA-World, a collection of over 10K long-horizon tasks spanning domains from medical science and astronomy to engineering and enterprise systems, each configured with realistic data along with train and test splits. CUA-World also includes CUA-World-Long, a challenging long-horizon benchmark with tasks often requiring over 500 steps, far exceeding existing benchmarks. Distilling successful trajectories from the training split into a 2B vision-language model outperforms models 2$\times$ its size. We also apply the same auditing principle at test time: a separate VLM reviews completed trajectories and provides feedback on what remains, improving Gemini-3-Flash on CUA-World-Long from 11.5% to 14.0%. We release all code, infrastructure, and benchmark data to facilitate future research in realistic computer-use agents.

---

## 论文详细总结（自动生成）

# 论文总结：Gym-Anything: Turn any Software into an Agent Environment

## 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前计算机使用智能体（CUA）的研究与评估存在两大瓶颈：一是任务多为短视界、低经济价值的简单操作（如改壁纸、填表单），无法反映真实专业工作；二是构建复杂、真实的软件环境（如医学影像分析工具、企业ERP系统）需要大量专家人力，成本高昂且不可扩展。
*   **整体含义**：本研究旨在解决CUA环境构建的可扩展性问题，通过自动化框架将任意软件转化为可交互的智能体环境，从而推动CUA在更真实、更具经济价值的场景中进行训练与评估，使其能力真正逼近人类专业工作水平。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
*   **核心思想**：将“环境创建”本身视为一个多智能体协作任务。利用AI智能体自动化完成软件安装、数据配置、任务设计等繁琐工作，并通过独立审计机制确保环境质量。
*   **关键技术细节**：
    *   **GDP导向的软件选择**：基于美国职业数据（O*NET）和GDP数据，估算各职业的GDP贡献，发现其使用的软件，并筛选出可沙盒化（免费、自托管、有GUI）的约200款软件，确保覆盖所有22个主要职业群体。
    *   **Gym-Anything库**：将环境定义标准化为“安装脚本、配置脚本、任务设置脚本 + 配置文件”的简单规范，统一管理跨操作系统（Linux/Windows/Android）和计算后端（Docker/QEMU等）的复杂性。
    *   **多智能体创建-审计循环**：
        *   **创建智能体**：编写脚本、下载真实数据、配置软件，并生成证据（截图、日志）。
        *   **审计智能体**：独立审查证据，对照质量检查清单（如软件是否正确启动、数据是否真实）进行验证，反馈问题。
        *   **共享记忆**：智能体将成功经验写入共享记忆库，后续创建可复用，实现效率提升。
    *   **任务生成（提议-放大策略）**：先用昂贵智能体模型为每个软件生成少量高质量“种子任务”，再用便宜LLM以种子为示例大规模生成更多任务，并通过VLM过滤确保任务起始状态合理。
    *   **验证系统**：采用**基于检查清单的VLM验证器**，结合从环境设置脚本中提取的**特权信息**（如已知答案），对智能体轨迹进行细粒度评分，并包含完整性检查以防止智能体作弊（如伪造结果）。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
*   **数据集/场景**：构建了 **CUA-World** 基准，包含：
    *   **规模**：200款软件，超过10,000个任务，覆盖医学、天文、工程、金融等领域。
    *   **划分**：包含训练集和测试集，以及一个极具挑战性的长视界子集 **CUA-World-Long**（200个任务，常需500+步）。
*   **Benchmark**：CUA-World本身是核心benchmark，其设计旨在填补现有基准（如OSWorld、WebArena）在规模、长视界任务、职业覆盖和经济相关性上的空白。
*   **对比方法**：
    *   **基准对比**：在表2中与现有静态数据集（Mind2Web, AITW）和交互式基准（OSWorld, WebArena, AndroidWorld等）进行多维度对比，突出CUA-World在软件数量、任务数量、长视界支持、职业覆盖等方面的优势。
    *   **模型对比**：评估了前沿模型（Gemini-3-Flash, GPT-5.4, Claude-Sonnet-4.6, Kimi-K2.5）及开源小模型（Qwen3-VL-2B/4B）在CUA-World上的表现。
    *   **训练方法对比**：对比了未经蒸馏的小模型与经过CUA-World轨迹蒸馏后的模型性能。

## 4. 资源与算力
*   **算力使用**：文中提到在实验中运行了 **400+并发环境**，使用了 **1600 CPUs**。附录提到获得了 **Microsoft Azure的算力信用额度**。
*   **未明确说明**：论文未明确列出具体GPU型号、数量或训练时长等细节。

## 5. 实验数量与充分性
*   **实验数量**：实验设计较为全面，包括：
    *   **主要性能评估**：在CUA-World-Test和CUA-World-Long上评估多个前沿模型。
    *   **训练数据缩放实验**：分析蒸馏模型性能随训练软件数量和任务数量的变化。
    *   **泛化性实验**：研究在部分软件上训练的模型对已见（IID）和未见（OOD）软件的泛化能力。
    *   **测试时计算缩放实验**：分析模型性能随允许步数预算增加的变化。
    *   **基准分析**：包括轨迹行为模式分析、难度分布分析、验证器鲁棒性分析等。
    *   **消融实验**：对蒸馏过程中的教师模型选择、轨迹长度、提议-放大策略、跨模型审计等关键设计进行了消融。
*   **充分性与客观性**：实验覆盖了性能、缩放、泛化、分析等多个层面，消融实验针对了方法中的关键组件，设计较为充分和客观。对比基准时使用了统一的评估指标（平均分数、通过率）。

## 6. 论文的主要结论与发现
*   **CUA-World极具挑战性**：即使最强前沿模型（如Gemini-3-Flash）在CUA-World-Long上的通过率也极低（7.5%-11.5%），表明长视界、真实任务仍是CUA的巨大挑战。
*   **轨迹蒸馏有效**：将CUA-World训练集中的成功轨迹蒸馏到2B参数的Qwen3-VL模型，使其性能超越了参数量为其两倍的未蒸馏模型。
*   **测试时审计反馈有益**：引入独立的VLM审计智能体在任务结束时提供反馈，能将模型在长视界任务上的通过率从11.5%提升至14.0%。
*   **训练数据缩放呈对数线性趋势**：模型性能随训练软件和任务数量的增加而提升，遵循约每翻倍数据提升3.5分的规律。
*   **跨软件泛化有限**：在部分软件上训练的模型，对未见软件的性能提升仅能恢复22-27%的差距，表明CUA需要针对多样化软件进行训练。
*   **失败轨迹特征**：失败轨迹常陷入重试循环，而成功轨迹更频繁地进行自我验证。

## 7. 优点：方法或实验设计上有哪些亮点
*   **GDP导向的软件选择**：创新性地将经济价值作为软件选择的核心依据，使基准与真实经济活动紧密关联。
*   **自动化环境构建**：通过多智能体协作和标准化库设计，成功解决了CUA环境构建的可扩展性瓶颈，实现了大规模、多样化环境的自动化生成。
*   **创建-审计循环**：引入独立审计智能体和证据验证机制，有效提升了环境构建的质量和可靠性，避免了智能体自我确认偏差。
*   **检查清单验证器**：结合特权信息的细粒度验证方案，既提供了部分评分能力，又通过完整性检查防止了智能体作弊，评估更全面可靠。
*   **长视界基准**：CUA-World-Long填补了现有基准在超长任务评估上的空白，为前沿CUA提供了新的挑战性测试平台。

## 8. 不足与局限
*   **GDP估算的近似性**：软件GDP贡献的估算依赖LLM和搜索，并非精确的美元级归因，可能存在偏差。
*   **软件覆盖限制**：大量专业软件因付费许可、组织凭证或特殊硬件要求无法被沙盒化，只能用替代品，其性能能否预测商业软件表现存疑。
*   **任务可解性未完全验证**：虽然人工验证了任务起始状态正确，但未对所有任务进行端到端求解验证，可能存在不可解任务。
*   **VLM验证器的潜在漏洞**：VLM验证器可能存在不完美之处，或可能被对抗性攻击利用，未来需开发更鲁棒的手动验证程序。
*   **伦理风险考虑**：论文在伦理声明中承认CUA若自主部署可能存在风险，但本研究未训练或发布超越现有前沿能力的模型。

（完）

---
title: Hierarchical Planning with Latent World Models
title_zh: 基于潜在世界模型的分层规划
authors: "Wancong Zhang, Basile Terver, Artem Zholus, Soham Chitnis, Harsh Sutaria, Mido Assran, Randall Balestriero, Amir Bar, Adrien Bardes, Yann LeCun, Nicolas Ballas"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03208v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 用于分层规划和具身控制的潜在世界模型
tldr: "针对学习型世界模型在长视界控制中面临预测误差累积和搜索空间爆炸的难题，本文提出了一种基于多时间尺度潜在世界模型的分层规划方法。该方法通过跨尺度分层规划实现长视界推理，显著降低了推理复杂度。实验显示，该方法在真实机器人任务中实现了70%的成功率，并在仿真环境中大幅提升了效率。"
selection_source: fresh_fetch
motivation: 现有的学习型世界模型在长视界控制任务中面临预测误差累积和搜索空间指数级增长的挑战。
method: 提出在多个时间尺度上学习潜在世界模型，并跨尺度执行分层规划以实现长视界推理并降低计算复杂度。
result: "该方法在真实机器人抓取任务中达到70%成功率（单层级模型为0%），并在仿真环境中显著提高了成功率且计算效率提升4倍。"
conclusion: 分层规划方法有效解决了长视界控制难题，实现了零样本泛化，并在性能和效率上显著优于单层级模型。
---

## 摘要
结合学习世界模型的模型预测控制（MPC）已成为具身控制的一种有前景的范式，特别是因为其在新环境中部署时具备零样本泛化的能力。然而，由于预测误差的累积和搜索空间的指数级增长，学习到的世界模型往往难以应对长视界控制任务。在这项工作中，我们通过在多个时间尺度上学习潜在世界模型并在这些尺度间进行分层规划来解决这些挑战，从而实现长视界推理并大幅降低推理时的规划复杂度。我们的方法作为一种模块化规划抽象，适用于多种潜在世界模型架构和领域。我们证明，这种分层方法能够在现实世界的非贪婪机器人任务中实现零样本控制，在仅使用最终目标规范的情况下，抓取与放置任务的成功率达到 70%，而单层世界模型的成功率为 0%。此外，在包括推动操作和迷宫导航在内的基于物理的仿真环境中，分层规划在取得更高成功率的同时，所需的规划时计算量减少了高达 4 倍。

## Abstract
Model predictive control (MPC) with learned world models has emerged as a promising paradigm for embodied control, particularly for its ability to generalize zero-shot when deployed in new environments. However, learned world models often struggle with long-horizon control due to the accumulation of prediction errors and the exponentially growing search space. In this work, we address these challenges by learning latent world models at multiple temporal scales and performing hierarchical planning across these scales, enabling long-horizon reasoning while substantially reducing inference-time planning complexity. Our approach serves as a modular planning abstraction that applies across diverse latent world-model architectures and domains. We demonstrate that this hierarchical approach enables zero-shot control on real-world non-greedy robotic tasks, achieving a 70% success rate on pick-&-place using only a final goal specification, compared to 0% for a single-level world model. In addition, across physics-based simulated environments including push manipulation and maze navigation, hierarchical planning achieves higher success while requiring up to 4x less planning-time compute.

---

## 论文详细总结（自动生成）

# 论文总结：基于潜在世界模型的分层规划

## 1. 论文的核心问题与整体含义
*   **研究背景**：基于学习型世界模型的模型预测控制（MPC）在具身智能领域展现出强大的零样本泛化能力。然而，现有的单层世界模型在面对长视界控制任务时存在两个根本性缺陷：一是预测误差随时间步累积，导致长程预测不准确；二是搜索空间随视界呈指数级增长，导致规划计算成本过高。
*   **核心问题**：如何在不依赖任务特定奖励或分层策略训练的前提下，利用学习到的世界模型解决长视界控制问题，并降低推理时的计算复杂度。
*   **整体含义**：本文提出了一种通用的分层规划框架（HWM），通过在多个时间尺度上建模世界模型并在推理时进行分层规划，实现了长视界推理能力的突破，证明了无需分层策略学习即可解决复杂非贪婪任务的可行性。

## 2. 论文提出的方法论
*   **核心思想**：HWM 将规划过程分解为两个时间尺度。高层世界模型在粗粒度的时间尺度上进行长期规划，生成中间子目标；低层世界模型在细粒度的时间尺度上进行短期规划，执行具体动作以到达子目标。该方法作为一种“即插即用”的规划抽象，适用于多种潜在世界模型架构。
*   **关键技术细节**：
    *   **低层世界模型**：预测短期状态转移 $P^{(1)}(z_{t+1}|z_t, a_t)$，以原始动作 $a_t$ 为条件。
    *   **高层世界模型**：预测长期状态转移 $P^{(2)}(z_{t+h}|z_t, l_t)$，以潜在宏动作 $l_t$ 为条件。
    *   **动作编码器**：将低层原始动作序列压缩为高层潜在宏动作，降低高层搜索空间的维度。
    *   **分层规划流程**：
        1.  **高层规划**：给定初始状态和目标状态，通过最小化能量函数 $E_2$ 优化宏动作序列，生成一系列潜在子目标。
        2.  **低层规划**：以高层生成的第一个子目标为当前目标，通过最小化能量函数 $E_1$ 优化原始动作序列。
        3.  **执行与重规划**：执行低层动作，并按 MPC 方式循环重规划。
*   **公式说明**：
    *   高层能量函数：$E_2(\hat{l}_{1:H}; z_1, z_g) \triangleq \|z_g - P^{(2)}(\hat{l}_{1:H}; z_1)\|_1$，旨在最小化预测状态与目标状态的距离。
    *   低层能量函数：$E_1(\hat{a}_{1:h}; z_1, \tilde{z}_1) \triangleq \|\tilde{z}_1 - P^{(1)}(\hat{a}_{1:h}; z_1)\|_1$，旨在最小化预测状态与子目标的距离。

## 3. 实验设计
*   **数据集与场景**：
    *   **真实机器人**：Franka 机械臂，使用 DROID 和 RoboSet 数据集（约 130 小时数据）。任务包括抓取放置和开关抽屉。
    *   **仿真环境**：
        *   **Push-T**：基于 DINO-WM 数据集，任务视界从 25 步延长至 75 步。
        *   **Diverse Maze**：基于 MuJoCo PointMaze，地图尺寸扩大 4 倍，测试未见过的布局。
*   **Benchmark 与对比方法**：
    *   **基座模型对比**：将 HWM 应用于 VJEPA2-AC、DINO-WM、PLDM 三种架构，对比单层规划与分层规划的性能。
    *   **VLA 模型对比**：对比 Octo、$\pi_0$-FAST-DROID、$\pi_{0.5}$-DROID 等大规模预训练视觉-语言-动作模型。
    *   **离线 RL 方法对比**：对比 GCIQL、HIQL、HILP 等目标条件强化学习方法。

## 4. 资源与算力
*   **训练资源**：文中提到在训练解码器时使用了 8 个节点，总 batch size 为 512。规划过程在 GPU 上并行执行。
*   **缺失信息**：论文未明确说明具体的 GPU 型号（如 A100/H100）以及各模型的总训练时长。

## 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 3 个主要领域（真实机器人操作、物体推动、迷宫导航），并在 3 种不同的世界模型架构上进行了验证。此外，还进行了消融实验，分析了潜在动作维度的影响、高层模型对长视界预测的准确性以及计算效率。
*   **充分性与客观性**：实验设计较为充分。不仅验证了方法在真实物理机器人上的有效性，还通过仿真环境控制变量验证了长视界能力。对比方法涵盖了当前主流的 VLA 模型和离线 RL 方法，且明确指出了 VLA 模型在非贪婪任务上的局限性，评价客观。

## 6. 论文的主要结论与发现
*   **性能突破**：在真实机器人抓取放置任务中，单层模型成功率为 0%，而 HWM 达到了 70% 的成功率，且优于训练数据量大 77 倍的 VLA 模型。
*   **长视界能力**：在 Push-T 和迷宫导航任务中，随着任务视界增加，HWM 相比单层规划器表现出更强的鲁棒性，成功率显著提升（如 Push-T 任务中提升 44%）。
*   **计算效率**：分层规划在达到相同或更高成功率的情况下，推理时的计算量减少了 3 到 4 倍。
*   **通用性**：HWM 作为一种模块化组件，能够提升多种潜在世界模型架构的性能。

## 7. 优点
*   **无需分层策略训练**：方法直接在预训练的世界模型上进行推理时的分层规划，无需学习分层策略或技能库，简化了训练流程。
*   **解决非贪婪任务**：成功解决了需要“先远离目标再接近”的非贪婪任务，这是单层规划器难以处理的。
*   **模型无关性**：方法具有通用性，可应用于 VJEPA2-AC、DINO-WM、PLDM 等多种架构。
*   **效率与效果兼顾**：同时提升了长视界任务的成功率和推理效率。

## 8. 不足与局限
*   **长视界仍有挑战**：尽管性能提升，但随着视界进一步增加，所有方法的性能均出现下降，表明长视界操作仍具挑战性。
*   **感知误差影响**：在真实机器人实验中，失败案例部分源于感知不精确（如深度估计误差）。
*   **高低层协同优化缺失**：当前方法未对高低层规划器进行联合优化，可能导致偶尔的执行偏差。
*   **应用限制**：方法依赖于离线数据训练的世界模型，数据分布的偏差可能影响泛化能力。

（完）

---
title: "PRAISE: Prefix-Based Rollout Reuse in Agentic Search Training"
title_zh: PRAISE：智能体搜索训练中基于前缀的推演复用
authors: "Erhan Zhang, Yiqun Chen, Zechun Niu, Wei Yang, Xiaochi Wei, Yan Gao, Yi Wu, Yao Hu, Jiaxin Mao"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03675v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于多轮检索和推理的智能体搜索训练
tldr: 针对智能体搜索训练中长程推演利用率低和奖励稀疏的问题，本文提出了PRAISE框架。该方法通过提取搜索轨迹前缀构建额外训练数据，并利用中间步骤性能差异推导步骤级奖励，实现了数据效率与信用分配的双重提升，在多跳问答任务上表现优异。
selection_source: fresh_fetch
motivation: 现有基于搜索的强化学习方法面临长程推演利用率低和奖励稀疏两大核心局限。
method: 提出PRAISE框架，通过复用搜索轨迹前缀构建训练数据并推导步骤级奖励，实现无需额外标注的联合优化。
result: 在多跳问答基准测试上的实验表明，该方法性能持续优于强基线模型。
conclusion: PRAISE有效提升了智能体搜索训练的数据效率和信用分配准确性。
---

## 摘要
在智能体搜索中，大语言模型 (LLM) 经过训练，能够执行多轮检索与推理，以处理诸如多跳问答 (QA) 等复杂任务。然而，当前的基于搜索的强化学习 (RL) 方法存在两个核心局限：代价高昂的长视界推演在训练过程中利用不足，且监督信号通常仅存在于最终答案处，导致严重的奖励稀疏性。我们提出了 PRAISE（用于智能体搜索的基于中间步骤奖励的前缀推演复用），这是一个旨在提高智能体搜索训练中数据效率和贡献分配的框架。给定一条完整的搜索轨迹，PRAISE 在不同的搜索轮次提取前缀状态，从中引出中间答案，并利用这些前缀来构建额外的训练轨迹，以及根据前缀间的性能差异推导步骤级奖励。我们的方法使用单一共享模型同时进行搜索策略学习和前缀答案评估，实现了无需额外人工标注或独立奖励模型的联合优化。在多跳问答基准测试上的实验表明，PRAISE 的性能持续优于强力基线。

## Abstract
In agentic search, large language models (LLMs) are trained to perform multi-turn retrieval and reasoning for complex tasks such as multi-hop question answering (QA). However, current search-based Reinforcement Learning (RL) methods suffer from two core limitations: expensive long-horizon rollouts are under-utilized during training, and supervision is typically available only at the final answer, resulting in severe reward sparsity. We present Prefix-based Rollout reuse for Agentic search with Intermediate Step rEwards (PRAISE), a framework for improving both data efficiency and credit assignment in agentic search training. Given a complete search trajectory, PRAISE extracts prefix states at different search turns, elicits intermediate answers from them, and uses these prefixes both to construct additional training trajectories and to derive step-level rewards from performance differences across prefixes. Our method uses a single shared model for both search policy learning and prefix answer evaluation, enabling joint optimization without extra human annotations or a separate reward model. Experiments on multi-hop QA benchmarks show that PRAISE consistently improves performance over strong baselines.

---

## 论文详细总结（自动生成）

# 论文总结：PRAISE——智能体搜索训练中基于前缀的推演复用

## 1. 论文的核心问题与整体含义
*   **研究背景**：在智能体搜索领域，大语言模型（LLM）被训练用于执行多轮检索与推理以解决复杂任务（如多跳问答）。当前主流的基于搜索的强化学习（RL）方法通常将整个搜索轨迹作为单一训练实例。
*   **核心问题**：现有方法存在两大局限：
    1.  **数据效率低**：生成长视界搜索轨迹代价高昂，但中间状态信息往往被浪费，未被充分利用为训练样本。
    2.  **奖励稀疏**：监督信号通常仅在最终答案处可用，导致难以判断中间搜索步骤的贡献，信用分配困难。
*   **整体含义**：本文旨在解决智能体搜索训练中的样本利用率低和反馈信号稀疏问题，提出了一种无需额外人工标注或独立奖励模型的框架，以提升训练效率和模型性能。

## 2. 论文提出的方法论
本文提出了 **PRAISE** 框架，其核心思想是利用搜索轨迹的自然前缀结构，将单一轨迹转化为多个训练样本，并基于前缀性能差异构建过程奖励。

*   **核心流程**：
    1.  **主搜索推演**：模型在搜索模式下生成完整的搜索轨迹 $\tau$，包含 $T$ 轮搜索交互（思考、搜索动作、观察结果）。
    2.  **前缀状态提取**：从完整轨迹中提取不同轮次的前缀状态 $s_t$（即前 $t$ 轮交互后的信息状态）。
    3.  **前缀回答生成**：在回答模式下，模型仅基于前缀状态 $s_t$ 生成中间答案 $\tilde{y}_t$。
    4.  **奖励构建**：
        *   **结果奖励**：基于最终答案与真实答案计算得分 $u$。
        *   **过程奖励**：计算相邻前缀状态下的答案得分差值 $r^{proc}_t = \alpha(v_t - v_{t-1})$，以此量化每一轮搜索步骤的边际贡献，将稀疏奖励转化为细粒度的步骤级奖励。
    5.  **联合优化**：使用单一共享模型同时执行搜索策略学习和前缀答案评估，通过 PPO（近端策略优化）算法进行联合训练。

*   **关键技术细节**：
    *   **推演复用**：将一条完整轨迹转化为 $T+1$ 个前缀回答样本，增加了训练数据的多样性。
    *   **信用分配**：通过比较相邻前缀的答案质量，自动推导中间步骤的奖励，无需人工标注中间步骤标签。

## 3. 实验设计
*   **数据集**：
    *   **训练集**：HotpotQA、2WikiMultihopQA。
    *   **测试集**：Natural Questions (NQ)、HotpotQA、2WikiMultihopQA、Bamboogle、MuSiQue。
*   **评估指标**：Exact Match (EM) 和 F1 分数。
*   **基线方法**：
    *   **非智能体方法**：Direct LLM、Naive RAG。
    *   **智能体搜索方法**：Search-o1、Search-R1、R1-Searcher。
    *   **过程监督方法**：StepSearch、ReasonRAG、TIPS。
*   **实现细节**：基础模型为 Qwen2.5-7B，检索器为 E5，检索语料库为 Wikipedia，训练框架为 verl。

## 4. 资源与算力
*   **文中未明确说明**具体的算力消耗细节（如 GPU 型号、数量、训练时长等）。仅提及使用了 Qwen2.5-7B 和 14B 模型以及 verl 训练框架。

## 5. 实验数量与充分性
*   **实验数量**：
    *   主实验：在 5 个基准数据集上进行了性能对比。
    *   消融实验：验证了联合优化策略、过程奖励及前缀评估器的有效性（涉及 3 个数据集）。
    *   分析实验：探究了联合优化对评估器性能的影响（分步骤分析）以及过程奖励权重 $\alpha$ 的敏感性（涉及 7B 和 14B 两个模型规模）。
*   **充分性与客观性**：实验设计较为充分，涵盖了多跳问答的典型基准，对比了不同类型的强基线方法。消融实验详细拆解了各模块贡献，且分析了不同模型规模下的表现，结论具有较好的说服力和客观性。

## 6. 论文的主要结论与发现
*   **性能提升**：PRAISE 在所有 5 个多跳问答基准测试上均取得了最优性能，显著优于现有的智能体搜索和过程监督基线。
*   **联合优化的必要性**：使用共享模型进行联合优化（同时训练搜索策略和评估能力）优于使用冻结的评估器或独立的评估模型，因为前者能保持评估器与策略演进的同步。
*   **过程奖励的有效性**：基于前缀增益的过程奖励能有效解决长程推理中的信用分配问题，且模型对奖励权重 $\alpha$ 具有较好的鲁棒性。

## 7. 优点
*   **创新的数据利用方式**：巧妙地利用轨迹前缀将单次昂贵的推演转化为多个训练样本，显著提升了数据效率。
*   **无需额外标注**：通过模型自评估生成过程奖励，避免了传统过程监督方法中对人工标注子问题或独立奖励模型的依赖，降低了成本。
*   **机制简洁有效**：方法逻辑清晰，易于集成到现有的 PPO 训练流程中，且实验证明了其在多跳推理任务上的有效性。

## 8. 不足与局限
*   **任务局限性**：实验主要集中在多跳问答任务，对于其他类型的智能体任务（如网页导航、代码执行等）的泛化能力尚未验证。
*   **奖励依赖性**：过程奖励的计算依赖于可验证的答案评分函数（如 EM/F1），这在开放域生成或主观性强的任务中可能难以直接应用。
*   **信息缺失**：论文未提供具体的计算资源消耗数据，难以评估该方法的实际训练成本。

（完）

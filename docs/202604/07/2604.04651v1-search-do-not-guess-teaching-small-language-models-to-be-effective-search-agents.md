---
title: "Search, Do not Guess: Teaching Small Language Models to Be Effective Search Agents"
title_zh: 搜索，而非猜测：教导小型语言模型成为高效的搜索智能体
authors: "Yizhou Liu, Qi Sun, Yulin Chen, Siyue Zhang, Chen Zhao"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04651v1"
tags: ["query:agent"]
score: 10.0
evidence: 教导小语言模型成为有效的搜索智能体
tldr: 针对小语言模型（SLM）作为搜索智能体时调用工具少且易产生幻觉的问题，本文提出一种轻量级微调方法，显式训练SLM可靠检索并基于证据生成答案。该方法显著提升了多跳推理任务的性能，达到大模型水平，并发现一致性搜索策略对SLM至关重要。
selection_source: fresh_fetch
motivation: 现有小语言模型在作为搜索智能体时调用工具频率低且易产生幻觉，难以胜任复杂推理任务。
method: 提出一种轻量级微调方法，显式训练小语言模型可靠地检索信息并基于检索到的证据生成答案。
result: 该方法在Bamboogle和HotpotQA数据集上性能显著提升，达到了大模型级别的表现。
conclusion: 研究发现小语言模型应采用一致的搜索行为而非自适应策略，以确保推理的可靠性。
---

## 摘要
配备搜索工具的智能体已成为解决知识密集型任务的有效方案。尽管大型语言模型展现出强大的推理能力，但其高昂的计算成本限制了搜索智能体的实际部署。因此，近期的研究重点在于将智能体行为从大型语言模型蒸馏到小型语言模型中。通过对复杂多跳推理任务的全面评估，我们发现尽管小型语言模型拥有的参数化知识较少，但它们调用搜索工具的频率较低，且更容易产生幻觉。为了解决这一问题，我们提出了 \policy，这是一种轻量级微调方法，显式训练小型语言模型以可靠地检索并生成基于检索证据的答案。与从大型语言模型进行的智能体蒸馏相比，我们的方法在 Bamboogle 上提升了 17.3 分，在 HotpotQA 上提升了 15.3 分，在各项基准测试中达到了大型语言模型水平的结果。我们的进一步分析表明，小型语言模型中的自适应搜索策略往往会导致性能下降，这凸显了一致的搜索行为对于可靠推理的必要性。

## Abstract
Agents equipped with search tools have emerged as effective solutions for knowledge-intensive tasks. While Large Language Models (LLMs) exhibit strong reasoning capabilities, their high computational cost limits practical deployment for search agents. Consequently, recent work has focused on distilling agentic behaviors from LLMs into Small Language Models (SLMs). Through comprehensive evaluation on complex multi-hop reasoning tasks, we find that despite possessing less parametric knowledge, SLMs invoke search tools less frequently and are more prone to hallucinations. To address this issue, we propose \policy, a lightweight fine-tuning approach that explicitly trains SLMs to reliably retrieve and generate answers grounded in retrieved evidence. Compared to agent distillation from LLMs, our approach improves performance by 17.3 scores on Bamboogle and 15.3 scores on HotpotQA, achieving LLM-level results across benchmarks. Our further analysis reveals that adaptive search strategies in SLMs often degrade performance, highlighting the necessity of consistent search behavior for reliable reasoning.

---

## 论文详细总结（自动生成）

基于提供的论文片段与元数据，以下是该论文的详细结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：大型语言模型（LLM）虽然推理能力强，但计算成本高昂，难以在实际搜索智能体场景中大规模部署。因此，业界倾向于将智能体能力蒸馏到小型语言模型（SLM）中。
*   **核心问题**：研究发现，SLM 在作为搜索智能体时存在两大缺陷：一是**工具调用频率低**，倾向于利用内部有限的参数化知识“猜测”答案而非检索；二是**容易产生幻觉**，且难以遵守结构化的工具调用语法约束。
*   **整体含义**：论文旨在解决 SLM 在智能体任务中的可靠性问题，提出应当教导 SLM “搜索而非猜测”，通过显式训练使其具备稳定的信息检索与推理能力，从而在低算力消耗下达到媲美大模型的效果。

### 2. 论文提出的方法论
*   **核心思想**：提出 **ASP（Always-Search Policy，始终搜索策略）**。论文认为 SLM 不应像 LLM 那样自适应地决定是否搜索（因为 SLM 知识匮乏，自适应策略往往导致性能下降），而应被训练为始终通过搜索获取信息，再基于证据生成答案。
*   **关键技术细节**：
    *   **解决推理惯性**：针对模型即使获取了信息仍怀疑检索内容的问题，通过蒸馏训练使模型克服“推理惯性”，能够信任并利用检索到的信息继续推理。
    *   **形式化工具调用**：解决 SLM 难以遵守结构约束的问题，防止模型输出泄漏为自然语言。
*   **探针实验设计**：
    *   为了捕捉 SLM 的内部不确定性，训练了一个探测分类器。
    *   **输入构建**：提取 SLM 最后四层的隐藏状态 $\{h_{L-3}, h_{L-2}, h_{L-1}, h_{L}\}$ 进行拼接作为输入。
    *   **网络结构**：三层多层感知机（MLP），维度变化为 $d_{model} \times 4 \to 512 \to 256 \to 128 \to 1$。
    *   **训练配置**：冻结骨干网络参数，使用 AdamW 优化器，学习率 $2e-6$，Batch Size 为 16。

### 3. 实验设计
*   **数据集与场景**：
    *   主要针对复杂多跳推理任务，使用了 **Bamboogle** 和 **HotpotQA** 数据集。
    *   通用推理能力测试使用了 **MMLU**、**GSM8K** 和 **GPQA**。
*   **Benchmark 与对比方法**：
    *   **对比基线**：不同参数规模的 Vanilla 模型（如 Qwen3-32B, 1.7B 等）、SFT 模型、以及 OPD（Optimal Policy Distillation，最优策略蒸馏）模型。
    *   **评估维度**：任务准确率、工具调用延迟、校准误差（ECE）、抗噪能力。

### 4. 资源与算力
*   **硬件环境**：推理延迟测试使用了 **4 张 H20 GPU**。
*   **软件环境**：使用了 vLLM 推理框架和 FAISS-GPU 进行向量检索。
*   **训练细节**：文中明确给出了探针训练的超参数设置，但未明确提及主模型训练的具体时长或总算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了性能对比、延迟分析、噪声消融、置信度校准分析以及通用推理能力评估，实验组别较为丰富。
*   **充分性与客观性**：
    *   **充分性**：不仅评估了最终准确率，还深入分析了“推理惯性”、噪声鲁棒性（纯噪声与相似噪声）以及模型置信度分布，论证较为全面。
    *   **客观性**：对比了不同规模的模型（0.6B 至 32B），并公开了具体的 ECE 分数和延迟数据，具有较高参考价值。

### 6. 论文的主要结论与发现
*   **性能表现**：ASP 训练的 SLM 在 Bamboogle 和 HotpotQA 上分别提升了 17.3 和 15.3 分，达到了 LLM 级别的表现。
*   **效率优势**：ASP 训练的 SLM 推理延迟约为 Qwen3-32B 的 1/3（~3.1s vs ~10.3s），实现了性能与效率的平衡。
*   **策略发现**：SLM 采用“始终搜索”策略优于自适应搜索策略，因为 SLM 缺乏足够的知识来判断何时该搜索。
*   **抗噪能力**：经过 SFT/OPD 训练的模型在面对噪声时能自适应重新规划搜索，而 Vanilla 模型容易产生幻觉。
*   **能力迁移**：ASP 训练不仅提升了搜索任务表现，还提升了模型在通用推理任务（如 MMLU）上的能力。

### 7. 优点
*   **策略明确**：针对 SLM 的弱点（知识匮乏），提出了反直觉但有效的“始终搜索”策略，而非盲目模仿 LLM 的自适应行为。
*   **分析深入**：引入了探针实验来量化模型的内部不确定性和置信度，为“搜索 vs 猜测”的决策机制提供了可解释性依据。
*   **兼顾效率**：在提升性能的同时，强调了低延迟和低算力消耗的优势，具有很强的实用价值。

### 8. 不足与局限
*   **实验覆盖**：虽然提到了噪声消融，但仅设计了两种特定类型的噪声，现实世界中的噪声可能更为复杂多样。
*   **信息缺失**：提供的片段中缺乏关于 ASP 具体训练数据构建过程（如数据量、标注成本）的详细描述。
*   **应用限制**：“始终搜索”策略虽然准确率高，但在极其简单的查询上可能会增加不必要的检索开销，文中未详细讨论此类边缘情况。

（完）

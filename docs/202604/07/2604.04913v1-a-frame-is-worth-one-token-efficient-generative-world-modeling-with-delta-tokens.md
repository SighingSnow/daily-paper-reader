---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧即一词元：基于Delta Token的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 明确的生成式世界建模，提出DeltaTok分词器和DeltaWorld模型
tldr: 针对视频世界模型中判别式方法预测单一且生成式方法计算昂贵的问题，本文提出DeltaTok编码器将帧间差异压缩为单个token，并构建DeltaWorld模型进行高效生成。该方法通过紧凑表示实现多假设训练，在密集预测任务中，以极低的参数量和计算成本实现了优于现有生成式模型的多样化未来预测。
selection_source: fresh_fetch
motivation: 现有判别式世界模型预测确定性结果，而生成式模型计算成本过高，亟需一种高效且能预测多样化未来的方法。
method: 提出DeltaTok将连续帧的特征差异编码为单个delta token，并构建DeltaWorld模型利用该紧凑表示进行多假设训练和高效生成。
result: 实验显示，DeltaWorld在参数量减少35倍、计算量减少2000倍的情况下，预测结果比现有生成式模型更符合真实情况。
conclusion: 该方法成功实现了高效且多样化的视频世界建模，显著降低了计算资源需求。
---

## 摘要
预测多样的未来状态是视频世界建模的一个核心挑战。判别式世界模型产生确定性预测，隐式地对可能的未来进行平均，而现有的生成式世界模型计算成本依然高昂。近期研究表明，在视觉基础模型（VFM）的特征空间而非针对像素重建优化的潜空间中预测未来，所需的世界模型参数显著减少。然而，大多数此类方法仍属于判别式方法。在本文中，我们提出了DeltaTok和DeltaWorld。DeltaTok是一种分词器，将连续帧之间的VFM特征差异编码为单个连续的“delta” token；DeltaWorld是一种作用于这些token的生成式世界模型，能够高效生成多样且合理的未来。Delta token将视频从三维时空表示简化为一维时间序列，例如在512x512帧下实现了1024倍的token压缩。这种紧凑的表示使得多假设训练变得可行，即在并行生成多个未来后，仅对最佳结果进行监督。在推理阶段，这实现了单次前向传播中的多样预测。在密集预测任务上的实验表明，DeltaWorld预测的未来与现实世界结果更加吻合，且参数量减少超过35倍，FLOPs减少超过2000倍，优于现有的生成式世界模型。代码和权重：https://deltatok.github.io。

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 论文总结：A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens

## 1. 论文的核心问题与整体含义
*   **研究背景**：世界模型对于自动驾驶和机器人等智能体预测未来状态至关重要。由于未来的不确定性，模型需要能够生成多种可能的未来。
*   **核心问题**：现有的**判别式世界模型**（Discriminative World Models）倾向于预测单一的平均结果，无法覆盖多模态的未来；而现有的**生成式世界模型**（Generative World Models，如基于扩散或自回归的模型）虽然能生成多样未来，但计算成本极高（需要多次迭代或大量空间Token），难以实际应用。
*   **研究动机**：为了结合两者的优点，即既能高效生成又能覆盖多样化未来，本文提出利用视觉基础模型（VFM）的语义特征空间，并进一步通过压缩帧间差异来降低计算冗余，实现高效生成式建模。

## 2. 论文提出的方法论
*   **核心思想**：提出 **DeltaTok** 分词器，将连续两帧之间的特征差异压缩为单个“Delta Token”，将视频从三维时空表示（H×W×T）简化为一维时间序列（T）。在此基础上构建 **DeltaWorld** 模型，结合 **Best-of-Many (BoM)** 训练策略，实现单次前向传播生成多样化未来。
*   **关键技术细节**：
    *   **DeltaTok 分词器**：
        *   **编码器**：输入当前帧 $x_t$ 和前一帧 $x_{t-1}$ 的 VFM 特征，输出单个 Delta Token $z_t$。该 Token 编码了帧间的变化信息。
        *   **解码器**：输入前一帧特征 $x_{t-1}$ 和 Delta Token $z_t$，重建当前帧特征 $\hat{x}_t$。
        *   **优势**：利用了视频帧间的高冗余性，大幅压缩了序列长度（例如 512×512 图像可减少 1024 倍 Token 数量）。
    *   **DeltaWorld 预测器**：
        *   基于 Transformer 架构，输入为过去帧的 Delta Token 序列，直接预测下一个 Delta Token。
        *   由于序列极短，预测器的计算量极小。
    *   **Best-of-Many (BoM) 训练目标**：
        *   **训练阶段**：从高斯分布采样 $K$ 个噪声查询，并行生成 $K$ 个未来预测，仅计算与真实值最接近的那个预测的损失并进行反向传播。这使得模型学会将不同的噪声映射为不同的合理未来。
        *   **推理阶段**：输入不同的噪声查询，仅需一次前向传播即可生成多样化的未来预测，避免了扩散模型的多步迭代去噪过程。

## 3. 实验设计
*   **数据集**：
    *   **训练集**：约 400 万样本的视频数据集，涵盖多种场景。
    *   **评估集**：使用了密集预测基准中的三个数据集：
        *   **VSPW**：视频场景解析（语义分割）。
        *   **Cityscapes**：街景语义分割。
        *   **KITTI**：单目深度估计。
*   **Benchmark**：
    *   **任务**：语义分割和深度估计。
    *   **预测跨度**：短期（~0.2秒，直接预测）和中期（~0.6秒，自回归滚动预测）。
    *   **评估指标**：分割任务使用 mIoU，深度任务使用 RMSE。对于生成模型，报告 20 个样本中的“最佳”和“平均”分数。
*   **对比方法**：
    *   **Copy last**：简单基线，复制最后一帧。
    *   **Present**：上界，使用真实未来帧。
    *   **DINO-world**：判别式基线（作者复现版本），在 VFM 特征空间进行确定性预测。
    *   **Cosmos-4B / 12B**：现有的生成式世界模型基线，基于像素生成的扩散模型。

## 4. 资源与算力
*   **硬件环境**：单节点配备 8 张 NVIDIA H200 GPU。
*   **训练配置**：
    *   使用 BF16 混合精度训练。
    *   Tokenizer 训练：50K 迭代，Batch size 1024。
    *   Predictor 训练：300K 迭代 + 5K 微调，Batch size 1024。
*   **算力说明**：论文未明确给出具体的总训练时长（如小时或天数），但提供了相对于基线模型的训练时间和显存占用倍率对比。例如，DeltaWorld 的训练时间约为判别式基线的 0.5 倍，显存占用为 0.2 倍。

## 5. 实验数量与充分性
*   **主实验**：在 3 个数据集、2 个任务、2 个时间跨度上进行了全面对比，验证了模型在预测精度和多样性上的优势。
*   **消融实验**：
    *   逐步展示了从“判别式模型” -> “BoM训练” -> “帧压缩” -> “Delta压缩”的演变过程，清晰验证了各组件的贡献。
    *   分析了 BoM 训练中采样数量 $K$ 对性能的影响。
*   **泛化性验证**：在附录中验证了 Delta Token 在判别式模型（如 DINO-world, DINO-Foresight）上的有效性，证明了该表示方法的通用性。
*   **评价**：实验设计充分且客观，对比方法涵盖了判别式和生成式的代表性工作，且复现了 DINO-world 作为强基线，对比公平。

## 6. 论文的主要结论与发现
*   **效率与性能双赢**：DeltaWorld 在参数量减少 **35 倍**、FLOPs 减少 **2000 倍** 的情况下，其生成的最佳预测质量优于现有的生成式大模型 Cosmos。
*   **紧凑表示的有效性**：将视频压缩为一维 Delta Token 序列不仅极大降低了计算成本，还通过“预测变化量”的自然先验（即不变化则保持原状）提升了预测稳定性。
*   **单次前向生成**：通过 BoM 训练策略，成功实现了在单次前向传播中生成多样化且合理的未来预测，解决了传统生成模型推理慢的问题。

## 7. 优点
*   **极高的计算效率**：通过 Delta Token 压缩，将预测器的计算负担降至极低，使得生成式世界模型在资源受限场景下变得可行。
*   **巧妙的训练策略**：BoM 训练目标简单有效，避免了复杂的变分推断或多步扩散过程，直接实现了多模态生成的能力。
*   **语义级建模**：在 VFM 特征空间操作，避免了像素级重建的冗余计算，专注于语义层面的动态变化。

## 8. 不足与局限
*   **概率分布建模缺失**：BoM 训练目标虽然能生成多样样本，但缺乏显式的概率分布建模（如扩散模型的去噪过程），无法保证采样概率与真实事件概率的一致性，且受限于训练时的采样数量 $K$。
*   **误差累积风险**：Delta Token 依赖于前一帧的特征进行解码，在自回归长序列预测中，解码误差可能会逐步累积，导致特征漂移。
*   **依赖 VFM 质量**：模型性能高度依赖于底层视觉基础模型（DINOv3）的特征表达能力，若 VFM 在某些场景下失效，世界模型也会受到影响。

（完）

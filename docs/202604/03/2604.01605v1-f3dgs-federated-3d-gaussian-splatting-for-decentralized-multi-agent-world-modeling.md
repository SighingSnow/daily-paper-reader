---
title: "F3DGS: Federated 3D Gaussian Splatting for Decentralized Multi-Agent World Modeling"
title_zh: F3DGS：面向去中心化多智能体世界建模的联邦3D高斯溅射
authors: "Morui Zhu, Mohammad Dehghani Tezerjani, Mátyás Szántó, Márton Vaitkus, Song Fu, Qing Yang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01605v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 联邦多智能体3D重建用于世界建模
tldr: 针对现有3DGS依赖中心化数据限制了分布式应用的问题，本文提出F3DGS框架。该方法利用LiDAR点云构建全局几何支架，在联邦优化中固定几何位置仅更新外观属性，并通过可见性感知聚合解决多智能体部分观测问题。实验显示其重建质量媲美中心化训练，并发布了相关数据集。
selection_source: fresh_fetch
motivation: 现有3DGS方法依赖中心化数据访问，难以适应通信受限的分布式多智能体场景。
method: 该方法利用LiDAR构建共享几何支架，固定几何位置并仅更新外观属性，通过可见性感知聚合实现联邦优化。
result: 实验表明该方法在实现分布式优化的同时，重建质量可媲美中心化训练。
conclusion: F3DGS有效解决了去中心化多智能体3D重建问题，并公开了数据集与源代码。
---

## 摘要
我们提出了F3DGS，这是一个用于去中心化多智能体3D重建的联邦3D高斯溅射框架。现有的3DGS流程假设可以集中访问所有观测数据，这限制了其在分布式机器人场景中的适用性，因为在这些场景中智能体独立运行，且集中式数据聚合可能受到限制。将集中式训练直接扩展到多智能体系统会引入通信开销和几何不一致性问题。F3DGS首先通过配准来自多个客户端的本地合并LiDAR点云，构建共享几何支架，以初始化全局3DGS模型。在联邦优化过程中，高斯位置被固定以保持几何对齐，而每个客户端仅更新外观相关属性，包括协方差、不透明度和球谐系数。服务器使用可见性感知聚合来聚合这些更新，根据每个客户端观测每个高斯的频率对其贡献进行加权，从而解决了多智能体探索中固有的部分可观测性挑战。为了评估去中心化重建效果，我们收集了一个包含同步LiDAR、RGB和IMU测量数据的多序列室内数据集。实验表明，F3DGS实现了与集中式训练相当的重建质量，同时支持跨智能体的分布式优化。数据集、开发工具包和源代码将公开发布。

## Abstract
We present F3DGS, a federated 3D Gaussian Splatting framework for decentralized multi-agent 3D reconstruction. Existing 3DGS pipelines assume centralized access to all observations, which limits their applicability in distributed robotic settings where agents operate independently, and centralized data aggregation may be restricted. Directly extending centralized training to multi-agent systems introduces communication overhead and geometric inconsistency. F3DGS first constructs a shared geometric scaffold by registering locally merged LiDAR point clouds from multiple clients to initialize a global 3DGS model. During federated optimization, Gaussian positions are fixed to preserve geometric alignment, while each client updates only appearance-related attributes, including covariance, opacity, and spherical harmonic coefficients. The server aggregates these updates using visibility-aware aggregation, weighting each client's contribution by how frequently it observed each Gaussian, resolving the partial-observability challenge inherent to multi-agent exploration. To evaluate decentralized reconstruction, we collect a multi-sequence indoor dataset with synchronized LiDAR, RGB, and IMU measurements. Experiments show that F3DGS achieves reconstruction quality comparable to centralized training while enabling distributed optimization across agents. The dataset, development kit, and source code will be publicly released.

---

## 论文详细总结（自动生成）

# 论文总结：F3DGS：面向去中心化多智能体世界建模的联邦3D高斯溅射

## 1. 论文的核心问题与整体含义

*   **研究背景**：现有的3D高斯溅射（3DGS）技术通常假设所有训练数据（图像）都集中在一台机器上。然而，在分布式机器人或多智能体场景中，由于通信带宽限制、数据隐私保护以及系统可扩展性需求，集中式数据聚合往往不可行。
*   **核心问题**：直接将联邦学习应用于3DGS面临两大挑战：
    1.  **几何漂移**：不同客户端独立优化会导致高斯原子的位置发生漂移，简单的参数平均会导致几何结构混乱。
    2.  **部分可观测性**：每个客户端只能观测到场景的一部分，简单的平均聚合会稀释观测较多区域的优质参数，被未观测区域的无效参数干扰。
*   **整体含义**：本文旨在解决去中心化环境下的高质量3D场景重建问题，提出了一种无需共享原始图像数据即可协同构建全局一致3D场景的方法。

## 2. 论文提出的方法论

*   **核心思想**：**几何与外观解耦**。构建一个共享的、固定的几何支架，在联邦优化过程中仅更新外观属性，并利用可见性感知聚合策略处理部分可观测问题。
*   **关键技术细节**：
    1.  **度量尺度位姿构建**：
        *   利用LiDAR SLAM生成全局点云地图作为锚点。
        *   各客户端通过单目视觉里程计估计本地轨迹，使用Umeyama算法将本地轨迹对齐到全局LiDAR地图，并通过边界平滑消除拼接处的跳变，生成全局一致的度量尺度位姿。
    2.  **全局高斯初始化**：
        *   将深度观测投影到3D空间生成密集点云，初始化约 $6 \times 10^5$ 个高斯原子的位置、颜色和尺度。
    3.  **本地联邦优化**：
        *   **冻结几何**：在本地训练中，固定高斯中心位置 $\mu$，仅更新外观相关参数 $\theta_{app} = \{s_i, q_i, \alpha_i, c_i\}$（尺度、旋转、不透明度、球谐系数）。
        *   **可见性追踪**：统计每个高斯在本地渲染中被投影的次数 $v_{k,i}$。
    4.  **可见性感知聚合**：
        *   服务器收集各客户端的参数更新和可见性计数。
        *   聚合权重公式：$\alpha_{k,i} = \frac{v_{k,i}}{\sum_{j=1}^K v_{j,i} + \epsilon}$。
        *   全局参数更新：$a_i = \sum_{k=1}^K \alpha_{k,i} a_{k,i}$。即根据客户端对特定高斯的观测频率加权平均，未观测到的客户端不参与该高斯参数的更新。

## 3. 实验设计

*   **数据集**：作者自建的 **MeanGreen 数据集**。包含9个室内走廊序列（序列03-11），总计8739帧数据。采集设备为配备单目RGB相机（OAK-D-Pro）和LiDAR（Velodyne VLP-16）的移动机器人。
*   **Benchmark与评估协议**：
    *   **本地评估**：在客户端各自的验证集上评估本地模型性能。
    *   **全局评估**：在所有客户端验证集的并集上评估聚合后的全局模型性能。
    *   **评价指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（学习感知图像块相似度）。
*   **对比方法**：论文主要进行了内部对比分析，比较了“本地模型”与“全局聚合模型”的性能差异，验证联邦聚合的有效性。并未直接与FedAvg等传统联邦学习算法或FedNeRF等其他联邦重建方法进行定量对比，而是定性分析了传统方法在3DGS上的局限性（如几何漂移）。

## 4. 资源与算力

*   **算力说明**：**论文正文中未明确说明**所使用的GPU型号、数量或具体的训练时长。
*   **训练配置**：提到了训练预算为固定7000步，通信轮次 $R=7$，每轮本地优化步数 $T=1000$。

## 5. 实验数量与充分性

*   **实验数量**：
    *   主实验覆盖了9个数据序列。
    *   进行了客户端级别的详细分析（表4），定位性能下降的具体来源。
    *   进行了通信频率的消融实验（表3），测试了不同通信轮次 $R$ 对性能的影响。
*   **充分性与客观性**：
    *   实验设计基本充分，能够验证方法的有效性。特别是客户端级别的分析深入探讨了部分可观测性带来的影响。
    *   **局限性**：缺乏与现有联邦学习基线（如直接应用FedAvg于3DGS）的定量对比实验，仅依靠理论分析说明其不足，略显主观。此外，数据集均为室内走廊环境，场景多样性有待进一步验证。

## 6. 论文的主要结论与发现

*   **结论**：F3DGS框架能够在不共享原始图像数据的前提下，实现高质量的分布式3D重建。
*   **发现**：
    1.  固定几何位置有效避免了联邦学习中的几何漂移问题。
    2.  全局聚合后的模型性能（PSNR）在大多数序列上接近本地训练模型（差距通常在2dB以内）。
    3.  性能下降主要集中在时间序列分割的边界处或观测条件恶劣的片段，而非均匀分布。
    4.  增加通信频率（聚合次数）有助于提升全局模型的一致性。

## 7. 优点

*   **创新性强**：首次针对3DGS在联邦学习场景下的“几何漂移”和“部分可观测”两大痛点提出了明确的解决方案（几何冻结+可见性加权）。
*   **实用价值高**：解决了多智能体协同作业中的数据隐私和通信瓶颈问题，适用于分布式机器人场景。
*   **数据贡献**：发布了包含LiDAR、RGB和IMU的多模态室内数据集，填补了相关领域的数据空白。

## 8. 不足与局限

*   **几何刚性**：由于高斯位置在初始化后被固定，模型无法修正初始化阶段产生的位姿误差或几何误差，缺乏后续优化几何的能力。
*   **场景限制**：实验仅在室内走廊环境进行，对于纹理丰富或结构复杂的室外大场景的适应性尚未验证。
*   **对比缺失**：未提供与现有联邦学习基线方法的定量对比结果，难以直观量化其相对于标准联邦平均算法的具体优势幅度。
*   **硬件细节缺失**：未报告计算资源消耗，难以评估其实际部署成本。

（完）

---
title: "Nomad: Autonomous Exploration and Discovery"
title_zh: Nomad：自主探索与发现
authors: "Bokang Jia, Samta Kamboj, Satheesh Katipomu, Seung Hun Han, Neha Sengupta, Andrew Jackson"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29353v1"
tags: ["query:agent"]
score: 9.0
evidence: 探索智能体具备文档搜索、网络搜索和数据库工具能力
tldr: Nomad 是一个自主数据探索与洞察发现系统，旨在解决传统查询驱动系统受限于人类预设框架的问题。它采用探索优先架构，通过构建探索地图平衡广度与深度，利用智能体生成并验证假设，最终生成带引用的高质量报告。实验表明，Nomad 在可信度、报告质量和洞察多样性上均优于基线，实现了从被动回答到主动发现的跨越。
selection_source: fresh_fetch
motivation: 现有的查询驱动系统受限于人类的预设框架，无法全面覆盖数据中潜在的洞察空间。
method: 提出探索优先架构，构建探索地图平衡广度与深度，通过智能体生成假设、调用工具调查并由独立验证器核查，最终生成报告。
result: 在联合国和世卫组织报告数据集上的实验显示，Nomad 生成的报告在可信度、质量和洞察多样性方面均优于基线模型。
conclusion: Nomad 迈出了自主系统的关键一步，不仅能回答问题，更能主动发现有价值的探索方向和洞察。
---

## 摘要
我们介绍了 Nomad，一个用于自主数据探索和洞察发现的系统。面对文档、数据库或其他数据源的语料库，用户往往不知道所有可能被探索的问题、假设或联系的全集。因此，查询驱动的问答系统和提示驱动的深度研究系统仍然受限于人类的框架，往往无法覆盖更广阔的洞察空间。Nomad 通过探索优先架构解决了这一问题。它在领域上构建显式的探索图，并系统地遍历该图以平衡广度与深度。它生成并选择假设，并利用能够使用文档搜索、网络搜索和数据库工具的探索智能体对这些假设进行调查。候选洞察随后由独立的验证器进行检查，之后进入报告生成流程，产出带有引用的报告和更高层次的元报告。我们还提出了一个针对自主发现系统的综合评估框架，用于衡量可信度、报告质量和多样性。利用选定的联合国（UN）和世界卫生组织（WHO）报告语料库，我们展示了 Nomad 生成的报告比基线更可信、质量更高，同时在多次运行中产生了更多样化的洞察。Nomad 是迈向自主系统的一步，这些系统不仅能回答用户问题或进行定向研究，还能发现哪些问题、研究方向和洞察在根本上值得被呈现。

## Abstract
We introduce Nomad, a system for autonomous data exploration and insight discovery. Given a corpus of documents, databases, or other data sources, users rarely know the full set of questions, hypotheses, or connections that could be explored. As a result, query-driven question answering and prompt-driven deep-research systems remain limited by human framing and often fail to cover the broader insight space.   Nomad addresses this problem with an exploration-first architecture. It constructs an explicit Exploration Map over the domain and systematically traverses it to balance breadth and depth. It generates and selects hypotheses and investigates them with an explorer agent that can use document search, web search, and database tools. Candidate insights are then checked by an independent verifier before entering a reporting pipeline that produces cited reports and higher-level meta-reports.   We also present a comprehensive evaluation framework for autonomous discovery systems that measures trustworthiness, report quality, and diversity. Using a corpus of selected UN and WHO reports, we show that \nomad{} produces more trustworthy and higher-quality reports than baselines, while also producing more diverse insights over several runs.   Nomad is a step toward autonomous systems that not only answer user questions or conduct directed research, but also discover which questions, research directions, and insights are worth surfacing in the first place.

---

## 论文详细总结（自动生成）

# 论文总结：Nomad: Autonomous Exploration and Discovery

## 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的查询驱动问答系统和提示驱动的深度研究系统，其探索范围受限于用户预先设定的框架或问题。面对大型文档集合，用户往往无法预知所有有价值的问题、假设或隐藏的联系，导致数据中蕴含的潜在洞察空间未被充分探索。
*   **整体含义**：论文旨在构建一个能够自主探索数据、主动发现有价值的研究方向和洞察的系统，而非仅仅被动执行用户指令。这标志着从“回答已知问题”到“发现未知问题”的范式转变，目标是最大化从数据中提取的价值，突破人类认知的局限。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
*   **核心思想**：提出“探索优先”架构，通过构建结构化的探索空间（探索地图），系统性地平衡探索的广度与深度，并引入独立的验证机制以确保洞察的可信度。
*   **关键技术细节**：
    *   **探索地图构建**：系统首先构建一个图结构 `G=(V,E)`，包含根节点、主题节点、概念节点、假设节点、洞察节点和文档节点。根据是否有语料库，采用“自顶向下”（从目标生成主题树）或“自底向上”（从文档提取概念并聚类形成主题树）两种方式构建。地图可通过Web搜索动态扩展。
    *   **主题选择与假设生成**：采用广度优先策略遍历主题树，优先选择探索分数低的节点以促进多样性。在叶节点处，系统生成多个假设，并根据相关性、影响力和多样性进行评分，选择最优假设进行探索。
    *   **探索者-验证者循环**：
        *   **探索者**：一个ReAct风格的智能体，使用文档搜索、Web搜索和数据库查询工具，迭代地收集证据、修正假设，最终提交候选洞察。
        *   **验证者**：一个独立的智能体，将候选洞察分解为原子子主张，使用相同工具逐一验证，并给出评分。只有通过验证的洞察才进入报告生成阶段。
    *   **报告生成与合成**：验证后的洞察被转化为结构化、带引用的报告。系统还会定期将多个报告合成为更高层次的“元报告”，以揭示跨报告的主题和趋势。
*   **算法流程**：整体流程为：探索地图构建 -> 主题选择 -> 假设生成与选择 -> 探索者调查 -> 验证者核查 -> 报告生成 -> 元报告合成。

## 3. 实验设计：数据集、场景、Benchmark与对比方法
*   **数据集/场景**：使用了一个公开的联合国（UN）和世界卫生组织（WHO）政策与统计报告语料库，涵盖健康、气候、发展、贸易等多个领域。实验设置了两种场景：
    *   **自底向上**：系统被提供语料库，从文档中构建探索地图。
    *   **自顶向下**：系统仅被提供研究目标，无语料库，完全依赖Web搜索构建地图。
*   **Benchmark**：提出了一个综合评估框架，衡量四个维度：
    *   **可信度**：包括数值证据的“数值接地度”和事实主张的“事实性”。
    *   **质量**：通过LLM评判，从分析质量、原创性、覆盖度、可行动性、呈现质量五个方面评估。
    *   **独特性**：衡量报告内部章节间的信息重复度。
    *   **多样性**：衡量多次运行生成的不同报告之间的差异度。
*   **对比方法**：选择了两个代表性基线：
    *   **GPT Researcher**：一个开源的深度研究工具。
    *   **OpenAI o3-deep-research**：一个商业化的深度研究API。

## 4. 资源与算力
*   **未明确说明**：论文中未提供关于具体使用的GPU型号、数量、训练时长或推理成本的详细信息。实验部分主要关注系统输出质量，而非计算效率分析。

## 5. 实验数量与充分性
*   **实验数量**：在两种场景（自底向上、自顶向下）下，分别与两个基线进行了对比实验。评估了多个报告（例如，自底向上场景下Nomad生成50份报告，基线分别生成57和44份）。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了系统设计的核心目标（可信度、质量、多样性），对比了当前主流的深度研究工具，基本验证了方法的有效性。
    *   **客观性**：评估框架设计较为细致，特别是数值接地度的规则化评估和事实性的LLM评判，减少了主观偏差。
    *   **不足**：论文未报告详细的消融实验（例如，单独评估探索地图、验证者等组件的贡献），也未深入分析不同模型选择对系统性能的影响。

## 6. 论文的主要结论与发现
*   **主要结论**：Nomad系统在自主数据探索和洞察发现任务上表现优异。
*   **具体发现**：
    *   **可信度更高**：Nomad在数值接地度和事实性上均显著优于基线，尤其在自底向上场景下优势明显。其报告包含大量数值证据，且大部分（75-81%）可被验证。
    *   **质量更优**：Nomad在整体质量、分析质量和可行动性上得分更高，表明其报告更具深度和实用价值。
    *   **多样性更强**：Nomad生成的报告在多次运行间表现出远高于基线的多样性（约5倍于GPT Researcher，2倍于o3-deep-research），证明了其探索优先架构能有效避免收敛到相似结论。
    *   **独特性较弱**：Nomad报告内部章节间的独特性得分低于基线，表明存在一定冗余，这是当前报告生成模块的一个弱点。

## 7. 优点：方法或实验设计上的亮点
*   **架构创新**：明确提出“探索优先”范式，将探索地图作为控制状态，系统性地驱动广度与深度探索，而非依赖随机采样或用户指令。
*   **验证机制**：引入独立的验证者智能体，将洞察分解为原子主张进行核查，有效降低了长文本生成中的幻觉风险，提升了报告可信度。
*   **评估框架全面**：提出了针对自主发现系统的多维度评估框架，特别是数值接地度的细粒度、规则化评估方法，为该领域提供了有价值的评估基准。
*   **流程完整**：从地图构建、假设生成、探索验证到报告合成，形成了端到端的自动化发现流程，产出可审计、带引用的报告。

## 8. 不足与局限
*   **实验覆盖**：缺少消融实验，未能量化各核心组件（如探索地图、验证者）的具体贡献。未详细分析系统在不同模型配置下的性能与成本。
*   **偏差风险**：评估中的质量、独特性等维度依赖LLM评判，可能存在模型自身的偏好或偏差。
*   **应用限制**：实验主要在公开的UN/WHO报告语料库上进行，虽然提及了在企业内部文档上的定性成功，但缺乏在更多领域、更大规模或更复杂数据（如多模态数据）上的定量验证。
*   **系统弱点**：报告生成模块导致内部章节独特性较低，存在信息冗余问题，有待改进。
*   **算力信息缺失**：未提供系统运行所需的计算资源详情，难以评估其实际部署成本和效率。

（完）

---
title: "Data-Model Co-Driven Continuous Channel Map Construction: A Perceptive Foundation for Embodied Intelligent Agents in 6G Networks"
title_zh: 数据-模型协同驱动的连续信道地图构建：6G网络中具身智能体的感知基础
authors: "Tianrun Qi, Cheng-Xiang Wang, Chen Huang, Junling Li, John S Thompson"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01060v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 作为具身智能体世界模型的信道地图
tldr: 针对 6G 网络中具身智能体实时信道感知需求，本文提出了一种数据-模型协同驱动的连续信道地图构建框架。该方法结合混合射线追踪模型与归纳图神经网络，利用物理先验和稀疏测量实现时空连续信道地图的高效插值。实验表明，该框架显著优于现有基线，为智能体提供了准确且可查询的信道信息基础。
selection_source: fresh_fetch
motivation: 针对现有信道地图构建中测量成本高、纯模型精度低及纯数据方法计算复杂的问题，旨在实现实时准确的连续信道感知。
method: 提出数据-模型协同驱动框架，结合混合射线追踪模型生成的物理先验与归纳边条件图神经网络，实现无需重训练的实时空间插值。
result: 基于实测数据集的评估显示，该框架在准确性和实时性上均显著优于纯数据驱动和纯模型驱动的基线方法。
conclusion: 该研究成功构建了高精度的连续信道地图，为 6G 网络中的具身智能体提供了可靠的无线信道感知基础。
---

## 摘要
未来的6G网络将容纳海量具身智能体，这些智能体需要连续空间上的实时信道感知能力以进行自主决策。通过预先获取特定位置的信道状态信息（CSI），信道地图可作为具身智能的基础世界模型，实现无线信道感知。然而，通过测量获取CSI成本高昂，实际中往往只能获得稀疏观测，导致智能体对未访问位置的信道状况一无所知。同时，纯模型驱动的信道地图虽能提供密集CSI，但往往准确性和鲁棒性不佳；而基于稀疏测量的纯数据驱动插值方法，其计算成本对于实时更新而言过高。为应对这些挑战，本文提出了一种数据-模型协同驱动（DMcD）框架，通过两阶段插值构建时空连续信道地图。首先，开发了一种混合射线追踪与几何信道模型（H-RT/GBSM），用于捕捉动态散射体，提供与测量统计特性相匹配的密集时变信道特性，作为物理一致性先验。随后，利用归纳式边条件图神经网络（InductE-GNN）将该先验与稀疏测量数据融合，执行实时空间插值，实现无需重训练的快速在线适应，确保与动态物理现实的同步。基于实测数据集的评估表明，所提出的DMcD框架显著优于纯数据和纯模型基线，为具身智能体提供了准确且可查询的信道信息。

## Abstract
Future 6G networks will host massive numbers of embodied intelligent agents, which require real-time channel awareness over continuous-space for autonomous decision-making. By pre-obtaining location-specific channel state information (CSI), channel map can be served as a foundational world model for embodied intelligence to achieve wireless channel perception. However, acquiring CSI via measurements is costly, so in practice only sparse observations are available, leaving agents blind to channel conditions at unvisited locations. Meanwhile, purely model-driven channel maps can provide dense CSI but often yields unsatisfactory accuracy and robustness, while purely data-driven interpolation from sparse measurements is computationally prohibitive for real-time updates. To address these challenges, this paper proposes a data-model co-driven (DMcD) framework that performs a two-stage interpolation toward a space-time continuous channel map, First, a hybrid ray tracing and geometry-based channel model (H-RT/GBSM) is developed to capture dynamic scatterers, providing dense, time-variant channel properties that match measurement statistics as a physically consistent prior. Then, an inductive edge-conditioned graph neural network (InductE-GNN) fuses the prior with sparse measurements to perform real-time spatial interpolation, enabling rapid online adaptation without retraining, ensuring the synchronization with the dynamic physical reality. Evaluations with measured datasets show that the proposed DMcD framework significantly outperforms data-only and model-only baselines, providing accurate and queryable channel information for embodied intelligent agents.

---

## 论文详细总结（自动生成）

以下是对论文《Data-Model Co-Driven Continuous Channel Map Construction: A Perceptive Foundation for Embodied Intelligent Agents in 6G Networks》的详细总结：

### 1. 论文的核心问题与整体含义

*   **研究背景**：未来的 6G 网络将支持海量具身智能体（如无人机、自动驾驶汽车、服务机器人），这些智能体需要实时的连续空间信道感知能力以进行自主决策（如轨迹规划、波束选择）。信道地图作为一种“世界模型”，能够提供特定位置的信道状态信息（CSI）。
*   **核心问题**：现有的信道地图构建方法存在显著局限：
    *   **测量成本高**：实际测量只能获得稀疏的观测数据，导致智能体在未测量位置“失明”。
    *   **纯模型驱动局限**：虽然能提供密集 CSI，但准确性和鲁棒性不足，难以精确匹配实际环境。
    *   **纯数据驱动局限**：基于稀疏测量的插值计算量大，且难以适应动态环境的实时变化（通常需要重新训练）。
*   **整体含义**：本文旨在解决如何在稀疏测量下构建高精度、时空连续且能实时更新的信道地图，为具身智能体提供可靠的无线环境感知基础。

### 2. 论文提出的方法论

本文提出了一种**数据-模型协同驱动框架**，通过两阶段插值实现时空连续信道地图的构建。

*   **核心思想**：利用物理模型生成动态先验，再利用 AI 算法融合稀疏测量数据进行实时修正，结合了物理模型的一致性和数据驱动的准确性。
*   **第一阶段：模型驱动阶段（混合信道模型 H-RT/GBSM）**
    *   **目的**：生成物理一致的密集先验数据。
    *   **技术细节**：提出了混合射线追踪与几何随机模型（H-RT/GBSM）。该模型包含三个分量：
        1.  **静态分量**：基于射线追踪（RT）技术模拟静态环境（墙壁、建筑等）的镜面反射路径。
        2.  **动态分量**：基于几何随机模型（GBSM）捕捉移动散射体（如行人、车辆）引起的时变多径分量。
        3.  **密集多径分量（DMC）**：模拟室内环境中粗糙表面引起的漫反射散射，弥补 RT 模型在功率时延谱上的不足。
    *   **输出**：能够生成符合测量统计特性的时变 CSI，作为后续 AI 模型的输入先验。
*   **第二阶段：数据驱动阶段（归纳式边条件图神经网络 InductE-GNN）**
    *   **目的**：利用稀疏测量数据对模型先验进行实时空间插值和修正。
    *   **图构建**：将空间位置建模为图节点。不同于传统的欧氏距离，本文利用 **Wasserstein 距离**构建 KNN 图，以捕捉信道分布的相似性。边特征包含相对位移、欧氏距离、Wasserstein 距离和视距（LoS/NLoS）状态。
    *   **特征工程**：对于未观测节点，使用反距离加权（IDW）插值作为初始特征先验。
    *   **网络架构**：设计了 **InductE-GNN**，包含两个关键步骤：
        1.  **广义聚合**：采用 GraphSAGE 的采样聚合策略，实现归纳学习，使模型能泛化到未见过的节点，无需重训练即可适应环境变化。
        2.  **边感知修正**：利用边条件卷积（ECC），根据边特征动态生成滤波器，进行消息传递。
    *   **损失函数**：设计了复合损失函数 $L = L_r + L_s + L_p$：
        *   $L_r$：观测节点上的重构损失（NMSE）。
        *   $L_s$：基于图拉普拉斯的平滑正则项，鼓励相邻相似节点预测一致。
        *   $L_p$：先验一致性损失，约束未观测节点的预测不偏离物理先验太远。

### 3. 实验设计

*   **数据集与场景**：
    *   **室内静态场景（28 GHz）**：包含丰富的密集多径分量（DMC），约 11,974 个空间位置点，用于验证 DMC 建模和静态插值。
    *   **室外动态场景（5.5 GHz）**：包含丰富的移动散射体（车辆、行人），用于验证动态信道建模和时空连续性。
*   **Benchmark 方法**：
    *   **传统方法**：反距离加权插值（IDW）。
    *   **深度学习基线**：GRU（门控循环单元）、GCN（图卷积网络）、CGAN（条件生成对抗网络）。
    *   **模型对比**：纯射线追踪（RT-only）与提出的混合模型（HCM）对比。
*   **评估指标**：
    *   统计特性：RMS 时延扩展（DS）、角度扩展（AS）的累积分布函数（CDF）。
    *   预测精度：未观测节点上的归一化均方误差（NMSE）。
    *   地图可视化：接收功率、DS、AS 的空间分布图。
    *   时域一致性：时变功率时延谱（PSD）对比。

### 4. 资源与算力

*   文中明确提到了在线更新延迟测试的硬件环境：**Intel Core i9-14900 CPU 和 32 GB RAM**。
*   论文未明确说明模型训练阶段使用的 GPU 型号或数量，也未详细列出具体的训练时长（仅展示了 NMSE 随训练轮次的变化趋势）。

### 5. 实验数量与充分性

*   **实验数量**：
    *   **模型验证**：对比了 HCM 模型与实测数据在 DS 和 AS 统计特性上的拟合程度，以及功率时延谱（PSD）的匹配度。
    *   **算法对比**：在相同训练预算下，对比了 InductE-GNN 与 GRU、GCN、CGAN 的收敛速度和最终 NMSE。
    *   **采样密度分析**：测试了不同观测节点数量（稀疏程度）下的插值性能。
    *   **消融实验**：验证了 Wasserstein 距离建图和 IDW 先验两个关键组件的有效性。
    *   **端到端评估**：可视化了 DMcD 框架生成的信道地图与实测参考图及 CGAN 结果的差异，并评估了室外动态场景的时序一致性。
*   **充分性与公平性**：实验设计较为全面，涵盖了静态/动态、室内/室外场景，对比了多种主流深度学习算法，并设置了消融实验验证各模块贡献。所有学习模型均在相同训练预算和采样协议下进行，保证了对比的公平性。

### 6. 论文的主要结论与发现

*   **模型层面**：提出的 H-RT/GBSM 模型能够准确捕捉静态、动态及 DMC 分量，其生成的统计特性（CDF 曲线）与实测数据高度吻合，优于纯 RT 模型。
*   **算法层面**：InductE-GNN 在稀疏观测下的插值精度（NMSE）显著优于 GCN、GRU 和 CGAN，且收敛更快。消融实验证明了 Wasserstein 图构建和先验一致性损失的有效性。
*   **框架层面**：DMcD 框架生成的信道地图在空间连续性和定位精度上均优于纯数据驱动方法（CGAN），有效消除了 NLoS 区域的伪影。同时，该框架实现了约 1.7 Hz 的更新频率，满足具身智能体的实时查询需求。

### 7. 优点

*   **创新性框架**：首次提出了数据-模型协同驱动的信道地图构建框架，有效解决了纯数据方法泛化差和纯模型方法精度低的问题。
*   **归纳式学习**：InductE-GNN 的设计实现了“免重训练”的在线适应，极大降低了动态环境下的计算开销，具有很高的工程实用价值。
*   **物理一致性**：引入 DMC 和动态散射体建模，弥补了传统 RT 模型的不足，提供了物理可信的先验。
*   **图构建策略**：利用 Wasserstein 距离代替欧氏距离构建图拓扑，更符合无线信道的物理传播特性。

### 8. 不足与局限

*   **计算延迟**：虽然满足实时性要求，但 DMcD 框架的更新周期（约 0.6 秒）相对于仅预测单一属性的方法仍有优化空间，因为其输出是完整的高维信道矩阵。
*   **硬件依赖**：实验主要基于 CPU 测试延迟，未详细探讨 GPU 加速下的训练和推理潜力，可能掩盖了算法在算力受限设备（如无人机端）上的实际表现。
*   **场景泛化**：虽然测试了室内外场景，但对于极端复杂或非视距（NLoS）极其严重的遮挡场景，模型先验的校准难度可能会增加，论文未深入讨论校准失败的风险。

（完）

---
title: Fuzzing with Agents? Generators Are All You Need
title_zh: 基于智能体的模糊测试？你只需要生成器
authors: "Vasudev Vikram, Rohan Padhye"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01442v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM代码智能体自动合成目标特定的模糊测试生成器
tldr: 现代模糊测试常结合生成器与变异策略，但编写定制生成器成本高。本文提出 Gentoo，利用 LLM Agent 自动合成特定目标的输入生成器。实验表明，Agent 合成的生成器在多数基准上分支覆盖率优于人类编写版本，且无需覆盖引导和变异即可达到高效测试效果，证明了高质量生成器足以独立驱动模糊测试。
selection_source: fresh_fetch
motivation: 针对编写定制化生成器成本高昂的问题，研究探索 AI Agent 能否自动合成高质量生成器从而取代传统的覆盖引导与变异策略。
method: 提出 Gentoo 框架，利用 LLM 编码 Agent 迭代合成并优化输入生成器，同时赋予其终端访问权限及可选的细粒度覆盖反馈。
result: 在 7 个真实 Java 库的测试中，Agent 合成的生成器在 4 个基准上分支覆盖率显著优于人类编写的基线。
conclusion: Agent 合成的生成器已编码足够的结构与语义逻辑，使得覆盖引导和变异策略变得不再必要，仅靠生成器即可实现高效测试。
---

## 摘要
现代基于生成器的模糊测试技术将轻量级输入生成器与覆盖引导变异相结合，以此作为探索目标程序深层执行路径的一种方法。先前研究中的另一种互补方法侧重于创建高度定制化、特定领域的生成器，这些生成器编码了足以触及深层程序状态的结构和语义逻辑；其挑战在于编写和测试这些复杂生成器的开销。我们研究了 AI 编程智能体是否能够自动合成此类针对特定目标的生成器，以及生成的生成器是否足够强大，从而完全消除对覆盖引导和变异的需求。我们的方法 Gentoo 包含一个 LLM 编程智能体（被授予终端访问权限及模糊测试目标与其库的源代码），该智能体被指示迭代地合成与优化输入生成器，并可选择为其提供细粒度的谓词级覆盖反馈。我们在 7 个现实世界 Java 库的模糊测试目标上，评估了 Gentoo 的三种配置与人工编写生成器的对比效果。研究结果表明，在 7 个基准测试中，智能体合成的生成器在 4 个测试上取得了比人工编写的基线生成器统计上显著更高的分支覆盖率。关键在于，使用覆盖引导和变异策略对智能体合成的生成器并无统计上显著的益处，但对所有人工编写的生成器却有显著效果，这表明智能体生成器中编码的结构和语义逻辑使得覆盖引导在很大程度上变得不再必要。

## Abstract
Modern generator-based fuzzing techniques combine lightweight input generators with coverage-guided mutation as a method of exploring deep execution paths in a target program. A complimentary approach in prior research focuses on creating highly customized, domain-specific generators that encode structural and semantic logic sufficient enough to reach deep program states; the challenge comes from the overhead of writing and testing these complex generators. We investigate whether AI coding agents can automatically synthesize such target-specific generators, and whether the resulting generators are strong enough to obviate the need for coverage guidance and mutation entirely.   Our approach, Gentoo, is comprised of an LLM coding agent (provided terminal access and source code of the fuzz target and its library) instructed to iteratively synthesize and refine an input generator, and optionally provided fine-grained predicate-level coverage feedback. We evaluate three configurations of Gentoo against human-written generators on fuzz targets for 7 real-world Java libraries. Our findings show that agent-synthesized generators achieve statistically significantly higher branch coverage than human-written baseline generators on 4 of 7 benchmarks. Critically, the use of coverage guidance and mutation strategies is not statistically significantly beneficial for agent-synthesized generators, but is significant for all human-written generators, suggesting that structural and semantic logic encoded in the agent generators makes coverage guidance largely unnecessary.

---

## 论文详细总结（自动生成）

# 论文总结：基于智能体的模糊测试？你只需要生成器

## 1. 论文的核心问题与整体含义
*   **研究背景**：现代模糊测试通常结合轻量级输入生成器与覆盖引导变异来探索深层路径。另一种方法是编写高度定制化的领域特定生成器（如 CSmith），虽能触及深层逻辑但人工成本极高。
*   **核心问题**：AI 编码智能体能否自动合成此类特定目标的生成器？合成出的生成器是否足够强大，以至于可以完全摒弃覆盖引导和变异策略？
*   **整体含义**：本研究挑战了传统模糊测试对“覆盖引导+变异”范式的依赖，提出若生成器本身编码了足够的结构与语义逻辑，则复杂的变异策略可能变得多余。

## 2. 论文提出的方法论
*   **核心思想**：提出 **Gentoo** 框架，利用 LLM 编码智能体迭代合成并优化输入生成器。智能体被赋予终端访问权限、目标库源码，并根据反馈不断修正生成器代码。
*   **三种配置**：
    *   **Gentoo-Base**：基础配置，智能体仅根据 JQF 执行统计和生成的输入样本进行优化，无额外分析。
    *   **Gentoo-S**：结合静态分析反馈。通过静态支配分析计算“分支支配得分”，识别控制大量未探索代码的谓词，并反馈给智能体。
    *   **Gentoo-L**：结合 LLM 分析反馈。让智能体自行阅读源码识别重要谓词，而非依赖静态分析工具。
*   **关键技术细节**：
    *   **分支支配得分**：公式为 $dom(b_i) = |R(b_i) \cap D(b_i)|$，其中 $R(b_i)$ 是从分支 $b_i$ 可达的节点集，$D(b_i)$ 是被 $b_i$ 支配的节点集。该得分用于量化谓词的重要性。
    *   **动态谓词反馈**：在模糊测试过程中插桩，记录每个谓词的分支覆盖情况，生成报告告知智能体哪些高价值分支未被覆盖。
    *   **迭代流程**：智能体读取源码 -> 编写/修改生成器 -> 运行短时模糊测试 -> 获取覆盖率/谓词反馈 -> 循环优化。

## 3. 实验设计
*   **数据集/场景**：选取了 7 个真实的 Java 库作为测试对象，涵盖字节码解析、ASN.1 加密结构、JavaScript 编译与解释、BZip2 压缩、JSON 解析和 ChocoPy 编译。
*   **Benchmark**：对比基线为先前研究中人工编写的生成器。
*   **对比方法**：
    *   **生成器来源对比**：Gentoo-Base, Gentoo-S, Gentoo-L vs. 人工编写。
    *   **模糊策略对比**：对每种生成器分别测试“覆盖引导变异”与“纯随机生成”的效果，以验证变异策略的必要性。

## 4. 资源与算力
*   **模型与成本**：使用 Claude Opus 4.5 模型（通过 AWS Bedrock）。平均每个生成器的合成成本为：Gentoo-Base $7.54，Gentoo-S $12.17，Gentoo-L $12.84。
*   **计算资源**：论文未提及具体 GPU 型号与数量。实验总计算量约为 39 个 CPU 天。
*   **时间预算**：每个生成器合成过程限时 1 小时，评估阶段每个模糊测试活动运行 3 小时。

## 5. 实验数量与充分性
*   **实验规模**：共进行了 105 次智能体合成运行（3 种配置 × 7 个基准 × 5 次重复），最终产生 101 个有效生成器（4 次失败）。评估阶段共进行了 280 次模糊测试实验。
*   **充分性与公平性**：
    *   实验设计包含多次重复（5次）以应对 LLM 和模糊测试的随机性。
    *   使用 Mann-Whitney U 检验验证统计显著性（$p < 0.05$），保证了结论的客观性。
    *   涵盖了多种类型的输入格式（编译器、解析器、压缩库），具有较好的代表性。

## 6. 论文的主要结论与发现
*   **覆盖率表现**：在 7 个基准测试中，智能体合成的生成器在 4 个上取得了显著高于人工编写基线的分支覆盖率（平均提升 11%-21%）。
*   **变异策略的必要性**：关键发现是，对于智能体合成的生成器，覆盖引导变异策略并未带来统计上显著的收益；而对于人工编写的生成器，变异策略在所有基准上均有显著帮助。这证明高质量的生成器可以独立于变异策略存在。
*   **反馈机制的作用**：谓词反馈（Gentoo-S 和 Gentoo-L）在 74% 的运行中提升了覆盖率，但最终结果与 Base 版本差距不大，说明智能体初始的代码合成能力是主要驱动力。

## 7. 优点
*   **范式创新**：成功论证了“生成器即所需”的观点，指出了从优化变异策略转向优化生成器合成的可能性。
*   **智能体设计**：赋予了智能体终端访问和迭代调试的能力，使其能像开发者一样运行测试、查看结果并修正代码，比单纯的代码生成模型更具实用性。
*   **深入的分析**：引入了分支支配得分和动态谓词反馈，为智能体提供了明确的优化方向，而非仅依赖模糊的覆盖率数字。

## 8. 不足与局限
*   **失败案例分析**：在 BCEL 基准测试中，智能体未能发现并利用库自带的高级构建 API，而是尝试手动拼接字节流，导致覆盖率远低于人工基线。这暴露了智能体在理解复杂 API 结构时的局限性。
*   **执行效率**：智能体生成的生成器往往比人工版本更复杂，导致执行吞吐量下降（中位数慢 32%）。
*   **数据污染风险**：论文承认无法排除 LLM 在预训练阶段见过相关测试库代码的可能性，这可能高估了智能体的零样本合成能力。
*   **适用范围**：实验仅限于 Java 语言和 JQF 框架，结论在其他语言或模糊测试工具上的通用性有待验证。

（完）

---
title: "DriveDreamer-Policy: A Geometry-Grounded World-Action Model for Unified Generation and Planning"
title_zh: DriveDreamer-Policy：一种用于统一生成与规划的基于几何的世界-动作模型
authors: "Yang Zhou, Xiaofeng Wang, Hao Shao, Letian Wang, Guosheng Zhao, Jiangnan Shao, Jiagang Zhu, Tingdong Yu, Zheng Zhu, Guan Huang, Steven L. Waslander"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01765v1"
tags: ["query:wmodel"]
score: 9.0
evidence: 世界-动作模型连接VLA与世界模型用于自动驾驶
tldr: 针对现有世界-动作模型缺乏几何基础的问题，本文提出了DriveDreamer-Policy，一种统一的驾驶世界-动作模型。该模型利用大语言模型处理多模态输入，并通过轻量级生成器联合生成深度、未来视频和动作。通过学习几何感知的世界表征，模型实现了更连贯的未来预测和更优的规划。实验显示，该模型在Navsim基准上取得领先性能，验证了几何信息对生成和规划任务的关键作用。
selection_source: fresh_fetch
motivation: 现有的世界-动作模型主要关注2D外观建模，缺乏对物理世界至关重要的几何基础。
method: 提出DriveDreamer-Policy模型，利用大语言模型处理输入，并通过轻量级生成器在统一框架下联合生成深度、视频和规划动作。
result: 模型在Navsim v1和v2基准上分别达到89.2 PDMS和88.7 EPDMS，性能优于现有方法且生成质量更高。
conclusion: 显式的深度学习不仅提升了视频想象质量，还增强了规划的鲁棒性，验证了几何基础的重要性。
---

## 摘要
近年来，世界-动作模型（WAM）应运而生，旨在连接视觉-语言-动作（VLA）模型与世界模型，统一了二者的推理与指令遵循能力以及时空世界建模能力。然而，现有的 WAM 方法通常侧重于对二维外观或潜在表征进行建模，其几何基础十分有限，而这对于在物理世界中运行的具身系统而言是一个关键要素。我们提出了 DriveDreamer-Policy，这是一种统一的驾驶世界-动作模型，它在单一的模块化架构中集成了深度生成、未来视频生成和运动规划。该模型采用大语言模型处理语言指令、多视角图像和动作，随后通过三个轻量级生成器分别生成深度、未来视频和动作。通过学习几何感知的世界表征，并在统一框架内利用其指导未来预测和规划，该模型生成了更连贯的想象未来场景和更合理的驾驶动作，同时保持了模块化和可控的延迟。在 Navsim v1 和 v2 基准测试上的实验表明，DriveDreamer-Policy 在闭环规划和世界生成任务上均取得了优异的性能。具体而言，我们的模型在 Navsim v1 上达到了 89.2 PDMS，在 Navsim v2 上达到了 88.7 EPDMS，超越了现有的基于世界模型的方法，同时生成了更高质量的未来视频和深度预测。消融实验进一步表明，显式深度学习为视频想象提供了互补优势，并提高了规划的鲁棒性。

## Abstract
Recently, world-action models (WAM) have emerged to bridge vision-language-action (VLA) models and world models, unifying their reasoning and instruction-following capabilities and spatio-temporal world modeling. However, existing WAM approaches often focus on modeling 2D appearance or latent representations, with limited geometric grounding-an essential element for embodied systems operating in the physical world. We present DriveDreamer-Policy, a unified driving world-action model that integrates depth generation, future video generation, and motion planning within a single modular architecture. The model employs a large language model to process language instructions, multi-view images, and actions, followed by three lightweight generators that produce depth, future video, and actions. By learning a geometry-aware world representation and using it to guide both future prediction and planning within a unified framework, the proposed model produces more coherent imagined futures and more informed driving actions, while maintaining modularity and controllable latency. Experiments on the Navsim v1 and v2 benchmarks demonstrate that DriveDreamer-Policy achieves strong performance on both closed-loop planning and world generation tasks. In particular, our model reaches 89.2 PDMS on Navsim v1 and 88.7 EPDMS on Navsim v2, outperforming existing world-model-based approaches while producing higher-quality future video and depth predictions. Ablation studies further show that explicit depth learning provides complementary benefits to video imagination and improves planning robustness.

---

## 论文详细总结（自动生成）

# 论文总结：DriveDreamer-Policy

## 1. 论文的核心问题与整体含义
*   **研究背景**：自动驾驶系统正从模块化堆栈向端到端学习系统演进，近期趋势是结合视觉-语言-动作（VLA）模型与世界模型，形成“世界-动作模型”。然而，现有的世界-动作模型主要关注2D外观或潜在表征的建模，缺乏对物理世界至关重要的**几何基础**。
*   **核心问题**：缺乏几何基础导致模型生成的未来世界虽然在视觉上合理，但无法为规划提供最大信息量，且难以利用几何布局和自由空间等结构化安全线索进行推理（如遮挡推理、距离估计）。
*   **整体含义**：自动驾驶本质上是4D物理过程（3D几何随时间演变）。本文提出引入显式的深度生成模块，作为几何支架，旨在解决现有模型几何感知缺失的问题，实现更连贯的未来预测和更鲁棒的规划。

## 2. 论文提出的方法论
*   **核心思想**：提出 **DriveDreamer-Policy**，一个统一的驾驶世界-动作模型。该模型利用大语言模型（LLM）作为感知与推理核心，通过固定大小的查询接口连接三个轻量级生成专家，联合生成深度、未来视频和动作。
*   **架构设计**：
    *   **输入处理**：将语言指令、多视角图像和当前动作编码为Token，输入LLM。
    *   **查询接口**：设计了三组可学习的查询Token（深度查询、视频查询、动作查询），按顺序附加在输入序列后。
    *   **因果注意力机制**：强制执行结构化的信息流：深度查询 $\rightarrow$ 视频查询 $\rightarrow$ 动作查询。这意味着视频生成可利用深度信息，动作规划可同时利用深度和未来视频信息。
*   **生成专家模块**：
    *   **深度生成器**：基于像素空间的扩散Transformer，以LLM输出的深度嵌入和RGB图像为条件，生成单目深度图。采用流匹配目标进行训练。
    *   **视频生成器**：基于潜在空间的扩散Transformer（初始化自Wan-2.1），以LLM视频嵌入和CLIP视觉特征为条件，生成未来视频帧。
    *   **动作生成器**：扩散Transformer，以动作嵌入为条件，生成未来轨迹（参数化为 $x, y, \cos\theta, \sin\theta$）。
*   **训练目标**：采用单阶段联合训练，总损失函数为：
    $$ \mathcal{L} = \lambda_d \mathcal{L}_d + \lambda_v \mathcal{L}_v + \lambda_a \mathcal{L}_a $$
    其中 $\mathcal{L}_d, \mathcal{L}_v, \mathcal{L}_a$ 分别为深度、视频和动作预测的流匹配损失。

## 3. 实验设计
*   **数据集**：Navsim v1 和 v2 基准数据集（基于真实世界驾驶日志）。
    *   训练集：navtrain split（约100k样本）。
    *   测试集：navtest split（约12k样本）。
*   **评估指标**：
    *   **规划性能**：PDMS（Predictive Driver Model Score，v1）和 EPDMS（Extended PDMS，v2），涵盖无过错碰撞、可行驶区域合规性、碰撞时间、舒适性等指标。
    *   **世界生成质量**：视频质量使用 FVD（Fréchet Video Distance）、LPIPS、PSNR；深度精度使用 AbsRel 和 $\delta$ 准确率。
*   **对比方法**：
    *   **视觉端到端方法**：TransFuser, UniAD, DiffusionDrive。
    *   **VLA方法**：AutoVLA, DriveVLA-W0, RecogDrive。
    *   **世界模型方法**：PWM, Epona, DrivingGPT, WoTE 等。

## 4. 资源与算力
*   **硬件环境**：8 张 NVIDIA H20 GPU。
*   **训练配置**：Batch Size 为 32，训练步数为 100k steps，使用 AdamW 优化器，学习率为 $1 \times 10^{-5}$。
*   **分辨率**：深度和视频生成的训练分辨率为 $144 \times 256$。
*   **深度标签来源**：使用 Depth Anything 3 (DA3) 模型生成的深度标签作为监督信号，而非LiDAR真值。

## 5. 实验数量与充分性
*   **实验数量**：
    *   主实验：在 Navsim v1 和 v2 上的规划性能对比（表1, 表2）。
    *   生成质量对比：视频生成质量对比（表3a），深度估计精度对比（表3b）。
    *   消融实验：世界学习对规划的影响（表4）、深度学习对视频生成的影响（表5）、查询Token数量的影响（表6）。
    *   定性分析：可视化结果展示（图3, 图4）。
*   **充分性与客观性**：
    *   实验设计较为充分，涵盖了规划、视频生成、深度估计三个任务的评估。
    *   消融实验清晰地验证了深度模块和视频模块对规划的互补作用。
    *   对比方法涵盖了主流的基线，且均在相同基准下进行对比，具有公平性。
    *   **注意点**：深度估计的评估是基于DA3生成的伪标签，而非绝对真值，这可能存在偏差。

## 6. 论文的主要结论与发现
*   **性能表现**：DriveDreamer-Policy 在 Navsim v1 上达到 **89.2 PDMS**，在 v2 上达到 **88.7 EPDMS**，超越了现有的世界模型方法。
*   **生成质量**：视频生成质量显著优于基线方法（如FVD相比PWM降低了32.36），深度估计精度也优于微调后的PPD模型。
*   **关键发现**：
    *   显式的深度学习不仅提升了视频生成的准确性（提供了3D几何支架），还显著提高了规划的鲁棒性。
    *   联合的世界学习（深度+视频）比单一模态的学习能带来更大的规划性能提升。
    *   深度 $\rightarrow$ 视频 $\rightarrow$ 动作的因果信息流设计有效促进了特征传递。

## 7. 优点
*   **几何感知创新**：首次在世界-动作模型中显式引入深度生成模块，解决了纯2D建模缺乏几何约束的问题。
*   **统一架构**：通过LLM和查询接口优雅地统一了深度估计、视频预测和运动规划三个任务，模块化程度高。
*   **因果设计**：提出的因果注意力掩码机制，使得几何信息能自然地指导未来预测和规划，逻辑清晰。
*   **性能优越**：在规划和生成任务上均取得了SOTA效果，验证了方法的有效性。

## 8. 不足与局限
*   **深度监督信号**：训练和评估深度模块使用的标签来自Depth Anything 3模型而非LiDAR真值，可能存在噪声和尺度误差，限制了几何建模的绝对精度。
*   **分辨率限制**：为了计算效率，训练分辨率较低（$144 \times 256$），可能影响对远距离小目标的感知和生成细节。
*   **数据集局限**：仅在Navsim基准上测试，Navsim是非反应式模拟环境，与真实世界的闭环交互存在差异。
*   **推理延迟**：虽然声称模块化设计可控，但引入了扩散模型进行生成，推理速度可能受限，文中未详细报告实时性指标（FPS）。

（完）

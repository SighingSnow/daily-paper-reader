---
title: Hierarchical Memory Orchestration for Personalized Persistent Agents
title_zh: 面向个性化持久化智能体的分层记忆编排
authors: "Junming Liu, Yifei Sun, Weihua Cheng, Haodong Lei, Yuqi Li, Yirong Chen, Ding Wang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01670v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于持久智能体的HMO记忆框架
tldr: 针对智能体长期记忆积累导致的性能瓶颈问题，本文提出了分层记忆编排（HMO）框架。该框架通过三层目录结构组织交互历史，利用用户画像驱动记忆动态重分配，在保持高效搜索空间的同时确保推理与用户行为特征对齐。实验表明，该方法在基准测试中达到SOTA性能，并在实际部署中显著提升了智能体的流畅性与个性化水平。
selection_source: fresh_fetch
motivation: 长期记忆的数据积累导致检索噪声增加和计算延迟，严重制约了资源受限设备上智能体的推理能力。
method: 提出分层记忆编排（HMO）框架，构建三层目录结构，利用用户画像驱动记忆在层级间的动态重分配以优化检索。
result: 该方法在多个基准测试中达到最先进性能，并在实际部署中显著增强了智能体的流畅性与个性化。
conclusion: HMO框架有效解决了记忆过载问题，成功实现了高效且个性化的智能体长期记忆管理。
---

## 摘要
虽然长期记忆对于智能体维持一致的历史感知至关重要，但海量交互数据的积累往往导致性能瓶颈。朴素的存储扩展会增加检索噪声和计算延迟，从而压垮部署在资源受限个人设备上的模型的推理能力。为了解决这一问题，我们提出了分层记忆编排（HMO），这是一个将以用户为中心的上下文相关性为驱动，将交互历史组织成三层目录结构的框架。我们的系统维护一个紧凑的主缓存，将近期的关键记忆与动态演化的用户画像相结合，以确保智能体的推理与个体行为特征保持一致。该主缓存由一个高优先级的二级层补充，两者均在完整交互历史记录的全局归档中进行管理。关键在于，用户画像主导着跨层级结构的记忆重分配，将映射到长期模式的记录提升至更活跃的层级，同时将相关性较低的信息降级。这种针对性的编排能在需要时精准呈现历史知识，同时维持精简高效的活跃搜索空间。在多个基准测试上的评估达到了最先进的性能。在 OpenClaw 等生态系统中的实际部署表明，HMO 显著增强了智能体的流畅度和个性化水平。

## Abstract
While long-term memory is essential for intelligent agents to maintain consistent historical awareness, the accumulation of extensive interaction data often leads to performance bottlenecks. Naive storage expansion increases retrieval noise and computational latency, overwhelming the reasoning capacity of models deployed on constrained personal devices. To address this, we propose Hierarchical Memory Orchestration (HMO), a framework that organizes interaction history into a three-tiered directory driven by user-centric contextual relevance. Our system maintains a compact primary cache, coupling recent and pivotal memories with an evolving user profile to ensure agent reasoning remains aligned with individual behavioral traits. This primary cache is complemented by a high-priority secondary layer, both of which are managed within a global archive of the full interaction history. Crucially, the user persona dictates memory redistribution across this hierarchy, promoting records mapped to long-term patterns toward more active tiers while relegating less relevant information. This targeted orchestration surfaces historical knowledge precisely when needed while maintaining a lean and efficient active search space. Evaluations on multiple benchmarks achieve state-of-the-art performance. Real-world deployments in ecosystems like OpenClaw demonstrate that HMO significantly enhances agent fluidity and personalization.

---

## 论文详细总结（自动生成）

# 论文总结：面向个性化持久化智能体的分层记忆编排

## 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLM）智能体在长期交互中需要依赖长期记忆来维持历史一致性，但随着交互数据的积累，传统的“朴素存储扩展”会导致检索噪声增加、搜索空间臃肿以及计算延迟显著上升。
*   **核心问题**：现有的记忆管理方法多采用扁平化存储或通用图结构，忽略了用户个性化差异对信息效用的影响。这种“用户无关”的处理方式使得关键记忆被淹没在海量无关数据中，严重制约了资源受限设备上的推理效率和交互流畅度。
*   **整体含义**：本研究旨在解决智能体记忆过载与个性化缺失的矛盾，提出一种高效的记忆管理框架，使智能体能够在保持轻量级搜索空间的同时，精准召回与用户行为特征高度相关的历史知识。

## 2. 论文提出的方法论
*   **核心思想**：提出分层记忆编排（HMO）框架。该框架构建了一个以用户画像为中心的三层目录结构，通过动态评估记忆片段的重要性与相关性，实现记忆在层级间的自动晋升与降级。
*   **关键技术细节**：
    *   **三层架构**：
        *   **Tier 1 (Primary Cache)**：主缓存，固定容量，存储最近的对话上下文和评分最高的关键记忆，直接参与模型推理。
        *   **Tier 2 (Reference Buffer)**：参考缓冲区，可扩展，存储高频访问的历史记录，作为二级缓存拦截检索请求。
        *   **Tier 3 (Global Archive)**：全局归档，存储全量交互历史，仅在上述层级未命中时访问。
    *   **记忆生命周期**：
        *   **自主摄入**：将交互记录 $(q, a)$ 转化为记忆片段 $m$，并利用MLLM评估其初始重要性 $I_m$。
        *   **自适应评分**：设计评分函数 $S_m$ 综合考量初始重要性 $I_m$、与用户画像的相似度 $Sim(m, P)$、访问频率 $C_m$ 及时间衰减因子。
        *   **分层重分配**：根据 $S_m$ 分数决定记忆的存储层级，高价值记忆晋升至活跃层，低价值记忆降级至归档层。
    *   **增量画像演化**：引入“漂移门控”机制，仅当用户画像偏移量超过阈值 $\tau$ 时才触发全量重算，平衡了个性化更新与计算开销。

## 3. 实验设计
*   **数据集**：
    *   **LoCoMo**：包含10个不同用户组的多轮对话数据集，用于评估长程对话记忆。
    *   **LongMemEval-S**：包含500段对话的大规模评估套件，专门用于评估长时程记忆处理能力。
*   **评估指标**：
    *   检索指标：Recall@5, NDCG@5。
    *   生成质量：Accuracy (使用LLM-as-a-judge), F1 Score。
    *   效率指标：推理延迟时间。
*   **对比方法**：与9种代表性框架对比，包括 MemoryBank, Mem0, A-MEM, MemoryOS, MemOS, LD-Agent, RMM, QRRetriever, MemVerse 以及传统的 RAG 方法。

## 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的GPU型号、数量或训练时长。
*   **实现细节**：框架主要基于 API 调用实现，使用 `text-embedding-small` 进行向量化，使用 `GPT-4o-mini` 作为响应生成模型。实验重点在于推理阶段的延迟统计与检索效率对比，而非模型训练。

## 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在2个主流基准数据集上进行了完整对比。
    *   **消融实验**：设计了多组消融实验，包括移除 Tier 1、移除 Tier 1&2、对比图结构、对比不同评分策略（仅MLLM评分 vs 仅相似度 vs 混合策略）。
    *   **扩展实验**：在更大规模的 LongMemEval-M 数据集上验证了可扩展性。
    *   **实际部署**：在 OpenClaw 平台进行了真实场景部署测试。
*   **充分性与客观性**：实验设计较为充分，不仅对比了SOTA方法，还通过消融实验详细验证了分层架构和评分机制的有效性。评估指标涵盖了检索准确率、生成质量和系统延迟，能够客观反映方法性能。

## 6. 论文的主要结论与发现
*   **性能表现**：HMO 在 LongMemEval 上取得了 SOTA 性能（Recall@5 达 81.1%，Accuracy 达 86.4%），超越了基于 Llama-3.1-70B 的检索基线。
*   **效率提升**：相比全局搜索和图结构遍历，HMO 显著降低了推理延迟。实验显示，在 LongMemEval-M 上，搜索时间减少了 86.5%（从 1453.21s 降至 195.75s），且精度损失极小。
*   **机制有效性**：用户画像驱动的分层机制能有效过滤噪声，确保活跃搜索空间的精简，同时漂移门控机制成功解决了画像演化带来的计算负担。

## 7. 优点
*   **创新性架构**：首次提出利用动态演化的用户画像来主导记忆的全生命周期管理（从存储优先级到检索），突破了传统扁平记忆库的局限。
*   **轻量高效**：通过三层架构和懒更新策略，有效解决了长程记忆检索中的“大海捞针”问题，极大降低了对算力的需求，适合端侧部署。
*   **即插即用**：框架设计为模块化，无需对底层模型进行微调，易于集成到现有的智能体系统中。

## 8. 不足与局限
*   **基准测试局限**：论文指出，现有的基准测试（如 LoCoMo）多采用静态评估范式，无法完全模拟真实世界中智能体与用户连续交互、记忆实时演化的动态过程。
*   **初始评分依赖**：记忆的初始重要性评分 $I_m$ 依赖 MLLM 的判断，虽然是一次性开销，但在处理海量历史数据初始化时仍可能产生较高的成本。
*   **懒更新延迟**：Tier 3 采用懒更新策略，虽然提高了效率，但可能导致部分高价值但长期未被访问的记忆无法及时晋升到活跃层，存在一定的滞后性。

（完）
